export { RelayerError, getUmbraRelayer, pollClaimUntilTerminal } from './chunk-4RHXVBNI.js';
import { getNetworkConfig, getNetworkSpecifier, getDefaultSchemeVersion, getDefaultAlgorithmVersion, getDefaultProtocolVersion, createSetComputeUnitLimitInstruction, TOKEN_2022_PROGRAM_ID, SPL_TOKEN_PROGRAM_ID } from './chunk-FQDYYTPG.js';
import { getPoseidonAggregator, getPoseidonHasher, getAesDecryptor, getUserAccountX25519KeypairDeriver, deriveNullifierFromModifiedGenerationIndex, deriveRandomSecretFromModifiedGenerationIndex, deriveKeystreamBlindingFactor, getRescueEncryptorFromPrivateKey, getRescueKeyGeneratorFromPrivateKey, getRescueCommitmentGenerator, getModuloPowCurve25519Function, getPolynomialEvaluatorFunction, getMasterViewingKeyDeriver, getMasterViewingKeyBlindingFactorDeriver, getPoseidonPrivateKeyDeriver, getPoseidonBlindingFactorDeriver, getSecondViewingKeyDeriver, getPoseidonEncryptor, getPoseidonKeystreamGenerator, getUserCommitmentGeneratorFunction, getKeystreamCommitmentGenerator, getFiatShamirChallengeGeneratorFunction, expandModifiedGenerationIndex, getEphemeralUtxoMasterViewingKeyDeriver, getEphemeralUtxoMasterViewingKeyBlindingFactorDeriver, getEphemeralUtxoPoseidonPrivateKeyDeriver, getEphemeralUtxoPoseidonPrivateKeyBlindingFactorDeriver, getMasterViewingKeyX25519KeypairDeriver, getMintX25519KeypairDeriver, getPoseidonKeystreamBlindingFactorDeriver, getUtxoCommitmentHashGenerator, getChallengePowersFunction, getAesEncryptor, deriveProofAccountOffsetFromModifiedGenerationIndex, deriveRandomFactorForPolynomialCommitmentFromModifiedGenerationIndex, deriveRescueEncryptionCommitmentBlindingFactorFromModifiedGenerationIndex, getRescueCommitmentBlindingFactorDeriver, getPolynomialCommitmentFactorDeriver, getRescueEncryptorWithNonceFromPrivateKey, getRescueDecryptorFromPrivateKey } from './chunk-YA66JLHI.js';
export { AES_AUTH_TAG_LENGTH, AES_IV_LENGTH, AES_KEY_LENGTH, AES_METADATA_OVERHEAD, ALPHA, ALPHA_INVERSE, BASE85_LIMB_MAX, BLOCK_SIZE, CIPHER_ROUND_COUNT, FIELD_ELEMENT_BYTE_LENGTH, FIELD_PRIME, GROTH16_G1_BYTE_LENGTH, GROTH16_G2_BYTE_LENGTH, HASH_CAPACITY, HASH_DIGEST_LENGTH, HASH_RATE, HASH_ROUND_COUNT, HASH_STATE_SIZE, MASTER_VIEWING_KEY_MAX, MAX_AGGREGATOR_INPUTS, MAX_POSEIDON_INPUTS, MAX_PRF_SEEDS, NONCE_BYTES, OPTIONAL_DATA_BYTE_LENGTH, PRECOMPUTED_FIELD_INVERSES, REDUCTION_CONST, SAMPLE_BUFFER_BYTE_LENGTH, SECRET_BYTES, SHAKE256_CIPHER_SEED, X25519_BYTE_LENGTH, ZK_PROOF_BYTE_LENGTH, assertAesCiphertextWithMetadata, assertAesKey, assertAesPlaintext, assertBase85Limb, assertCurve25519KeypairResult, assertDailyViewingKey, assertEd25519Keypair, assertGenerationSeed, assertGroth16ProofA, assertGroth16ProofB, assertGroth16ProofC, assertHourlyViewingKey, assertKeccak256Hash, assertKeccak512Hash, assertMasterSeed, assertMasterViewingKey, assertMintViewingKey, assertMinuteViewingKey, assertMonthlyViewingKey, assertOptionalData32, assertPoseidonCiphertext, assertPoseidonCounter, assertPoseidonHash, assertPoseidonKey, assertPoseidonKeystream, assertPoseidonPlaintext, assertRcCiphertext, assertRcCounter, assertRcEncryptionNonce, assertRcKey, assertRcPlaintext, assertSecondViewingKey, assertSharedSecret, assertX25519Bytes, assertX25519Keypair, assertX25519PrivateKey, assertX25519PublicKey, assertYearlyViewingKey, assertZkProofBytes, buildPersonalizationString, defaultAesDecryptor, defaultAesEncryptor, deriveBn254FieldElementFromKeccak512, deriveEphemeralMasterViewingKey, deriveEphemeralMasterViewingKeyBlindingFactor, deriveEphemeralPoseidonPrivateKey, deriveEphemeralPoseidonPrivateKeyBlindingFactor, deriveKeystreamBlindingFactor, deriveModifiedGenerationIndexForReceiverClaim, deriveModifiedGenerationIndexForSenderClaim, deriveNullifierFromModifiedGenerationIndex, deriveProofAccountOffsetFromModifiedGenerationIndex, deriveRandomFactorForPolynomialCommitmentFromModifiedGenerationIndex, deriveRandomSecretFromModifiedGenerationIndex, deriveRescueEncryptionCommitmentBlindingFactorFromModifiedGenerationIndex, expandModifiedGenerationIndex, getAesDecryptor, getAesEncryptor, getBn254FieldElementFromKeccak512Deriver, getChallengePowersFunction, getDailyViewingKeyDeriver, getEphemeralMasterSeedDeriver, getEphemeralRescueCommitmentBlindingFactorDeriver, getEphemeralUtxoH2RandomSecretDeriver, getEphemeralUtxoMasterViewingKeyBlindingFactorDeriver, getEphemeralUtxoMasterViewingKeyDeriver, getEphemeralUtxoNullifierDeriver, getEphemeralUtxoPoseidonPrivateKeyBlindingFactorDeriver, getEphemeralUtxoPoseidonPrivateKeyDeriver, getFiatShamirChallengeGeneratorFunction, getFieldArithmetic, getHourlyViewingKeyDeriver, getKeystreamCommitmentGenerator, getMasterSeedBasedFieldElementDeriver, getMasterViewingKeyBlindingFactorDeriver, getMasterViewingKeyDeriver, getMasterViewingKeyX25519KeypairDeriver, getMintViewingKeyDeriver, getMintX25519KeypairDeriver, getMinuteViewingKeyDeriver, getModuloPowCurve25519Function, getMonthlyViewingKeyDeriver, getPersonalizationStringBuilder, getPolynomialCommitmentFactorDeriver, getPolynomialEvaluatorFunction, getPoseidonAggregator, getPoseidonBlindingFactorDeriver, getPoseidonDecryptor, getPoseidonEncryptor, getPoseidonHasher, getPoseidonKeystreamBlindingFactorDeriver, getPoseidonKeystreamGenerator, getPoseidonPrf, getPoseidonPrivateKeyDeriver, getPseudorandomU512Deriver, getRescueCipher, getRescueCipherFromKeyPair, getRescueCipherInstance, getRescueCommitmentBlindingFactorDeriver, getRescueCommitmentGenerator, getRescueDecryptorFromPrivateKey, getRescueEncryptorFromPrivateKey, getRescueEncryptorWithNonceFromPrivateKey, getRescueKeyGeneratorFromPrivateKey, getRescuePrimeHashFunction, getSecondViewingKeyDeriver, getUserAccountX25519KeypairDeriver, getUserCommitmentGeneratorFunction, getUtxoCommitmentHashGenerator, getYearlyViewingKeyDeriver } from './chunk-YA66JLHI.js';
import { splitAddressToLowHigh, createBn254FieldElement, createPoseidonKey, splitU256IntoTwoU128s, createRcPlaintext, splitX25519PublicKeyIntoTwoU128s, convertU256ToBase85Limbs, createU256, createCurve25519FieldElement, bigintToBinaryArrayLSB, generateRandomU256, generateRandomU128, createU64, extractClusterOffsetFromMxeAccount, generateRandomU64, extractTransferFeeConfig, calculateTransferFee, generateComputationOffset, extractUtcComponents, getByteAt } from './chunk-UXMQI6B7.js';
import { findEncryptedUserAccountPda, findArciumInfrastructurePdas, findUserComplianceGrantPda, findNetworkComplianceGrantPda, findSharedComplianceGrantPda, findEncryptedTokenAccountPda, findStealthPoolPda, findUtxoInputBufferPda, findPublicUtxoInputBufferPda } from './chunk-KH3YTDKA.js';
import './chunk-EEKF4553.js';
import { ComputationMonitorError, FetchUtxosError, ClaimUtxoError, RpcError, ConversionError, TransactionSigningError, TransactionError, EncryptedDepositError, CreateUtxoError, QueryError, RegistrationError, MasterSeedSigningRejectedError, EncryptedWithdrawalError } from './chunk-AUNYA6JP.js';
export { ClaimUtxoError, ComputationMonitorError, ConversionError, CreateUtxoError, EncryptedDepositError, EncryptedWithdrawalError, FetchUtxosError, QueryError, RegistrationError, isClaimUtxoError, isComputationMonitorError, isConversionError, isCreateUtxoError, isEncryptedDepositError, isEncryptedWithdrawalError, isFetchUtxosError, isQueryError, isRegistrationError } from './chunk-AUNYA6JP.js';
import { computeBn254LimbwiseSumInverse, computeBn254ModularInverse } from './chunk-OFDWNWCL.js';
export { bn254Add, bn254ModuloPow, bn254Mul, bn254Sub, computeBn254LimbwiseSumInverse, computeBn254ModularInverse } from './chunk-OFDWNWCL.js';
import { bn254FieldElementSampler, getBn254ModularInverter, computeBn254LimbwiseSumInverse as computeBn254LimbwiseSumInverse$1 } from './chunk-WN75ORDT.js';
export { bn254FieldElementSampler, curve25519FieldElementSampler, curve25519ModuloAdd, curve25519ModuloInv, curve25519ModuloMul, curve25519ModuloPow, curve25519ModuloSub, getBn254FieldElementSampler, getBn254ModularAdder, getBn254ModularInverter, getBn254ModularMultiplier, getBn254ModularNegator, getBn254ModularSubtractor, getCurve25519FieldElementSampler, getCurve25519ModularAddFunction, getCurve25519ModularInvFunction, getCurve25519ModularMulFunction, getCurve25519ModularPowFunction, getCurve25519ModularSubFunction } from './chunk-WN75ORDT.js';
import './chunk-UOFYS6M3.js';
import { decodeU64LeBytesToU64, decodeU128LeBytesToU128, encodeU256ToU256BeBytes, decodeU256BeBytesToU256, encodeU128ToU128LeBytes, encodeU256ToU256LeBytes, decodeU256LeBytesToU256, encodeU64ToU64LeBytes } from './chunk-FQX6ZYGJ.js';
import { CryptographyError } from './chunk-5KPQXPQM.js';
import './chunk-4TZVXB5G.js';
import { getRpcAccountInfoProvider, getRpcBlockhashProvider, getWebsocketTransactionForwarder, getRpcEpochInfoProvider, lookupAltEntry, buildAltAddressesRecord } from './chunk-HA5FLM63.js';
export { createInMemorySigner, createSignerFromKeyPair, createSignerFromPrivateKeyBytes, createSignerFromWalletAccount, getPollingTransactionForwarder, getRpcAccountInfoProvider, getRpcBlockhashProvider, getRpcEpochInfoProvider, getWebsocketTransactionForwarder } from './chunk-HA5FLM63.js';
import { base64ToUint8Array, sleep } from './chunk-TLR7A64G.js';
import { assertTransactionSignature, assertYear, assertMonth, assertDay, assertHour, assertMinute, assertSecond } from './chunk-DD2WCK4C.js';
import { assertMasterSeed, assertAesKey, assertX25519PrivateKey, assertU128, assertU256, MathematicsAssertionError, assertU256LeBytes, assertU64, assertBn254FieldElement, assertCurve25519FieldElement, assertRcEncryptionNonce, assertU512BeBytes, assertGroth16ProofA, assertGroth16ProofB, assertGroth16ProofC, assertOptionalData32, assertBase85Limb, assertPoseidonPlaintext, assertPoseidonKey, assertRcPlaintext, assertRcCiphertext, assertRcKey, assertAesPlaintext, assertX25519PublicKey, CURVE25519_FIELD_PRIME } from './chunk-CFTW5WNG.js';
import { __name } from './chunk-7QVYU63E.js';
import { kmac256 } from '@noble/hashes/sha3-addons.js';
import { createSolanaRpcSubscriptions, createSolanaRpc, getAddressDecoder, createKeyPairSignerFromPrivateKeyBytes, createNoopSigner, pipe, createTransactionMessage, setTransactionMessageFeePayer, setTransactionMessageLifetimeUsingBlockhash, appendTransactionMessageInstructions, partiallySignTransactionMessageWithSigners, compressTransactionMessageUsingAddressLookupTables, getSignatureFromTransaction, getAddressEncoder, compileTransaction, fetchEncodedAccount } from '@solana/kit';
import { getClaimComputationRentInstruction, decodeComputationAccount, ComputationStatus } from '@umbra-privacy/arcium-codama';
import { x25519 } from '@noble/curves/ed25519.js';
import { keccak_256 } from '@noble/hashes/sha3.js';
import { getCreateUserGrantInstructionAsync, getDeleteUserGrantInstructionAsync, getReencryptUserGrantV11InstructionAsync, getReencryptNetworkGrantForNetworkBalanceV11InstructionAsync, getReencryptNetworkGrantForSharedBalanceV11InstructionAsync, decodeEncryptedTokenAccount, getConvertNetworkBalanceToSharedBalanceV11InstructionAsync, getReencryptSharedBalanceV11InstructionAsync, decodeEncryptedUserAccount, getResetSharedEncryptedTokenAccountQueueV11InstructionAsync, getResetNetworkEncryptedTokenAccountQueueV11InstructionAsync, getDepositFromPublicBalanceIntoExistingSharedBalanceV11InstructionAsync, getDepositFromPublicBalanceIntoNewSharedBalanceV11InstructionAsync, getDepositFromPublicBalanceIntoExistingNetworkBalanceV11InstructionAsync, getDepositFromPublicBalanceIntoNewNetworkBalanceV11InstructionAsync, getCloseStealthPoolDepositInputBufferInstructionAsync, getCreateStealthPoolDepositInputBufferInstructionAsync, getDepositIntoStealthPoolFromSharedBalanceV11InstructionAsync, getClosePublicStealthPoolDepositInputBufferInstructionAsync, getCreatePublicStealthPoolDepositInputBufferInstructionAsync, getDepositIntoStealthPoolFromPublicBalanceInstructionAsync, getClaimStagedSolFromPoolInstructionAsync, getClaimStagedSplFromPoolInstructionAsync, getEncryptedUserAccountSize, ENCRYPTED_USER_ACCOUNT_DISCRIMINATOR, getInitialiseEncryptedUserAccountInstructionAsync, getRegisterTokenPublicKeyInstructionAsync, getRegisterUserForAnonymousUsageV11InstructionAsync, getUpdateRandomGenerationSeedInstructionAsync, getUpdateTokenAccountRandomGenerationSeedInstructionAsync, getWithdrawFromSharedBalanceIntoPublicBalanceV11InstructionAsync } from '@umbra-privacy/umbra-codama';

// src/umbra/constants.ts
var UMBRA_MESSAGE_TO_SIGN = `
Umbra Protocol – User Consent & Acknowledgement

Please read carefully before continuing. Umbra is a non-custodial, open-source protocol. Umbra does not hold, control, manage, or access your digital assets, private keys, viewing keys, transactions, or personal data. All interactions with the Umbra protocol occur through autonomous smart contracts and wallet-signed transactions executed directly on the blockchain.

By using the Umbra protocol, you acknowledge and agree that:

1. You are solely responsible for managing your wallet, private keys, privacy settings, viewing key disclosures, and any transactions or interactions you authorize.
2.Blockchain transactions are generally irreversible, and smart contracts may contain bugs, vulnerabilities, or unintended behavior.
3. Errors, misuse, misconfiguration, third-party tools, relayers, or loss of keys may result in permanent and unrecoverable loss of assets or privacy.
4. Privacy features are designed to enhance confidentiality but do not guarantee absolute or unconditional anonymity.
5. Transactions may involve independent relayers or third-party infrastructure that Umbra does not operate, control, or supervise.
6. By clicking "I Agree", you confirm that you have read, understood, and accepted all applicable terms, policies, risk disclosures, notices, and other documentation governing your access to and use of the Umbra protocol, as published or made available by Umbra from time to time.
You acknowledge that such documentation may be updated or modified, and that continued access to or use of the Umbra Protocol constitutes acceptance of the then-current versions. If you do not agree, do not proceed and discontinue use of the Umbra protocol.
`;
var FEE_OFFSETS = {
  /**
   * Offset used in `FeeVault` PDA derivation for the unified protocol fees pool.
   *
   * @remarks
   * Passed as the final seed to `findProtocolFeeVaultPda` and
   * `findRelayerFeeVaultPda`. Currently `0n`.
   *
   * @readonly
   */
  PROTOCOL_FEES_POOL: 0n,
  /**
   * Offset used in `FeeSchedule` PDA derivation.
   *
   * @remarks
   * Passed as the final seed to `findFeeSchedulePda`. Currently `0n`.
   *
   * @readonly
   */
  PROTOCOL_FEES_CONFIG: 0n,
  /**
   * Offset used in relayer fee configuration PDA derivation.
   *
   * @remarks
   * Passed as the offset seed when looking up the relayer's fee configuration account.
   * Currently `0n`.
   *
   * @readonly
   */
  RELAYER_FEES_CONFIG: 0n
};
var BPS_DIVISOR = 16384n;

// src/common/storage.ts
function getDefaultMasterSeedStorage() {
  let cachedSeed = null;
  return {
    load: /* @__PURE__ */ __name(async () => {
      if (cachedSeed !== null) {
        return { exists: true, seed: cachedSeed };
      }
      return { exists: false };
    }, "load"),
    store: /* @__PURE__ */ __name(async (seed) => {
      cachedSeed = seed;
      return { success: true };
    }, "store")
  };
}
__name(getDefaultMasterSeedStorage, "getDefaultMasterSeedStorage");
var MASTER_SEED_DOMAIN = new TextEncoder().encode("Umbra Privacy - MasterSeedGeneration");
var SEED_PERSONALIZATION = new TextEncoder().encode("umbra/1.0.0|kmac256/1.0.0|kdf/1.0.0|seed");
function getDefaultMasterSeedGenerator(signer) {
  return async () => {
    const messageBytes = new TextEncoder().encode(UMBRA_MESSAGE_TO_SIGN);
    const signature = await signer.signMessage(messageBytes);
    const seed = kmac256(
      MASTER_SEED_DOMAIN,
      // key (domain separator)
      signature.signature,
      // message (wallet signature)
      { dkLen: 64, personalization: SEED_PERSONALIZATION }
      // 64 bytes = 512 bits
    );
    assertMasterSeed(seed);
    return seed;
  };
}
__name(getDefaultMasterSeedGenerator, "getDefaultMasterSeedGenerator");
function getDefaultKeyLoader() {
  return async () => {
    return { exists: false };
  };
}
__name(getDefaultKeyLoader, "getDefaultKeyLoader");
function getDefaultKeyStorer() {
  return async () => {
    return { success: true };
  };
}
__name(getDefaultKeyStorer, "getDefaultKeyStorer");
var DEFAULT_MAX_SLOT_WINDOW = 200;
var DEFAULT_SAFETY_TIMEOUT_MS = 3e5;
var DEFAULT_POLLING_INTERVAL_MS = 2e3;
var DEFAULT_SIGNATURE_RETRIEVAL_LIMIT = 20;
function notificationValueToEncodedAccount(address, value) {
  const data = base64ToUint8Array(value.data[0]);
  return {
    address,
    data,
    executable: value.executable,
    lamports: value.lamports,
    programAddress: value.owner,
    space: BigInt(data.length),
    exists: true
  };
}
__name(notificationValueToEncodedAccount, "notificationValueToEncodedAccount");
function checkAborted(signal) {
  if (signal?.aborted) {
    throw new ComputationMonitorError(
      "timeout",
      "Computation monitoring cancelled via AbortSignal"
    );
  }
}
__name(checkAborted, "checkAborted");
async function fetchCurrentSlot(rpc, commitment) {
  const slot = await rpc.getSlot({ commitment }).send();
  return slot;
}
__name(fetchCurrentSlot, "fetchCurrentSlot");
function isSlotWindowExceeded(queuedSlot, currentSlot, maxSlotWindow) {
  return currentSlot - queuedSlot > BigInt(maxSlotWindow);
}
__name(isSlotWindowExceeded, "isSlotWindowExceeded");
async function retrieveCallbackTransactionSignature(rpc, computationAddress) {
  try {
    const response = await rpc.getSignaturesForAddress(computationAddress, {
      limit: DEFAULT_SIGNATURE_RETRIEVAL_LIMIT
    }).send();
    for (const entry of response) {
      if (entry.err === null) {
        const sig = entry.signature.toString();
        assertTransactionSignature(sig);
        return sig;
      }
    }
    throw new ComputationMonitorError(
      "signature-retrieval",
      `No successful callback transaction found (checked ${String(DEFAULT_SIGNATURE_RETRIEVAL_LIMIT)} signatures)`
    );
  } catch (error) {
    if (error instanceof ComputationMonitorError) {
      throw error;
    }
    throw new ComputationMonitorError(
      "signature-retrieval",
      "Failed to retrieve callback transaction signature",
      error instanceof Error ? error : void 0
    );
  }
}
__name(retrieveCallbackTransactionSignature, "retrieveCallbackTransactionSignature");
async function buildFinalizedResult(rpc, computationAddress, startTime, queuedSlot, shouldRetrieveSignature, onProgress) {
  const elapsedMs = Date.now() - startTime;
  if (shouldRetrieveSignature) {
    const callbackSignature = await retrieveCallbackTransactionSignature(rpc, computationAddress);
    onProgress?.({ type: "finalized", callbackSignature });
    return { status: "finalized", elapsedMs, queuedSlot, callbackSignature };
  }
  onProgress?.({ type: "finalized" });
  return { status: "finalized", elapsedMs, queuedSlot };
}
__name(buildFinalizedResult, "buildFinalizedResult");
function buildPrunedResult(startTime, queuedSlot, currentSlot, onProgress) {
  onProgress?.({ type: "pruned", queuedSlot, currentSlot });
  return {
    status: "pruned",
    elapsedMs: Date.now() - startTime,
    queuedSlot,
    currentSlot
  };
}
__name(buildPrunedResult, "buildPrunedResult");
function buildTimedOutResult(startTime) {
  return {
    status: "timed-out",
    elapsedMs: Date.now() - startTime
  };
}
__name(buildTimedOutResult, "buildTimedOutResult");
function getWebsocketComputationMonitor(config, deps) {
  const { rpcUrl, rpcSubscriptionsUrl } = config;
  const {
    createRpc = createSolanaRpc,
    createRpcSubscriptions = createSolanaRpcSubscriptions
  } = deps ?? {};
  const rpc = createRpc(rpcUrl);
  const rpcSubscriptions = createRpcSubscriptions(rpcSubscriptionsUrl);
  async function prepareMonitor(computationAddress, options) {
    const {
      maxSlotWindow = DEFAULT_MAX_SLOT_WINDOW,
      safetyTimeoutMs = DEFAULT_SAFETY_TIMEOUT_MS,
      retrieveCallbackSignature: shouldRetrieveSignature = false,
      onProgress,
      commitment = "confirmed",
      signal: externalSignal
    } = options ?? {};
    const abortController = new AbortController();
    let cleaned = false;
    function cleanup() {
      if (cleaned) return;
      cleaned = true;
      abortController.abort();
    }
    __name(cleanup, "cleanup");
    let notifications;
    try {
      notifications = await rpcSubscriptions.accountNotifications(computationAddress, { encoding: "base64", commitment }).subscribe({ abortSignal: abortController.signal });
    } catch (error) {
      cleanup();
      throw new ComputationMonitorError(
        "subscription",
        "Failed to establish WebSocket subscription",
        error instanceof Error ? error : void 0
      );
    }
    async function awaitComputation() {
      const startTime = Date.now();
      let queuedSlot = null;
      let receivedFirstNotification = false;
      checkAborted(externalSignal);
      const safetyTimeoutId = setTimeout(() => abortController.abort(), safetyTimeoutMs);
      const onExternalAbort = /* @__PURE__ */ __name(() => abortController.abort(), "onExternalAbort");
      externalSignal?.addEventListener("abort", onExternalAbort, { once: true });
      let slotCheckIntervalId = null;
      let pruneDetected = null;
      try {
        for await (const notification of notifications) {
          try {
            const encodedAccount = notificationValueToEncodedAccount(
              computationAddress,
              notification.value
            );
            const decoded = decodeComputationAccount(encodedAccount);
            if (!receivedFirstNotification) {
              receivedFirstNotification = true;
              queuedSlot = decoded.data.slot;
              const capturedQueuedSlot = queuedSlot;
              slotCheckIntervalId = setInterval(async () => {
                try {
                  const currentSlot = await fetchCurrentSlot(rpc, commitment);
                  if (isSlotWindowExceeded(capturedQueuedSlot, currentSlot, maxSlotWindow)) {
                    pruneDetected = buildPrunedResult(
                      startTime,
                      capturedQueuedSlot,
                      currentSlot,
                      onProgress
                    );
                    abortController.abort();
                  }
                } catch {
                }
              }, 5e3);
              if (decoded.data.status === ComputationStatus.Finalized) {
                return buildFinalizedResult(
                  rpc,
                  computationAddress,
                  startTime,
                  queuedSlot,
                  shouldRetrieveSignature,
                  onProgress
                );
              }
              continue;
            }
            if (decoded.data.status === ComputationStatus.Finalized) {
              return buildFinalizedResult(
                rpc,
                computationAddress,
                startTime,
                queuedSlot,
                shouldRetrieveSignature,
                onProgress
              );
            }
          } catch {
            continue;
          }
        }
        if (pruneDetected) {
          return pruneDetected;
        }
        return buildTimedOutResult(startTime);
      } catch (error) {
        if (error instanceof ComputationMonitorError) {
          throw error;
        }
        if (pruneDetected) {
          return pruneDetected;
        }
        if (abortController.signal.aborted) {
          return buildTimedOutResult(startTime);
        }
        throw new ComputationMonitorError(
          "subscription",
          "WebSocket subscription failed",
          error instanceof Error ? error : void 0
        );
      } finally {
        clearTimeout(safetyTimeoutId);
        if (slotCheckIntervalId !== null) {
          clearInterval(slotCheckIntervalId);
        }
        externalSignal?.removeEventListener("abort", onExternalAbort);
        cleanup();
      }
    }
    __name(awaitComputation, "awaitComputation");
    return { awaitComputation, cleanup };
  }
  __name(prepareMonitor, "prepareMonitor");
  return { prepareMonitor };
}
__name(getWebsocketComputationMonitor, "getWebsocketComputationMonitor");
function getPollingComputationMonitor(config, deps) {
  const { rpcUrl } = config;
  const { createRpc = createSolanaRpc } = deps ?? {};
  const rpc = createRpc(rpcUrl);
  async function prepareMonitor(computationAddress, options) {
    const {
      maxSlotWindow = DEFAULT_MAX_SLOT_WINDOW,
      safetyTimeoutMs = DEFAULT_SAFETY_TIMEOUT_MS,
      pollingIntervalMs = DEFAULT_POLLING_INTERVAL_MS,
      retrieveCallbackSignature: shouldRetrieveSignature = false,
      onProgress,
      commitment = "confirmed",
      signal: externalSignal
    } = options ?? {};
    let aborted = false;
    function cleanup() {
      aborted = true;
    }
    __name(cleanup, "cleanup");
    async function awaitComputation() {
      const startTime = Date.now();
      let queuedSlot = null;
      let receivedFirstDecode = false;
      while (Date.now() - startTime < safetyTimeoutMs) {
        if (aborted) {
          return buildTimedOutResult(startTime);
        }
        checkAborted(externalSignal);
        try {
          const [maybeAccount, currentSlot] = await Promise.all([
            fetchEncodedAccount(rpc, computationAddress, { commitment }),
            fetchCurrentSlot(rpc, commitment)
          ]);
          if (!maybeAccount.exists) {
            await sleep(pollingIntervalMs);
            continue;
          }
          const decoded = decodeComputationAccount(maybeAccount);
          if (queuedSlot === null) {
            queuedSlot = decoded.data.slot;
          }
          if (!receivedFirstDecode) {
            receivedFirstDecode = true;
            if (decoded.data.status === ComputationStatus.Finalized) {
              return buildFinalizedResult(
                rpc,
                computationAddress,
                startTime,
                queuedSlot,
                shouldRetrieveSignature,
                onProgress
              );
            }
            await sleep(pollingIntervalMs);
            continue;
          }
          if (decoded.data.status === ComputationStatus.Finalized) {
            return buildFinalizedResult(
              rpc,
              computationAddress,
              startTime,
              queuedSlot,
              shouldRetrieveSignature,
              onProgress
            );
          }
          if (isSlotWindowExceeded(queuedSlot, currentSlot, maxSlotWindow)) {
            return buildPrunedResult(startTime, queuedSlot, currentSlot, onProgress);
          }
        } catch (error) {
          if (error instanceof ComputationMonitorError) {
            throw error;
          }
        }
        await sleep(pollingIntervalMs);
      }
      return buildTimedOutResult(startTime);
    }
    __name(awaitComputation, "awaitComputation");
    return { awaitComputation, cleanup };
  }
  __name(prepareMonitor, "prepareMonitor");
  return { prepareMonitor };
}
__name(getPollingComputationMonitor, "getPollingComputationMonitor");

// src/umbra/client.ts
var notImplementedGenerator = /* @__PURE__ */ __name(async () => {
  throw new Error("Not implemented yet");
}, "notImplementedGenerator");
async function getUmbraClient(args, deps) {
  const {
    signer,
    network,
    rpcUrl,
    rpcSubscriptionsUrl,
    indexerApiEndpoint,
    versions: customVersions,
    offsets: customOffsets,
    deferMasterSeedSignature = false
  } = args;
  const networkConfig = getNetworkConfig(network);
  const versions = {
    protocol: customVersions?.protocol ?? getDefaultProtocolVersion,
    algorithm: customVersions?.algorithm ?? getDefaultAlgorithmVersion,
    scheme: customVersions?.scheme ?? getDefaultSchemeVersion,
    network: getNetworkSpecifier(network)
  };
  const offsets = {
    masterViewingKey: customOffsets?.masterViewingKey ?? 0n,
    poseidonPrivateKey: customOffsets?.poseidonPrivateKey ?? 0n,
    x25519UserAccountPrivateKey: customOffsets?.x25519UserAccountPrivateKey ?? 0n,
    x25519MasterViewingKeyEncryptingPrivateKey: customOffsets?.x25519MasterViewingKeyEncryptingPrivateKey ?? 0n,
    mintX25519PrivateKey: customOffsets?.mintX25519PrivateKey ?? 0n,
    rescueCommitmentBlindingFactor: customOffsets?.rescueCommitmentBlindingFactor ?? 0n,
    randomCommitmentFactor: customOffsets?.randomCommitmentFactor ?? 0n
  };
  const accountInfoProvider = deps?.accountInfoProvider ?? getRpcAccountInfoProvider({ rpcUrl });
  const blockhashProvider = deps?.blockhashProvider ?? getRpcBlockhashProvider({ rpcUrl });
  const transactionForwarder = deps?.transactionForwarder ?? getWebsocketTransactionForwarder({
    rpcUrl,
    rpcSubscriptionsUrl
  });
  const epochInfoProvider = deps?.epochInfoProvider ?? getRpcEpochInfoProvider({ rpcUrl });
  const computationMonitor = deps?.computationMonitor ?? getWebsocketComputationMonitor({
    rpcUrl,
    rpcSubscriptionsUrl
  });
  let fetchMerkleProof;
  let fetchBatchMerkleProof;
  let fetchUtxoData;
  if (indexerApiEndpoint !== void 0 && indexerApiEndpoint !== "") {
    const { getMerkleProofFetcher, getBatchMerkleProofFetcher, getUtxoDataFetcher } = await import('./indexer-NHZDFABY.js');
    fetchMerkleProof = getMerkleProofFetcher({ apiEndpoint: indexerApiEndpoint });
    fetchBatchMerkleProof = getBatchMerkleProofFetcher({ apiEndpoint: indexerApiEndpoint });
    fetchUtxoData = getUtxoDataFetcher({ apiEndpoint: indexerApiEndpoint });
  }
  const defaultMasterSeedStorage = getDefaultMasterSeedStorage();
  const defaultMasterSeedGenerator = getDefaultMasterSeedGenerator(signer);
  const masterSeedLoad = deps?.masterSeedStorage?.load ?? defaultMasterSeedStorage.load;
  const masterSeedStore = deps?.masterSeedStorage?.store ?? defaultMasterSeedStorage.store;
  const masterSeedGenerate = deps?.masterSeedStorage?.generate ?? defaultMasterSeedGenerator;
  const getMasterSeed = /* @__PURE__ */ __name(async () => {
    const loadResult = await masterSeedLoad();
    if (loadResult.exists) {
      return loadResult.seed;
    }
    const seed = await masterSeedGenerate();
    await masterSeedStore(seed);
    return seed;
  }, "getMasterSeed");
  const masterViewingKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const poseidonPrivateKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const x25519PrivateKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const yearlyViewingKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const monthlyViewingKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const dailyViewingKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const mintViewingKeyStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const rescueCommitmentBlindingFactorStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const randomCommitmentFactorStorage = {
    load: getDefaultKeyLoader(),
    store: getDefaultKeyStorer(),
    generate: notImplementedGenerator
  };
  const client = {
    signer,
    network,
    networkConfig,
    versions,
    offsets,
    accountInfoProvider,
    blockhashProvider,
    transactionForwarder,
    epochInfoProvider,
    computationMonitor,
    ...fetchMerkleProof !== void 0 && { fetchMerkleProof },
    ...fetchBatchMerkleProof !== void 0 && { fetchBatchMerkleProof },
    ...fetchUtxoData !== void 0 && { fetchUtxoData },
    masterSeed: {
      load: masterSeedLoad,
      store: masterSeedStore,
      generate: masterSeedGenerate,
      getMasterSeed
    },
    masterViewingKey: masterViewingKeyStorage,
    poseidonPrivateKey: poseidonPrivateKeyStorage,
    x25519PrivateKey: x25519PrivateKeyStorage,
    yearlyViewingKey: yearlyViewingKeyStorage,
    monthlyViewingKey: monthlyViewingKeyStorage,
    dailyViewingKey: dailyViewingKeyStorage,
    mintViewingKey: mintViewingKeyStorage,
    rescueCommitmentBlindingFactor: rescueCommitmentBlindingFactorStorage,
    randomCommitmentFactor: randomCommitmentFactorStorage
  };
  if (!deferMasterSeedSignature) {
    await getMasterSeed();
  }
  return client;
}
__name(getUmbraClient, "getUmbraClient");

// src/umbra/fee-provider.ts
var TREE_DEPTH = 4;
var MAX_LEAVES = 1 << TREE_DEPTH;
async function computeLeafHash(slab) {
  const aggregator = getPoseidonAggregator();
  return await aggregator([
    slab.amountLowerBound,
    slab.amountUpperBound,
    slab.baseFee,
    slab.bps
  ]);
}
__name(computeLeafHash, "computeLeafHash");
async function buildFullTree(slabs) {
  if (slabs.length > MAX_LEAVES) {
    throw new Error(
      `Too many fee slabs: ${String(slabs.length)} (max ${String(MAX_LEAVES)})`
    );
  }
  const hasher = getPoseidonHasher();
  const leafHashes = Array.from({ length: MAX_LEAVES }, () => 0n);
  for (const [index, slab] of slabs.entries()) {
    leafHashes[index] = await computeLeafHash(slab);
  }
  const layers = [leafHashes];
  let currentLevel = leafHashes;
  for (let depth = 0; depth < TREE_DEPTH; depth++) {
    const nextLevel = [];
    for (let pairIndex = 0; pairIndex < currentLevel.length; pairIndex += 2) {
      const left = currentLevel[pairIndex];
      const right = currentLevel[pairIndex + 1];
      const parent = await hasher([left, right]);
      nextLevel.push(parent);
    }
    layers.push(nextLevel);
    currentLevel = nextLevel;
  }
  return layers;
}
__name(buildFullTree, "buildFullTree");
function extractMerklePath(layers, leafIndex) {
  const path = [];
  let currentIndex = leafIndex;
  for (let depth = 0; depth < TREE_DEPTH; depth++) {
    const siblingIndex = currentIndex % 2 === 0 ? currentIndex + 1 : currentIndex - 1;
    path.push(layers[depth][siblingIndex]);
    currentIndex = Math.floor(currentIndex / 2);
  }
  return path;
}
__name(extractMerklePath, "extractMerklePath");
async function buildPrecomputedTree(slabs) {
  const layers = await buildFullTree(slabs);
  const root = layers[TREE_DEPTH][0];
  return { slabs, layers, root };
}
__name(buildPrecomputedTree, "buildPrecomputedTree");
function lookupSlab(tree, amount) {
  for (const [index, slab] of tree.slabs.entries()) {
    if (amount >= slab.amountLowerBound && amount <= slab.amountUpperBound) {
      return {
        slab,
        leafIndex: BigInt(index),
        merklePath: extractMerklePath(tree.layers, index),
        merkleRoot: tree.root
      };
    }
  }
  throw new Error(
    `No fee slab covers amount ${String(amount)}. Available slabs: ${tree.slabs.map((s) => `[${String(s.amountLowerBound)}, ${String(s.amountUpperBound)}]`).join(", ")}`
  );
}
__name(lookupSlab, "lookupSlab");
function calculateFee(slab, amount) {
  const amountAfterBaseFee = amount - slab.baseFee;
  const bpsQuotient = amountAfterBaseFee * slab.bps / BPS_DIVISOR;
  const bpsRemainder = amountAfterBaseFee * slab.bps % BPS_DIVISOR;
  const fee = slab.baseFee + bpsQuotient;
  return { fee, bpsQuotient, bpsRemainder };
}
__name(calculateFee, "calculateFee");
function leafIndexToPathIndices(leafIndex) {
  const indices = [];
  let currentIndex = Number(leafIndex);
  for (let depth = 0; depth < TREE_DEPTH; depth++) {
    indices.push(BigInt(currentIndex & 1 ^ 1));
    currentIndex >>= 1;
  }
  return indices;
}
__name(leafIndexToPathIndices, "leafIndexToPathIndices");
function bigintTo32BytesLE(value) {
  const bytes = new Uint8Array(32);
  let remaining = value;
  for (let byteIndex = 0; byteIndex < 32; byteIndex++) {
    bytes[byteIndex] = Number(remaining & 0xffn);
    remaining >>= 8n;
  }
  return bytes;
}
__name(bigintTo32BytesLE, "bigintTo32BytesLE");
function feeSlabResultToInstructionFields(result) {
  return {
    feesAmountLowerBound: { first: result.slab.amountLowerBound },
    feesAmountUpperBound: { first: result.slab.amountUpperBound },
    feesBaseFeesInSpl: { first: result.slab.baseFee },
    feesCommissionFeeInSpl: { first: Number(result.slab.bps) },
    feesMerklePath: result.merklePath.map((h) => ({ first: bigintTo32BytesLE(h) })),
    feesLeafIndex: { first: Number(result.leafIndex) }
  };
}
__name(feeSlabResultToInstructionFields, "feeSlabResultToInstructionFields");
function protocolFeeSlabResultToInstructionFields(result) {
  return {
    protocolFeesAmountLowerBound: { first: result.slab.amountLowerBound },
    protocolFeesAmountUpperBound: { first: result.slab.amountUpperBound },
    protocolFeesBaseFeesInSpl: { first: result.slab.baseFee },
    protocolFeesCommissionFeeInSpl: { first: Number(result.slab.bps) },
    protocolFeesMerklePath: result.merklePath.map((h) => ({ first: bigintTo32BytesLE(h) })),
    protocolFeesLeafIndex: { first: Number(result.leafIndex) }
  };
}
__name(protocolFeeSlabResultToInstructionFields, "protocolFeeSlabResultToInstructionFields");
function relayerFeeSlabResultToInstructionFields(result) {
  return {
    relayerFeesAmountLowerBound: { first: result.slab.amountLowerBound },
    relayerFeesAmountUpperBound: { first: result.slab.amountUpperBound },
    relayerFeesBaseFeesInSpl: { first: result.slab.baseFee },
    relayerFeesCommissionFeeInSpl: { first: Number(result.slab.bps) },
    relayerFeesMerklePath: result.merklePath.map((h) => ({ first: bigintTo32BytesLE(h) })),
    relayerFeesLeafIndex: { first: Number(result.leafIndex) }
  };
}
__name(relayerFeeSlabResultToInstructionFields, "relayerFeeSlabResultToInstructionFields");
var MAX_U64 = BigInt("18446744073709551615");
var ZERO_FEE_SLAB = {
  amountLowerBound: 0n,
  amountUpperBound: MAX_U64,
  baseFee: 0n,
  bps: 0n
};
var CLAIM_PROTOCOL_FEE_SLAB = {
  amountLowerBound: 0n,
  amountUpperBound: MAX_U64,
  baseFee: 0n,
  bps: 35n
};
var CLAIM_RELAYER_FEE_SLAB = {
  amountLowerBound: 0n,
  amountUpperBound: MAX_U64,
  baseFee: 0n,
  bps: 35n
};
function createHardcodedFeeProvider(slabs) {
  let cachedTree;
  return async (amount) => {
    cachedTree ??= buildPrecomputedTree(slabs);
    const tree = await cachedTree;
    return lookupSlab(tree, amount);
  };
}
__name(createHardcodedFeeProvider, "createHardcodedFeeProvider");
function getHardcodedDepositProtocolFeeProvider() {
  return createHardcodedFeeProvider([ZERO_FEE_SLAB]);
}
__name(getHardcodedDepositProtocolFeeProvider, "getHardcodedDepositProtocolFeeProvider");
function getHardcodedCreateUtxoProtocolFeeProvider() {
  return createHardcodedFeeProvider([ZERO_FEE_SLAB]);
}
__name(getHardcodedCreateUtxoProtocolFeeProvider, "getHardcodedCreateUtxoProtocolFeeProvider");
function getHardcodedWithdrawalProtocolFeeProvider() {
  return createHardcodedFeeProvider([ZERO_FEE_SLAB]);
}
__name(getHardcodedWithdrawalProtocolFeeProvider, "getHardcodedWithdrawalProtocolFeeProvider");
function getHardcodedClaimUtxoProtocolFeeProvider() {
  return createHardcodedFeeProvider([CLAIM_PROTOCOL_FEE_SLAB]);
}
__name(getHardcodedClaimUtxoProtocolFeeProvider, "getHardcodedClaimUtxoProtocolFeeProvider");
function getHardcodedClaimUtxoRelayerFeeProvider() {
  return createHardcodedFeeProvider([CLAIM_RELAYER_FEE_SLAB]);
}
__name(getHardcodedClaimUtxoRelayerFeeProvider, "getHardcodedClaimUtxoRelayerFeeProvider");
var AES_DOMAIN_SEPARATOR_EPHEMERAL = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateSelfClaimableUtxoFromEncryptedBalance")
).slice(0, 12);
var AES_DOMAIN_SEPARATOR_RECEIVER = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateReceiverClaimableUtxoFromEncryptedBalance")
).slice(0, 12);
var AES_DOMAIN_SEPARATOR_PUBLIC_EPHEMERAL = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateSelfClaimableUtxoFromPublicBalance")
).slice(0, 12);
var AES_DOMAIN_SEPARATOR_PUBLIC_RECEIVER = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateReceiverClaimableUtxoFromPublicBalance")
).slice(0, 12);
var MAX_LEAVES_PER_TREE = BigInt(2 ** 20);
var DEFAULT_PAGINATION_LIMIT = 1000n;
function arraysEqual(a, b) {
  if (a.length !== b.length) {
    return false;
  }
  for (const [index, element] of a.entries()) {
    if (element !== b[index]) {
      return false;
    }
  }
  return true;
}
__name(arraysEqual, "arraysEqual");
async function tryDecryptUtxo(utxo, clientX25519PrivateKey, aesDecryptor, x25519GetSharedSecret) {
  try {
    const sharedSecret = x25519GetSharedSecret(
      clientX25519PrivateKey,
      utxo.depositorX25519PublicKey
    );
    const aesKeyBytes = keccak_256(sharedSecret).slice(0, 32);
    assertAesKey(aesKeyBytes);
    const plaintext = await aesDecryptor(aesKeyBytes, utxo.aesEncryptedData);
    const domainSeparator = plaintext.slice(56, 68);
    let unlockerType;
    if (arraysEqual(domainSeparator, AES_DOMAIN_SEPARATOR_EPHEMERAL)) {
      unlockerType = "self-burnable";
    } else if (arraysEqual(domainSeparator, AES_DOMAIN_SEPARATOR_RECEIVER)) {
      unlockerType = "received";
    } else if (arraysEqual(domainSeparator, AES_DOMAIN_SEPARATOR_PUBLIC_EPHEMERAL)) {
      unlockerType = "public-self-burnable";
    } else if (arraysEqual(domainSeparator, AES_DOMAIN_SEPARATOR_PUBLIC_RECEIVER)) {
      unlockerType = "public-received";
    } else {
      return null;
    }
    const amountBytes = plaintext.slice(0, 8);
    const destinationBytes = plaintext.slice(8, 40);
    const modifiedGenIndexBytes = plaintext.slice(40, 56);
    const amount = decodeU64LeBytesToU64(amountBytes);
    const destinationAddress = getAddressDecoder().decode(destinationBytes);
    return {
      amount,
      destinationAddress,
      modifiedGenerationIndex: modifiedGenIndexBytes,
      treeIndex: utxo.treeIndex,
      insertionIndex: utxo.insertionIndex,
      h1Components: utxo.h1Components,
      h1Hash: utxo.h1Hash,
      h2Hash: utxo.h2Hash,
      unlockerType
    };
  } catch {
    return null;
  }
}
__name(tryDecryptUtxo, "tryDecryptUtxo");
function enrichWithMerkleProof(scannedUtxos, batchProof) {
  return scannedUtxos.map((utxo) => {
    const proofEntry = batchProof.proofs.get(utxo.insertionIndex);
    if (proofEntry === void 0) {
      throw new FetchUtxosError(
        "proof-enrichment",
        `Missing proof for insertion index ${String(utxo.insertionIndex)}`
      );
    }
    return {
      // Merkle proof data (from batch proof endpoint — all share the same root)
      merkleRoot: batchProof.root,
      merklePath: proofEntry.merklePath,
      leafIndex: BigInt(utxo.insertionIndex),
      // Decrypted values
      amount: utxo.amount,
      destinationAddress: utxo.destinationAddress,
      depositModifiedGenerationIndex: decodeU128LeBytesToU128(utxo.modifiedGenerationIndex),
      // H1 components (flattened)
      version: utxo.h1Components.version,
      commitmentIndex: utxo.h1Components.commitmentIndex,
      senderAddressLow: utxo.h1Components.senderAddressLow,
      senderAddressHigh: utxo.h1Components.senderAddressHigh,
      relayerFixedSolFees: utxo.h1Components.relayerFixedSolFees,
      mintAddressLow: utxo.h1Components.mintAddressLow,
      mintAddressHigh: utxo.h1Components.mintAddressHigh,
      timestamp: utxo.h1Components.timestamp,
      poolVolumeSpl: utxo.h1Components.poolVolumeSpl,
      poolVolumeSol: utxo.h1Components.poolVolumeSol
    };
  });
}
__name(enrichWithMerkleProof, "enrichWithMerkleProof");
function getClaimableUtxoScannerFunction(args, deps) {
  const { client } = args;
  if (client.fetchUtxoData === void 0) {
    throw new FetchUtxosError(
      "initialization",
      "Client must have fetchUtxoData configured for UTXO discovery. Provide indexerApiEndpoint when creating the client."
    );
  }
  const fetchUtxoData = deps?.fetchUtxoData ?? client.fetchUtxoData;
  const aesDecryptor = deps?.aesDecryptor ?? getAesDecryptor();
  const x25519GetSharedSecret = deps?.x25519GetSharedSecret ?? ((privateKey, publicKey) => x25519.getSharedSecret(privateKey, publicKey));
  const mvkX25519KeypairDeriver = getUserAccountX25519KeypairDeriver({
    client
  });
  return /* @__PURE__ */ __name(async function fetchClaimableUtxos(treeIndex, startInsertionIndex, endInsertionIndex) {
    if (treeIndex < 0n) {
      throw new FetchUtxosError(
        "validation",
        `Invalid tree index: ${String(treeIndex)}. Must be non-negative.`
      );
    }
    if (startInsertionIndex < 0n) {
      throw new FetchUtxosError(
        "validation",
        `Invalid start insertion index: ${String(startInsertionIndex)}. Must be non-negative.`
      );
    }
    if (endInsertionIndex !== void 0 && endInsertionIndex < startInsertionIndex) {
      throw new FetchUtxosError(
        "validation",
        `Invalid end insertion index: ${String(endInsertionIndex)}. Must be >= start index.`
      );
    }
    let clientX25519PrivateKey;
    try {
      const keypairResult = await mvkX25519KeypairDeriver();
      clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      assertX25519PrivateKey(clientX25519PrivateKey);
    } catch (error) {
      if (error instanceof FetchUtxosError) throw error;
      throw new FetchUtxosError(
        "key-derivation",
        `Failed to derive X25519 private key: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    const treeOffset = treeIndex * MAX_LEAVES_PER_TREE;
    const absoluteStartIndex = treeOffset + startInsertionIndex;
    const treeEndAbsoluteIndex = treeOffset + MAX_LEAVES_PER_TREE - 1n;
    const absoluteEndIndex = endInsertionIndex === void 0 ? treeEndAbsoluteIndex : treeOffset + endInsertionIndex;
    const allDecrypted = [];
    let cursor = absoluteStartIndex;
    let lastSeenInsertionIndex = null;
    while (cursor <= absoluteEndIndex) {
      let result;
      try {
        result = await fetchUtxoData(cursor, absoluteEndIndex, DEFAULT_PAGINATION_LIMIT);
      } catch (error) {
        if (error instanceof FetchUtxosError) throw error;
        throw new FetchUtxosError(
          "indexer-fetch",
          `Failed to fetch UTXO data from indexer: ${error instanceof Error ? error.message : String(error)}`,
          error instanceof Error ? error : void 0
        );
      }
      const treeUtxos = [];
      for (const [, utxo] of result.items) {
        if (utxo.treeIndex !== treeIndex) {
          continue;
        }
        if (lastSeenInsertionIndex === null || utxo.insertionIndex > lastSeenInsertionIndex) {
          lastSeenInsertionIndex = utxo.insertionIndex;
        }
        treeUtxos.push(utxo);
      }
      const decryptedResults = await Promise.all(
        treeUtxos.map(
          (utxo) => tryDecryptUtxo(utxo, clientX25519PrivateKey, aesDecryptor, x25519GetSharedSecret)
        )
      );
      for (const decrypted of decryptedResults) {
        if (decrypted !== null) {
          allDecrypted.push(decrypted);
        }
      }
      if (!result.hasMore || result.nextCursor === void 0) {
        break;
      }
      if (result.nextCursor > absoluteEndIndex) {
        break;
      }
      cursor = result.nextCursor;
    }
    const nextScanStartIndex = lastSeenInsertionIndex !== null ? lastSeenInsertionIndex + 1n : startInsertionIndex;
    if (allDecrypted.length === 0) {
      return { selfBurnable: [], received: [], publicSelfBurnable: [], publicReceived: [], nextScanStartIndex };
    }
    const selfBurnableUtxos = [];
    const receivedUtxos = [];
    const publicSelfBurnableUtxos = [];
    const publicReceivedUtxos = [];
    for (const decrypted of allDecrypted) {
      switch (decrypted.unlockerType) {
        case "self-burnable": {
          selfBurnableUtxos.push(decrypted);
          break;
        }
        case "received": {
          receivedUtxos.push(decrypted);
          break;
        }
        case "public-self-burnable": {
          publicSelfBurnableUtxos.push(decrypted);
          break;
        }
        case "public-received": {
          publicReceivedUtxos.push(decrypted);
          break;
        }
      }
    }
    return {
      selfBurnable: selfBurnableUtxos,
      received: receivedUtxos,
      publicSelfBurnable: publicSelfBurnableUtxos,
      publicReceived: publicReceivedUtxos,
      nextScanStartIndex
    };
  }, "fetchClaimableUtxos");
}
__name(getClaimableUtxoScannerFunction, "getClaimableUtxoScannerFunction");
var MAX_UTXOS_PER_PROOF = 4;
var DOMAIN_CLAIM_MODIFIED_GEN_INDEX = "ClaimReceiverClaimableUtxoIntoEncryptedBalance / modifiedGenerationIndex";
var DOMAIN_CLAIM_EXPANDED_GEN_INDEX = "ClaimReceiverClaimableUtxoIntoEncryptedBalance / expandedGenerationIndex";
var BPS_DIVISOR2 = 16384n;
var LINKER_ENCRYPTIONS_PER_LEAF = 6;
var LINKER_KEYSTREAM_BLINDING_FACTOR_DOMAIN = "receiverClaimUtxoToEta / linkerKeystreamBlindingFactor";
var RESCUE_ENCRYPTIONS_COUNT = 7;
var RANDOM_FACTOR_FOR_POLYNOMIAL_COMMITMENT_DOMAIN = "receiverClaimUtxoToEta / randomFactorForPolynomialCommitment";
var RESCUE_ENCRYPTION_COMMITMENT_BLINDING_FACTOR_DOMAIN = "receiverClaimUtxoToEta / rescueEncryptionCommitmentBlindingFactor";
var KMAC_512_OUTPUT_LENGTH = 64;
var AGGREGATED_HASH_INPUT_COUNT = 64;
function reconstructAddressFromU128Parts(low, high) {
  const bytes = new Uint8Array(32);
  let lowValue = BigInt(low);
  for (let index = 0; index < 16; index++) {
    bytes[index] = Number(lowValue & 0xffn);
    lowValue >>= 8n;
  }
  let highValue = BigInt(high);
  for (let index = 0; index < 16; index++) {
    bytes[16 + index] = Number(highValue & 0xffn);
    highValue >>= 8n;
  }
  const decoder = getAddressDecoder();
  return decoder.decode(bytes);
}
__name(reconstructAddressFromU128Parts, "reconstructAddressFromU128Parts");
function limbsToTuple(limbs) {
  return [limbs.low, limbs.middle, limbs.high];
}
__name(limbsToTuple, "limbsToTuple");
async function processUtxoInBatch(args, deps) {
  const {
    utxosBatch,
    stealthPoolIndex,
    generationIndex,
    masterSeedBytes,
    relayerAddress: relayerPublicKey,
    clientX25519PrivateKey,
    clientX25519PublicKey,
    networkConfig,
    optionalData,
    microLamportsPerAcu
  } = args;
  const utxoIds = utxosBatch.map(
    (utxo) => `${String(stealthPoolIndex)}:${String(utxo.leafIndex)}`
  );
  const {
    submitClaim,
    pollClaimStatus,
    awaitCompletion,
    pollingIntervalMs,
    timeoutMs,
    onProgress,
    getRelayerAddress,
    secondViewingKeyGenerator,
    masterViewingKeyGenerator,
    masterViewingKeyBlindingFactorGenerator,
    poseidonPrivateKeyGenerator,
    poseidonBlindingFactorGenerator,
    poseidonHasher,
    poseidonAggregator,
    poseidonEncryptor,
    poseidonKeystreamGenerator,
    userCommitmentGenerator,
    keystreamCommitmentGenerator,
    fiatShamirChallengeGenerator,
    zkProver,
    generateRandomU128: generateRandomU1282
  } = deps;
  const protocolFeeProvider = getHardcodedClaimUtxoProtocolFeeProvider();
  const relayerFeeProvider = getHardcodedClaimUtxoRelayerFeeProvider();
  assertU128(BigInt(stealthPoolIndex), "stealthPoolIndex");
  assertU256(BigInt(generationIndex), "generationIndex");
  const nLeaves = utxosBatch.length;
  if (nLeaves === 0) {
    throw new MathematicsAssertionError("utxosBatch must be non-empty", {
      value: nLeaves,
      expectedType: "utxosBatch",
      constraint: "length > 0"
    });
  }
  if (nLeaves > MAX_UTXOS_PER_PROOF) {
    throw new MathematicsAssertionError(
      `utxosBatch length ${String(nLeaves)} exceeds maximum ${String(MAX_UTXOS_PER_PROOF)}`,
      {
        value: nLeaves,
        expectedType: "utxosBatch",
        constraint: `length <= ${String(MAX_UTXOS_PER_PROOF)}`
      }
    );
  }
  const destinationAddress = utxosBatch[0].destinationAddress;
  for (let index = 1; index < nLeaves; index++) {
    if (utxosBatch[index].destinationAddress !== destinationAddress) {
      throw new MathematicsAssertionError(
        `Destination address mismatch at index ${String(index)}: expected ${destinationAddress}, got ${utxosBatch[index].destinationAddress}`,
        {
          value: utxosBatch[index].destinationAddress,
          expectedType: "destinationAddress",
          constraint: `all UTXOs must have same destination: ${destinationAddress}`
        }
      );
    }
  }
  const firstUtxo = utxosBatch[0];
  const merkleRoot = firstUtxo.merkleRoot;
  assertU256LeBytes(merkleRoot);
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    if (utxo.merklePath.length === 0) {
      throw new MathematicsAssertionError(`merklePath[${String(index)}] must be non-empty`, {
        value: utxo.merklePath.length,
        expectedType: `merklePath[${String(index)}]`,
        constraint: "length > 0"
      });
    }
    for (const sibling of utxo.merklePath) {
      assertU256LeBytes(sibling);
    }
    const maxLeafIndex = (1n << BigInt(utxo.merklePath.length)) - 1n;
    const leafIndexBigInt = BigInt(utxo.leafIndex);
    if (leafIndexBigInt < 0n || leafIndexBigInt > maxLeafIndex) {
      throw new MathematicsAssertionError(
        `leafIndex[${String(index)}] value ${String(leafIndexBigInt)} is out of range for tree depth ${String(utxo.merklePath.length)}`,
        {
          value: leafIndexBigInt,
          expectedType: `leafIndex[${String(index)}]`,
          constraint: `0 <= leafIndex <= ${String(maxLeafIndex)}`
        }
      );
    }
    assertU128(leafIndexBigInt, `leafIndex[${String(index)}]`);
    const versionBigInt = BigInt(utxo.version);
    assertU64(versionBigInt, `version[${String(index)}]`);
    if (versionBigInt === 0n) {
      throw new MathematicsAssertionError(`version[${String(index)}] must not be zero`, {
        value: versionBigInt,
        expectedType: `version[${String(index)}]`,
        constraint: "version !== 0"
      });
    }
    assertU64(BigInt(utxo.amount), `amount[${String(index)}]`);
    assertU128(BigInt(utxo.senderAddressLow), `senderAddressLow[${String(index)}]`);
    assertU128(BigInt(utxo.senderAddressHigh), `senderAddressHigh[${String(index)}]`);
    assertU128(BigInt(utxo.mintAddressLow), `mintAddressLow[${String(index)}]`);
    assertU128(BigInt(utxo.mintAddressHigh), `mintAddressHigh[${String(index)}]`);
    assertU64(BigInt(utxo.relayerFixedSolFees), `relayerFixedSolFees[${String(index)}]`);
    assertYear(BigInt(utxo.timestamp.year));
    assertMonth(BigInt(utxo.timestamp.month));
    assertDay(BigInt(utxo.timestamp.day));
    assertHour(BigInt(utxo.timestamp.hour));
    assertMinute(BigInt(utxo.timestamp.minute));
    assertSecond(BigInt(utxo.timestamp.second));
    assertU128(
      BigInt(utxo.depositModifiedGenerationIndex),
      `depositModifiedGenerationIndex[${String(index)}]`
    );
  }
  const generationIndexBytes = new TextEncoder().encode(generationIndex.toString());
  const modifiedGenerationIndex = kmac256(
    new TextEncoder().encode(DOMAIN_CLAIM_MODIFIED_GEN_INDEX),
    masterSeedBytes,
    { dkLen: 16, personalization: generationIndexBytes }
  );
  const claimModifiedGenerationIndex = kmac256(
    new TextEncoder().encode(DOMAIN_CLAIM_EXPANDED_GEN_INDEX),
    masterSeedBytes,
    { dkLen: 32, personalization: modifiedGenerationIndex }
  );
  let claimModifiedGenerationIndexU256 = 0n;
  for (let index = 0; index < 32; index++) {
    claimModifiedGenerationIndexU256 |= BigInt(claimModifiedGenerationIndex[index]) << BigInt(index * 8);
  }
  const executionTimestamp = /* @__PURE__ */ new Date();
  const tvkYear = BigInt(executionTimestamp.getUTCFullYear());
  const tvkMonth = BigInt(executionTimestamp.getUTCMonth() + 1);
  const tvkDay = BigInt(executionTimestamp.getUTCDate());
  const tvkHour = BigInt(executionTimestamp.getUTCHours());
  const tvkMinute = BigInt(executionTimestamp.getUTCMinutes());
  const tvkSecond = BigInt(executionTimestamp.getUTCSeconds());
  const mintAddress = reconstructAddressFromU128Parts(
    firstUtxo.mintAddressLow,
    firstUtxo.mintAddressHigh
  );
  const transactionViewingKey = await secondViewingKeyGenerator(
    mintAddress,
    tvkYear,
    tvkMonth,
    tvkDay,
    tvkHour,
    tvkMinute,
    tvkSecond
  );
  const [
    masterViewingKey,
    masterViewingKeyBlindingFactor,
    poseidonPrivateKey,
    poseidonPrivateKeyBlindingFactor
  ] = await Promise.all([
    masterViewingKeyGenerator(),
    masterViewingKeyBlindingFactorGenerator(),
    poseidonPrivateKeyGenerator(),
    poseidonBlindingFactorGenerator()
  ]);
  const userCommitment = await userCommitmentGenerator(
    masterViewingKey,
    masterViewingKeyBlindingFactor,
    poseidonPrivateKey,
    poseidonPrivateKeyBlindingFactor
  );
  assertBn254FieldElement(masterViewingKey);
  assertBn254FieldElement(masterViewingKeyBlindingFactor);
  assertBn254FieldElement(poseidonPrivateKey);
  assertBn254FieldElement(poseidonPrivateKeyBlindingFactor);
  assertBn254FieldElement(userCommitment);
  assertBn254FieldElement(transactionViewingKey);
  assertYear(tvkYear);
  assertMonth(tvkMonth);
  assertDay(tvkDay);
  assertHour(tvkHour);
  assertMinute(tvkMinute);
  assertSecond(tvkSecond);
  const perLeafNullifiers = [];
  const perLeafRandomSecrets = [];
  const perLeafAmounts = [];
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    const depositModifiedGenerationIndexValue = BigInt(utxo.depositModifiedGenerationIndex);
    const depositModifiedGenerationIndexBytes = new Uint8Array(16);
    let indexValue = depositModifiedGenerationIndexValue;
    for (let index2 = 0; index2 < 16; index2++) {
      depositModifiedGenerationIndexBytes[index2] = Number(indexValue & 0xffn);
      indexValue >>= 8n;
    }
    const nullifier = deriveNullifierFromModifiedGenerationIndex(
      depositModifiedGenerationIndexBytes
    );
    const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(
      depositModifiedGenerationIndexBytes
    );
    perLeafNullifiers.push(nullifier);
    perLeafRandomSecrets.push(randomSecret);
    perLeafAmounts.push(utxo.amount);
    assertBn254FieldElement(nullifier);
    assertBn254FieldElement(randomSecret);
  }
  const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
  assertU128(destinationAddressLow, "destinationAddressLow");
  assertU128(destinationAddressHigh, "destinationAddressHigh");
  let totalAmount = 0n;
  for (const amt of perLeafAmounts) {
    totalAmount += BigInt(amt);
  }
  assertU64(totalAmount, "totalAmount");
  const protocolFeeConfig = await protocolFeeProvider(totalAmount);
  const PROTOCOL_FEE_BASE_FEE = protocolFeeConfig.slab.baseFee;
  const PROTOCOL_FEE_BPS = protocolFeeConfig.slab.bps;
  const PROTOCOL_FEE_LOWER_BOUND = protocolFeeConfig.slab.amountLowerBound;
  const PROTOCOL_FEE_UPPER_BOUND = protocolFeeConfig.slab.amountUpperBound;
  const amountAfterProtocolBaseFeeForBps = totalAmount - PROTOCOL_FEE_BASE_FEE;
  if (amountAfterProtocolBaseFeeForBps < 0n) {
    throw new MathematicsAssertionError(
      `Total amount ${String(totalAmount)} is less than protocol base fee ${String(PROTOCOL_FEE_BASE_FEE)}`,
      {
        value: amountAfterProtocolBaseFeeForBps,
        expectedType: "protocolFeeCalculation",
        constraint: ">= 0"
      }
    );
  }
  const protocolFeeBpsQuotient = amountAfterProtocolBaseFeeForBps * PROTOCOL_FEE_BPS / BPS_DIVISOR2;
  const protocolFeeBpsRemainder = amountAfterProtocolBaseFeeForBps * PROTOCOL_FEE_BPS % BPS_DIVISOR2;
  const protocolFees = PROTOCOL_FEE_BASE_FEE + protocolFeeBpsQuotient;
  const amountAfterProtocolFee = totalAmount - protocolFees;
  assertU64(protocolFees, "protocolFees");
  assertU64(amountAfterProtocolFee, "amountAfterProtocolFee");
  const relayerFeeConfig = await relayerFeeProvider(amountAfterProtocolFee);
  const RELAYER_FEE_BASE_FEE = relayerFeeConfig.slab.baseFee;
  const RELAYER_FEE_BPS = relayerFeeConfig.slab.bps;
  const RELAYER_FEE_LOWER_BOUND = relayerFeeConfig.slab.amountLowerBound;
  const RELAYER_FEE_UPPER_BOUND = relayerFeeConfig.slab.amountUpperBound;
  const amountAfterRelayerBaseFeeForBps = amountAfterProtocolFee - RELAYER_FEE_BASE_FEE;
  if (amountAfterRelayerBaseFeeForBps < 0n) {
    throw new MathematicsAssertionError(
      `Amount after protocol fee ${String(amountAfterProtocolFee)} is less than relayer base fee ${String(RELAYER_FEE_BASE_FEE)}`,
      {
        value: amountAfterRelayerBaseFeeForBps,
        expectedType: "relayerFeeCalculation",
        constraint: ">= 0"
      }
    );
  }
  const relayerFeeBpsQuotient = amountAfterRelayerBaseFeeForBps * RELAYER_FEE_BPS / BPS_DIVISOR2;
  const relayerFeeBpsRemainder = amountAfterRelayerBaseFeeForBps * RELAYER_FEE_BPS % BPS_DIVISOR2;
  const relayerFees = RELAYER_FEE_BASE_FEE + relayerFeeBpsQuotient;
  const finalAmount = amountAfterProtocolFee - relayerFees;
  assertU64(relayerFees, "relayerFees");
  assertU64(finalAmount, "finalAmount");
  if (finalAmount < 0n) {
    throw new MathematicsAssertionError(
      `Final amount ${String(finalAmount)} is negative after fee deductions`,
      {
        value: finalAmount,
        expectedType: "finalAmount",
        constraint: ">= 0"
      }
    );
  }
  const perLeafCommitments = [];
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    const depositTimestamp = utxo.timestamp;
    const h1Hash = await poseidonAggregator([
      createBn254FieldElement(BigInt(utxo.version)),
      createBn254FieldElement(BigInt(utxo.commitmentIndex)),
      createBn254FieldElement(utxo.senderAddressLow),
      createBn254FieldElement(utxo.senderAddressHigh),
      createBn254FieldElement(BigInt(utxo.relayerFixedSolFees)),
      createBn254FieldElement(utxo.mintAddressLow),
      createBn254FieldElement(utxo.mintAddressHigh),
      BigInt(depositTimestamp.year),
      BigInt(depositTimestamp.month),
      BigInt(depositTimestamp.day),
      BigInt(depositTimestamp.hour),
      BigInt(depositTimestamp.minute),
      BigInt(depositTimestamp.second),
      createBn254FieldElement(BigInt(utxo.poolVolumeSpl)),
      createBn254FieldElement(BigInt(utxo.poolVolumeSol))
    ]);
    const h2Hash = await poseidonHasher([
      BigInt(perLeafAmounts[index]),
      BigInt(perLeafNullifiers[index]),
      BigInt(userCommitment),
      BigInt(destinationAddressLow),
      BigInt(destinationAddressHigh),
      BigInt(perLeafRandomSecrets[index])
    ]);
    const leafCommitment = await poseidonHasher([h1Hash, h2Hash]);
    assertBn254FieldElement(h1Hash, `h1Hash[${String(index)}]`);
    assertBn254FieldElement(h2Hash, `h2Hash[${String(index)}]`);
    assertBn254FieldElement(leafCommitment, `leafCommitment[${String(index)}]`);
    perLeafCommitments.push(leafCommitment);
  }
  const allLinkerEncryptions = [];
  const allLinkerKeystreamCommitments = [];
  const allLinkerKeystreamBlindingFactors = [];
  for (let leafIndex = 0; leafIndex < nLeaves; leafIndex++) {
    const utxo = utxosBatch[leafIndex];
    const leafPlaintexts = [
      BigInt(utxo.senderAddressLow),
      BigInt(utxo.senderAddressHigh),
      BigInt(perLeafAmounts[leafIndex]),
      BigInt(utxo.mintAddressLow),
      BigInt(utxo.mintAddressHigh),
      BigInt(perLeafCommitments[leafIndex])
    ];
    const leafPcCiphertexts = await poseidonEncryptor(
      leafPlaintexts,
      createPoseidonKey(transactionViewingKey)
    );
    if (leafPcCiphertexts.length !== LINKER_ENCRYPTIONS_PER_LEAF) {
      throw new MathematicsAssertionError(
        `Expected ${String(LINKER_ENCRYPTIONS_PER_LEAF)} ciphertexts for leaf ${String(leafIndex)}, got ${String(leafPcCiphertexts.length)}`,
        {
          value: leafPcCiphertexts.length,
          expectedType: "linkerCiphertextCount",
          constraint: `=== ${String(LINKER_ENCRYPTIONS_PER_LEAF)}`
        }
      );
    }
    for (const [index, leafPcCiphertext] of leafPcCiphertexts.entries()) {
      assertBn254FieldElement(
        BigInt(leafPcCiphertext),
        `pcCiphertext[${String(leafIndex)}][${String(index)}]`
      );
    }
    allLinkerEncryptions.push(...leafPcCiphertexts);
    const keystreamCounters = [
      0n,
      1n,
      2n,
      3n,
      4n,
      5n
    ];
    const leafPcKeys = await poseidonKeystreamGenerator(
      keystreamCounters,
      createPoseidonKey(transactionViewingKey)
    );
    for (let index = 0; index < LINKER_ENCRYPTIONS_PER_LEAF; index++) {
      const key = leafPcKeys.get(BigInt(index));
      if (key === void 0) {
        throw new MathematicsAssertionError(
          `Missing keystream key for leaf ${String(leafIndex)}, index ${String(index)}`,
          {
            value: void 0,
            expectedType: "keystreamKey",
            constraint: "defined"
          }
        );
      }
      assertBn254FieldElement(BigInt(key), `pcKey[${String(leafIndex)}][${String(index)}]`);
    }
    for (let keyIndex = 0; keyIndex < LINKER_ENCRYPTIONS_PER_LEAF; keyIndex++) {
      const globalKeyIndex = leafIndex * LINKER_ENCRYPTIONS_PER_LEAF + keyIndex;
      const blindingFactorOffsetBytes = kmac256(
        new TextEncoder().encode(
          `${LINKER_KEYSTREAM_BLINDING_FACTOR_DOMAIN}${String(globalKeyIndex)}`
        ),
        encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256)
      );
      const blindingFactorOffset = decodeU256BeBytesToU256(
        blindingFactorOffsetBytes
      );
      const blindingFactor = deriveKeystreamBlindingFactor(blindingFactorOffset);
      assertBn254FieldElement(
        blindingFactor,
        `linkerKeystreamBlindingFactor[${String(leafIndex)}][${String(keyIndex)}]`
      );
      allLinkerKeystreamBlindingFactors.push(blindingFactor);
      const keystreamKeyValue = leafPcKeys.get(BigInt(keyIndex));
      if (keystreamKeyValue === void 0) {
        throw new MathematicsAssertionError(
          `Missing keystream key for leaf ${String(leafIndex)}, keyIndex ${String(keyIndex)}`,
          {
            value: void 0,
            expectedType: "keystreamKey",
            constraint: "defined"
          }
        );
      }
      const keystreamKey = keystreamKeyValue;
      const keystreamCommitment = await keystreamCommitmentGenerator(
        BigInt(keystreamKey),
        blindingFactor
      );
      assertBn254FieldElement(
        keystreamCommitment,
        `linkerKeystreamCommitment[${String(leafIndex)}][${String(keyIndex)}]`
      );
      allLinkerKeystreamCommitments.push(keystreamCommitment);
    }
  }
  const expectedLinkerCount = nLeaves * LINKER_ENCRYPTIONS_PER_LEAF;
  if (allLinkerEncryptions.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} linker encryptions, got ${String(allLinkerEncryptions.length)}`,
      {
        value: allLinkerEncryptions.length,
        expectedType: "linkerEncryptionsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  if (allLinkerKeystreamBlindingFactors.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} keystream blinding factors, got ${String(allLinkerKeystreamBlindingFactors.length)}`,
      {
        value: allLinkerKeystreamBlindingFactors.length,
        expectedType: "keystreamBlindingFactorsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  if (allLinkerKeystreamCommitments.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} keystream commitments, got ${String(allLinkerKeystreamCommitments.length)}`,
      {
        value: allLinkerKeystreamCommitments.length,
        expectedType: "keystreamCommitmentsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  const linkerEncryptionsAggregatedHash = await poseidonAggregator(allLinkerEncryptions);
  const linkerKeystreamCommitmentsAggregatedHash = await poseidonAggregator(
    allLinkerKeystreamCommitments
  );
  assertBn254FieldElement(linkerEncryptionsAggregatedHash, "linkerEncryptionsAggregatedHash");
  assertBn254FieldElement(
    linkerKeystreamCommitmentsAggregatedHash,
    "linkerKeystreamCommitmentsAggregatedHash"
  );
  const nullifierHashes = [];
  for (let index = 0; index < nLeaves; index++) {
    const nullifierHash = await poseidonHasher([
      poseidonPrivateKey,
      perLeafNullifiers[index]
    ]);
    assertBn254FieldElement(nullifierHash, `nullifierHash[${String(index)}]`);
    nullifierHashes.push(nullifierHash);
  }
  const nullifierHashesAggregatedHash = await poseidonAggregator(nullifierHashes);
  assertBn254FieldElement(nullifierHashesAggregatedHash, "nullifierHashesAggregatedHash");
  const rcEncryptor = getRescueEncryptorFromPrivateKey(clientX25519PrivateKey, {
    umbraX25519PublicKey: networkConfig.mxePubkey
  });
  const randomFactorBytes = kmac256(
    new TextEncoder().encode(RANDOM_FACTOR_FOR_POLYNOMIAL_COMMITMENT_DOMAIN),
    encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256)
  );
  const randomFactorU256 = decodeU256BeBytesToU256(randomFactorBytes);
  const randomFactorForPolynomialCommitment = randomFactorU256 % CURVE25519_FIELD_PRIME;
  assertCurve25519FieldElement(
    randomFactorForPolynomialCommitment,
    "randomFactorForPolynomialCommitment"
  );
  const { low: randomFactorLow, high: randomFactorHigh } = splitU256IntoTwoU128s(
    randomFactorForPolynomialCommitment
  );
  assertU128(randomFactorLow, "randomFactorLow");
  assertU128(randomFactorHigh, "randomFactorHigh");
  const mvkLow = BigInt(masterViewingKey) & (1n << 128n) - 1n;
  const mvkHigh = BigInt(masterViewingKey) >> 128n;
  assertU128(mvkLow, "mvkLow");
  assertU128(mvkHigh, "mvkHigh");
  const reconstructedMvk = mvkLow + (mvkHigh << 128n);
  if (reconstructedMvk !== BigInt(masterViewingKey)) {
    throw new MathematicsAssertionError(
      `MVK decomposition failed: ${String(reconstructedMvk)} !== ${String(masterViewingKey)}`,
      {
        value: reconstructedMvk,
        expectedType: "mvkDecomposition",
        constraint: "=== masterViewingKey"
      }
    );
  }
  const rcCiphertextOutputs = await rcEncryptor([
    BigInt(finalAmount),
    BigInt(protocolFees),
    BigInt(relayerFees),
    createRcPlaintext(mvkLow),
    createRcPlaintext(mvkHigh),
    createRcPlaintext(randomFactorLow),
    createRcPlaintext(randomFactorHigh)
  ]);
  const rcEncryptionNonce = rcCiphertextOutputs.nonce;
  assertRcEncryptionNonce(rcEncryptionNonce);
  if (rcCiphertextOutputs.ciphertexts.length !== RESCUE_ENCRYPTIONS_COUNT) {
    throw new MathematicsAssertionError(
      `Expected ${String(RESCUE_ENCRYPTIONS_COUNT)} rescue ciphertexts, got ${String(rcCiphertextOutputs.ciphertexts.length)}`,
      {
        value: rcCiphertextOutputs.ciphertexts.length,
        expectedType: "rcCiphertextCount",
        constraint: `=== ${String(RESCUE_ENCRYPTIONS_COUNT)}`
      }
    );
  }
  const rcEncryptedFinalAmount = rcCiphertextOutputs.ciphertexts[0];
  const rcEncryptedProtocolFees = rcCiphertextOutputs.ciphertexts[1];
  const rcEncryptedRelayerFees = rcCiphertextOutputs.ciphertexts[2];
  const rcEncryptedMvkLow = rcCiphertextOutputs.ciphertexts[3];
  const rcEncryptedMvkHigh = rcCiphertextOutputs.ciphertexts[4];
  const rcEncryptedRandomFactorLow = rcCiphertextOutputs.ciphertexts[5];
  const rcEncryptedRandomFactorHigh = rcCiphertextOutputs.ciphertexts[6];
  assertCurve25519FieldElement(rcEncryptedFinalAmount, "rcEncryptedFinalAmount");
  assertCurve25519FieldElement(rcEncryptedProtocolFees, "rcEncryptedProtocolFees");
  assertCurve25519FieldElement(rcEncryptedRelayerFees, "rcEncryptedRelayerFees");
  assertCurve25519FieldElement(rcEncryptedMvkLow, "rcEncryptedMvkLow");
  assertCurve25519FieldElement(rcEncryptedMvkHigh, "rcEncryptedMvkHigh");
  assertCurve25519FieldElement(rcEncryptedRandomFactorLow, "rcEncryptedRandomFactorLow");
  assertCurve25519FieldElement(rcEncryptedRandomFactorHigh, "rcEncryptedRandomFactorHigh");
  const rcKeyGenerator = getRescueKeyGeneratorFromPrivateKey(clientX25519PrivateKey, {
    umbraX25519PublicKey: networkConfig.mxePubkey
  });
  const rcCounters = [
    0n,
    1n,
    2n,
    3n,
    4n,
    5n,
    6n
  ];
  const rcKeys = await rcKeyGenerator(rcCounters, rcEncryptionNonce);
  for (let index = 0; index < RESCUE_ENCRYPTIONS_COUNT; index++) {
    const key = rcKeys.get(BigInt(index));
    if (key === void 0) {
      throw new MathematicsAssertionError(`Missing rescue key for counter ${String(index)}`, {
        value: void 0,
        expectedType: "rcKey",
        constraint: "defined"
      });
    }
    assertCurve25519FieldElement(key, `rcKey[${String(index)}]`);
  }
  const rcKey0 = rcKeys.get(0n);
  const rcKey1 = rcKeys.get(1n);
  const rcKey2 = rcKeys.get(2n);
  const rcKey3 = rcKeys.get(3n);
  const rcKey4 = rcKeys.get(4n);
  const rcKey5 = rcKeys.get(5n);
  const rcKey6 = rcKeys.get(6n);
  const { low: arcisPublicKeyLow, high: arcisPublicKeyHigh } = splitX25519PublicKeyIntoTwoU128s(clientX25519PublicKey);
  const finalAmountAsLimbs = convertU256ToBase85Limbs(BigInt(finalAmount));
  const protocolFeesAsLimbs = convertU256ToBase85Limbs(BigInt(protocolFees));
  const relayerFeesAsLimbs = convertU256ToBase85Limbs(BigInt(relayerFees));
  const mvkLowAsLimbs = convertU256ToBase85Limbs(createU256(mvkLow));
  const mvkHighAsLimbs = convertU256ToBase85Limbs(createU256(mvkHigh));
  const randomFactorLowAsLimbs = convertU256ToBase85Limbs(BigInt(randomFactorLow));
  const randomFactorHighAsLimbs = convertU256ToBase85Limbs(BigInt(randomFactorHigh));
  const rcEncryptedFinalAmountAsLimbs = convertU256ToBase85Limbs(rcEncryptedFinalAmount);
  const rcEncryptedProtocolFeesAsLimbs = convertU256ToBase85Limbs(rcEncryptedProtocolFees);
  const rcEncryptedRelayerFeesAsLimbs = convertU256ToBase85Limbs(rcEncryptedRelayerFees);
  const rcEncryptedMvkLowAsLimbs = convertU256ToBase85Limbs(rcEncryptedMvkLow);
  const rcEncryptedMvkHighAsLimbs = convertU256ToBase85Limbs(rcEncryptedMvkHigh);
  const rcEncryptedRandomFactorLowAsLimbs = convertU256ToBase85Limbs(rcEncryptedRandomFactorLow);
  const rcEncryptedRandomFactorHighAsLimbs = convertU256ToBase85Limbs(rcEncryptedRandomFactorHigh);
  const keysForFinalAmountLimbs = convertU256ToBase85Limbs(rcKey0);
  const keysForProtocolFeeLimbs = convertU256ToBase85Limbs(rcKey1);
  const keysForRelayerFeeLimbs = convertU256ToBase85Limbs(rcKey2);
  const keysForMvkLowLimbs = convertU256ToBase85Limbs(rcKey3);
  const keysForMvkHighLimbs = convertU256ToBase85Limbs(rcKey4);
  const keysForRandomFactorLowLimbs = convertU256ToBase85Limbs(rcKey5);
  const keysForRandomFactorHighLimbs = convertU256ToBase85Limbs(rcKey6);
  const proofAccountOffset = generateRandomU1282();
  const burnerAccountOffset = generateRandomU1282();
  assertU128(BigInt(proofAccountOffset), "proofAccountOffset");
  assertU128(BigInt(burnerAccountOffset), "burnerAccountOffset");
  const rescueEncryptionCommitmentBlindingFactorOffsetBytes = kmac256(
    new TextEncoder().encode(RESCUE_ENCRYPTION_COMMITMENT_BLINDING_FACTOR_DOMAIN),
    encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256),
    { dkLen: KMAC_512_OUTPUT_LENGTH }
  );
  assertU512BeBytes(rescueEncryptionCommitmentBlindingFactorOffsetBytes);
  const blindingFactorForRescueCommitment = bn254FieldElementSampler(
    rescueEncryptionCommitmentBlindingFactorOffsetBytes
  );
  assertBn254FieldElement(blindingFactorForRescueCommitment, "blindingFactorForRescueCommitment");
  const rescueEncryptionCommitmentGenerator = getRescueCommitmentGenerator();
  const getRcKey = /* @__PURE__ */ __name((counter) => {
    const key = rcKeys.get(counter);
    if (key === void 0) {
      throw new MathematicsAssertionError(`Missing rescue key for counter ${String(counter)}`, {
        value: void 0,
        expectedType: "rcKey",
        constraint: "defined"
      });
    }
    return key;
  }, "getRcKey");
  const rcEncryptionCommitment = await rescueEncryptionCommitmentGenerator(
    [
      rcEncryptedFinalAmount,
      rcEncryptedProtocolFees,
      rcEncryptedRelayerFees,
      rcEncryptedMvkLow,
      rcEncryptedMvkHigh,
      rcEncryptedRandomFactorLow,
      rcEncryptedRandomFactorHigh
    ],
    [
      createCurve25519FieldElement(finalAmount),
      createCurve25519FieldElement(protocolFees),
      createCurve25519FieldElement(relayerFees),
      createCurve25519FieldElement(mvkLow),
      createCurve25519FieldElement(mvkHigh),
      createCurve25519FieldElement(randomFactorLow),
      createCurve25519FieldElement(randomFactorHigh)
    ],
    [
      getRcKey(0n),
      getRcKey(1n),
      getRcKey(2n),
      getRcKey(3n),
      getRcKey(4n),
      getRcKey(5n),
      getRcKey(6n)
    ],
    blindingFactorForRescueCommitment,
    rcEncryptionNonce,
    arcisPublicKeyLow,
    arcisPublicKeyHigh
  );
  assertBn254FieldElement(BigInt(rcEncryptionCommitment), "rcEncryptionCommitment");
  let totalRelayerFixedSolFees = 0n;
  for (let index = 0; index < nLeaves; index++) {
    totalRelayerFixedSolFees += BigInt(utxosBatch[index].relayerFixedSolFees);
  }
  assertU64(totalRelayerFixedSolFees, "totalRelayerFixedSolFees");
  const optionalDataBytes = optionalData;
  const fiatShamirInputBytes = new Uint8Array([
    // X25519 public key used for rescue encryption (32 bytes)
    ...clientX25519PublicKey,
    // Encryption nonce (16 bytes LE)
    ...encodeU128ToU128LeBytes(rcEncryptionNonce),
    // 7 rescue ciphertexts (32 bytes each)
    ...encodeU256ToU256LeBytes(rcEncryptedFinalAmount),
    ...encodeU256ToU256LeBytes(rcEncryptedRelayerFees),
    ...encodeU256ToU256LeBytes(rcEncryptedProtocolFees),
    ...encodeU256ToU256LeBytes(rcEncryptedMvkLow),
    ...encodeU256ToU256LeBytes(rcEncryptedMvkHigh),
    ...encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow),
    ...encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh),
    // Rescue encryption commitment (32 bytes)
    ...encodeU256ToU256LeBytes(BigInt(rcEncryptionCommitment))
  ]);
  const fiatShamirChallenge = fiatShamirChallengeGenerator(fiatShamirInputBytes);
  assertCurve25519FieldElement(fiatShamirChallenge, "fiatShamirChallenge");
  const fiatShamirChallengeLimbs = convertU256ToBase85Limbs(fiatShamirChallenge);
  const modulePow = getModuloPowCurve25519Function();
  const challengeSquared = modulePow(fiatShamirChallenge, 2n);
  const challengeCubed = modulePow(fiatShamirChallenge, 3n);
  const challengeToFourth = modulePow(fiatShamirChallenge, 4n);
  const challengeToFifth = modulePow(fiatShamirChallenge, 5n);
  assertCurve25519FieldElement(challengeSquared, "challengeSquared");
  assertCurve25519FieldElement(challengeCubed, "challengeCubed");
  assertCurve25519FieldElement(challengeToFourth, "challengeToFourth");
  assertCurve25519FieldElement(challengeToFifth, "challengeToFifth");
  const challengeSquaredLimbs = convertU256ToBase85Limbs(challengeSquared);
  const challengeCubedLimbs = convertU256ToBase85Limbs(challengeCubed);
  const challengeToFourthLimbs = convertU256ToBase85Limbs(challengeToFourth);
  const challengeToFifthLimbs = convertU256ToBase85Limbs(challengeToFifth);
  const inverseForFinalAmountLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(finalAmountAsLimbs)
  );
  const inverseForMvkLowLimbs = computeBn254LimbwiseSumInverse(limbsToTuple(mvkLowAsLimbs));
  const inverseForMvkHighLimbs = computeBn254LimbwiseSumInverse(limbsToTuple(mvkHighAsLimbs));
  const inverseForRandomFactorLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(randomFactorLowAsLimbs)
  );
  const inverseForRandomFactorHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(randomFactorHighAsLimbs)
  );
  const inverseForChallengeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(fiatShamirChallengeLimbs)
  );
  const inverseForChallengeSquaredLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(challengeSquaredLimbs)
  );
  const inverseForChallengeCubedLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(challengeCubedLimbs)
  );
  const inverseForChallengeToFourthLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(challengeToFourthLimbs)
  );
  const inverseForChallengeToFifthLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(challengeToFifthLimbs)
  );
  const inverseForKeysForFinalAmountLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForFinalAmountLimbs)
  );
  const inverseForKeysForProtocolFeeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForProtocolFeeLimbs)
  );
  const inverseForKeysForRelayerFeeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForRelayerFeeLimbs)
  );
  const inverseForKeysForMvkLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForMvkLowLimbs)
  );
  const inverseForKeysForMvkHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForMvkHighLimbs)
  );
  const inverseForKeysForRandomFactorLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForRandomFactorLowLimbs)
  );
  const inverseForKeysForRandomFactorHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple(keysForRandomFactorHighLimbs)
  );
  assertBn254FieldElement(inverseForFinalAmountLimbs, "inverseForFinalAmountLimbs");
  assertBn254FieldElement(inverseForMvkLowLimbs, "inverseForMvkLowLimbs");
  assertBn254FieldElement(inverseForMvkHighLimbs, "inverseForMvkHighLimbs");
  assertBn254FieldElement(inverseForRandomFactorLowLimbs, "inverseForRandomFactorLowLimbs");
  assertBn254FieldElement(inverseForRandomFactorHighLimbs, "inverseForRandomFactorHighLimbs");
  assertBn254FieldElement(inverseForChallengeLimbs, "inverseForChallengeLimbs");
  assertBn254FieldElement(inverseForChallengeSquaredLimbs, "inverseForChallengeSquaredLimbs");
  assertBn254FieldElement(inverseForChallengeCubedLimbs, "inverseForChallengeCubedLimbs");
  assertBn254FieldElement(inverseForChallengeToFourthLimbs, "inverseForChallengeToFourthLimbs");
  assertBn254FieldElement(inverseForChallengeToFifthLimbs, "inverseForChallengeToFifthLimbs");
  assertBn254FieldElement(inverseForKeysForFinalAmountLimbs, "inverseForKeysForFinalAmountLimbs");
  assertBn254FieldElement(inverseForKeysForProtocolFeeLimbs, "inverseForKeysForProtocolFeeLimbs");
  assertBn254FieldElement(inverseForKeysForRelayerFeeLimbs, "inverseForKeysForRelayerFeeLimbs");
  assertBn254FieldElement(inverseForKeysForMvkLowLimbs, "inverseForKeysForMvkLowLimbs");
  assertBn254FieldElement(inverseForKeysForMvkHighLimbs, "inverseForKeysForMvkHighLimbs");
  assertBn254FieldElement(
    inverseForKeysForRandomFactorLowLimbs,
    "inverseForKeysForRandomFactorLowLimbs"
  );
  assertBn254FieldElement(
    inverseForKeysForRandomFactorHighLimbs,
    "inverseForKeysForRandomFactorHighLimbs"
  );
  const polynomialEvaluator = getPolynomialEvaluatorFunction();
  const polynomialEvaluationResultScalar = polynomialEvaluator(
    [
      createCurve25519FieldElement(finalAmount),
      createCurve25519FieldElement(protocolFees),
      createCurve25519FieldElement(relayerFees),
      createCurve25519FieldElement(mvkLow),
      createCurve25519FieldElement(mvkHigh),
      randomFactorForPolynomialCommitment
    ],
    fiatShamirChallenge
  );
  assertCurve25519FieldElement(
    polynomialEvaluationResultScalar,
    "polynomialEvaluationResultScalar"
  );
  const polynomialEvaluationResultLimbs = convertU256ToBase85Limbs(
    polynomialEvaluationResultScalar
  );
  const merkleTreeDepth = utxosBatch[0].merklePath.length;
  const perLeafPathIndices = [];
  for (let index = 0; index < nLeaves; index++) {
    const rawPathBits = bigintToBinaryArrayLSB(
      BigInt(utxosBatch[index].leafIndex),
      merkleTreeDepth
    );
    const pathIndices = rawPathBits.map((bit) => BigInt(1 - bit));
    perLeafPathIndices.push(pathIndices);
  }
  const { low: relayerPublicKeyLow, high: relayerPublicKeyHigh } = splitAddressToLowHigh(relayerPublicKey);
  assertU128(relayerPublicKeyLow, "relayerPublicKeyLow");
  assertU128(relayerPublicKeyHigh, "relayerPublicKeyHigh");
  const optionalDataLow = BigInt(
    "0x" + Array.from(optionalDataBytes.slice(0, 16)).reverse().map((b) => b.toString(16).padStart(2, "0")).join("")
  );
  const optionalDataHigh = BigInt(
    "0x" + Array.from(optionalDataBytes.slice(16, 32)).reverse().map((b) => b.toString(16).padStart(2, "0")).join("")
  );
  assertU128(optionalDataLow, "optionalDataLow");
  assertU128(optionalDataHigh, "optionalDataHigh");
  const aggregatedHashInputs = [
    // [0] root (U256 LE bytes → Bn254FieldElement)
    createBn254FieldElement(decodeU256LeBytesToU256(merkleRoot)),
    // [1-2] mint address
    createBn254FieldElement(firstUtxo.mintAddressLow),
    createBn254FieldElement(firstUtxo.mintAddressHigh),
    // [3] protocol fees root
    createBn254FieldElement(protocolFeeConfig.merkleRoot),
    // [4] relayer fees root
    createBn254FieldElement(relayerFeeConfig.merkleRoot),
    // [5-7] challenge limbs
    createBn254FieldElement(fiatShamirChallengeLimbs.low),
    createBn254FieldElement(fiatShamirChallengeLimbs.middle),
    createBn254FieldElement(fiatShamirChallengeLimbs.high),
    // [8-10] challenge squared limbs
    createBn254FieldElement(challengeSquaredLimbs.low),
    createBn254FieldElement(challengeSquaredLimbs.middle),
    createBn254FieldElement(challengeSquaredLimbs.high),
    // [11-13] challenge cubed limbs
    createBn254FieldElement(challengeCubedLimbs.low),
    createBn254FieldElement(challengeCubedLimbs.middle),
    createBn254FieldElement(challengeCubedLimbs.high),
    // [14-16] challenge to fourth limbs
    createBn254FieldElement(challengeToFourthLimbs.low),
    createBn254FieldElement(challengeToFourthLimbs.middle),
    createBn254FieldElement(challengeToFourthLimbs.high),
    // [17-19] challenge to fifth limbs
    createBn254FieldElement(challengeToFifthLimbs.low),
    createBn254FieldElement(challengeToFifthLimbs.middle),
    createBn254FieldElement(challengeToFifthLimbs.high),
    // [20-40] rescue encryptions (7 encryptions × 3 limbs = 21 elements)
    createBn254FieldElement(rcEncryptedFinalAmountAsLimbs.low),
    createBn254FieldElement(rcEncryptedFinalAmountAsLimbs.middle),
    createBn254FieldElement(rcEncryptedFinalAmountAsLimbs.high),
    createBn254FieldElement(rcEncryptedProtocolFeesAsLimbs.low),
    createBn254FieldElement(rcEncryptedProtocolFeesAsLimbs.middle),
    createBn254FieldElement(rcEncryptedProtocolFeesAsLimbs.high),
    createBn254FieldElement(rcEncryptedRelayerFeesAsLimbs.low),
    createBn254FieldElement(rcEncryptedRelayerFeesAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRelayerFeesAsLimbs.high),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.low),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.middle),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.high),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.low),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.middle),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.high),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.low),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.high),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.low),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.high),
    // [41] rescue encryption commitment
    rcEncryptionCommitment,
    // [42-44] polynomial evaluation result
    createBn254FieldElement(polynomialEvaluationResultLimbs.low),
    createBn254FieldElement(polynomialEvaluationResultLimbs.middle),
    createBn254FieldElement(polynomialEvaluationResultLimbs.high),
    // [45-46] relayer public key
    createBn254FieldElement(relayerPublicKeyLow),
    createBn254FieldElement(relayerPublicKeyHigh),
    // [47-48] final destination address
    createBn254FieldElement(destinationAddressLow),
    createBn254FieldElement(destinationAddressHigh),
    // [49] linker encryptions aggregated hash
    linkerEncryptionsAggregatedHash,
    // [50] linker keystream commitments aggregated hash
    linkerKeystreamCommitmentsAggregatedHash,
    // [51] nullifier hashes aggregated hash
    nullifierHashesAggregatedHash,
    // [52] total relayer fixed SOL fees
    createBn254FieldElement(totalRelayerFixedSolFees),
    // [53-54] optional data (replaces ephemeral signer address)
    createBn254FieldElement(optionalDataLow),
    createBn254FieldElement(optionalDataHigh),
    // [55-60] TVK timestamp fields (from executionTimestamp in UTC)
    createBn254FieldElement(tvkYear),
    createBn254FieldElement(tvkMonth),
    createBn254FieldElement(tvkDay),
    createBn254FieldElement(tvkHour),
    createBn254FieldElement(tvkMinute),
    createBn254FieldElement(tvkSecond),
    // [61-63] arcis public key and nonce
    createBn254FieldElement(arcisPublicKeyLow),
    createBn254FieldElement(arcisPublicKeyHigh),
    createBn254FieldElement(rcEncryptionNonce)
  ];
  if (aggregatedHashInputs.length !== AGGREGATED_HASH_INPUT_COUNT) {
    throw new MathematicsAssertionError(
      `Expected ${String(AGGREGATED_HASH_INPUT_COUNT)} aggregated hash inputs, got ${String(aggregatedHashInputs.length)}`,
      {
        value: aggregatedHashInputs.length,
        expectedType: "aggregatedHashInputCount",
        constraint: `=== ${String(AGGREGATED_HASH_INPUT_COUNT)}`
      }
    );
  }
  const publicAggregatedHash = await poseidonAggregator(aggregatedHashInputs);
  assertBn254FieldElement(publicAggregatedHash, "publicAggregatedHash");
  const bpsDivisorInverse = computeBn254ModularInverse(createBn254FieldElement(BPS_DIVISOR2));
  const zkCircuitInputs = {
    // Per-UTXO H1 data
    version: utxosBatch.map((u) => createBn254FieldElement(u.version)),
    commitmentIndex: utxosBatch.map((u) => createBn254FieldElement(u.commitmentIndex)),
    senderAddressLow: utxosBatch.map((u) => createBn254FieldElement(u.senderAddressLow)),
    senderAddressHigh: utxosBatch.map((u) => createBn254FieldElement(u.senderAddressHigh)),
    relayerFixedSolFees: utxosBatch.map((u) => createBn254FieldElement(u.relayerFixedSolFees)),
    poolVolumeSpl: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.poolVolumeSpl))),
    poolVolumeSol: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.poolVolumeSol))),
    // Per-UTXO H2 data
    amount: perLeafAmounts.map((a) => createBn254FieldElement(a)),
    nullifier: perLeafNullifiers,
    h2BlindingFactor: perLeafRandomSecrets,
    // Mint address (shared)
    mintAddressLow: createBn254FieldElement(firstUtxo.mintAddressLow),
    mintAddressHigh: createBn254FieldElement(firstUtxo.mintAddressHigh),
    // Unlocking user (from client, NOT ephemeral)
    unlockingUserMasterViewingKey: masterViewingKey,
    unlockingUserMasterViewingKeyBlindingFactor: masterViewingKeyBlindingFactor,
    unlockingUserMasterViewingKeyLow: createBn254FieldElement(mvkLow),
    unlockingUserMasterViewingKeyHigh: createBn254FieldElement(mvkHigh),
    unlockingUserPrivateKey: poseidonPrivateKey,
    unlockingUserPrivateKeyBlindingFactor: poseidonPrivateKeyBlindingFactor,
    // Optional data (replaces ephemeral address)
    ephemeralAddressLow: createBn254FieldElement(optionalDataLow),
    ephemeralAddressHigh: createBn254FieldElement(optionalDataHigh),
    // Merkle tree
    root: createBn254FieldElement(decodeU256LeBytesToU256(merkleRoot)),
    pathElements: utxosBatch.flatMap(
      (u) => u.merklePath.map((p) => createBn254FieldElement(decodeU256LeBytesToU256(p)))
    ),
    pathIndices: perLeafPathIndices.flat(),
    // Per-UTXO timestamps (from deposit)
    utxoYear: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.year))),
    utxoMonth: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.month))),
    utxoDay: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.day))),
    utxoHour: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.hour))),
    utxoMinute: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.minute))),
    utxoSecond: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.second))),
    // TVK timestamps (from execution timestamp in UTC)
    tvkYear: createBn254FieldElement(tvkYear),
    tvkMonth: createBn254FieldElement(tvkMonth),
    tvkDay: createBn254FieldElement(tvkDay),
    tvkHour: createBn254FieldElement(tvkHour),
    tvkMinute: createBn254FieldElement(tvkMinute),
    tvkSecond: createBn254FieldElement(tvkSecond),
    // Linker keystream blinding factors
    linkerKeystreamBlindingFactors: allLinkerKeystreamBlindingFactors,
    // Protocol fee
    protocolFeeBaseFee: createBn254FieldElement(PROTOCOL_FEE_BASE_FEE),
    protocolFeeLowerBound: createBn254FieldElement(PROTOCOL_FEE_LOWER_BOUND),
    protocolFeeUpperBound: createBn254FieldElement(PROTOCOL_FEE_UPPER_BOUND),
    protocolFeeBps: createBn254FieldElement(PROTOCOL_FEE_BPS),
    protocolFeeBpsQuotient: createBn254FieldElement(protocolFeeBpsQuotient),
    protocolFeeBpsRemainder: createBn254FieldElement(protocolFeeBpsRemainder),
    protocolFeeDivisorInverse: bpsDivisorInverse,
    // Relayer fee
    relayerFeeBaseFee: createBn254FieldElement(RELAYER_FEE_BASE_FEE),
    relayerFeeLowerBound: createBn254FieldElement(RELAYER_FEE_LOWER_BOUND),
    relayerFeeUpperBound: createBn254FieldElement(RELAYER_FEE_UPPER_BOUND),
    relayerFeeBps: createBn254FieldElement(RELAYER_FEE_BPS),
    relayerFeeBpsQuotient: createBn254FieldElement(relayerFeeBpsQuotient),
    relayerFeeBpsRemainder: createBn254FieldElement(relayerFeeBpsRemainder),
    relayerFeeDivisorInverse: bpsDivisorInverse,
    // Fee merkle proofs
    protocolFeesRoot: createBn254FieldElement(protocolFeeConfig.merkleRoot),
    protocolFeesMerklePath: protocolFeeConfig.merklePath.map(
      (h) => createBn254FieldElement(h)
    ),
    protocolFeesPathIndices: leafIndexToPathIndices(protocolFeeConfig.leafIndex).map(
      (idx) => createBn254FieldElement(idx)
    ),
    relayerFeesRoot: createBn254FieldElement(relayerFeeConfig.merkleRoot),
    relayerFeesMerklePath: relayerFeeConfig.merklePath.map(
      (h) => createBn254FieldElement(h)
    ),
    relayerFeesPathIndices: leafIndexToPathIndices(relayerFeeConfig.leafIndex).map(
      (idx) => createBn254FieldElement(idx)
    ),
    // Limbwise representations
    finalAmountLimbs: [finalAmountAsLimbs.low, finalAmountAsLimbs.middle, finalAmountAsLimbs.high],
    protocolFeeLimbs: [
      protocolFeesAsLimbs.low,
      protocolFeesAsLimbs.middle,
      protocolFeesAsLimbs.high
    ],
    relayerFeeLimbs: [relayerFeesAsLimbs.low, relayerFeesAsLimbs.middle, relayerFeesAsLimbs.high],
    unlockingUserMasterViewingKeyLowLimbs: [
      mvkLowAsLimbs.low,
      mvkLowAsLimbs.middle,
      mvkLowAsLimbs.high
    ],
    unlockingUserMasterViewingKeyHighLimbs: [
      mvkHighAsLimbs.low,
      mvkHighAsLimbs.middle,
      mvkHighAsLimbs.high
    ],
    randomFactorForPolynomialEvaluationLowLimbs: [
      randomFactorLowAsLimbs.low,
      randomFactorLowAsLimbs.middle,
      randomFactorLowAsLimbs.high
    ],
    randomFactorForPolynomialEvaluationHighLimbs: [
      randomFactorHighAsLimbs.low,
      randomFactorHighAsLimbs.middle,
      randomFactorHighAsLimbs.high
    ],
    // Challenge powers
    challengeLimbs: [
      fiatShamirChallengeLimbs.low,
      fiatShamirChallengeLimbs.middle,
      fiatShamirChallengeLimbs.high
    ],
    challengeSquaredLimbs: [
      challengeSquaredLimbs.low,
      challengeSquaredLimbs.middle,
      challengeSquaredLimbs.high
    ],
    challengeCubedLimbs: [
      challengeCubedLimbs.low,
      challengeCubedLimbs.middle,
      challengeCubedLimbs.high
    ],
    challengeToFourthLimbs: [
      challengeToFourthLimbs.low,
      challengeToFourthLimbs.middle,
      challengeToFourthLimbs.high
    ],
    challengeToFifthLimbs: [
      challengeToFifthLimbs.low,
      challengeToFifthLimbs.middle,
      challengeToFifthLimbs.high
    ],
    // Inverses
    randomFactorForPolynomialEvaluationLowLimbsInverse: inverseForRandomFactorLowLimbs,
    randomFactorForPolynomialEvaluationHighLimbsInverse: inverseForRandomFactorHighLimbs,
    challengeLimbsInverse: inverseForChallengeLimbs,
    challengeSquaredLimbsInverse: inverseForChallengeSquaredLimbs,
    challengeCubedLimbsInverse: inverseForChallengeCubedLimbs,
    challengeToFourthLimbsInverse: inverseForChallengeToFourthLimbs,
    challengeToFifthLimbsInverse: inverseForChallengeToFifthLimbs,
    inverseForUnlockingUserMasterViewingKeyLowLimbs: inverseForMvkLowLimbs,
    inverseForUnlockingUserMasterViewingKeyHighLimbs: inverseForMvkHighLimbs,
    finalAmountLimbsInverse: inverseForFinalAmountLimbs,
    // Rescue encryption keys
    keysForFinalAmountLimbs: [
      keysForFinalAmountLimbs.low,
      keysForFinalAmountLimbs.middle,
      keysForFinalAmountLimbs.high
    ],
    keysForProtocolFeeLimbs: [
      keysForProtocolFeeLimbs.low,
      keysForProtocolFeeLimbs.middle,
      keysForProtocolFeeLimbs.high
    ],
    keysForRelayerFeeLimbs: [
      keysForRelayerFeeLimbs.low,
      keysForRelayerFeeLimbs.middle,
      keysForRelayerFeeLimbs.high
    ],
    keysForUnlockingMasterViewingKeyLowLimbs: [
      keysForMvkLowLimbs.low,
      keysForMvkLowLimbs.middle,
      keysForMvkLowLimbs.high
    ],
    keysForUnlockingMasterViewingKeyHighLimbs: [
      keysForMvkHighLimbs.low,
      keysForMvkHighLimbs.middle,
      keysForMvkHighLimbs.high
    ],
    keysForRandomFactorForPolynomialEvaluationLowLimbs: [
      keysForRandomFactorLowLimbs.low,
      keysForRandomFactorLowLimbs.middle,
      keysForRandomFactorLowLimbs.high
    ],
    keysForRandomFactorForPolynomialEvaluationHighLimbs: [
      keysForRandomFactorHighLimbs.low,
      keysForRandomFactorHighLimbs.middle,
      keysForRandomFactorHighLimbs.high
    ],
    // Key inverses
    keysForFinalAmountLimbsInverse: inverseForKeysForFinalAmountLimbs,
    keysForProtocolFeeLimbsInverse: inverseForKeysForProtocolFeeLimbs,
    keysForRelayerFeeLimbsInverse: inverseForKeysForRelayerFeeLimbs,
    keysForUnlockingMasterViewingKeyLowLimbsInverse: inverseForKeysForMvkLowLimbs,
    keysForUnlockingMasterViewingKeyHighLimbsInverse: inverseForKeysForMvkHighLimbs,
    keysForRandomFactorForPolynomialEvaluationLowLimbsInverse: inverseForKeysForRandomFactorLowLimbs,
    keysForRandomFactorForPolynomialEvaluationHighLimbsInverse: inverseForKeysForRandomFactorHighLimbs,
    // Rescue commitment
    blindingFactorForRescueCommitment,
    nonce: createBn254FieldElement(rcEncryptionNonce),
    arcisPublicKeyLow: createBn254FieldElement(arcisPublicKeyLow),
    arcisPublicKeyHigh: createBn254FieldElement(arcisPublicKeyHigh),
    // Addresses
    relayerPublicKeyLow: createBn254FieldElement(relayerPublicKeyLow),
    relayerPublicKeyHigh: createBn254FieldElement(relayerPublicKeyHigh),
    finalDestinationAddressLow: createBn254FieldElement(destinationAddressLow),
    finalDestinationAddressHigh: createBn254FieldElement(destinationAddressHigh),
    // Public input
    publicAggregatedHash
  };
  const { proofA, proofB, proofC } = await zkProver.prove(
    zkCircuitInputs,
    nLeaves
  );
  assertGroth16ProofA(proofA, "proofA");
  assertGroth16ProofB(proofB, "proofB");
  assertGroth16ProofC(proofC, "proofC");
  const toBase64 = /* @__PURE__ */ __name((bytes) => {
    let binary = "";
    for (const byte of bytes) {
      binary += String.fromCharCode(byte);
    }
    return btoa(binary);
  }, "toBase64");
  const proofAccountData = {
    tvk_timestamp: Math.floor(executionTimestamp.getTime() / 1e3),
    rescue_encryption_public_key: toBase64(new Uint8Array(clientX25519PublicKey)),
    encryption_nonce: rcEncryptionNonce.toString(),
    rescue_encrypted_total_amount: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedFinalAmount))
    ),
    rescue_encrypted_relayer_commission_fee: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRelayerFees))
    ),
    rescue_encrypted_protocol_commission_fee: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedProtocolFees))
    ),
    rescue_encrypted_master_viewing_key_low: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedMvkLow))
    ),
    rescue_encrypted_master_viewing_key_high: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedMvkHigh))
    ),
    rescue_encrypted_blinding_factor_low: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow))
    ),
    rescue_encrypted_blinding_factor_high: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh))
    ),
    rescue_encryption_commitment: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(BigInt(rcEncryptionCommitment)))
    ),
    encryption_validation_polynomial: toBase64(
      new Uint8Array(encodeU256ToU256LeBytes(polynomialEvaluationResultScalar))
    ),
    merkle_root: toBase64(new Uint8Array(merkleRoot)),
    total_relayer_fees: Number(totalRelayerFixedSolFees),
    groth16_proof_a: toBase64(new Uint8Array(proofA)),
    groth16_proof_b: toBase64(new Uint8Array(proofB)),
    groth16_proof_c: toBase64(new Uint8Array(proofC))
  };
  const utxoSlotData = [];
  for (let leafIdx = 0; leafIdx < nLeaves; leafIdx++) {
    const linkerEncStartIdx = leafIdx * 6;
    utxoSlotData.push({
      slot_index: leafIdx,
      nullifier: toBase64(
        new Uint8Array(encodeU256ToU256LeBytes(BigInt(nullifierHashes[leafIdx])))
      ),
      linker_encryptions: allLinkerEncryptions.slice(linkerEncStartIdx, linkerEncStartIdx + 6).map((enc) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(enc))))),
      linker_key_commitments: allLinkerKeystreamCommitments.slice(linkerEncStartIdx, linkerEncStartIdx + 6).map((comm) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(comm)))))
    });
  }
  const claimRequest = {
    variant: "encrypted_balance",
    user_pubkey: destinationAddress,
    mint: mintAddress,
    stealth_pool_index: Number(stealthPoolIndex),
    proof_account_data: proofAccountData,
    utxo_slot_data: utxoSlotData,
    max_utxo_capacity: nLeaves,
    optional_data: toBase64(optionalDataBytes)
  };
  const submitResponse = await submitClaim(claimRequest);
  if (awaitCompletion) {
    const { pollClaimUntilTerminal: pollClaimUntilTerminal2 } = await import('./relayer-NRRMSMNB.js');
    const result = await pollClaimUntilTerminal2(pollClaimStatus, submitResponse.requestId, {
      pollingIntervalMs,
      timeoutMs,
      onProgress
    });
    return { ...result, utxoIds };
  }
  return {
    requestId: submitResponse.requestId,
    status: "received",
    utxoIds
  };
}
__name(processUtxoInBatch, "processUtxoInBatch");
function getReceiverClaimableUtxoToEncryptedBalanceClaimerFunction(args, deps) {
  const { client, generationIndex: providedGenerationIndex } = args;
  if (deps === void 0) {
    throw new MathematicsAssertionError("deps with zkProver and relayer is required", {
      value: void 0,
      expectedType: "GetReceiverClaimableUtxoToEncryptedBalanceClaimerFunctionDeps",
      constraint: "must be provided"
    });
  }
  const fetchBatchMerkleProof = deps.fetchBatchMerkleProof;
  const submitClaim = deps.relayer.submitClaim;
  const pollClaimStatus = deps.relayer.pollClaimStatus;
  const getRelayerAddress = deps.relayer.getRelayerAddress;
  const awaitCompletion = deps.awaitCompletion ?? true;
  const pollingIntervalMs = deps.pollingIntervalMs ?? 3e3;
  const timeoutMs = deps.timeoutMs ?? 12e4;
  const onProgress = deps.onProgress;
  const masterViewingKeyGenerator = deps.keys?.masterViewingKeyGenerator ?? getMasterViewingKeyDeriver({ client });
  const masterViewingKeyBlindingFactorGenerator = deps.keys?.masterViewingKeyBlindingFactorGenerator ?? getMasterViewingKeyBlindingFactorDeriver({ client });
  const poseidonPrivateKeyGenerator = deps.keys?.poseidonPrivateKeyGenerator ?? getPoseidonPrivateKeyDeriver({ client });
  const poseidonBlindingFactorGenerator = deps.keys?.poseidonBlindingFactorGenerator ?? getPoseidonBlindingFactorDeriver({ client });
  const secondViewingKeyGenerator = deps.keys?.secondViewingKeyGenerator ?? getSecondViewingKeyDeriver({ client });
  const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const poseidonHasher = deps.crypto?.poseidonHasher ?? getPoseidonHasher();
  const poseidonAggregator = deps.crypto?.poseidonAggregator ?? getPoseidonAggregator();
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGeneratorFunction = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const userCommitmentGeneratorFunction = deps.crypto?.userCommitmentGenerator ?? getUserCommitmentGeneratorFunction();
  const keystreamCommitmentGeneratorFunction = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const fiatShamirChallengeGeneratorFunction = deps.crypto?.fiatShamirChallengeGenerator ?? getFiatShamirChallengeGeneratorFunction();
  const generateRandomU2562 = deps.random?.generateRandomU256 ?? generateRandomU256;
  const generateRandomU1282 = deps.random?.generateRandomU128 ?? generateRandomU128;
  return async (utxos, optionalData, microLamportsPerAcu) => {
    try {
      if (utxos.length === 0) {
        return { batches: /* @__PURE__ */ new Map() };
      }
      const masterSeedBytes = await client.masterSeed.getMasterSeed();
      const generationIndex = providedGenerationIndex ?? generateRandomU2562();
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      const clientX25519PublicKey = keypairResult.x25519Keypair.publicKey;
      const relayerAddress = await getRelayerAddress();
      const stealthPoolIndex = 0n;
      const utxosByDestination = /* @__PURE__ */ new Map();
      for (const utxo of utxos) {
        const existing = utxosByDestination.get(utxo.destinationAddress) ?? [];
        existing.push(utxo);
        utxosByDestination.set(utxo.destinationAddress, existing);
      }
      const batchConfigs = [];
      let batchIndex = 0n;
      for (const [, destinationUtxos] of utxosByDestination) {
        for (let index = 0; index < destinationUtxos.length; index += MAX_UTXOS_PER_PROOF) {
          const batch = destinationUtxos.slice(index, index + MAX_UTXOS_PER_PROOF);
          const batchGenerationOffset = BigInt(generationIndex) + batchIndex;
          batchConfigs.push({
            batchIndex,
            utxos: batch,
            generationOffset: batchGenerationOffset
          });
          batchIndex = batchIndex + 1n;
        }
      }
      const batchResults = await Promise.all(
        batchConfigs.map(async (config) => {
          const treeIndex = config.utxos[0].treeIndex;
          const insertionIndices = config.utxos.map((u) => u.insertionIndex);
          const batchProof = await fetchBatchMerkleProof(treeIndex, insertionIndices);
          const claimableUtxos = enrichWithMerkleProof(config.utxos, batchProof);
          const batchArgs = {
            utxosBatch: claimableUtxos,
            stealthPoolIndex,
            generationIndex: config.generationOffset,
            masterSeedBytes,
            relayerAddress,
            clientX25519PrivateKey,
            clientX25519PublicKey,
            networkConfig: client.networkConfig,
            optionalData: optionalData ?? new Uint8Array(32),
            microLamportsPerAcu: microLamportsPerAcu ?? 0n
          };
          const batchDeps = {
            submitClaim,
            pollClaimStatus,
            awaitCompletion,
            pollingIntervalMs,
            timeoutMs,
            onProgress,
            getRelayerAddress,
            secondViewingKeyGenerator,
            masterViewingKeyGenerator,
            masterViewingKeyBlindingFactorGenerator,
            poseidonPrivateKeyGenerator,
            poseidonBlindingFactorGenerator,
            poseidonHasher,
            poseidonAggregator,
            poseidonEncryptor,
            poseidonKeystreamGenerator: poseidonKeystreamGeneratorFunction,
            userCommitmentGenerator: userCommitmentGeneratorFunction,
            keystreamCommitmentGenerator: keystreamCommitmentGeneratorFunction,
            fiatShamirChallengeGenerator: fiatShamirChallengeGeneratorFunction,
            zkProver: deps.zkProver,
            generateRandomU128: generateRandomU1282
          };
          const batchResult = await processUtxoInBatch(batchArgs, batchDeps);
          return {
            batchIndex: config.batchIndex,
            batchResult
          };
        })
      );
      const resultsByBatch = /* @__PURE__ */ new Map();
      for (const result of batchResults) {
        resultsByBatch.set(result.batchIndex, result.batchResult);
      }
      return {
        batches: resultsByBatch
      };
    } catch (error) {
      if (error instanceof ClaimUtxoError) {
        throw error;
      }
      if (error instanceof CryptographyError) {
        throw new ClaimUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new ClaimUtxoError("initialization", error.message, error);
      }
      throw new ClaimUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  };
}
__name(getReceiverClaimableUtxoToEncryptedBalanceClaimerFunction, "getReceiverClaimableUtxoToEncryptedBalanceClaimerFunction");
var MAX_UTXOS_PER_PROOF2 = 1;
var DOMAIN_CLAIM_MODIFIED_GEN_INDEX2 = "ClaimSelfClaimableUtxoIntoEncryptedBalance / modifiedGenerationIndex";
var DOMAIN_CLAIM_EXPANDED_GEN_INDEX2 = "ClaimSelfClaimableUtxoIntoEncryptedBalance / expandedGenerationIndex";
var BPS_DIVISOR3 = 16384n;
var LINKER_ENCRYPTIONS_PER_LEAF2 = 6;
var LINKER_KEYSTREAM_BLINDING_FACTOR_DOMAIN2 = "receiverClaimUtxoToEta / linkerKeystreamBlindingFactor";
var RESCUE_ENCRYPTIONS_COUNT2 = 7;
var RANDOM_FACTOR_FOR_POLYNOMIAL_COMMITMENT_DOMAIN2 = "receiverClaimUtxoToEta / randomFactorForPolynomialCommitment";
var RESCUE_ENCRYPTION_COMMITMENT_BLINDING_FACTOR_DOMAIN2 = "receiverClaimUtxoToEta / rescueEncryptionCommitmentBlindingFactor";
var KMAC_512_OUTPUT_LENGTH2 = 64;
var AGGREGATED_HASH_INPUT_COUNT2 = 64;
function reconstructAddressFromU128Parts2(low, high) {
  const bytes = new Uint8Array(32);
  let lowValue = BigInt(low);
  for (let index = 0; index < 16; index++) {
    bytes[index] = Number(lowValue & 0xffn);
    lowValue >>= 8n;
  }
  let highValue = BigInt(high);
  for (let index = 0; index < 16; index++) {
    bytes[16 + index] = Number(highValue & 0xffn);
    highValue >>= 8n;
  }
  const decoder = getAddressDecoder();
  return decoder.decode(bytes);
}
__name(reconstructAddressFromU128Parts2, "reconstructAddressFromU128Parts");
async function computeTransactionViewingKeyFromMvk(masterViewingKey, mintAddressLow, mintAddressHigh, year, month, day, hour, minute, second, hasher) {
  const h0 = await hasher([
    masterViewingKey,
    createBn254FieldElement(mintAddressLow),
    createBn254FieldElement(mintAddressHigh)
  ]);
  const h1 = await hasher([h0, createBn254FieldElement(year)]);
  const h2 = await hasher([h1, createBn254FieldElement(month)]);
  const h3 = await hasher([h2, createBn254FieldElement(day)]);
  const h4 = await hasher([h3, createBn254FieldElement(hour)]);
  const h5 = await hasher([h4, createBn254FieldElement(minute)]);
  const tvk = await hasher([h5, createBn254FieldElement(second)]);
  return tvk;
}
__name(computeTransactionViewingKeyFromMvk, "computeTransactionViewingKeyFromMvk");
function limbsToTuple2(limbs) {
  return [limbs.low, limbs.middle, limbs.high];
}
__name(limbsToTuple2, "limbsToTuple");
async function processUtxoInBatch2(args, deps) {
  const {
    utxosBatch,
    stealthPoolIndex,
    generationIndex,
    masterSeedBytes,
    relayerAddress: relayerPublicKey,
    clientX25519PrivateKey,
    clientX25519PublicKey,
    networkConfig,
    optionalData,
    microLamportsPerAcu
  } = args;
  const utxoIds = utxosBatch.map(
    (utxo) => `${String(stealthPoolIndex)}:${String(utxo.leafIndex)}`
  );
  const {
    submitClaim,
    pollClaimStatus,
    awaitCompletion,
    pollingIntervalMs,
    timeoutMs,
    onProgress,
    getRelayerAddress,
    ephemeralUtxoMvkGenerator,
    ephemeralUtxoMvkBlindingFactorGenerator,
    ephemeralUtxoPoseidonKeyGenerator,
    ephemeralUtxoPoseidonKeyBlindingFactorGenerator,
    poseidonHasher,
    poseidonAggregator,
    poseidonEncryptor,
    poseidonKeystreamGenerator,
    userCommitmentGenerator,
    keystreamCommitmentGenerator,
    fiatShamirChallengeGenerator,
    zkProver,
    generateRandomU128: generateRandomU1282
  } = deps;
  const protocolFeeProvider = getHardcodedClaimUtxoProtocolFeeProvider();
  const relayerFeeProvider = getHardcodedClaimUtxoRelayerFeeProvider();
  assertU128(BigInt(stealthPoolIndex), "stealthPoolIndex");
  assertU256(BigInt(generationIndex), "generationIndex");
  const nLeaves = utxosBatch.length;
  if (nLeaves === 0) {
    throw new MathematicsAssertionError("utxosBatch must be non-empty", {
      value: nLeaves,
      expectedType: "utxosBatch",
      constraint: "length > 0"
    });
  }
  if (nLeaves > MAX_UTXOS_PER_PROOF2) {
    throw new MathematicsAssertionError(
      `utxosBatch length ${String(nLeaves)} exceeds maximum ${String(MAX_UTXOS_PER_PROOF2)}`,
      {
        value: nLeaves,
        expectedType: "utxosBatch",
        constraint: `length <= ${String(MAX_UTXOS_PER_PROOF2)}`
      }
    );
  }
  const destinationAddress = utxosBatch[0].destinationAddress;
  for (let index = 1; index < nLeaves; index++) {
    if (utxosBatch[index].destinationAddress !== destinationAddress) {
      throw new MathematicsAssertionError(
        `Destination address mismatch at index ${String(index)}: expected ${destinationAddress}, got ${utxosBatch[index].destinationAddress}`,
        {
          value: utxosBatch[index].destinationAddress,
          expectedType: "destinationAddress",
          constraint: `all UTXOs must have same destination: ${destinationAddress}`
        }
      );
    }
  }
  const firstUtxo = utxosBatch[0];
  const merkleRoot = firstUtxo.merkleRoot;
  assertU256LeBytes(merkleRoot);
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    if (utxo.merklePath.length === 0) {
      throw new MathematicsAssertionError(`merklePath[${String(index)}] must be non-empty`, {
        value: utxo.merklePath.length,
        expectedType: `merklePath[${String(index)}]`,
        constraint: "length > 0"
      });
    }
    for (const sibling of utxo.merklePath) {
      assertU256LeBytes(sibling);
    }
    const maxLeafIndex = (1n << BigInt(utxo.merklePath.length)) - 1n;
    const leafIndexBigInt = BigInt(utxo.leafIndex);
    if (leafIndexBigInt < 0n || leafIndexBigInt > maxLeafIndex) {
      throw new MathematicsAssertionError(
        `leafIndex[${String(index)}] value ${String(leafIndexBigInt)} is out of range for tree depth ${String(utxo.merklePath.length)}`,
        {
          value: leafIndexBigInt,
          expectedType: `leafIndex[${String(index)}]`,
          constraint: `0 <= leafIndex <= ${String(maxLeafIndex)}`
        }
      );
    }
    assertU128(leafIndexBigInt, `leafIndex[${String(index)}]`);
    const versionBigInt = BigInt(utxo.version);
    assertU64(versionBigInt, `version[${String(index)}]`);
    if (versionBigInt === 0n) {
      throw new MathematicsAssertionError(`version[${String(index)}] must not be zero`, {
        value: versionBigInt,
        expectedType: `version[${String(index)}]`,
        constraint: "version !== 0"
      });
    }
    assertU64(BigInt(utxo.amount), `amount[${String(index)}]`);
    assertU128(BigInt(utxo.senderAddressLow), `senderAddressLow[${String(index)}]`);
    assertU128(BigInt(utxo.senderAddressHigh), `senderAddressHigh[${String(index)}]`);
    assertU128(BigInt(utxo.mintAddressLow), `mintAddressLow[${String(index)}]`);
    assertU128(BigInt(utxo.mintAddressHigh), `mintAddressHigh[${String(index)}]`);
    assertU64(BigInt(utxo.relayerFixedSolFees), `relayerFixedSolFees[${String(index)}]`);
    assertYear(BigInt(utxo.timestamp.year));
    assertMonth(BigInt(utxo.timestamp.month));
    assertDay(BigInt(utxo.timestamp.day));
    assertHour(BigInt(utxo.timestamp.hour));
    assertMinute(BigInt(utxo.timestamp.minute));
    assertSecond(BigInt(utxo.timestamp.second));
    assertU128(
      BigInt(utxo.depositModifiedGenerationIndex),
      `depositModifiedGenerationIndex[${String(index)}]`
    );
  }
  const generationIndexBytes = new TextEncoder().encode(String(generationIndex));
  const modifiedGenerationIndex = kmac256(
    new TextEncoder().encode(DOMAIN_CLAIM_MODIFIED_GEN_INDEX2),
    masterSeedBytes,
    { dkLen: 16, personalization: generationIndexBytes }
  );
  const claimModifiedGenerationIndex = kmac256(
    new TextEncoder().encode(DOMAIN_CLAIM_EXPANDED_GEN_INDEX2),
    masterSeedBytes,
    { dkLen: 32, personalization: modifiedGenerationIndex }
  );
  let claimModifiedGenerationIndexU256 = 0n;
  for (let index = 0; index < 32; index++) {
    claimModifiedGenerationIndexU256 |= BigInt(claimModifiedGenerationIndex[index]) << BigInt(index * 8);
  }
  const depositModifiedGenerationIndexValue = BigInt(firstUtxo.depositModifiedGenerationIndex);
  const depositModifiedGenerationIndexBytes = new Uint8Array(16);
  let indexValueForExpansion = depositModifiedGenerationIndexValue;
  for (let index = 0; index < 16; index++) {
    depositModifiedGenerationIndexBytes[index] = Number(indexValueForExpansion & 0xffn);
    indexValueForExpansion >>= 8n;
  }
  const expandedModifiedGenerationIndex = expandModifiedGenerationIndex(
    depositModifiedGenerationIndexBytes
  );
  let ephemeralOffset = 0n;
  for (let index = 0; index < 32; index++) {
    ephemeralOffset |= BigInt(expandedModifiedGenerationIndex[index]) << BigInt(index * 8);
  }
  const ephemeralOffsetU256 = ephemeralOffset;
  const [
    ephemeralMasterViewingKey,
    ephemeralMasterViewingKeyBlindingFactor,
    ephemeralPoseidonPrivateKey,
    ephemeralPoseidonPrivateKeyBlindingFactor
  ] = await Promise.all([
    ephemeralUtxoMvkGenerator(ephemeralOffsetU256),
    ephemeralUtxoMvkBlindingFactorGenerator(ephemeralOffsetU256),
    ephemeralUtxoPoseidonKeyGenerator(ephemeralOffsetU256),
    ephemeralUtxoPoseidonKeyBlindingFactorGenerator(ephemeralOffsetU256)
  ]);
  const userCommitment = await userCommitmentGenerator(
    ephemeralMasterViewingKey,
    ephemeralMasterViewingKeyBlindingFactor,
    ephemeralPoseidonPrivateKey,
    ephemeralPoseidonPrivateKeyBlindingFactor
  );
  assertBn254FieldElement(ephemeralMasterViewingKey);
  assertBn254FieldElement(ephemeralMasterViewingKeyBlindingFactor);
  assertBn254FieldElement(ephemeralPoseidonPrivateKey);
  assertBn254FieldElement(ephemeralPoseidonPrivateKeyBlindingFactor);
  assertBn254FieldElement(userCommitment);
  const executionTimestamp = /* @__PURE__ */ new Date();
  const tvkYear = BigInt(executionTimestamp.getUTCFullYear());
  const tvkMonth = BigInt(executionTimestamp.getUTCMonth() + 1);
  const tvkDay = BigInt(executionTimestamp.getUTCDate());
  const tvkHour = BigInt(executionTimestamp.getUTCHours());
  const tvkMinute = BigInt(executionTimestamp.getUTCMinutes());
  const tvkSecond = BigInt(executionTimestamp.getUTCSeconds());
  assertYear(tvkYear);
  assertMonth(tvkMonth);
  assertDay(tvkDay);
  assertHour(tvkHour);
  assertMinute(tvkMinute);
  assertSecond(tvkSecond);
  const mintAddress = reconstructAddressFromU128Parts2(
    firstUtxo.mintAddressLow,
    firstUtxo.mintAddressHigh
  );
  const transactionViewingKey = await computeTransactionViewingKeyFromMvk(
    ephemeralMasterViewingKey,
    firstUtxo.mintAddressLow,
    firstUtxo.mintAddressHigh,
    tvkYear,
    tvkMonth,
    tvkDay,
    tvkHour,
    tvkMinute,
    tvkSecond,
    poseidonHasher
  );
  assertBn254FieldElement(transactionViewingKey);
  const perLeafNullifiers = [];
  const perLeafRandomSecrets = [];
  const perLeafAmounts = [];
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    const depositModifiedGenerationIndexValue2 = BigInt(utxo.depositModifiedGenerationIndex);
    const depositModifiedGenerationIndexBytes2 = new Uint8Array(16);
    let indexValue = depositModifiedGenerationIndexValue2;
    for (let index2 = 0; index2 < 16; index2++) {
      depositModifiedGenerationIndexBytes2[index2] = Number(indexValue & 0xffn);
      indexValue >>= 8n;
    }
    const nullifier = deriveNullifierFromModifiedGenerationIndex(
      depositModifiedGenerationIndexBytes2
    );
    const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(
      depositModifiedGenerationIndexBytes2
    );
    perLeafNullifiers.push(nullifier);
    perLeafRandomSecrets.push(randomSecret);
    perLeafAmounts.push(utxo.amount);
    assertBn254FieldElement(nullifier);
    assertBn254FieldElement(randomSecret);
  }
  const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
  assertU128(destinationAddressLow, "destinationAddressLow");
  assertU128(destinationAddressHigh, "destinationAddressHigh");
  let totalAmount = 0n;
  for (const amt of perLeafAmounts) {
    totalAmount += BigInt(amt);
  }
  assertU64(totalAmount, "totalAmount");
  const protocolFeeConfig = await protocolFeeProvider(totalAmount);
  const PROTOCOL_FEE_BASE_FEE = protocolFeeConfig.slab.baseFee;
  const PROTOCOL_FEE_BPS = protocolFeeConfig.slab.bps;
  const PROTOCOL_FEE_LOWER_BOUND = protocolFeeConfig.slab.amountLowerBound;
  const PROTOCOL_FEE_UPPER_BOUND = protocolFeeConfig.slab.amountUpperBound;
  const amountAfterProtocolBaseFeeForBps = totalAmount - PROTOCOL_FEE_BASE_FEE;
  if (amountAfterProtocolBaseFeeForBps < 0n) {
    throw new MathematicsAssertionError(
      `Total amount ${String(totalAmount)} is less than protocol base fee ${String(PROTOCOL_FEE_BASE_FEE)}`,
      {
        value: amountAfterProtocolBaseFeeForBps,
        expectedType: "protocolFeeCalculation",
        constraint: ">= 0"
      }
    );
  }
  const protocolFeeBpsQuotient = amountAfterProtocolBaseFeeForBps * PROTOCOL_FEE_BPS / BPS_DIVISOR3;
  const protocolFeeBpsRemainder = amountAfterProtocolBaseFeeForBps * PROTOCOL_FEE_BPS % BPS_DIVISOR3;
  const protocolFees = PROTOCOL_FEE_BASE_FEE + protocolFeeBpsQuotient;
  const amountAfterProtocolFee = totalAmount - protocolFees;
  assertU64(protocolFees, "protocolFees");
  assertU64(amountAfterProtocolFee, "amountAfterProtocolFee");
  const relayerFeeConfig = await relayerFeeProvider(amountAfterProtocolFee);
  const RELAYER_FEE_BASE_FEE = relayerFeeConfig.slab.baseFee;
  const RELAYER_FEE_BPS = relayerFeeConfig.slab.bps;
  const RELAYER_FEE_LOWER_BOUND = relayerFeeConfig.slab.amountLowerBound;
  const RELAYER_FEE_UPPER_BOUND = relayerFeeConfig.slab.amountUpperBound;
  const amountAfterRelayerBaseFeeForBps = amountAfterProtocolFee - RELAYER_FEE_BASE_FEE;
  if (amountAfterRelayerBaseFeeForBps < 0n) {
    throw new MathematicsAssertionError(
      `Amount after protocol fee ${String(amountAfterProtocolFee)} is less than relayer base fee ${String(RELAYER_FEE_BASE_FEE)}`,
      {
        value: amountAfterRelayerBaseFeeForBps,
        expectedType: "relayerFeeCalculation",
        constraint: ">= 0"
      }
    );
  }
  const relayerFeeBpsQuotient = amountAfterRelayerBaseFeeForBps * RELAYER_FEE_BPS / BPS_DIVISOR3;
  const relayerFeeBpsRemainder = amountAfterRelayerBaseFeeForBps * RELAYER_FEE_BPS % BPS_DIVISOR3;
  const relayerFees = RELAYER_FEE_BASE_FEE + relayerFeeBpsQuotient;
  const finalAmount = amountAfterProtocolFee - relayerFees;
  assertU64(relayerFees, "relayerFees");
  assertU64(finalAmount, "finalAmount");
  if (finalAmount < 0n) {
    throw new MathematicsAssertionError(
      `Final amount ${String(finalAmount)} is negative after fee deductions`,
      {
        value: finalAmount,
        expectedType: "finalAmount",
        constraint: ">= 0"
      }
    );
  }
  const perLeafCommitments = [];
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    const depositTimestamp = utxo.timestamp;
    const h1Hash = await poseidonAggregator([
      createBn254FieldElement(BigInt(utxo.version)),
      createBn254FieldElement(BigInt(utxo.commitmentIndex)),
      createBn254FieldElement(utxo.senderAddressLow),
      createBn254FieldElement(utxo.senderAddressHigh),
      createBn254FieldElement(BigInt(utxo.relayerFixedSolFees)),
      createBn254FieldElement(utxo.mintAddressLow),
      createBn254FieldElement(utxo.mintAddressHigh),
      BigInt(depositTimestamp.year),
      BigInt(depositTimestamp.month),
      BigInt(depositTimestamp.day),
      BigInt(depositTimestamp.hour),
      BigInt(depositTimestamp.minute),
      BigInt(depositTimestamp.second),
      createBn254FieldElement(BigInt(utxo.poolVolumeSpl)),
      createBn254FieldElement(BigInt(utxo.poolVolumeSol))
    ]);
    const h2Hash = await poseidonHasher([
      createBn254FieldElement(perLeafAmounts[index]),
      perLeafNullifiers[index],
      userCommitment,
      createBn254FieldElement(destinationAddressLow),
      createBn254FieldElement(destinationAddressHigh),
      perLeafRandomSecrets[index]
    ]);
    const leafCommitment = await poseidonHasher([h1Hash, h2Hash]);
    assertBn254FieldElement(h1Hash, `h1Hash[${String(index)}]`);
    assertBn254FieldElement(h2Hash, `h2Hash[${String(index)}]`);
    assertBn254FieldElement(leafCommitment, `leafCommitment[${String(index)}]`);
    perLeafCommitments.push(leafCommitment);
  }
  const allLinkerEncryptions = [];
  const allLinkerKeystreamCommitments = [];
  const allLinkerKeystreamBlindingFactors = [];
  for (let leafIndex = 0; leafIndex < nLeaves; leafIndex++) {
    const utxo = utxosBatch[leafIndex];
    const leafPlaintexts = [
      BigInt(utxo.senderAddressLow),
      BigInt(utxo.senderAddressHigh),
      BigInt(perLeafAmounts[leafIndex]),
      BigInt(utxo.mintAddressLow),
      BigInt(utxo.mintAddressHigh),
      BigInt(perLeafCommitments[leafIndex])
    ];
    const leafPcCiphertexts = await poseidonEncryptor(
      leafPlaintexts,
      createPoseidonKey(transactionViewingKey)
    );
    if (leafPcCiphertexts.length !== LINKER_ENCRYPTIONS_PER_LEAF2) {
      throw new MathematicsAssertionError(
        `Expected ${String(LINKER_ENCRYPTIONS_PER_LEAF2)} ciphertexts for leaf ${String(leafIndex)}, got ${String(leafPcCiphertexts.length)}`,
        {
          value: leafPcCiphertexts.length,
          expectedType: "linkerCiphertextCount",
          constraint: `=== ${String(LINKER_ENCRYPTIONS_PER_LEAF2)}`
        }
      );
    }
    for (const [index, leafPcCiphertext] of leafPcCiphertexts.entries()) {
      assertBn254FieldElement(
        BigInt(leafPcCiphertext),
        `pcCiphertext[${String(leafIndex)}][${String(index)}]`
      );
    }
    allLinkerEncryptions.push(...leafPcCiphertexts);
    const keystreamCounters = [
      0n,
      1n,
      2n,
      3n,
      4n,
      5n
    ];
    const leafPcKeys = await poseidonKeystreamGenerator(
      keystreamCounters,
      createPoseidonKey(transactionViewingKey)
    );
    for (let index = 0; index < LINKER_ENCRYPTIONS_PER_LEAF2; index++) {
      const key = leafPcKeys.get(BigInt(index));
      if (key === void 0) {
        throw new MathematicsAssertionError(
          `Missing keystream key for leaf ${String(leafIndex)}, index ${String(index)}`,
          {
            value: void 0,
            expectedType: "keystreamKey",
            constraint: "defined"
          }
        );
      }
      assertBn254FieldElement(BigInt(key), `pcKey[${String(leafIndex)}][${String(index)}]`);
    }
    for (let keyIndex = 0; keyIndex < LINKER_ENCRYPTIONS_PER_LEAF2; keyIndex++) {
      const globalKeyIndex = leafIndex * LINKER_ENCRYPTIONS_PER_LEAF2 + keyIndex;
      const blindingFactorOffsetBytes = kmac256(
        new TextEncoder().encode(
          `${LINKER_KEYSTREAM_BLINDING_FACTOR_DOMAIN2}${String(globalKeyIndex)}`
        ),
        encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256)
      );
      const blindingFactorOffset = decodeU256BeBytesToU256(
        blindingFactorOffsetBytes
      );
      const blindingFactor = deriveKeystreamBlindingFactor(blindingFactorOffset);
      assertBn254FieldElement(
        blindingFactor,
        `linkerKeystreamBlindingFactor[${String(leafIndex)}][${String(keyIndex)}]`
      );
      allLinkerKeystreamBlindingFactors.push(blindingFactor);
      const keystreamKey = leafPcKeys.get(BigInt(keyIndex));
      if (keystreamKey === void 0) {
        throw new MathematicsAssertionError(
          `Missing keystream key for commitment at leaf ${String(leafIndex)}, key ${String(keyIndex)}`,
          {
            value: void 0,
            expectedType: "keystreamKey",
            constraint: "defined"
          }
        );
      }
      const keystreamCommitment = await keystreamCommitmentGenerator(
        BigInt(keystreamKey),
        blindingFactor
      );
      assertBn254FieldElement(
        keystreamCommitment,
        `linkerKeystreamCommitment[${String(leafIndex)}][${String(keyIndex)}]`
      );
      allLinkerKeystreamCommitments.push(keystreamCommitment);
    }
  }
  const expectedLinkerCount = nLeaves * LINKER_ENCRYPTIONS_PER_LEAF2;
  if (allLinkerEncryptions.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} linker encryptions, got ${String(allLinkerEncryptions.length)}`,
      {
        value: allLinkerEncryptions.length,
        expectedType: "linkerEncryptionsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  if (allLinkerKeystreamBlindingFactors.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} keystream blinding factors, got ${String(allLinkerKeystreamBlindingFactors.length)}`,
      {
        value: allLinkerKeystreamBlindingFactors.length,
        expectedType: "keystreamBlindingFactorsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  if (allLinkerKeystreamCommitments.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} keystream commitments, got ${String(allLinkerKeystreamCommitments.length)}`,
      {
        value: allLinkerKeystreamCommitments.length,
        expectedType: "keystreamCommitmentsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  const linkerEncryptionsAggregatedHash = await poseidonAggregator(allLinkerEncryptions);
  const linkerKeystreamCommitmentsAggregatedHash = await poseidonAggregator(
    allLinkerKeystreamCommitments
  );
  assertBn254FieldElement(linkerEncryptionsAggregatedHash, "linkerEncryptionsAggregatedHash");
  assertBn254FieldElement(
    linkerKeystreamCommitmentsAggregatedHash,
    "linkerKeystreamCommitmentsAggregatedHash"
  );
  const nullifierHashes = [];
  for (let index = 0; index < nLeaves; index++) {
    const nullifierHash = await poseidonHasher([
      ephemeralPoseidonPrivateKey,
      perLeafNullifiers[index]
    ]);
    assertBn254FieldElement(nullifierHash, `nullifierHash[${String(index)}]`);
    nullifierHashes.push(nullifierHash);
  }
  const nullifierHashesAggregatedHash = await poseidonAggregator(nullifierHashes);
  assertBn254FieldElement(nullifierHashesAggregatedHash, "nullifierHashesAggregatedHash");
  const rcEncryptor = getRescueEncryptorFromPrivateKey(clientX25519PrivateKey, {
    umbraX25519PublicKey: networkConfig.mxePubkey
  });
  const randomFactorBytes = kmac256(
    new TextEncoder().encode(RANDOM_FACTOR_FOR_POLYNOMIAL_COMMITMENT_DOMAIN2),
    encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256)
  );
  const randomFactorU256 = decodeU256BeBytesToU256(randomFactorBytes);
  const randomFactorForPolynomialCommitment = randomFactorU256 % CURVE25519_FIELD_PRIME;
  assertCurve25519FieldElement(
    randomFactorForPolynomialCommitment,
    "randomFactorForPolynomialCommitment"
  );
  const { low: randomFactorLow, high: randomFactorHigh } = splitU256IntoTwoU128s(
    randomFactorForPolynomialCommitment
  );
  assertU128(randomFactorLow, "randomFactorLow");
  assertU128(randomFactorHigh, "randomFactorHigh");
  const ephemeralMvkLow = BigInt(ephemeralMasterViewingKey) & (1n << 128n) - 1n;
  const ephemeralMvkHigh = BigInt(ephemeralMasterViewingKey) >> 128n;
  assertU128(ephemeralMvkLow, "ephemeralMvkLow");
  assertU128(ephemeralMvkHigh, "ephemeralMvkHigh");
  const reconstructedEphemeralMvk = ephemeralMvkLow + (ephemeralMvkHigh << 128n);
  if (reconstructedEphemeralMvk !== BigInt(ephemeralMasterViewingKey)) {
    throw new MathematicsAssertionError(
      `Ephemeral MVK decomposition failed: ${String(reconstructedEphemeralMvk)} !== ${String(ephemeralMasterViewingKey)}`,
      {
        value: reconstructedEphemeralMvk,
        expectedType: "ephemeralMvkDecomposition",
        constraint: "=== ephemeralMasterViewingKey"
      }
    );
  }
  const rcCiphertextOutputs = await rcEncryptor([
    BigInt(finalAmount),
    BigInt(protocolFees),
    BigInt(relayerFees),
    createRcPlaintext(ephemeralMvkLow),
    createRcPlaintext(ephemeralMvkHigh),
    createRcPlaintext(randomFactorLow),
    createRcPlaintext(randomFactorHigh)
  ]);
  const rcEncryptionNonce = rcCiphertextOutputs.nonce;
  assertRcEncryptionNonce(rcEncryptionNonce);
  if (rcCiphertextOutputs.ciphertexts.length !== RESCUE_ENCRYPTIONS_COUNT2) {
    throw new MathematicsAssertionError(
      `Expected ${String(RESCUE_ENCRYPTIONS_COUNT2)} rescue ciphertexts, got ${String(rcCiphertextOutputs.ciphertexts.length)}`,
      {
        value: rcCiphertextOutputs.ciphertexts.length,
        expectedType: "rcCiphertextCount",
        constraint: `=== ${String(RESCUE_ENCRYPTIONS_COUNT2)}`
      }
    );
  }
  const rcEncryptedFinalAmount = rcCiphertextOutputs.ciphertexts[0];
  const rcEncryptedProtocolFees = rcCiphertextOutputs.ciphertexts[1];
  const rcEncryptedRelayerFees = rcCiphertextOutputs.ciphertexts[2];
  const rcEncryptedMvkLow = rcCiphertextOutputs.ciphertexts[3];
  const rcEncryptedMvkHigh = rcCiphertextOutputs.ciphertexts[4];
  const rcEncryptedRandomFactorLow = rcCiphertextOutputs.ciphertexts[5];
  const rcEncryptedRandomFactorHigh = rcCiphertextOutputs.ciphertexts[6];
  assertCurve25519FieldElement(rcEncryptedFinalAmount, "rcEncryptedFinalAmount");
  assertCurve25519FieldElement(rcEncryptedProtocolFees, "rcEncryptedProtocolFees");
  assertCurve25519FieldElement(rcEncryptedRelayerFees, "rcEncryptedRelayerFees");
  assertCurve25519FieldElement(rcEncryptedMvkLow, "rcEncryptedMvkLow");
  assertCurve25519FieldElement(rcEncryptedMvkHigh, "rcEncryptedMvkHigh");
  assertCurve25519FieldElement(rcEncryptedRandomFactorLow, "rcEncryptedRandomFactorLow");
  assertCurve25519FieldElement(rcEncryptedRandomFactorHigh, "rcEncryptedRandomFactorHigh");
  const rcKeyGenerator = getRescueKeyGeneratorFromPrivateKey(clientX25519PrivateKey, {
    umbraX25519PublicKey: networkConfig.mxePubkey
  });
  const rcCounters = [
    0n,
    1n,
    2n,
    3n,
    4n,
    5n,
    6n
  ];
  const rcKeys = await rcKeyGenerator(rcCounters, rcEncryptionNonce);
  for (let index = 0; index < RESCUE_ENCRYPTIONS_COUNT2; index++) {
    const key = rcKeys.get(BigInt(index));
    if (key === void 0) {
      throw new MathematicsAssertionError(`Missing rescue key for counter ${String(index)}`, {
        value: void 0,
        expectedType: "rcKey",
        constraint: "defined"
      });
    }
    assertCurve25519FieldElement(key, `rcKey[${String(index)}]`);
  }
  const rcKey0 = rcKeys.get(0n);
  const rcKey1 = rcKeys.get(1n);
  const rcKey2 = rcKeys.get(2n);
  const rcKey3 = rcKeys.get(3n);
  const rcKey4 = rcKeys.get(4n);
  const rcKey5 = rcKeys.get(5n);
  const rcKey6 = rcKeys.get(6n);
  const { low: arcisPublicKeyLow, high: arcisPublicKeyHigh } = splitX25519PublicKeyIntoTwoU128s(clientX25519PublicKey);
  const finalAmountAsLimbs = convertU256ToBase85Limbs(BigInt(finalAmount));
  const protocolFeesAsLimbs = convertU256ToBase85Limbs(BigInt(protocolFees));
  const relayerFeesAsLimbs = convertU256ToBase85Limbs(BigInt(relayerFees));
  const ephemeralMvkLowAsLimbs = convertU256ToBase85Limbs(createU256(ephemeralMvkLow));
  const ephemeralMvkHighAsLimbs = convertU256ToBase85Limbs(createU256(ephemeralMvkHigh));
  const randomFactorLowAsLimbs = convertU256ToBase85Limbs(BigInt(randomFactorLow));
  const randomFactorHighAsLimbs = convertU256ToBase85Limbs(BigInt(randomFactorHigh));
  const rcEncryptedFinalAmountAsLimbs = convertU256ToBase85Limbs(rcEncryptedFinalAmount);
  const rcEncryptedProtocolFeesAsLimbs = convertU256ToBase85Limbs(rcEncryptedProtocolFees);
  const rcEncryptedRelayerFeesAsLimbs = convertU256ToBase85Limbs(rcEncryptedRelayerFees);
  const rcEncryptedMvkLowAsLimbs = convertU256ToBase85Limbs(rcEncryptedMvkLow);
  const rcEncryptedMvkHighAsLimbs = convertU256ToBase85Limbs(rcEncryptedMvkHigh);
  const rcEncryptedRandomFactorLowAsLimbs = convertU256ToBase85Limbs(rcEncryptedRandomFactorLow);
  const rcEncryptedRandomFactorHighAsLimbs = convertU256ToBase85Limbs(rcEncryptedRandomFactorHigh);
  const keysForFinalAmountLimbs = convertU256ToBase85Limbs(rcKey0);
  const keysForProtocolFeeLimbs = convertU256ToBase85Limbs(rcKey1);
  const keysForRelayerFeeLimbs = convertU256ToBase85Limbs(rcKey2);
  const keysForMvkLowLimbs = convertU256ToBase85Limbs(rcKey3);
  const keysForMvkHighLimbs = convertU256ToBase85Limbs(rcKey4);
  const keysForRandomFactorLowLimbs = convertU256ToBase85Limbs(rcKey5);
  const keysForRandomFactorHighLimbs = convertU256ToBase85Limbs(rcKey6);
  const proofAccountOffset = generateRandomU1282();
  const burnerAccountOffset = generateRandomU1282();
  assertU128(BigInt(proofAccountOffset), "proofAccountOffset");
  assertU128(BigInt(burnerAccountOffset), "burnerAccountOffset");
  const rescueEncryptionCommitmentBlindingFactorOffsetBytes = kmac256(
    new TextEncoder().encode(RESCUE_ENCRYPTION_COMMITMENT_BLINDING_FACTOR_DOMAIN2),
    encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256),
    { dkLen: KMAC_512_OUTPUT_LENGTH2 }
  );
  assertU512BeBytes(rescueEncryptionCommitmentBlindingFactorOffsetBytes);
  const blindingFactorForRescueCommitment = bn254FieldElementSampler(
    rescueEncryptionCommitmentBlindingFactorOffsetBytes
  );
  assertBn254FieldElement(blindingFactorForRescueCommitment, "blindingFactorForRescueCommitment");
  const rescueEncryptionCommitmentGenerator = getRescueCommitmentGenerator();
  const rcEncryptionCommitment = await rescueEncryptionCommitmentGenerator(
    [
      rcEncryptedFinalAmount,
      rcEncryptedProtocolFees,
      rcEncryptedRelayerFees,
      rcEncryptedMvkLow,
      rcEncryptedMvkHigh,
      rcEncryptedRandomFactorLow,
      rcEncryptedRandomFactorHigh
    ],
    [
      createCurve25519FieldElement(finalAmount),
      createCurve25519FieldElement(protocolFees),
      createCurve25519FieldElement(relayerFees),
      createCurve25519FieldElement(ephemeralMvkLow),
      createCurve25519FieldElement(ephemeralMvkHigh),
      createCurve25519FieldElement(randomFactorLow),
      createCurve25519FieldElement(randomFactorHigh)
    ],
    [
      createCurve25519FieldElement(rcKey0),
      createCurve25519FieldElement(rcKey1),
      createCurve25519FieldElement(rcKey2),
      createCurve25519FieldElement(rcKey3),
      createCurve25519FieldElement(rcKey4),
      createCurve25519FieldElement(rcKey5),
      createCurve25519FieldElement(rcKey6)
    ],
    blindingFactorForRescueCommitment,
    rcEncryptionNonce,
    arcisPublicKeyLow,
    arcisPublicKeyHigh
  );
  assertBn254FieldElement(BigInt(rcEncryptionCommitment), "rcEncryptionCommitment");
  let totalRelayerFixedSolFees = 0n;
  for (let index = 0; index < nLeaves; index++) {
    totalRelayerFixedSolFees += BigInt(utxosBatch[index].relayerFixedSolFees);
  }
  assertU64(totalRelayerFixedSolFees, "totalRelayerFixedSolFees");
  const optionalDataBytes = optionalData;
  const fiatShamirInputBytes = new Uint8Array([
    // X25519 public key used for rescue encryption (32 bytes)
    ...clientX25519PublicKey,
    // Encryption nonce (16 bytes LE)
    ...encodeU128ToU128LeBytes(rcEncryptionNonce),
    // 7 rescue ciphertexts (32 bytes each)
    ...encodeU256ToU256LeBytes(rcEncryptedFinalAmount),
    ...encodeU256ToU256LeBytes(rcEncryptedRelayerFees),
    ...encodeU256ToU256LeBytes(rcEncryptedProtocolFees),
    ...encodeU256ToU256LeBytes(rcEncryptedMvkLow),
    ...encodeU256ToU256LeBytes(rcEncryptedMvkHigh),
    ...encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow),
    ...encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh),
    // Rescue encryption commitment (32 bytes)
    ...encodeU256ToU256LeBytes(BigInt(rcEncryptionCommitment))
  ]);
  const fiatShamirChallenge = fiatShamirChallengeGenerator(fiatShamirInputBytes);
  assertCurve25519FieldElement(fiatShamirChallenge, "fiatShamirChallenge");
  const fiatShamirChallengeLimbs = convertU256ToBase85Limbs(fiatShamirChallenge);
  const modulePow = getModuloPowCurve25519Function();
  const challengeSquared = modulePow(fiatShamirChallenge, 2n);
  const challengeCubed = modulePow(fiatShamirChallenge, 3n);
  const challengeToFourth = modulePow(fiatShamirChallenge, 4n);
  const challengeToFifth = modulePow(fiatShamirChallenge, 5n);
  assertCurve25519FieldElement(challengeSquared, "challengeSquared");
  assertCurve25519FieldElement(challengeCubed, "challengeCubed");
  assertCurve25519FieldElement(challengeToFourth, "challengeToFourth");
  assertCurve25519FieldElement(challengeToFifth, "challengeToFifth");
  const challengeSquaredLimbs = convertU256ToBase85Limbs(challengeSquared);
  const challengeCubedLimbs = convertU256ToBase85Limbs(challengeCubed);
  const challengeToFourthLimbs = convertU256ToBase85Limbs(challengeToFourth);
  const challengeToFifthLimbs = convertU256ToBase85Limbs(challengeToFifth);
  const inverseForFinalAmountLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(finalAmountAsLimbs)
  );
  const inverseForMvkLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(ephemeralMvkLowAsLimbs)
  );
  const inverseForMvkHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(ephemeralMvkHighAsLimbs)
  );
  const inverseForRandomFactorLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(randomFactorLowAsLimbs)
  );
  const inverseForRandomFactorHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(randomFactorHighAsLimbs)
  );
  const inverseForChallengeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(fiatShamirChallengeLimbs)
  );
  const inverseForChallengeSquaredLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(challengeSquaredLimbs)
  );
  const inverseForChallengeCubedLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(challengeCubedLimbs)
  );
  const inverseForChallengeToFourthLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(challengeToFourthLimbs)
  );
  const inverseForChallengeToFifthLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(challengeToFifthLimbs)
  );
  const inverseForKeysForFinalAmountLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForFinalAmountLimbs)
  );
  const inverseForKeysForProtocolFeeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForProtocolFeeLimbs)
  );
  const inverseForKeysForRelayerFeeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForRelayerFeeLimbs)
  );
  const inverseForKeysForMvkLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForMvkLowLimbs)
  );
  const inverseForKeysForMvkHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForMvkHighLimbs)
  );
  const inverseForKeysForRandomFactorLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForRandomFactorLowLimbs)
  );
  const inverseForKeysForRandomFactorHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple2(keysForRandomFactorHighLimbs)
  );
  assertBn254FieldElement(inverseForFinalAmountLimbs, "inverseForFinalAmountLimbs");
  assertBn254FieldElement(inverseForMvkLowLimbs, "inverseForMvkLowLimbs");
  assertBn254FieldElement(inverseForMvkHighLimbs, "inverseForMvkHighLimbs");
  assertBn254FieldElement(inverseForRandomFactorLowLimbs, "inverseForRandomFactorLowLimbs");
  assertBn254FieldElement(inverseForRandomFactorHighLimbs, "inverseForRandomFactorHighLimbs");
  assertBn254FieldElement(inverseForChallengeLimbs, "inverseForChallengeLimbs");
  assertBn254FieldElement(inverseForChallengeSquaredLimbs, "inverseForChallengeSquaredLimbs");
  assertBn254FieldElement(inverseForChallengeCubedLimbs, "inverseForChallengeCubedLimbs");
  assertBn254FieldElement(inverseForChallengeToFourthLimbs, "inverseForChallengeToFourthLimbs");
  assertBn254FieldElement(inverseForChallengeToFifthLimbs, "inverseForChallengeToFifthLimbs");
  assertBn254FieldElement(inverseForKeysForFinalAmountLimbs, "inverseForKeysForFinalAmountLimbs");
  assertBn254FieldElement(inverseForKeysForProtocolFeeLimbs, "inverseForKeysForProtocolFeeLimbs");
  assertBn254FieldElement(inverseForKeysForRelayerFeeLimbs, "inverseForKeysForRelayerFeeLimbs");
  assertBn254FieldElement(inverseForKeysForMvkLowLimbs, "inverseForKeysForMvkLowLimbs");
  assertBn254FieldElement(inverseForKeysForMvkHighLimbs, "inverseForKeysForMvkHighLimbs");
  assertBn254FieldElement(
    inverseForKeysForRandomFactorLowLimbs,
    "inverseForKeysForRandomFactorLowLimbs"
  );
  assertBn254FieldElement(
    inverseForKeysForRandomFactorHighLimbs,
    "inverseForKeysForRandomFactorHighLimbs"
  );
  const polynomialEvaluator = getPolynomialEvaluatorFunction();
  const polynomialEvaluationResultScalar = polynomialEvaluator(
    [
      createCurve25519FieldElement(finalAmount),
      createCurve25519FieldElement(protocolFees),
      createCurve25519FieldElement(relayerFees),
      createCurve25519FieldElement(ephemeralMvkLow),
      createCurve25519FieldElement(ephemeralMvkHigh),
      randomFactorForPolynomialCommitment
    ],
    fiatShamirChallenge
  );
  assertCurve25519FieldElement(
    polynomialEvaluationResultScalar,
    "polynomialEvaluationResultScalar"
  );
  const polynomialEvaluationResultLimbs = convertU256ToBase85Limbs(
    polynomialEvaluationResultScalar
  );
  const merkleTreeDepth = utxosBatch[0].merklePath.length;
  const perLeafPathIndices = [];
  for (let index = 0; index < nLeaves; index++) {
    const rawPathBits = bigintToBinaryArrayLSB(
      BigInt(utxosBatch[index].leafIndex),
      merkleTreeDepth
    );
    const pathIndices = rawPathBits.map((bit) => BigInt(1 - bit));
    perLeafPathIndices.push(pathIndices);
  }
  const { low: relayerPublicKeyLow, high: relayerPublicKeyHigh } = splitAddressToLowHigh(relayerPublicKey);
  assertU128(relayerPublicKeyLow, "relayerPublicKeyLow");
  assertU128(relayerPublicKeyHigh, "relayerPublicKeyHigh");
  const optionalDataLow = BigInt(
    "0x" + Array.from(optionalDataBytes.slice(0, 16)).reverse().map((b) => b.toString(16).padStart(2, "0")).join("")
  );
  const optionalDataHigh = BigInt(
    "0x" + Array.from(optionalDataBytes.slice(16, 32)).reverse().map((b) => b.toString(16).padStart(2, "0")).join("")
  );
  assertU128(optionalDataLow, "optionalDataLow");
  assertU128(optionalDataHigh, "optionalDataHigh");
  const aggregatedHashInputs = [
    // [0] root (U256 LE bytes → Bn254FieldElement)
    createBn254FieldElement(decodeU256LeBytesToU256(merkleRoot)),
    // [1-2] mint address
    createBn254FieldElement(firstUtxo.mintAddressLow),
    createBn254FieldElement(firstUtxo.mintAddressHigh),
    // [3] protocol fees root
    createBn254FieldElement(protocolFeeConfig.merkleRoot),
    // [4] relayer fees root
    createBn254FieldElement(relayerFeeConfig.merkleRoot),
    // [5-7] challenge limbs
    createBn254FieldElement(fiatShamirChallengeLimbs.low),
    createBn254FieldElement(fiatShamirChallengeLimbs.middle),
    createBn254FieldElement(fiatShamirChallengeLimbs.high),
    // [8-10] challenge squared limbs
    createBn254FieldElement(challengeSquaredLimbs.low),
    createBn254FieldElement(challengeSquaredLimbs.middle),
    createBn254FieldElement(challengeSquaredLimbs.high),
    // [11-13] challenge cubed limbs
    createBn254FieldElement(challengeCubedLimbs.low),
    createBn254FieldElement(challengeCubedLimbs.middle),
    createBn254FieldElement(challengeCubedLimbs.high),
    // [14-16] challenge to fourth limbs
    createBn254FieldElement(challengeToFourthLimbs.low),
    createBn254FieldElement(challengeToFourthLimbs.middle),
    createBn254FieldElement(challengeToFourthLimbs.high),
    // [17-19] challenge to fifth limbs
    createBn254FieldElement(challengeToFifthLimbs.low),
    createBn254FieldElement(challengeToFifthLimbs.middle),
    createBn254FieldElement(challengeToFifthLimbs.high),
    // [20-40] rescue encryptions (7 encryptions × 3 limbs = 21 elements)
    createBn254FieldElement(rcEncryptedFinalAmountAsLimbs.low),
    createBn254FieldElement(rcEncryptedFinalAmountAsLimbs.middle),
    createBn254FieldElement(rcEncryptedFinalAmountAsLimbs.high),
    createBn254FieldElement(rcEncryptedProtocolFeesAsLimbs.low),
    createBn254FieldElement(rcEncryptedProtocolFeesAsLimbs.middle),
    createBn254FieldElement(rcEncryptedProtocolFeesAsLimbs.high),
    createBn254FieldElement(rcEncryptedRelayerFeesAsLimbs.low),
    createBn254FieldElement(rcEncryptedRelayerFeesAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRelayerFeesAsLimbs.high),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.low),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.middle),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.high),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.low),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.middle),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.high),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.low),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.high),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.low),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.high),
    // [41] rescue encryption commitment
    rcEncryptionCommitment,
    // [42-44] polynomial evaluation result
    createBn254FieldElement(polynomialEvaluationResultLimbs.low),
    createBn254FieldElement(polynomialEvaluationResultLimbs.middle),
    createBn254FieldElement(polynomialEvaluationResultLimbs.high),
    // [45-46] relayer public key
    createBn254FieldElement(relayerPublicKeyLow),
    createBn254FieldElement(relayerPublicKeyHigh),
    // [47-48] final destination address
    createBn254FieldElement(destinationAddressLow),
    createBn254FieldElement(destinationAddressHigh),
    // [49] linker encryptions aggregated hash
    linkerEncryptionsAggregatedHash,
    // [50] linker keystream commitments aggregated hash
    linkerKeystreamCommitmentsAggregatedHash,
    // [51] nullifier hashes aggregated hash
    nullifierHashesAggregatedHash,
    // [52] total relayer fixed SOL fees
    createBn254FieldElement(totalRelayerFixedSolFees),
    // [53-54] optional data (replaces ephemeral signer address)
    createBn254FieldElement(optionalDataLow),
    createBn254FieldElement(optionalDataHigh),
    // [55-60] TVK timestamp fields (from executionTimestamp in UTC)
    createBn254FieldElement(tvkYear),
    createBn254FieldElement(tvkMonth),
    createBn254FieldElement(tvkDay),
    createBn254FieldElement(tvkHour),
    createBn254FieldElement(tvkMinute),
    createBn254FieldElement(tvkSecond),
    // [61-63] arcis public key and nonce
    createBn254FieldElement(arcisPublicKeyLow),
    createBn254FieldElement(arcisPublicKeyHigh),
    createBn254FieldElement(rcEncryptionNonce)
  ];
  if (aggregatedHashInputs.length !== AGGREGATED_HASH_INPUT_COUNT2) {
    throw new MathematicsAssertionError(
      `Expected ${String(AGGREGATED_HASH_INPUT_COUNT2)} aggregated hash inputs, got ${String(aggregatedHashInputs.length)}`,
      {
        value: aggregatedHashInputs.length,
        expectedType: "aggregatedHashInputCount",
        constraint: `=== ${String(AGGREGATED_HASH_INPUT_COUNT2)}`
      }
    );
  }
  const publicAggregatedHash = await poseidonAggregator(aggregatedHashInputs);
  assertBn254FieldElement(publicAggregatedHash, "publicAggregatedHash");
  const bpsDivisorInverse = computeBn254ModularInverse(createBn254FieldElement(BPS_DIVISOR3));
  const zkCircuitInputs = {
    // Per-UTXO H1 data
    version: utxosBatch.map((u) => createBn254FieldElement(u.version)),
    commitmentIndex: utxosBatch.map((u) => createBn254FieldElement(u.commitmentIndex)),
    senderAddressLow: utxosBatch.map((u) => createBn254FieldElement(u.senderAddressLow)),
    senderAddressHigh: utxosBatch.map((u) => createBn254FieldElement(u.senderAddressHigh)),
    relayerFixedSolFees: utxosBatch.map((u) => createBn254FieldElement(u.relayerFixedSolFees)),
    poolVolumeSpl: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.poolVolumeSpl))),
    poolVolumeSol: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.poolVolumeSol))),
    // Per-UTXO H2 data
    amount: perLeafAmounts.map((a) => createBn254FieldElement(a)),
    nullifier: perLeafNullifiers,
    h2BlindingFactor: perLeafRandomSecrets,
    // Mint address (shared)
    mintAddressLow: createBn254FieldElement(firstUtxo.mintAddressLow),
    mintAddressHigh: createBn254FieldElement(firstUtxo.mintAddressHigh),
    // Unlocking user (from EPHEMERAL derivation, NOT client)
    unlockingUserMasterViewingKey: ephemeralMasterViewingKey,
    unlockingUserMasterViewingKeyBlindingFactor: ephemeralMasterViewingKeyBlindingFactor,
    unlockingUserMasterViewingKeyLow: createBn254FieldElement(ephemeralMvkLow),
    unlockingUserMasterViewingKeyHigh: createBn254FieldElement(ephemeralMvkHigh),
    unlockingUserPrivateKey: ephemeralPoseidonPrivateKey,
    unlockingUserPrivateKeyBlindingFactor: ephemeralPoseidonPrivateKeyBlindingFactor,
    // Optional data (replaces ephemeral address)
    ephemeralAddressLow: createBn254FieldElement(optionalDataLow),
    ephemeralAddressHigh: createBn254FieldElement(optionalDataHigh),
    // Merkle tree
    root: createBn254FieldElement(decodeU256LeBytesToU256(merkleRoot)),
    pathElements: utxosBatch.flatMap(
      (u) => u.merklePath.map((p) => createBn254FieldElement(decodeU256LeBytesToU256(p)))
    ),
    pathIndices: perLeafPathIndices.flat(),
    // Per-UTXO timestamps (from deposit)
    utxoYear: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.year))),
    utxoMonth: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.month))),
    utxoDay: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.day))),
    utxoHour: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.hour))),
    utxoMinute: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.minute))),
    utxoSecond: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.second))),
    // TVK timestamps (from execution timestamp in UTC)
    tvkYear: createBn254FieldElement(tvkYear),
    tvkMonth: createBn254FieldElement(tvkMonth),
    tvkDay: createBn254FieldElement(tvkDay),
    tvkHour: createBn254FieldElement(tvkHour),
    tvkMinute: createBn254FieldElement(tvkMinute),
    tvkSecond: createBn254FieldElement(tvkSecond),
    // Linker keystream blinding factors
    linkerKeystreamBlindingFactors: allLinkerKeystreamBlindingFactors,
    // Protocol fee
    protocolFeeBaseFee: createBn254FieldElement(PROTOCOL_FEE_BASE_FEE),
    protocolFeeLowerBound: createBn254FieldElement(PROTOCOL_FEE_LOWER_BOUND),
    protocolFeeUpperBound: createBn254FieldElement(PROTOCOL_FEE_UPPER_BOUND),
    protocolFeeBps: createBn254FieldElement(PROTOCOL_FEE_BPS),
    protocolFeeBpsQuotient: createBn254FieldElement(protocolFeeBpsQuotient),
    protocolFeeBpsRemainder: createBn254FieldElement(protocolFeeBpsRemainder),
    protocolFeeDivisorInverse: bpsDivisorInverse,
    // Relayer fee
    relayerFeeBaseFee: createBn254FieldElement(RELAYER_FEE_BASE_FEE),
    relayerFeeLowerBound: createBn254FieldElement(RELAYER_FEE_LOWER_BOUND),
    relayerFeeUpperBound: createBn254FieldElement(RELAYER_FEE_UPPER_BOUND),
    relayerFeeBps: createBn254FieldElement(RELAYER_FEE_BPS),
    relayerFeeBpsQuotient: createBn254FieldElement(relayerFeeBpsQuotient),
    relayerFeeBpsRemainder: createBn254FieldElement(relayerFeeBpsRemainder),
    relayerFeeDivisorInverse: bpsDivisorInverse,
    // Fee merkle proofs
    protocolFeesRoot: createBn254FieldElement(protocolFeeConfig.merkleRoot),
    protocolFeesMerklePath: protocolFeeConfig.merklePath.map((h) => createBn254FieldElement(h)),
    protocolFeesPathIndices: leafIndexToPathIndices(protocolFeeConfig.leafIndex).map((idx) => createBn254FieldElement(idx)),
    relayerFeesRoot: createBn254FieldElement(relayerFeeConfig.merkleRoot),
    relayerFeesMerklePath: relayerFeeConfig.merklePath.map((h) => createBn254FieldElement(h)),
    relayerFeesPathIndices: leafIndexToPathIndices(relayerFeeConfig.leafIndex).map((idx) => createBn254FieldElement(idx)),
    // Limbwise representations
    finalAmountLimbs: [finalAmountAsLimbs.low, finalAmountAsLimbs.middle, finalAmountAsLimbs.high],
    protocolFeeLimbs: [
      protocolFeesAsLimbs.low,
      protocolFeesAsLimbs.middle,
      protocolFeesAsLimbs.high
    ],
    relayerFeeLimbs: [relayerFeesAsLimbs.low, relayerFeesAsLimbs.middle, relayerFeesAsLimbs.high],
    unlockingUserMasterViewingKeyLowLimbs: [
      ephemeralMvkLowAsLimbs.low,
      ephemeralMvkLowAsLimbs.middle,
      ephemeralMvkLowAsLimbs.high
    ],
    unlockingUserMasterViewingKeyHighLimbs: [
      ephemeralMvkHighAsLimbs.low,
      ephemeralMvkHighAsLimbs.middle,
      ephemeralMvkHighAsLimbs.high
    ],
    randomFactorForPolynomialEvaluationLowLimbs: [
      randomFactorLowAsLimbs.low,
      randomFactorLowAsLimbs.middle,
      randomFactorLowAsLimbs.high
    ],
    randomFactorForPolynomialEvaluationHighLimbs: [
      randomFactorHighAsLimbs.low,
      randomFactorHighAsLimbs.middle,
      randomFactorHighAsLimbs.high
    ],
    // Challenge powers
    challengeLimbs: [
      fiatShamirChallengeLimbs.low,
      fiatShamirChallengeLimbs.middle,
      fiatShamirChallengeLimbs.high
    ],
    challengeSquaredLimbs: [
      challengeSquaredLimbs.low,
      challengeSquaredLimbs.middle,
      challengeSquaredLimbs.high
    ],
    challengeCubedLimbs: [
      challengeCubedLimbs.low,
      challengeCubedLimbs.middle,
      challengeCubedLimbs.high
    ],
    challengeToFourthLimbs: [
      challengeToFourthLimbs.low,
      challengeToFourthLimbs.middle,
      challengeToFourthLimbs.high
    ],
    challengeToFifthLimbs: [
      challengeToFifthLimbs.low,
      challengeToFifthLimbs.middle,
      challengeToFifthLimbs.high
    ],
    // Inverses
    randomFactorForPolynomialEvaluationLowLimbsInverse: inverseForRandomFactorLowLimbs,
    randomFactorForPolynomialEvaluationHighLimbsInverse: inverseForRandomFactorHighLimbs,
    challengeLimbsInverse: inverseForChallengeLimbs,
    challengeSquaredLimbsInverse: inverseForChallengeSquaredLimbs,
    challengeCubedLimbsInverse: inverseForChallengeCubedLimbs,
    challengeToFourthLimbsInverse: inverseForChallengeToFourthLimbs,
    challengeToFifthLimbsInverse: inverseForChallengeToFifthLimbs,
    inverseForUnlockingUserMasterViewingKeyLowLimbs: inverseForMvkLowLimbs,
    inverseForUnlockingUserMasterViewingKeyHighLimbs: inverseForMvkHighLimbs,
    finalAmountLimbsInverse: inverseForFinalAmountLimbs,
    // Rescue encryption keys
    keysForFinalAmountLimbs: [
      keysForFinalAmountLimbs.low,
      keysForFinalAmountLimbs.middle,
      keysForFinalAmountLimbs.high
    ],
    keysForProtocolFeeLimbs: [
      keysForProtocolFeeLimbs.low,
      keysForProtocolFeeLimbs.middle,
      keysForProtocolFeeLimbs.high
    ],
    keysForRelayerFeeLimbs: [
      keysForRelayerFeeLimbs.low,
      keysForRelayerFeeLimbs.middle,
      keysForRelayerFeeLimbs.high
    ],
    keysForUnlockingMasterViewingKeyLowLimbs: [
      keysForMvkLowLimbs.low,
      keysForMvkLowLimbs.middle,
      keysForMvkLowLimbs.high
    ],
    keysForUnlockingMasterViewingKeyHighLimbs: [
      keysForMvkHighLimbs.low,
      keysForMvkHighLimbs.middle,
      keysForMvkHighLimbs.high
    ],
    keysForRandomFactorForPolynomialEvaluationLowLimbs: [
      keysForRandomFactorLowLimbs.low,
      keysForRandomFactorLowLimbs.middle,
      keysForRandomFactorLowLimbs.high
    ],
    keysForRandomFactorForPolynomialEvaluationHighLimbs: [
      keysForRandomFactorHighLimbs.low,
      keysForRandomFactorHighLimbs.middle,
      keysForRandomFactorHighLimbs.high
    ],
    // Key inverses
    keysForFinalAmountLimbsInverse: inverseForKeysForFinalAmountLimbs,
    keysForProtocolFeeLimbsInverse: inverseForKeysForProtocolFeeLimbs,
    keysForRelayerFeeLimbsInverse: inverseForKeysForRelayerFeeLimbs,
    keysForUnlockingMasterViewingKeyLowLimbsInverse: inverseForKeysForMvkLowLimbs,
    keysForUnlockingMasterViewingKeyHighLimbsInverse: inverseForKeysForMvkHighLimbs,
    keysForRandomFactorForPolynomialEvaluationLowLimbsInverse: inverseForKeysForRandomFactorLowLimbs,
    keysForRandomFactorForPolynomialEvaluationHighLimbsInverse: inverseForKeysForRandomFactorHighLimbs,
    // Rescue commitment
    blindingFactorForRescueCommitment,
    nonce: createBn254FieldElement(rcEncryptionNonce),
    arcisPublicKeyLow: createBn254FieldElement(arcisPublicKeyLow),
    arcisPublicKeyHigh: createBn254FieldElement(arcisPublicKeyHigh),
    // Addresses
    relayerPublicKeyLow: createBn254FieldElement(relayerPublicKeyLow),
    relayerPublicKeyHigh: createBn254FieldElement(relayerPublicKeyHigh),
    finalDestinationAddressLow: createBn254FieldElement(destinationAddressLow),
    finalDestinationAddressHigh: createBn254FieldElement(destinationAddressHigh),
    // Public input
    publicAggregatedHash
  };
  const { proofA, proofB, proofC } = await zkProver.prove(
    zkCircuitInputs,
    nLeaves
  );
  assertGroth16ProofA(proofA, "proofA");
  assertGroth16ProofB(proofB, "proofB");
  assertGroth16ProofC(proofC, "proofC");
  const toBase64 = /* @__PURE__ */ __name((bytes) => {
    let binary = "";
    for (const byte of bytes) {
      binary += String.fromCharCode(byte);
    }
    return btoa(binary);
  }, "toBase64");
  const proofAccountData = {
    tvk_timestamp: Math.floor(executionTimestamp.getTime() / 1e3),
    rescue_encryption_public_key: toBase64(new Uint8Array(clientX25519PublicKey)),
    encryption_nonce: rcEncryptionNonce.toString(),
    rescue_encrypted_total_amount: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedFinalAmount))),
    rescue_encrypted_relayer_commission_fee: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRelayerFees))),
    rescue_encrypted_protocol_commission_fee: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedProtocolFees))),
    rescue_encrypted_master_viewing_key_low: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedMvkLow))),
    rescue_encrypted_master_viewing_key_high: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedMvkHigh))),
    rescue_encrypted_blinding_factor_low: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow))),
    rescue_encrypted_blinding_factor_high: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh))),
    rescue_encryption_commitment: toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(rcEncryptionCommitment)))),
    encryption_validation_polynomial: toBase64(new Uint8Array(encodeU256ToU256LeBytes(polynomialEvaluationResultScalar))),
    merkle_root: toBase64(new Uint8Array(merkleRoot)),
    total_relayer_fees: Number(totalRelayerFixedSolFees),
    groth16_proof_a: toBase64(new Uint8Array(proofA)),
    groth16_proof_b: toBase64(new Uint8Array(proofB)),
    groth16_proof_c: toBase64(new Uint8Array(proofC))
  };
  const utxoSlotData = [];
  for (let leafIdx = 0; leafIdx < nLeaves; leafIdx++) {
    const linkerEncStartIdx = leafIdx * 6;
    utxoSlotData.push({
      slot_index: leafIdx,
      nullifier: toBase64(
        new Uint8Array(encodeU256ToU256LeBytes(BigInt(nullifierHashes[leafIdx])))
      ),
      linker_encryptions: allLinkerEncryptions.slice(linkerEncStartIdx, linkerEncStartIdx + 6).map((enc) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(enc))))),
      linker_key_commitments: allLinkerKeystreamCommitments.slice(linkerEncStartIdx, linkerEncStartIdx + 6).map((comm) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(comm)))))
    });
  }
  const claimRequest = {
    variant: "encrypted_balance",
    user_pubkey: destinationAddress,
    mint: mintAddress,
    stealth_pool_index: Number(stealthPoolIndex),
    proof_account_data: proofAccountData,
    utxo_slot_data: utxoSlotData,
    max_utxo_capacity: nLeaves,
    optional_data: toBase64(optionalDataBytes)
  };
  const submitResponse = await submitClaim(claimRequest);
  if (awaitCompletion) {
    const { pollClaimUntilTerminal: pollClaimUntilTerminal2 } = await import('./relayer-NRRMSMNB.js');
    const result = await pollClaimUntilTerminal2(pollClaimStatus, submitResponse.requestId, {
      pollingIntervalMs,
      timeoutMs,
      onProgress
    });
    return { ...result, utxoIds };
  }
  return {
    requestId: submitResponse.requestId,
    status: "received",
    utxoIds
  };
}
__name(processUtxoInBatch2, "processUtxoInBatch");
function getSelfClaimableUtxoToEncryptedBalanceClaimerFunction(args, deps) {
  const { client, generationIndex: providedGenerationIndex } = args;
  if (deps === void 0) {
    throw new MathematicsAssertionError("deps with zkProver and submitClaimFunction is required", {
      value: void 0,
      expectedType: "GetSelfClaimableUtxoToEncryptedBalanceClaimerFunctionDeps",
      constraint: "must be provided"
    });
  }
  const fetchBatchMerkleProof = deps.fetchBatchMerkleProof;
  const submitClaim = deps.relayer.submitClaim;
  const pollClaimStatus = deps.relayer.pollClaimStatus;
  const getRelayerAddress = deps.relayer.getRelayerAddress;
  const awaitCompletion = deps.awaitCompletion ?? true;
  const pollingIntervalMs = deps.pollingIntervalMs ?? 3e3;
  const timeoutMs = deps.timeoutMs ?? 12e4;
  const onProgress = deps.onProgress;
  const ephemeralUtxoMvkGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyDeriver ?? getEphemeralUtxoMasterViewingKeyDeriver({ client });
  const ephemeralUtxoMvkBlindingFactorGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyBlindingFactorDeriver ?? getEphemeralUtxoMasterViewingKeyBlindingFactorDeriver({
    client
  });
  const ephemeralUtxoPoseidonKeyGenerator = deps.keys?.ephemeralUtxoPoseidonKeyGenerator ?? getEphemeralUtxoPoseidonPrivateKeyDeriver({ client });
  const ephemeralUtxoPoseidonKeyBlindingFactorGenerator = deps.keys?.ephemeralUtxoPoseidonKeyBlindingFactorGenerator ?? getEphemeralUtxoPoseidonPrivateKeyBlindingFactorDeriver({
    client
  });
  const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const poseidonHasher = deps.crypto?.poseidonHasher ?? getPoseidonHasher();
  const poseidonAggregator = deps.crypto?.poseidonAggregator ?? getPoseidonAggregator();
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGeneratorFunction = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const userCommitmentGeneratorFunction = deps.crypto?.userCommitmentGenerator ?? getUserCommitmentGeneratorFunction();
  const keystreamCommitmentGeneratorFunction = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const fiatShamirChallengeGeneratorFunction = deps.crypto?.fiatShamirChallengeGenerator ?? getFiatShamirChallengeGeneratorFunction();
  const generateRandomU2562 = deps.random?.generateRandomU256 ?? generateRandomU256;
  const generateRandomU1282 = deps.random?.generateRandomU128 ?? generateRandomU128;
  return async (utxos, optionalData, microLamportsPerAcu) => {
    try {
      if (utxos.length === 0) {
        return { batches: /* @__PURE__ */ new Map() };
      }
      const masterSeedBytes = await client.masterSeed.getMasterSeed();
      const generationIndex = providedGenerationIndex ?? generateRandomU2562();
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      const clientX25519PublicKey = keypairResult.x25519Keypair.publicKey;
      const relayerAddress = await getRelayerAddress();
      const stealthPoolIndex = 0n;
      const utxosByDestination = /* @__PURE__ */ new Map();
      for (const utxo of utxos) {
        const existing = utxosByDestination.get(utxo.destinationAddress) ?? [];
        existing.push(utxo);
        utxosByDestination.set(utxo.destinationAddress, existing);
      }
      const batchConfigs = [];
      let batchIndex = 0n;
      for (const [, destinationUtxos] of utxosByDestination) {
        for (let index = 0; index < destinationUtxos.length; index += MAX_UTXOS_PER_PROOF2) {
          const batch = destinationUtxos.slice(index, index + MAX_UTXOS_PER_PROOF2);
          const batchGenerationOffset = BigInt(generationIndex) + batchIndex;
          batchConfigs.push({
            batchIndex,
            utxos: batch,
            generationOffset: batchGenerationOffset
          });
          batchIndex = batchIndex + 1n;
        }
      }
      const batchResults = await Promise.all(
        batchConfigs.map(async (config) => {
          const treeIndex = config.utxos[0].treeIndex;
          const insertionIndices = config.utxos.map((u) => u.insertionIndex);
          const batchProof = await fetchBatchMerkleProof(treeIndex, insertionIndices);
          const claimableUtxos = enrichWithMerkleProof(config.utxos, batchProof);
          const batchArgs = {
            utxosBatch: claimableUtxos,
            stealthPoolIndex,
            generationIndex: config.generationOffset,
            masterSeedBytes,
            relayerAddress,
            clientX25519PrivateKey,
            clientX25519PublicKey,
            networkConfig: client.networkConfig,
            optionalData: optionalData ?? new Uint8Array(32),
            microLamportsPerAcu: microLamportsPerAcu ?? 0n
          };
          const batchDeps = {
            submitClaim,
            pollClaimStatus,
            awaitCompletion,
            pollingIntervalMs,
            timeoutMs,
            onProgress,
            getRelayerAddress,
            ephemeralUtxoMvkGenerator,
            ephemeralUtxoMvkBlindingFactorGenerator,
            ephemeralUtxoPoseidonKeyGenerator,
            ephemeralUtxoPoseidonKeyBlindingFactorGenerator,
            poseidonHasher,
            poseidonAggregator,
            poseidonEncryptor,
            poseidonKeystreamGenerator: poseidonKeystreamGeneratorFunction,
            userCommitmentGenerator: userCommitmentGeneratorFunction,
            keystreamCommitmentGenerator: keystreamCommitmentGeneratorFunction,
            fiatShamirChallengeGenerator: fiatShamirChallengeGeneratorFunction,
            zkProver: deps.zkProver,
            generateRandomU128: generateRandomU1282
          };
          const batchResult = await processUtxoInBatch2(batchArgs, batchDeps);
          return {
            batchIndex: config.batchIndex,
            batchResult
          };
        })
      );
      const resultsByBatch = /* @__PURE__ */ new Map();
      for (const result of batchResults) {
        resultsByBatch.set(result.batchIndex, result.batchResult);
      }
      return {
        batches: resultsByBatch
      };
    } catch (error) {
      if (error instanceof ClaimUtxoError) {
        throw error;
      }
      if (error instanceof CryptographyError) {
        throw new ClaimUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new ClaimUtxoError("initialization", error.message, error);
      }
      throw new ClaimUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  };
}
__name(getSelfClaimableUtxoToEncryptedBalanceClaimerFunction, "getSelfClaimableUtxoToEncryptedBalanceClaimerFunction");
var MAX_UTXOS_PER_PROOF3 = 1;
var DOMAIN_CLAIM_MODIFIED_GEN_INDEX3 = "ClaimSelfClaimableUtxoIntoPublicBalance / modifiedGenerationIndex";
var DOMAIN_CLAIM_EXPANDED_GEN_INDEX3 = "ClaimSelfClaimableUtxoIntoPublicBalance / expandedGenerationIndex";
var LINKER_ENCRYPTIONS_PER_LEAF3 = 5;
var LINKER_KEYSTREAM_BLINDING_FACTOR_DOMAIN3 = "ephemeralClaimUtxoToAta / linkerKeystreamBlindingFactor";
var RESCUE_ENCRYPTIONS_COUNT3 = 4;
var RANDOM_FACTOR_FOR_POLYNOMIAL_COMMITMENT_DOMAIN3 = "ephemeralClaimUtxoToAta / randomFactorForPolynomialCommitment";
var RESCUE_ENCRYPTION_COMMITMENT_BLINDING_FACTOR_DOMAIN3 = "ephemeralClaimUtxoToAta / rescueEncryptionCommitmentBlindingFactor";
var KMAC_512_OUTPUT_LENGTH3 = 64;
var AGGREGATED_HASH_INPUT_COUNT3 = 45;
function reconstructAddressFromU128Parts3(low, high) {
  const bytes = new Uint8Array(32);
  let lowValue = BigInt(low);
  for (let index = 0; index < 16; index++) {
    bytes[index] = Number(lowValue & 0xffn);
    lowValue >>= 8n;
  }
  let highValue = BigInt(high);
  for (let index = 0; index < 16; index++) {
    bytes[16 + index] = Number(highValue & 0xffn);
    highValue >>= 8n;
  }
  const decoder = getAddressDecoder();
  return decoder.decode(bytes);
}
__name(reconstructAddressFromU128Parts3, "reconstructAddressFromU128Parts");
async function computeTransactionViewingKeyFromMvk2(masterViewingKey, mintAddressLow, mintAddressHigh, year, month, day, hour, minute, second, hasher) {
  const h0 = await hasher([
    masterViewingKey,
    createBn254FieldElement(mintAddressLow),
    createBn254FieldElement(mintAddressHigh)
  ]);
  const h1 = await hasher([h0, createBn254FieldElement(year)]);
  const h2 = await hasher([h1, createBn254FieldElement(month)]);
  const h3 = await hasher([h2, createBn254FieldElement(day)]);
  const h4 = await hasher([h3, createBn254FieldElement(hour)]);
  const h5 = await hasher([h4, createBn254FieldElement(minute)]);
  const tvk = await hasher([h5, createBn254FieldElement(second)]);
  return tvk;
}
__name(computeTransactionViewingKeyFromMvk2, "computeTransactionViewingKeyFromMvk");
function limbsToTuple3(limbs) {
  return [limbs.low, limbs.middle, limbs.high];
}
__name(limbsToTuple3, "limbsToTuple");
async function processUtxoInBatch3(args, deps) {
  const {
    utxosBatch,
    stealthPoolIndex,
    generationIndex,
    masterSeedBytes,
    relayerAddress: relayerPublicKey,
    clientX25519PrivateKey,
    clientX25519PublicKey,
    networkConfig,
    optionalData,
    microLamportsPerAcu
  } = args;
  const utxoIds = utxosBatch.map(
    (utxo) => `${String(stealthPoolIndex)}:${String(utxo.leafIndex)}`
  );
  const {
    submitClaim,
    pollClaimStatus,
    awaitCompletion,
    pollingIntervalMs,
    timeoutMs,
    onProgress,
    getRelayerAddress,
    ephemeralUtxoMvkGenerator,
    ephemeralUtxoMvkBlindingFactorGenerator,
    ephemeralUtxoPoseidonKeyGenerator,
    ephemeralUtxoPoseidonKeyBlindingFactorGenerator,
    poseidonHasher,
    poseidonAggregator,
    poseidonEncryptor,
    poseidonKeystreamGenerator,
    userCommitmentGenerator,
    keystreamCommitmentGenerator,
    fiatShamirChallengeGenerator,
    zkProver,
    generateRandomU128: generateRandomU1282,
    protocolFeeProvider,
    relayerFeeProvider
  } = deps;
  assertU128(BigInt(stealthPoolIndex), "stealthPoolIndex");
  assertU256(BigInt(generationIndex), "generationIndex");
  const nLeaves = utxosBatch.length;
  if (nLeaves === 0) {
    throw new MathematicsAssertionError("utxosBatch must be non-empty", {
      value: nLeaves,
      expectedType: "utxosBatch",
      constraint: "length > 0"
    });
  }
  if (nLeaves > MAX_UTXOS_PER_PROOF3) {
    throw new MathematicsAssertionError(
      `utxosBatch length ${String(nLeaves)} exceeds maximum ${String(MAX_UTXOS_PER_PROOF3)}`,
      {
        value: nLeaves,
        expectedType: "utxosBatch",
        constraint: `length <= ${String(MAX_UTXOS_PER_PROOF3)}`
      }
    );
  }
  const destinationAddress = utxosBatch[0].destinationAddress;
  for (let index = 1; index < nLeaves; index++) {
    if (utxosBatch[index].destinationAddress !== destinationAddress) {
      throw new MathematicsAssertionError(
        `Destination address mismatch at index ${String(index)}: expected ${destinationAddress}, got ${utxosBatch[index].destinationAddress}`,
        {
          value: utxosBatch[index].destinationAddress,
          expectedType: "destinationAddress",
          constraint: `all UTXOs must have same destination: ${destinationAddress}`
        }
      );
    }
  }
  const firstUtxo = utxosBatch[0];
  const merkleRoot = firstUtxo.merkleRoot;
  assertU256LeBytes(merkleRoot);
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    if (utxo.merklePath.length === 0) {
      throw new MathematicsAssertionError(`merklePath[${String(index)}] must be non-empty`, {
        value: utxo.merklePath.length,
        expectedType: `merklePath[${String(index)}]`,
        constraint: "length > 0"
      });
    }
    for (const sibling of utxo.merklePath) {
      assertU256LeBytes(sibling);
    }
    const maxLeafIndex = (1n << BigInt(utxo.merklePath.length)) - 1n;
    const leafIndexBigInt = BigInt(utxo.leafIndex);
    if (leafIndexBigInt < 0n || leafIndexBigInt > maxLeafIndex) {
      throw new MathematicsAssertionError(
        `leafIndex[${String(index)}] value ${String(leafIndexBigInt)} is out of range for tree depth ${String(utxo.merklePath.length)}`,
        {
          value: leafIndexBigInt,
          expectedType: `leafIndex[${String(index)}]`,
          constraint: `0 <= leafIndex <= ${String(maxLeafIndex)}`
        }
      );
    }
    assertU128(leafIndexBigInt, `leafIndex[${String(index)}]`);
    const versionBigInt = BigInt(utxo.version);
    assertU64(versionBigInt, `version[${String(index)}]`);
    if (versionBigInt === 0n) {
      throw new MathematicsAssertionError(`version[${String(index)}] must not be zero`, {
        value: versionBigInt,
        expectedType: `version[${String(index)}]`,
        constraint: "version !== 0"
      });
    }
    assertU64(BigInt(utxo.amount), `amount[${String(index)}]`);
    assertU128(BigInt(utxo.senderAddressLow), `senderAddressLow[${String(index)}]`);
    assertU128(BigInt(utxo.senderAddressHigh), `senderAddressHigh[${String(index)}]`);
    assertU128(BigInt(utxo.mintAddressLow), `mintAddressLow[${String(index)}]`);
    assertU128(BigInt(utxo.mintAddressHigh), `mintAddressHigh[${String(index)}]`);
    assertU64(BigInt(utxo.relayerFixedSolFees), `relayerFixedSolFees[${String(index)}]`);
    assertYear(BigInt(utxo.timestamp.year));
    assertMonth(BigInt(utxo.timestamp.month));
    assertDay(BigInt(utxo.timestamp.day));
    assertHour(BigInt(utxo.timestamp.hour));
    assertMinute(BigInt(utxo.timestamp.minute));
    assertSecond(BigInt(utxo.timestamp.second));
    assertU128(
      BigInt(utxo.depositModifiedGenerationIndex),
      `depositModifiedGenerationIndex[${String(index)}]`
    );
  }
  const generationIndexBytes = new TextEncoder().encode(String(generationIndex));
  const modifiedGenerationIndex = kmac256(
    new TextEncoder().encode(DOMAIN_CLAIM_MODIFIED_GEN_INDEX3),
    masterSeedBytes,
    { dkLen: 16, personalization: generationIndexBytes }
  );
  const claimModifiedGenerationIndex = kmac256(
    new TextEncoder().encode(DOMAIN_CLAIM_EXPANDED_GEN_INDEX3),
    masterSeedBytes,
    { dkLen: 32, personalization: modifiedGenerationIndex }
  );
  let claimModifiedGenerationIndexU256 = 0n;
  for (let index = 0; index < 32; index++) {
    claimModifiedGenerationIndexU256 |= BigInt(claimModifiedGenerationIndex[index]) << BigInt(index * 8);
  }
  const depositModifiedGenerationIndexValue = BigInt(firstUtxo.depositModifiedGenerationIndex);
  const depositModifiedGenerationIndexBytes = new Uint8Array(16);
  let indexValueForExpansion = depositModifiedGenerationIndexValue;
  for (let index = 0; index < 16; index++) {
    depositModifiedGenerationIndexBytes[index] = Number(indexValueForExpansion & 0xffn);
    indexValueForExpansion >>= 8n;
  }
  const expandedModifiedGenerationIndex = expandModifiedGenerationIndex(
    depositModifiedGenerationIndexBytes
  );
  let ephemeralOffset = 0n;
  for (let index = 0; index < 32; index++) {
    ephemeralOffset |= BigInt(expandedModifiedGenerationIndex[index]) << BigInt(index * 8);
  }
  const ephemeralOffsetU256 = ephemeralOffset;
  const [
    ephemeralMasterViewingKey,
    ephemeralMasterViewingKeyBlindingFactor,
    ephemeralPoseidonPrivateKey,
    ephemeralPoseidonPrivateKeyBlindingFactor
  ] = await Promise.all([
    ephemeralUtxoMvkGenerator(ephemeralOffsetU256),
    ephemeralUtxoMvkBlindingFactorGenerator(ephemeralOffsetU256),
    ephemeralUtxoPoseidonKeyGenerator(ephemeralOffsetU256),
    ephemeralUtxoPoseidonKeyBlindingFactorGenerator(ephemeralOffsetU256)
  ]);
  const userCommitment = await userCommitmentGenerator(
    ephemeralMasterViewingKey,
    ephemeralMasterViewingKeyBlindingFactor,
    ephemeralPoseidonPrivateKey,
    ephemeralPoseidonPrivateKeyBlindingFactor
  );
  assertBn254FieldElement(ephemeralMasterViewingKey);
  assertBn254FieldElement(ephemeralMasterViewingKeyBlindingFactor);
  assertBn254FieldElement(ephemeralPoseidonPrivateKey);
  assertBn254FieldElement(ephemeralPoseidonPrivateKeyBlindingFactor);
  assertBn254FieldElement(userCommitment);
  const executionTimestamp = /* @__PURE__ */ new Date();
  const tvkYear = BigInt(executionTimestamp.getUTCFullYear());
  const tvkMonth = BigInt(executionTimestamp.getUTCMonth() + 1);
  const tvkDay = BigInt(executionTimestamp.getUTCDate());
  const tvkHour = BigInt(executionTimestamp.getUTCHours());
  const tvkMinute = BigInt(executionTimestamp.getUTCMinutes());
  const tvkSecond = BigInt(executionTimestamp.getUTCSeconds());
  assertYear(tvkYear);
  assertMonth(tvkMonth);
  assertDay(tvkDay);
  assertHour(tvkHour);
  assertMinute(tvkMinute);
  assertSecond(tvkSecond);
  const mintAddress = reconstructAddressFromU128Parts3(
    firstUtxo.mintAddressLow,
    firstUtxo.mintAddressHigh
  );
  const transactionViewingKey = await computeTransactionViewingKeyFromMvk2(
    ephemeralMasterViewingKey,
    firstUtxo.mintAddressLow,
    firstUtxo.mintAddressHigh,
    tvkYear,
    tvkMonth,
    tvkDay,
    tvkHour,
    tvkMinute,
    tvkSecond,
    poseidonHasher
  );
  assertBn254FieldElement(transactionViewingKey);
  const perLeafNullifiers = [];
  const perLeafRandomSecrets = [];
  const perLeafAmounts = [];
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    const depositModifiedGenerationIndexValue2 = BigInt(utxo.depositModifiedGenerationIndex);
    const depositModifiedGenerationIndexBytes2 = new Uint8Array(16);
    let indexValue = depositModifiedGenerationIndexValue2;
    for (let index2 = 0; index2 < 16; index2++) {
      depositModifiedGenerationIndexBytes2[index2] = Number(indexValue & 0xffn);
      indexValue >>= 8n;
    }
    const nullifier = deriveNullifierFromModifiedGenerationIndex(
      depositModifiedGenerationIndexBytes2
    );
    const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(
      depositModifiedGenerationIndexBytes2
    );
    perLeafNullifiers.push(nullifier);
    perLeafRandomSecrets.push(randomSecret);
    perLeafAmounts.push(utxo.amount);
    assertBn254FieldElement(nullifier);
    assertBn254FieldElement(randomSecret);
  }
  const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
  assertU128(destinationAddressLow, "destinationAddressLow");
  assertU128(destinationAddressHigh, "destinationAddressHigh");
  let totalAmountAccumulator = 0n;
  for (const amt of perLeafAmounts) {
    totalAmountAccumulator += BigInt(amt);
  }
  const totalAmount = createU64(totalAmountAccumulator, "totalAmount");
  const perLeafCommitments = [];
  for (let index = 0; index < nLeaves; index++) {
    const utxo = utxosBatch[index];
    const depositTimestamp = utxo.timestamp;
    const h1Hash = await poseidonAggregator([
      createBn254FieldElement(BigInt(utxo.version)),
      createBn254FieldElement(BigInt(utxo.commitmentIndex)),
      createBn254FieldElement(utxo.senderAddressLow),
      createBn254FieldElement(utxo.senderAddressHigh),
      createBn254FieldElement(BigInt(utxo.relayerFixedSolFees)),
      createBn254FieldElement(utxo.mintAddressLow),
      createBn254FieldElement(utxo.mintAddressHigh),
      BigInt(depositTimestamp.year),
      BigInt(depositTimestamp.month),
      BigInt(depositTimestamp.day),
      BigInt(depositTimestamp.hour),
      BigInt(depositTimestamp.minute),
      BigInt(depositTimestamp.second),
      createBn254FieldElement(BigInt(utxo.poolVolumeSpl)),
      createBn254FieldElement(BigInt(utxo.poolVolumeSol))
    ]);
    const h2Hash = await poseidonHasher([
      BigInt(perLeafAmounts[index]),
      BigInt(perLeafNullifiers[index]),
      BigInt(userCommitment),
      BigInt(destinationAddressLow),
      BigInt(destinationAddressHigh),
      BigInt(perLeafRandomSecrets[index])
    ]);
    const leafCommitment = await poseidonHasher([h1Hash, h2Hash]);
    assertBn254FieldElement(h1Hash, `h1Hash[${String(index)}]`);
    assertBn254FieldElement(h2Hash, `h2Hash[${String(index)}]`);
    assertBn254FieldElement(leafCommitment, `leafCommitment[${String(index)}]`);
    perLeafCommitments.push(leafCommitment);
  }
  const allLinkerEncryptions = [];
  const allLinkerKeystreamCommitments = [];
  const allLinkerKeystreamBlindingFactors = [];
  for (let leafIndex = 0; leafIndex < nLeaves; leafIndex++) {
    const utxo = utxosBatch[leafIndex];
    const leafPlaintexts = [
      BigInt(utxo.senderAddressLow),
      BigInt(utxo.senderAddressHigh),
      BigInt(utxo.mintAddressLow),
      BigInt(utxo.mintAddressHigh),
      BigInt(perLeafCommitments[leafIndex])
    ];
    const leafPcCiphertexts = await poseidonEncryptor(
      leafPlaintexts,
      createPoseidonKey(transactionViewingKey)
    );
    if (leafPcCiphertexts.length !== LINKER_ENCRYPTIONS_PER_LEAF3) {
      throw new MathematicsAssertionError(
        `Expected ${String(LINKER_ENCRYPTIONS_PER_LEAF3)} ciphertexts for leaf ${String(leafIndex)}, got ${String(leafPcCiphertexts.length)}`,
        {
          value: leafPcCiphertexts.length,
          expectedType: "linkerCiphertextCount",
          constraint: `=== ${String(LINKER_ENCRYPTIONS_PER_LEAF3)}`
        }
      );
    }
    for (const [index, leafPcCiphertext] of leafPcCiphertexts.entries()) {
      assertBn254FieldElement(
        BigInt(leafPcCiphertext),
        `pcCiphertext[${String(leafIndex)}][${String(index)}]`
      );
    }
    allLinkerEncryptions.push(...leafPcCiphertexts);
    const keystreamCounters = [
      0n,
      1n,
      2n,
      3n,
      4n
    ];
    const leafPcKeys = await poseidonKeystreamGenerator(
      keystreamCounters,
      createPoseidonKey(transactionViewingKey)
    );
    for (let index = 0; index < LINKER_ENCRYPTIONS_PER_LEAF3; index++) {
      const key = leafPcKeys.get(BigInt(index));
      if (key === void 0) {
        throw new MathematicsAssertionError(
          `Missing keystream key for leaf ${String(leafIndex)}, index ${String(index)}`,
          {
            value: void 0,
            expectedType: "keystreamKey",
            constraint: "defined"
          }
        );
      }
      assertBn254FieldElement(BigInt(key), `pcKey[${String(leafIndex)}][${String(index)}]`);
    }
    for (let keyIndex = 0; keyIndex < LINKER_ENCRYPTIONS_PER_LEAF3; keyIndex++) {
      const globalKeyIndex = leafIndex * LINKER_ENCRYPTIONS_PER_LEAF3 + keyIndex;
      const blindingFactorOffsetBytes = kmac256(
        new TextEncoder().encode(
          `${LINKER_KEYSTREAM_BLINDING_FACTOR_DOMAIN3}${String(globalKeyIndex)}`
        ),
        encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256)
      );
      const blindingFactorOffset = decodeU256BeBytesToU256(
        blindingFactorOffsetBytes
      );
      const blindingFactor = deriveKeystreamBlindingFactor(blindingFactorOffset);
      assertBn254FieldElement(
        blindingFactor,
        `linkerKeystreamBlindingFactor[${String(leafIndex)}][${String(keyIndex)}]`
      );
      allLinkerKeystreamBlindingFactors.push(blindingFactor);
      const keystreamKey = leafPcKeys.get(BigInt(keyIndex));
      if (keystreamKey === void 0) {
        throw new MathematicsAssertionError(
          `Missing keystream key for commitment at leaf ${String(leafIndex)}, key ${String(keyIndex)}`,
          {
            value: void 0,
            expectedType: "keystreamKey",
            constraint: "defined"
          }
        );
      }
      const keystreamCommitment = await keystreamCommitmentGenerator(
        BigInt(keystreamKey),
        blindingFactor
      );
      assertBn254FieldElement(
        keystreamCommitment,
        `linkerKeystreamCommitment[${String(leafIndex)}][${String(keyIndex)}]`
      );
      allLinkerKeystreamCommitments.push(keystreamCommitment);
    }
  }
  const expectedLinkerCount = nLeaves * LINKER_ENCRYPTIONS_PER_LEAF3;
  if (allLinkerEncryptions.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} linker encryptions, got ${String(allLinkerEncryptions.length)}`,
      {
        value: allLinkerEncryptions.length,
        expectedType: "linkerEncryptionsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  if (allLinkerKeystreamBlindingFactors.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} keystream blinding factors, got ${String(allLinkerKeystreamBlindingFactors.length)}`,
      {
        value: allLinkerKeystreamBlindingFactors.length,
        expectedType: "keystreamBlindingFactorsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  if (allLinkerKeystreamCommitments.length !== expectedLinkerCount) {
    throw new MathematicsAssertionError(
      `Expected ${String(expectedLinkerCount)} keystream commitments, got ${String(allLinkerKeystreamCommitments.length)}`,
      {
        value: allLinkerKeystreamCommitments.length,
        expectedType: "keystreamCommitmentsCount",
        constraint: `=== ${String(expectedLinkerCount)}`
      }
    );
  }
  const linkerEncryptionsAggregatedHash = await poseidonAggregator(allLinkerEncryptions);
  const linkerKeystreamCommitmentsAggregatedHash = await poseidonAggregator(
    allLinkerKeystreamCommitments
  );
  assertBn254FieldElement(linkerEncryptionsAggregatedHash, "linkerEncryptionsAggregatedHash");
  assertBn254FieldElement(
    linkerKeystreamCommitmentsAggregatedHash,
    "linkerKeystreamCommitmentsAggregatedHash"
  );
  const nullifierHashes = [];
  for (let index = 0; index < nLeaves; index++) {
    const nullifierHash = await poseidonHasher([
      ephemeralPoseidonPrivateKey,
      perLeafNullifiers[index]
    ]);
    assertBn254FieldElement(nullifierHash, `nullifierHash[${String(index)}]`);
    nullifierHashes.push(nullifierHash);
  }
  const nullifierHashesAggregatedHash = await poseidonAggregator(nullifierHashes);
  assertBn254FieldElement(nullifierHashesAggregatedHash, "nullifierHashesAggregatedHash");
  const rcEncryptor = getRescueEncryptorFromPrivateKey(clientX25519PrivateKey, {
    umbraX25519PublicKey: networkConfig.mxePubkey
  });
  const randomFactorBytes = kmac256(
    new TextEncoder().encode(RANDOM_FACTOR_FOR_POLYNOMIAL_COMMITMENT_DOMAIN3),
    encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256)
  );
  const randomFactorU256 = decodeU256BeBytesToU256(randomFactorBytes);
  const randomFactorForPolynomialCommitment = randomFactorU256 % CURVE25519_FIELD_PRIME;
  assertCurve25519FieldElement(
    randomFactorForPolynomialCommitment,
    "randomFactorForPolynomialCommitment"
  );
  const { low: randomFactorLow, high: randomFactorHigh } = splitU256IntoTwoU128s(
    randomFactorForPolynomialCommitment
  );
  assertU128(randomFactorLow, "randomFactorLow");
  assertU128(randomFactorHigh, "randomFactorHigh");
  const ephemeralMvkLow = createCurve25519FieldElement(
    BigInt(ephemeralMasterViewingKey) & (1n << 128n) - 1n,
    "ephemeralMvkLow"
  );
  const ephemeralMvkHigh = createCurve25519FieldElement(
    BigInt(ephemeralMasterViewingKey) >> 128n,
    "ephemeralMvkHigh"
  );
  const reconstructedEphemeralMvk = BigInt(ephemeralMvkLow) + (BigInt(ephemeralMvkHigh) << 128n);
  if (reconstructedEphemeralMvk !== BigInt(ephemeralMasterViewingKey)) {
    throw new MathematicsAssertionError(
      `Ephemeral MVK decomposition failed: ${String(reconstructedEphemeralMvk)} !== ${String(ephemeralMasterViewingKey)}`,
      {
        value: reconstructedEphemeralMvk,
        expectedType: "ephemeralMvkDecomposition",
        constraint: "=== ephemeralMasterViewingKey"
      }
    );
  }
  const rcCiphertextOutputs = await rcEncryptor([
    createRcPlaintext(ephemeralMvkLow),
    createRcPlaintext(ephemeralMvkHigh),
    createRcPlaintext(randomFactorLow),
    createRcPlaintext(randomFactorHigh)
  ]);
  const rcEncryptionNonce = rcCiphertextOutputs.nonce;
  assertRcEncryptionNonce(rcEncryptionNonce);
  if (rcCiphertextOutputs.ciphertexts.length !== RESCUE_ENCRYPTIONS_COUNT3) {
    throw new MathematicsAssertionError(
      `Expected ${String(RESCUE_ENCRYPTIONS_COUNT3)} rescue ciphertexts, got ${String(rcCiphertextOutputs.ciphertexts.length)}`,
      {
        value: rcCiphertextOutputs.ciphertexts.length,
        expectedType: "rcCiphertextCount",
        constraint: `=== ${String(RESCUE_ENCRYPTIONS_COUNT3)}`
      }
    );
  }
  const rcEncryptedMvkLow = rcCiphertextOutputs.ciphertexts[0];
  const rcEncryptedMvkHigh = rcCiphertextOutputs.ciphertexts[1];
  const rcEncryptedRandomFactorLow = rcCiphertextOutputs.ciphertexts[2];
  const rcEncryptedRandomFactorHigh = rcCiphertextOutputs.ciphertexts[3];
  assertCurve25519FieldElement(rcEncryptedMvkLow, "rcEncryptedMvkLow");
  assertCurve25519FieldElement(rcEncryptedMvkHigh, "rcEncryptedMvkHigh");
  assertCurve25519FieldElement(rcEncryptedRandomFactorLow, "rcEncryptedRandomFactorLow");
  assertCurve25519FieldElement(rcEncryptedRandomFactorHigh, "rcEncryptedRandomFactorHigh");
  const rcKeyGenerator = getRescueKeyGeneratorFromPrivateKey(clientX25519PrivateKey, {
    umbraX25519PublicKey: networkConfig.mxePubkey
  });
  const rcCounters = [
    0n,
    1n,
    2n,
    3n
  ];
  const rcKeys = await rcKeyGenerator(rcCounters, rcEncryptionNonce);
  for (let index = 0; index < RESCUE_ENCRYPTIONS_COUNT3; index++) {
    const key = rcKeys.get(BigInt(index));
    if (key === void 0) {
      throw new MathematicsAssertionError(`Missing rescue key for counter ${String(index)}`, {
        value: void 0,
        expectedType: "rcKey",
        constraint: "defined"
      });
    }
    assertCurve25519FieldElement(key, `rcKey[${String(index)}]`);
  }
  const { low: arcisPublicKeyLow, high: arcisPublicKeyHigh } = splitX25519PublicKeyIntoTwoU128s(clientX25519PublicKey);
  const ephemeralMvkLowAsLimbs = convertU256ToBase85Limbs(createU256(ephemeralMvkLow));
  const ephemeralMvkHighAsLimbs = convertU256ToBase85Limbs(createU256(ephemeralMvkHigh));
  const randomFactorLowAsLimbs = convertU256ToBase85Limbs(BigInt(randomFactorLow));
  const randomFactorHighAsLimbs = convertU256ToBase85Limbs(BigInt(randomFactorHigh));
  const rcEncryptedMvkLowAsLimbs = convertU256ToBase85Limbs(rcEncryptedMvkLow);
  const rcEncryptedMvkHighAsLimbs = convertU256ToBase85Limbs(rcEncryptedMvkHigh);
  const rcEncryptedRandomFactorLowAsLimbs = convertU256ToBase85Limbs(rcEncryptedRandomFactorLow);
  const rcEncryptedRandomFactorHighAsLimbs = convertU256ToBase85Limbs(rcEncryptedRandomFactorHigh);
  const keysForMvkLowLimbs = convertU256ToBase85Limbs(rcKeys.get(0n));
  const keysForMvkHighLimbs = convertU256ToBase85Limbs(rcKeys.get(1n));
  const keysForRandomFactorLowLimbs = convertU256ToBase85Limbs(rcKeys.get(2n));
  const keysForRandomFactorHighLimbs = convertU256ToBase85Limbs(rcKeys.get(3n));
  const proofAccountOffset = generateRandomU1282();
  const burnerAccountOffset = generateRandomU1282();
  assertU128(BigInt(proofAccountOffset), "proofAccountOffset");
  assertU128(BigInt(burnerAccountOffset), "burnerAccountOffset");
  const rescueEncryptionCommitmentBlindingFactorOffsetBytes = kmac256(
    new TextEncoder().encode(RESCUE_ENCRYPTION_COMMITMENT_BLINDING_FACTOR_DOMAIN3),
    encodeU256ToU256BeBytes(claimModifiedGenerationIndexU256),
    { dkLen: KMAC_512_OUTPUT_LENGTH3 }
  );
  assertU512BeBytes(rescueEncryptionCommitmentBlindingFactorOffsetBytes);
  const blindingFactorForRescueCommitment = bn254FieldElementSampler(
    rescueEncryptionCommitmentBlindingFactorOffsetBytes
  );
  assertBn254FieldElement(blindingFactorForRescueCommitment, "blindingFactorForRescueCommitment");
  const rescueEncryptionCommitmentGenerator = getRescueCommitmentGenerator();
  const rcEncryptionCommitment = await rescueEncryptionCommitmentGenerator(
    [
      rcEncryptedMvkLow,
      rcEncryptedMvkHigh,
      rcEncryptedRandomFactorLow,
      rcEncryptedRandomFactorHigh
    ],
    [
      ephemeralMvkLow,
      ephemeralMvkHigh,
      createCurve25519FieldElement(randomFactorLow, "randomFactorLow"),
      createCurve25519FieldElement(randomFactorHigh, "randomFactorHigh")
    ],
    [
      rcKeys.get(0n),
      rcKeys.get(1n),
      rcKeys.get(2n),
      rcKeys.get(3n)
    ],
    blindingFactorForRescueCommitment,
    rcEncryptionNonce,
    arcisPublicKeyLow,
    arcisPublicKeyHigh
  );
  assertBn254FieldElement(BigInt(rcEncryptionCommitment), "rcEncryptionCommitment");
  let totalRelayerFixedSolFeesAccumulator = 0n;
  for (const utxo of utxosBatch) {
    totalRelayerFixedSolFeesAccumulator += BigInt(utxo.relayerFixedSolFees);
  }
  const totalRelayerFixedSolFees = createU64(
    totalRelayerFixedSolFeesAccumulator,
    "totalRelayerFixedSolFees"
  );
  const optionalDataBytes = optionalData;
  const fiatShamirInputBytes = new Uint8Array([
    // X25519 public key used for rescue encryption (32 bytes)
    ...clientX25519PublicKey,
    // Encryption nonce (16 bytes LE)
    ...encodeU128ToU128LeBytes(rcEncryptionNonce),
    // 4 rescue ciphertexts for ATA (32 bytes each)
    ...encodeU256ToU256LeBytes(rcEncryptedMvkLow),
    ...encodeU256ToU256LeBytes(rcEncryptedMvkHigh),
    ...encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow),
    ...encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh),
    // Rescue encryption commitment (32 bytes)
    ...encodeU256ToU256LeBytes(BigInt(rcEncryptionCommitment))
  ]);
  const fiatShamirChallenge = fiatShamirChallengeGenerator(fiatShamirInputBytes);
  assertCurve25519FieldElement(fiatShamirChallenge, "fiatShamirChallenge");
  const fiatShamirChallengeLimbs = convertU256ToBase85Limbs(fiatShamirChallenge);
  const modulePow = getModuloPowCurve25519Function();
  const challengeSquared = modulePow(fiatShamirChallenge, 2n);
  assertCurve25519FieldElement(challengeSquared, "challengeSquared");
  const challengeSquaredLimbs = convertU256ToBase85Limbs(challengeSquared);
  const inverseForMvkLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(ephemeralMvkLowAsLimbs)
  );
  const inverseForMvkHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(ephemeralMvkHighAsLimbs)
  );
  const inverseForRandomFactorLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(randomFactorLowAsLimbs)
  );
  const inverseForRandomFactorHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(randomFactorHighAsLimbs)
  );
  const inverseForChallengeLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(fiatShamirChallengeLimbs)
  );
  const inverseForChallengeSquaredLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(challengeSquaredLimbs)
  );
  const inverseForKeysForMvkLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(keysForMvkLowLimbs)
  );
  const inverseForKeysForMvkHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(keysForMvkHighLimbs)
  );
  const inverseForKeysForRandomFactorLowLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(keysForRandomFactorLowLimbs)
  );
  const inverseForKeysForRandomFactorHighLimbs = computeBn254LimbwiseSumInverse(
    limbsToTuple3(keysForRandomFactorHighLimbs)
  );
  assertBn254FieldElement(inverseForMvkLowLimbs, "inverseForMvkLowLimbs");
  assertBn254FieldElement(inverseForMvkHighLimbs, "inverseForMvkHighLimbs");
  assertBn254FieldElement(inverseForRandomFactorLowLimbs, "inverseForRandomFactorLowLimbs");
  assertBn254FieldElement(inverseForRandomFactorHighLimbs, "inverseForRandomFactorHighLimbs");
  assertBn254FieldElement(inverseForChallengeLimbs, "inverseForChallengeLimbs");
  assertBn254FieldElement(inverseForChallengeSquaredLimbs, "inverseForChallengeSquaredLimbs");
  assertBn254FieldElement(inverseForKeysForMvkLowLimbs, "inverseForKeysForMvkLowLimbs");
  assertBn254FieldElement(inverseForKeysForMvkHighLimbs, "inverseForKeysForMvkHighLimbs");
  assertBn254FieldElement(
    inverseForKeysForRandomFactorLowLimbs,
    "inverseForKeysForRandomFactorLowLimbs"
  );
  assertBn254FieldElement(
    inverseForKeysForRandomFactorHighLimbs,
    "inverseForKeysForRandomFactorHighLimbs"
  );
  const polynomialEvaluator = getPolynomialEvaluatorFunction();
  const polynomialEvaluationResultScalar = polynomialEvaluator(
    [ephemeralMvkLow, ephemeralMvkHigh, randomFactorForPolynomialCommitment],
    fiatShamirChallenge
  );
  assertCurve25519FieldElement(
    polynomialEvaluationResultScalar,
    "polynomialEvaluationResultScalar"
  );
  const polynomialEvaluationResultLimbs = convertU256ToBase85Limbs(
    polynomialEvaluationResultScalar
  );
  const merkleTreeDepth = utxosBatch[0].merklePath.length;
  const perLeafPathIndices = [];
  for (let index = 0; index < nLeaves; index++) {
    const rawPathBits = bigintToBinaryArrayLSB(
      BigInt(utxosBatch[index].leafIndex),
      merkleTreeDepth
    );
    const pathIndices = rawPathBits.map((bit) => BigInt(1 - bit));
    perLeafPathIndices.push(pathIndices);
  }
  const { low: relayerPublicKeyLow, high: relayerPublicKeyHigh } = splitAddressToLowHigh(relayerPublicKey);
  assertU128(relayerPublicKeyLow, "relayerPublicKeyLow");
  assertU128(relayerPublicKeyHigh, "relayerPublicKeyHigh");
  const optionalDataLow = BigInt(
    "0x" + Array.from(optionalDataBytes.slice(0, 16)).reverse().map((b) => b.toString(16).padStart(2, "0")).join("")
  );
  const optionalDataHigh = BigInt(
    "0x" + Array.from(optionalDataBytes.slice(16, 32)).reverse().map((b) => b.toString(16).padStart(2, "0")).join("")
  );
  assertU128(optionalDataLow, "optionalDataLow");
  assertU128(optionalDataHigh, "optionalDataHigh");
  const aggregatedHashInputs = [
    // [0] root (U256 LE bytes → Bn254FieldElement)
    createBn254FieldElement(decodeU256LeBytesToU256(merkleRoot)),
    // [1-2] mint address
    createBn254FieldElement(firstUtxo.mintAddressLow),
    createBn254FieldElement(firstUtxo.mintAddressHigh),
    // [3-5] challenge limbs
    createBn254FieldElement(fiatShamirChallengeLimbs.low),
    createBn254FieldElement(fiatShamirChallengeLimbs.middle),
    createBn254FieldElement(fiatShamirChallengeLimbs.high),
    // [6-8] challenge squared limbs
    createBn254FieldElement(challengeSquaredLimbs.low),
    createBn254FieldElement(challengeSquaredLimbs.middle),
    createBn254FieldElement(challengeSquaredLimbs.high),
    // [9-20] rescue encryptions (4 encryptions × 3 limbs = 12 elements for ATA)
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.low),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.middle),
    createBn254FieldElement(rcEncryptedMvkLowAsLimbs.high),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.low),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.middle),
    createBn254FieldElement(rcEncryptedMvkHighAsLimbs.high),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.low),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRandomFactorLowAsLimbs.high),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.low),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.middle),
    createBn254FieldElement(rcEncryptedRandomFactorHighAsLimbs.high),
    // [21] rescue encryption commitment
    rcEncryptionCommitment,
    // [22-24] polynomial evaluation result
    createBn254FieldElement(polynomialEvaluationResultLimbs.low),
    createBn254FieldElement(polynomialEvaluationResultLimbs.middle),
    createBn254FieldElement(polynomialEvaluationResultLimbs.high),
    // [25-26] relayer public key
    createBn254FieldElement(relayerPublicKeyLow),
    createBn254FieldElement(relayerPublicKeyHigh),
    // [27-28] final destination address
    createBn254FieldElement(destinationAddressLow),
    createBn254FieldElement(destinationAddressHigh),
    // [29] linker encryptions aggregated hash
    linkerEncryptionsAggregatedHash,
    // [30] linker keystream commitments aggregated hash
    linkerKeystreamCommitmentsAggregatedHash,
    // [31] nullifier hashes aggregated hash
    nullifierHashesAggregatedHash,
    // [32] total amount (PUBLIC in ATA - circuit order)
    createBn254FieldElement(totalAmount),
    // [33] total relayer fixed SOL fees (circuit order)
    createBn254FieldElement(totalRelayerFixedSolFees),
    // [34-35] optional data (low/high)
    createBn254FieldElement(optionalDataLow),
    createBn254FieldElement(optionalDataHigh),
    // [36-41] TVK timestamp fields (from executionTimestamp in UTC)
    createBn254FieldElement(tvkYear),
    createBn254FieldElement(tvkMonth),
    createBn254FieldElement(tvkDay),
    createBn254FieldElement(tvkHour),
    createBn254FieldElement(tvkMinute),
    createBn254FieldElement(tvkSecond),
    // [42-43] arcis public key (X25519 public key used for rescue encryption)
    createBn254FieldElement(arcisPublicKeyLow),
    createBn254FieldElement(arcisPublicKeyHigh),
    // [44] nonce (rescue encryption nonce)
    createBn254FieldElement(rcEncryptionNonce)
  ];
  if (aggregatedHashInputs.length !== AGGREGATED_HASH_INPUT_COUNT3) {
    throw new MathematicsAssertionError(
      `Expected ${String(AGGREGATED_HASH_INPUT_COUNT3)} aggregated hash inputs, got ${String(aggregatedHashInputs.length)}`,
      {
        value: aggregatedHashInputs.length,
        expectedType: "aggregatedHashInputCount",
        constraint: `=== ${String(AGGREGATED_HASH_INPUT_COUNT3)}`
      }
    );
  }
  const publicAggregatedHash = await poseidonAggregator(aggregatedHashInputs);
  assertBn254FieldElement(publicAggregatedHash, "publicAggregatedHash");
  const zkCircuitInputs = {
    // Per-UTXO H1 data
    version: utxosBatch.map((u) => createBn254FieldElement(u.version)),
    commitmentIndex: utxosBatch.map((u) => createBn254FieldElement(u.commitmentIndex)),
    senderAddressLow: utxosBatch.map((u) => createBn254FieldElement(u.senderAddressLow)),
    senderAddressHigh: utxosBatch.map((u) => createBn254FieldElement(u.senderAddressHigh)),
    relayerFixedSolFees: utxosBatch.map((u) => createBn254FieldElement(u.relayerFixedSolFees)),
    poolVolumeSpl: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.poolVolumeSpl))),
    poolVolumeSol: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.poolVolumeSol))),
    // Per-UTXO H2 data (amounts are PUBLIC in ATA)
    amount: perLeafAmounts.map((a) => createBn254FieldElement(a)),
    nullifier: perLeafNullifiers,
    h2BlindingFactor: perLeafRandomSecrets,
    // Mint address (shared)
    mintAddressLow: createBn254FieldElement(firstUtxo.mintAddressLow),
    mintAddressHigh: createBn254FieldElement(firstUtxo.mintAddressHigh),
    // Unlocking user (from EPHEMERAL derivation, NOT client)
    unlockingUserMasterViewingKey: ephemeralMasterViewingKey,
    unlockingUserMasterViewingKeyBlindingFactor: ephemeralMasterViewingKeyBlindingFactor,
    unlockingUserMasterViewingKeyLow: createBn254FieldElement(ephemeralMvkLow),
    unlockingUserMasterViewingKeyHigh: createBn254FieldElement(ephemeralMvkHigh),
    unlockingUserPrivateKey: ephemeralPoseidonPrivateKey,
    unlockingUserPrivateKeyBlindingFactor: ephemeralPoseidonPrivateKeyBlindingFactor,
    // Optional data (replaces ephemeral address)
    ephemeralAddressLow: createBn254FieldElement(optionalDataLow),
    ephemeralAddressHigh: createBn254FieldElement(optionalDataHigh),
    // Merkle tree - Note: ATA interface expects nested arrays for pathElements/pathIndices
    root: createBn254FieldElement(decodeU256LeBytesToU256(merkleRoot)),
    pathElements: utxosBatch.map(
      (u) => u.merklePath.map((p) => createBn254FieldElement(decodeU256LeBytesToU256(p)))
    ),
    pathIndices: perLeafPathIndices,
    // Per-UTXO timestamps (from deposit)
    utxoYear: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.year))),
    utxoMonth: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.month))),
    utxoDay: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.day))),
    utxoHour: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.hour))),
    utxoMinute: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.minute))),
    utxoSecond: utxosBatch.map((u) => createBn254FieldElement(BigInt(u.timestamp.second))),
    // TVK timestamps (from execution timestamp in UTC)
    tvkYear: createBn254FieldElement(tvkYear),
    tvkMonth: createBn254FieldElement(tvkMonth),
    tvkDay: createBn254FieldElement(tvkDay),
    tvkHour: createBn254FieldElement(tvkHour),
    tvkMinute: createBn254FieldElement(tvkMinute),
    tvkSecond: createBn254FieldElement(tvkSecond),
    // Linker keystream blinding factors (5 per leaf for ATA)
    linkerKeystreamBlindingFactors: allLinkerKeystreamBlindingFactors,
    // Limbwise representations (4 values for ATA - no amount/fees)
    unlockingUserMasterViewingKeyLowLimbs: [
      ephemeralMvkLowAsLimbs.low,
      ephemeralMvkLowAsLimbs.middle,
      ephemeralMvkLowAsLimbs.high
    ],
    unlockingUserMasterViewingKeyHighLimbs: [
      ephemeralMvkHighAsLimbs.low,
      ephemeralMvkHighAsLimbs.middle,
      ephemeralMvkHighAsLimbs.high
    ],
    randomFactorForPolynomialEvaluationLowLimbs: [
      randomFactorLowAsLimbs.low,
      randomFactorLowAsLimbs.middle,
      randomFactorLowAsLimbs.high
    ],
    randomFactorForPolynomialEvaluationHighLimbs: [
      randomFactorHighAsLimbs.low,
      randomFactorHighAsLimbs.middle,
      randomFactorHighAsLimbs.high
    ],
    // Challenge powers (only c and c^2 for ATA - degree 3 polynomial)
    challengeLimbs: [
      fiatShamirChallengeLimbs.low,
      fiatShamirChallengeLimbs.middle,
      fiatShamirChallengeLimbs.high
    ],
    challengeSquaredLimbs: [
      challengeSquaredLimbs.low,
      challengeSquaredLimbs.middle,
      challengeSquaredLimbs.high
    ],
    // Inverses (simplified for ATA - no fee inverses)
    inverseForUnlockingUserMasterViewingKeyLowLimbs: inverseForMvkLowLimbs,
    inverseForUnlockingUserMasterViewingKeyHighLimbs: inverseForMvkHighLimbs,
    randomFactorForPolynomialEvaluationLowLimbsInverse: inverseForRandomFactorLowLimbs,
    randomFactorForPolynomialEvaluationHighLimbsInverse: inverseForRandomFactorHighLimbs,
    challengeLimbsInverse: inverseForChallengeLimbs,
    challengeSquaredLimbsInverse: inverseForChallengeSquaredLimbs,
    // Rescue encryption keys (4 keys for ATA - no amount/fee keys)
    keysForUnlockingMasterViewingKeyLowLimbs: [
      keysForMvkLowLimbs.low,
      keysForMvkLowLimbs.middle,
      keysForMvkLowLimbs.high
    ],
    keysForUnlockingMasterViewingKeyHighLimbs: [
      keysForMvkHighLimbs.low,
      keysForMvkHighLimbs.middle,
      keysForMvkHighLimbs.high
    ],
    keysForRandomFactorForPolynomialEvaluationLowLimbs: [
      keysForRandomFactorLowLimbs.low,
      keysForRandomFactorLowLimbs.middle,
      keysForRandomFactorLowLimbs.high
    ],
    keysForRandomFactorForPolynomialEvaluationHighLimbs: [
      keysForRandomFactorHighLimbs.low,
      keysForRandomFactorHighLimbs.middle,
      keysForRandomFactorHighLimbs.high
    ],
    // Key inverses (4 for ATA)
    keysForUnlockingMasterViewingKeyLowLimbsInverse: inverseForKeysForMvkLowLimbs,
    keysForUnlockingMasterViewingKeyHighLimbsInverse: inverseForKeysForMvkHighLimbs,
    keysForRandomFactorForPolynomialEvaluationLowLimbsInverse: inverseForKeysForRandomFactorLowLimbs,
    keysForRandomFactorForPolynomialEvaluationHighLimbsInverse: inverseForKeysForRandomFactorHighLimbs,
    // Rescue commitment
    blindingFactorForRescueCommitment,
    nonce: createBn254FieldElement(rcEncryptionNonce),
    arcisPublicKeyLow: createBn254FieldElement(arcisPublicKeyLow),
    arcisPublicKeyHigh: createBn254FieldElement(arcisPublicKeyHigh),
    // Addresses
    relayerPublicKeyLow: createBn254FieldElement(relayerPublicKeyLow),
    relayerPublicKeyHigh: createBn254FieldElement(relayerPublicKeyHigh),
    finalDestinationAddressLow: createBn254FieldElement(destinationAddressLow),
    finalDestinationAddressHigh: createBn254FieldElement(destinationAddressHigh),
    // Public input
    publicAggregatedHash
  };
  const { proofA, proofB, proofC } = await zkProver.prove(zkCircuitInputs);
  assertGroth16ProofA(proofA, "proofA");
  assertGroth16ProofB(proofB, "proofB");
  assertGroth16ProofC(proofC, "proofC");
  const toBase64 = /* @__PURE__ */ __name((bytes) => {
    let binary = "";
    for (const byte of bytes) {
      binary += String.fromCharCode(byte);
    }
    return btoa(binary);
  }, "toBase64");
  const proofAccountData = {
    tvk_timestamp: Math.floor(executionTimestamp.getTime() / 1e3),
    rescue_encryption_public_key: toBase64(new Uint8Array(clientX25519PublicKey)),
    encryption_nonce: rcEncryptionNonce.toString(),
    rescue_encrypted_master_viewing_key_low: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedMvkLow))),
    rescue_encrypted_master_viewing_key_high: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedMvkHigh))),
    rescue_encrypted_blinding_factor_low: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow))),
    rescue_encrypted_blinding_factor_high: toBase64(new Uint8Array(encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh))),
    rescue_encryption_commitment: toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(rcEncryptionCommitment)))),
    encryption_validation_polynomial: toBase64(new Uint8Array(encodeU256ToU256LeBytes(polynomialEvaluationResultScalar))),
    merkle_root: toBase64(new Uint8Array(merkleRoot)),
    groth16_proof_a: toBase64(new Uint8Array(proofA)),
    groth16_proof_b: toBase64(new Uint8Array(proofB)),
    groth16_proof_c: toBase64(new Uint8Array(proofC))
  };
  const utxoSlotData = [];
  for (let leafIdx = 0; leafIdx < nLeaves; leafIdx++) {
    const linkerEncStartIdx = leafIdx * LINKER_ENCRYPTIONS_PER_LEAF3;
    utxoSlotData.push({
      slot_index: leafIdx,
      nullifier: toBase64(
        new Uint8Array(encodeU256ToU256LeBytes(BigInt(nullifierHashes[leafIdx])))
      ),
      linker_encryptions: allLinkerEncryptions.slice(linkerEncStartIdx, linkerEncStartIdx + LINKER_ENCRYPTIONS_PER_LEAF3).map((enc) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(enc))))),
      linker_key_commitments: allLinkerKeystreamCommitments.slice(linkerEncStartIdx, linkerEncStartIdx + LINKER_ENCRYPTIONS_PER_LEAF3).map((comm) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(BigInt(comm)))))
    });
  }
  const protocolFeeConfig = await protocolFeeProvider(BigInt(totalAmount));
  const relayerFeeConfig = await relayerFeeProvider(BigInt(totalAmount));
  const feeProofData = {
    amount: String(totalAmount),
    relayer_fixed_sol_fees: String(totalRelayerFixedSolFees),
    protocol_fees_amount_lower_bound: String(protocolFeeConfig.slab.amountLowerBound),
    protocol_fees_amount_upper_bound: String(protocolFeeConfig.slab.amountUpperBound),
    protocol_fees_base_fees_in_spl: String(protocolFeeConfig.slab.baseFee),
    protocol_fees_commission_fee_in_spl: Number(protocolFeeConfig.slab.bps),
    protocol_fees_merkle_path: protocolFeeConfig.merklePath.map(
      (h) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(h)))
    ),
    protocol_fees_leaf_index: Number(protocolFeeConfig.leafIndex),
    relayer_fees_amount_lower_bound: String(relayerFeeConfig.slab.amountLowerBound),
    relayer_fees_amount_upper_bound: String(relayerFeeConfig.slab.amountUpperBound),
    relayer_fees_base_fees_in_spl: String(relayerFeeConfig.slab.baseFee),
    relayer_fees_commission_fee_in_spl: Number(relayerFeeConfig.slab.bps),
    relayer_fees_merkle_path: relayerFeeConfig.merklePath.map(
      (h) => toBase64(new Uint8Array(encodeU256ToU256LeBytes(h)))
    ),
    relayer_fees_leaf_index: Number(relayerFeeConfig.leafIndex)
  };
  const claimRequest = {
    variant: "public_balance",
    user_pubkey: destinationAddress,
    mint: mintAddress,
    stealth_pool_index: Number(stealthPoolIndex),
    proof_account_data: proofAccountData,
    utxo_slot_data: utxoSlotData,
    max_utxo_capacity: nLeaves,
    optional_data: toBase64(optionalDataBytes),
    fee_proof_data: feeProofData
  };
  const submitResponse = await submitClaim(claimRequest);
  if (awaitCompletion) {
    const { pollClaimUntilTerminal: pollClaimUntilTerminal2 } = await import('./relayer-NRRMSMNB.js');
    const result = await pollClaimUntilTerminal2(pollClaimStatus, submitResponse.requestId, {
      pollingIntervalMs,
      timeoutMs,
      onProgress
    });
    return { ...result, utxoIds };
  }
  return {
    requestId: submitResponse.requestId,
    status: "received",
    utxoIds
  };
}
__name(processUtxoInBatch3, "processUtxoInBatch");
function getSelfClaimableUtxoToPublicBalanceClaimerFunction(args, deps) {
  const { client, generationIndex: providedGenerationIndex } = args;
  if (deps === void 0) {
    throw new MathematicsAssertionError("deps with zkProver and relayer is required", {
      value: void 0,
      expectedType: "GetSelfClaimableUtxoToPublicBalanceClaimerFunctionDeps",
      constraint: "must be provided"
    });
  }
  const fetchBatchMerkleProof = deps.fetchBatchMerkleProof;
  const submitClaim = deps.relayer.submitClaim;
  const pollClaimStatus = deps.relayer.pollClaimStatus;
  const getRelayerAddress = deps.relayer.getRelayerAddress;
  const awaitCompletion = deps.awaitCompletion ?? true;
  const pollingIntervalMs = deps.pollingIntervalMs ?? 3e3;
  const timeoutMs = deps.timeoutMs ?? 12e4;
  const onProgress = deps.onProgress;
  const ephemeralUtxoMvkGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyDeriver ?? getEphemeralUtxoMasterViewingKeyDeriver({ client });
  const ephemeralUtxoMvkBlindingFactorGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyBlindingFactorDeriver ?? getEphemeralUtxoMasterViewingKeyBlindingFactorDeriver({
    client
  });
  const ephemeralUtxoPoseidonKeyGenerator = deps.keys?.ephemeralUtxoPoseidonKeyGenerator ?? getEphemeralUtxoPoseidonPrivateKeyDeriver({ client });
  const ephemeralUtxoPoseidonKeyBlindingFactorGenerator = deps.keys?.ephemeralUtxoPoseidonKeyBlindingFactorGenerator ?? getEphemeralUtxoPoseidonPrivateKeyBlindingFactorDeriver({
    client
  });
  const poseidonHasher = deps.crypto?.poseidonHasher ?? getPoseidonHasher();
  const poseidonAggregator = deps.crypto?.poseidonAggregator ?? getPoseidonAggregator();
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGeneratorFunction = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const userCommitmentGeneratorFunction = deps.crypto?.userCommitmentGenerator ?? getUserCommitmentGeneratorFunction();
  const keystreamCommitmentGeneratorFunction = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const fiatShamirChallengeGeneratorFunction = deps.crypto?.fiatShamirChallengeGenerator ?? getFiatShamirChallengeGeneratorFunction();
  const generateRandomU2562 = deps.random?.generateRandomU256 ?? generateRandomU256;
  const generateRandomU1282 = deps.random?.generateRandomU128 ?? generateRandomU128;
  const protocolFeeProviderFn = deps.fees?.protocolFeeProvider ?? getHardcodedClaimUtxoProtocolFeeProvider();
  const relayerFeeProviderFn = deps.fees?.relayerFeeProvider ?? getHardcodedClaimUtxoRelayerFeeProvider();
  return async (utxos, optionalData, microLamportsPerAcu) => {
    try {
      if (utxos.length === 0) {
        return { batches: /* @__PURE__ */ new Map() };
      }
      const masterSeedBytes = await client.masterSeed.getMasterSeed();
      const generationIndex = providedGenerationIndex ?? generateRandomU2562();
      const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      const clientX25519PublicKey = keypairResult.x25519Keypair.publicKey;
      const relayerAddress = await getRelayerAddress();
      const stealthPoolIndex = 0n;
      const utxosByDestination = /* @__PURE__ */ new Map();
      for (const utxo of utxos) {
        const existing = utxosByDestination.get(utxo.destinationAddress) ?? [];
        existing.push(utxo);
        utxosByDestination.set(utxo.destinationAddress, existing);
      }
      const batchConfigs = [];
      let batchIndex = 0n;
      for (const [, destinationUtxos] of utxosByDestination) {
        for (let index = 0; index < destinationUtxos.length; index += MAX_UTXOS_PER_PROOF3) {
          const batch = destinationUtxos.slice(index, index + MAX_UTXOS_PER_PROOF3);
          const batchGenerationOffset = BigInt(generationIndex) + batchIndex;
          batchConfigs.push({
            batchIndex,
            utxos: batch,
            generationOffset: batchGenerationOffset
          });
          batchIndex = batchIndex + 1n;
        }
      }
      const batchResults = await Promise.all(
        batchConfigs.map(async (config) => {
          const treeIndex = config.utxos[0].treeIndex;
          const insertionIndices = config.utxos.map((u) => u.insertionIndex);
          const batchProof = await fetchBatchMerkleProof(treeIndex, insertionIndices);
          const claimableUtxos = enrichWithMerkleProof(config.utxos, batchProof);
          const batchArgs = {
            utxosBatch: claimableUtxos,
            stealthPoolIndex,
            generationIndex: config.generationOffset,
            masterSeedBytes,
            relayerAddress,
            clientX25519PrivateKey,
            clientX25519PublicKey,
            networkConfig: client.networkConfig,
            optionalData: optionalData ?? new Uint8Array(32),
            microLamportsPerAcu: microLamportsPerAcu ?? 0n
          };
          const batchDeps = {
            submitClaim,
            pollClaimStatus,
            awaitCompletion,
            pollingIntervalMs,
            timeoutMs,
            onProgress,
            getRelayerAddress,
            ephemeralUtxoMvkGenerator,
            ephemeralUtxoMvkBlindingFactorGenerator,
            ephemeralUtxoPoseidonKeyGenerator,
            ephemeralUtxoPoseidonKeyBlindingFactorGenerator,
            poseidonHasher,
            poseidonAggregator,
            poseidonEncryptor,
            poseidonKeystreamGenerator: poseidonKeystreamGeneratorFunction,
            userCommitmentGenerator: userCommitmentGeneratorFunction,
            keystreamCommitmentGenerator: keystreamCommitmentGeneratorFunction,
            fiatShamirChallengeGenerator: fiatShamirChallengeGeneratorFunction,
            zkProver: deps.zkProver,
            generateRandomU128: generateRandomU1282,
            protocolFeeProvider: protocolFeeProviderFn,
            relayerFeeProvider: relayerFeeProviderFn
          };
          const batchResult = await processUtxoInBatch3(batchArgs, batchDeps);
          return {
            batchIndex: config.batchIndex,
            batchResult
          };
        })
      );
      const resultsByBatch = /* @__PURE__ */ new Map();
      for (const result of batchResults) {
        resultsByBatch.set(result.batchIndex, result.batchResult);
      }
      return {
        batches: resultsByBatch
      };
    } catch (error) {
      if (error instanceof ClaimUtxoError) {
        throw error;
      }
      if (error instanceof CryptographyError) {
        throw new ClaimUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new ClaimUtxoError("initialization", error.message, error);
      }
      throw new ClaimUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  };
}
__name(getSelfClaimableUtxoToPublicBalanceClaimerFunction, "getSelfClaimableUtxoToPublicBalanceClaimerFunction");
var DEFAULT_OPTIONAL_DATA_BYTES = new Uint8Array(32);
function getComplianceGrantIssuerFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  const masterViewingKeyX25519KeypairDeriver = deps?.keys?.masterViewingKeyX25519KeypairDeriver ?? getMasterViewingKeyX25519KeypairDeriver({ client });
  return /* @__PURE__ */ __name(async function createUserGrantedComplianceGrant(receiver, granterX25519, receiverX25519, nonce, optionalData, callbacks) {
    const userAddress = signer.address;
    const programId = client.networkConfig.programId;
    const keypairResult = await masterViewingKeyX25519KeypairDeriver();
    const mvkProvingSigner = await createKeyPairSignerFromPrivateKeyBytes(
      keypairResult.ed25519Keypair.seed
    );
    await findEncryptedUserAccountPda(userAddress, programId);
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getCreateUserGrantInstructionAsync({
      granterX25519ProvingSigner: mvkProvingSigner,
      feePayer: noopSigner,
      nonce: { first: nonce },
      granterX25519: { first: granterX25519 },
      receiverX25519: { first: receiverX25519 },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
    const transactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([computeBudgetInstruction, instruction], m)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "createUserGrantedComplianceGrant");
}
__name(getComplianceGrantIssuerFunction, "getComplianceGrantIssuerFunction");
var DEFAULT_OPTIONAL_DATA_BYTES2 = new Uint8Array(32);
function getComplianceGrantRevokerFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  const masterViewingKeyX25519KeypairDeriver = deps?.keys?.masterViewingKeyX25519KeypairDeriver ?? getMasterViewingKeyX25519KeypairDeriver({ client });
  return /* @__PURE__ */ __name(async function deleteUserGrantedComplianceGrant(receiver, granterX25519, receiverX25519, nonce, optionalData, callbacks) {
    const userAddress = signer.address;
    const programId = client.networkConfig.programId;
    const keypairResult = await masterViewingKeyX25519KeypairDeriver();
    const mvkProvingSigner = await createKeyPairSignerFromPrivateKeyBytes(
      keypairResult.ed25519Keypair.seed
    );
    await findEncryptedUserAccountPda(userAddress, programId);
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getDeleteUserGrantInstructionAsync({
      feePayer: noopSigner,
      granterX25519ProvingSigner: mvkProvingSigner,
      nonce: { first: nonce },
      granterX25519: { first: granterX25519 },
      receiverX25519: { first: receiverX25519 },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES2 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
    const transactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([computeBudgetInstruction, instruction], m)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "deleteUserGrantedComplianceGrant");
}
__name(getComplianceGrantRevokerFunction, "getComplianceGrantRevokerFunction");
var INSTRUCTION_NAME = "reencrypt_user_grant_v11";
var ZERO_CIPHERTEXT = { first: new Uint8Array(32) };
var DEFAULT_OPTIONAL_DATA_BYTES3 = new Uint8Array(32);
var MAX_CIPHERTEXTS = 6;
function getSharedCiphertextReencryptorForUserGrantFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = client.accountInfoProvider;
  const getLatestBlockhash = deps?.getLatestBlockhash ?? client.blockhashProvider;
  const transactionForwarder = deps?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function reencryptSharedCiphertextsUserGrant(granterX25519Key, receiverX25519Key, nonce, inputEncryptionNonce, ciphertexts, optionalData, callbacks, microLamportsPerAcu, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    if (ciphertexts.length === 0 || ciphertexts.length > MAX_CIPHERTEXTS) {
      throw new Error(
        `ciphertexts must have 1–${String(MAX_CIPHERTEXTS)} elements, got ${String(ciphertexts.length)}`
      );
    }
    const userAddress = signer.address;
    const padded = Array.from(
      { length: MAX_CIPHERTEXTS },
      (_, index) => index < ciphertexts.length ? { first: ciphertexts[index] } : ZERO_CIPHERTEXT
    );
    const accountMap = await accountInfoProvider([client.networkConfig.mxeAccountAddress], { commitment: accountInfoCommitment });
    const clusterOffset = extractClusterOffsetFromMxeAccount(
      accountMap.get(client.networkConfig.mxeAccountAddress)
    );
    const computationOffset = generateRandomU64();
    const mpcCallbackDataOffset = generateRandomU128();
    const arciumAccounts = await findArciumInfrastructurePdas(
      client.networkConfig.programId,
      client.networkConfig.arciumProgramAddress,
      clusterOffset,
      BigInt(computationOffset),
      INSTRUCTION_NAME
    );
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getReencryptUserGrantV11InstructionAsync(
      {
        receiver: noopSigner,
        feePayer: noopSigner,
        mxeAccount: arciumAccounts.mxeAccount,
        mempoolAccount: arciumAccounts.mempoolAccount,
        executingPool: arciumAccounts.executingPoolAccount,
        computationAccount: arciumAccounts.computationAccount,
        compDefAccount: arciumAccounts.compDefAccount,
        clusterAccount: arciumAccounts.clusterAccount,
        computationOffset: { first: computationOffset },
        granterX25519Key: { first: granterX25519Key },
        receiverX25519Key: { first: receiverX25519Key },
        nonce: { first: nonce },
        inputEncryptionNonce: { first: inputEncryptionNonce },
        inputCiphertext0: padded[0],
        inputCiphertext1: padded[1],
        inputCiphertext2: padded[2],
        inputCiphertext3: padded[3],
        inputCiphertext4: padded[4],
        inputCiphertext5: padded[5],
        mpcCallbackDataOffset: { first: mpcCallbackDataOffset },
        priorityFees: { first: microLamportsPerAcu ?? 0n },
        optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES3 }
      },
      { programAddress: client.networkConfig.programId }
    );
    const latestBlockhash = await getLatestBlockhash();
    const baseTransactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, INSTRUCTION_NAME);
    const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
      baseTransactionMessage,
      buildAltAddressesRecord(altEntry)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "reencryptSharedCiphertextsUserGrant");
}
__name(getSharedCiphertextReencryptorForUserGrantFunction, "getSharedCiphertextReencryptorForUserGrantFunction");
var INSTRUCTION_NAME2 = "reencrypt_network_grant_for_network_balance_v11";
var ZERO_CIPHERTEXT2 = { first: new Uint8Array(32) };
var DEFAULT_OPTIONAL_DATA_BYTES4 = new Uint8Array(32);
var MAX_CIPHERTEXTS2 = 6;
function getNetworkCiphertextReencryptorForNetworkGrantFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = client.accountInfoProvider;
  const getLatestBlockhash = deps?.getLatestBlockhash ?? client.blockhashProvider;
  const transactionForwarder = deps?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function reencryptMxeCiphertextsNetworkGrant(receiverX25519Key, nonce, inputEncryptionNonce, ciphertexts, optionalData, callbacks, microLamportsPerAcu, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    if (ciphertexts.length === 0 || ciphertexts.length > MAX_CIPHERTEXTS2) {
      throw new Error(
        `ciphertexts must have 1–${String(MAX_CIPHERTEXTS2)} elements, got ${String(ciphertexts.length)}`
      );
    }
    const userAddress = signer.address;
    const padded = Array.from(
      { length: MAX_CIPHERTEXTS2 },
      (_, index) => index < ciphertexts.length ? { first: ciphertexts[index] } : ZERO_CIPHERTEXT2
    );
    const accountMap = await accountInfoProvider([client.networkConfig.mxeAccountAddress], { commitment: accountInfoCommitment });
    const clusterOffset = extractClusterOffsetFromMxeAccount(
      accountMap.get(client.networkConfig.mxeAccountAddress)
    );
    const computationOffset = generateRandomU64();
    const mpcCallbackDataOffset = generateRandomU128();
    const arciumAccounts = await findArciumInfrastructurePdas(
      client.networkConfig.programId,
      client.networkConfig.arciumProgramAddress,
      clusterOffset,
      BigInt(computationOffset),
      INSTRUCTION_NAME2
    );
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getReencryptNetworkGrantForNetworkBalanceV11InstructionAsync(
      {
        receiver: noopSigner,
        feePayer: noopSigner,
        mxeAccount: arciumAccounts.mxeAccount,
        mempoolAccount: arciumAccounts.mempoolAccount,
        executingPool: arciumAccounts.executingPoolAccount,
        computationAccount: arciumAccounts.computationAccount,
        compDefAccount: arciumAccounts.compDefAccount,
        clusterAccount: arciumAccounts.clusterAccount,
        computationOffset: { first: computationOffset },
        receiverX25519Key: { first: receiverX25519Key },
        nonce: { first: nonce },
        inputEncryptionNonce: { first: inputEncryptionNonce },
        inputCiphertext0: padded[0],
        inputCiphertext1: padded[1],
        inputCiphertext2: padded[2],
        inputCiphertext3: padded[3],
        inputCiphertext4: padded[4],
        inputCiphertext5: padded[5],
        mpcCallbackDataOffset: { first: mpcCallbackDataOffset },
        priorityFees: { first: microLamportsPerAcu ?? 0n },
        optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES4 }
      },
      { programAddress: client.networkConfig.programId }
    );
    const latestBlockhash = await getLatestBlockhash();
    const baseTransactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, INSTRUCTION_NAME2);
    const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
      baseTransactionMessage,
      buildAltAddressesRecord(altEntry)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "reencryptMxeCiphertextsNetworkGrant");
}
__name(getNetworkCiphertextReencryptorForNetworkGrantFunction, "getNetworkCiphertextReencryptorForNetworkGrantFunction");
var INSTRUCTION_NAME3 = "reencrypt_network_grant_for_shared_balance_v11";
var ZERO_CIPHERTEXT3 = { first: new Uint8Array(32) };
var DEFAULT_OPTIONAL_DATA_BYTES5 = new Uint8Array(32);
var MAX_CIPHERTEXTS3 = 6;
function getSharedCiphertextReencryptorForNetworkGrantFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = client.accountInfoProvider;
  const getLatestBlockhash = deps?.getLatestBlockhash ?? client.blockhashProvider;
  const transactionForwarder = deps?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function reencryptSharedCiphertextsNetworkGrant(granterX25519Key, receiverX25519Key, nonce, inputEncryptionNonce, ciphertexts, optionalData, callbacks, microLamportsPerAcu, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    if (ciphertexts.length === 0 || ciphertexts.length > MAX_CIPHERTEXTS3) {
      throw new Error(
        `ciphertexts must have 1–${String(MAX_CIPHERTEXTS3)} elements, got ${String(ciphertexts.length)}`
      );
    }
    const userAddress = signer.address;
    const padded = Array.from(
      { length: MAX_CIPHERTEXTS3 },
      (_, index) => index < ciphertexts.length ? { first: ciphertexts[index] } : ZERO_CIPHERTEXT3
    );
    const accountMap = await accountInfoProvider([client.networkConfig.mxeAccountAddress], { commitment: accountInfoCommitment });
    const clusterOffset = extractClusterOffsetFromMxeAccount(
      accountMap.get(client.networkConfig.mxeAccountAddress)
    );
    const computationOffset = generateRandomU64();
    const mpcCallbackDataOffset = generateRandomU128();
    const arciumAccounts = await findArciumInfrastructurePdas(
      client.networkConfig.programId,
      client.networkConfig.arciumProgramAddress,
      clusterOffset,
      BigInt(computationOffset),
      INSTRUCTION_NAME3
    );
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getReencryptNetworkGrantForSharedBalanceV11InstructionAsync({
      receiver: noopSigner,
      feePayer: noopSigner,
      mxeAccount: arciumAccounts.mxeAccount,
      mempoolAccount: arciumAccounts.mempoolAccount,
      executingPool: arciumAccounts.executingPoolAccount,
      computationAccount: arciumAccounts.computationAccount,
      compDefAccount: arciumAccounts.compDefAccount,
      clusterAccount: arciumAccounts.clusterAccount,
      computationOffset: { first: computationOffset },
      granterX25519Key: { first: granterX25519Key },
      receiverX25519Key: { first: receiverX25519Key },
      nonce: { first: nonce },
      inputEncryptionNonce: { first: inputEncryptionNonce },
      inputCiphertext0: padded[0],
      inputCiphertext1: padded[1],
      inputCiphertext2: padded[2],
      inputCiphertext3: padded[3],
      inputCiphertext4: padded[4],
      inputCiphertext5: padded[5],
      mpcCallbackDataOffset: { first: mpcCallbackDataOffset },
      priorityFees: { first: microLamportsPerAcu ?? 0n },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES5 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const baseTransactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, INSTRUCTION_NAME3);
    const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
      baseTransactionMessage,
      buildAltAddressesRecord(altEntry)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "reencryptSharedCiphertextsNetworkGrant");
}
__name(getSharedCiphertextReencryptorForNetworkGrantFunction, "getSharedCiphertextReencryptorForNetworkGrantFunction");

// src/umbra/services/compliance/query-compliance-grant.ts
function getUserComplianceGrantQuerierFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps?.accountInfoProvider ?? client.accountInfoProvider;
  const umbraProgram = client.networkConfig.programId;
  return /* @__PURE__ */ __name(async function queryUserComplianceGrant(granterX25519, nonce, receiverX25519, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const pda = await findUserComplianceGrantPda(
      granterX25519,
      nonce,
      receiverX25519,
      umbraProgram
    );
    const accountMap = await accountInfoProvider([pda], { commitment: accountInfoCommitment });
    const maybeAccount = accountMap.get(pda);
    if (maybeAccount?.exists !== true) {
      return { state: "non_existent" };
    }
    return { state: "exists" };
  }, "queryUserComplianceGrant");
}
__name(getUserComplianceGrantQuerierFunction, "getUserComplianceGrantQuerierFunction");
function getNetworkComplianceGrantQuerierFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps?.accountInfoProvider ?? client.accountInfoProvider;
  const umbraProgram = client.networkConfig.programId;
  return /* @__PURE__ */ __name(async function queryNetworkMxeComplianceGrant(nonce, receiverX25519, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const pda = await findNetworkComplianceGrantPda(nonce, receiverX25519, umbraProgram);
    const accountMap = await accountInfoProvider([pda], { commitment: accountInfoCommitment });
    const maybeAccount = accountMap.get(pda);
    if (maybeAccount?.exists !== true) {
      return { state: "non_existent" };
    }
    return { state: "exists" };
  }, "queryNetworkMxeComplianceGrant");
}
__name(getNetworkComplianceGrantQuerierFunction, "getNetworkComplianceGrantQuerierFunction");
function getSharedComplianceGrantQuerierFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps?.accountInfoProvider ?? client.accountInfoProvider;
  const umbraProgram = client.networkConfig.programId;
  return /* @__PURE__ */ __name(async function queryNetworkSharedComplianceGrant(granterX25519, nonce, receiverX25519, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const pda = await findSharedComplianceGrantPda(
      granterX25519,
      nonce,
      receiverX25519,
      umbraProgram
    );
    const accountMap = await accountInfoProvider([pda], { commitment: accountInfoCommitment });
    const maybeAccount = accountMap.get(pda);
    if (maybeAccount?.exists !== true) {
      return { state: "non_existent" };
    }
    return { state: "exists" };
  }, "queryNetworkSharedComplianceGrant");
}
__name(getSharedComplianceGrantQuerierFunction, "getSharedComplianceGrantQuerierFunction");
var TOKEN_ACCOUNT_STATUS_BITS = {
  IS_INITIALISED: 0,
  IS_SHARED_MODE: 3,
  IS_ARCIUM_BALANCE_INITIALISED: 4
};
function isBitSet(value, bitPosition) {
  return (BigInt(value) & 1n << BigInt(bitPosition)) !== 0n;
}
__name(isBitSet, "isBitSet");
var DEFAULT_OPTIONAL_DATA_BYTES6 = new Uint8Array(32);
function getNetworkEncryptionToSharedEncryptionConverterFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = deps?.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function convertToSharedEncryption(mints, optionalData, callbacks, microLamportsPerAcu, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    try {
      const converted = /* @__PURE__ */ new Map();
      const skipped = /* @__PURE__ */ new Map();
      if (mints.length === 0) {
        return { converted, skipped };
      }
      const userAddress = signer.address;
      const pdaDerivations = await Promise.all(
        mints.map(async (mint) => ({
          mint,
          pda: await findEncryptedTokenAccountPda(
            userAddress,
            mint,
            client.networkConfig.programId
          )
        }))
      );
      const pdas = pdaDerivations.map(({ pda }) => pda);
      const accountMap = await accountInfoProvider([client.networkConfig.mxeAccountAddress, ...pdas], { commitment: accountInfoCommitment });
      const clusterOffset = extractClusterOffsetFromMxeAccount(
        accountMap.get(client.networkConfig.mxeAccountAddress)
      );
      const eligibleMints = [];
      for (const { mint, pda } of pdaDerivations) {
        const maybeAccount = accountMap.get(pda);
        if (maybeAccount === void 0 || !maybeAccount.exists || maybeAccount.data === void 0) {
          skipped.set(mint, "non_existent");
          continue;
        }
        const decodedAccount = decodeEncryptedTokenAccount(
          maybeAccount
        );
        const statusBits = decodedAccount.data.statusBits.first;
        const isInitialised = isBitSet(statusBits, TOKEN_ACCOUNT_STATUS_BITS.IS_INITIALISED);
        const isSharedMode = isBitSet(statusBits, TOKEN_ACCOUNT_STATUS_BITS.IS_SHARED_MODE);
        const isArciumBalanceInitialised = isBitSet(
          statusBits,
          TOKEN_ACCOUNT_STATUS_BITS.IS_ARCIUM_BALANCE_INITIALISED
        );
        if (!isInitialised) {
          skipped.set(mint, "not_initialised");
          continue;
        }
        if (isSharedMode) {
          skipped.set(mint, "already_shared");
          continue;
        }
        if (!isArciumBalanceInitialised) {
          skipped.set(mint, "balance_not_initialised");
          continue;
        }
        eligibleMints.push(mint);
      }
      if (eligibleMints.length === 0) {
        return { converted, skipped };
      }
      const latestBlockhash = await getLatestBlockhash();
      const transactionPromises = eligibleMints.map(async (mint) => {
        const computationOffset = generateRandomU64();
        const mpcCallbackDataOffset = generateRandomU128();
        const arciumAccounts = await findArciumInfrastructurePdas(
          client.networkConfig.programId,
          client.networkConfig.arciumProgramAddress,
          clusterOffset,
          BigInt(computationOffset),
          "convert_network_balance_to_shared_balance_v11"
        );
        const noopSigner = createNoopSigner(userAddress);
        const instruction = await getConvertNetworkBalanceToSharedBalanceV11InstructionAsync({
          // Signers (noop signer for instruction building)
          userAddress: noopSigner,
          feePayer: noopSigner,
          // Arcium accounts
          mxeAccount: arciumAccounts.mxeAccount,
          mempoolAccount: arciumAccounts.mempoolAccount,
          executingPool: arciumAccounts.executingPoolAccount,
          computationAccount: arciumAccounts.computationAccount,
          compDefAccount: arciumAccounts.compDefAccount,
          clusterAccount: arciumAccounts.clusterAccount,
          // Token mint
          mint,
          // Instruction data
          computationOffset: {
            first: computationOffset
          },
          mpcCallbackDataOffset: {
            first: mpcCallbackDataOffset
          },
          priorityFees: { first: microLamportsPerAcu ?? 0n },
          optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES6 }
        }, { programAddress: client.networkConfig.programId });
        const baseTransactionMessage = pipe(
          createTransactionMessage({ version: 0 }),
          (m) => setTransactionMessageFeePayer(userAddress, m),
          (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
          (m) => appendTransactionMessageInstructions([instruction], m)
        );
        const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, "convert_network_balance_to_shared_balance_v11");
        const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
          baseTransactionMessage,
          buildAltAddressesRecord(altEntry)
        );
        const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
        const signedTransaction = await signer.signTransaction(
          partiallySignedTransaction
        );
        return { mint, signedTransaction };
      });
      const signedTransactions = await Promise.all(transactionPromises);
      const transactionsToForward = signedTransactions.map(
        ({ signedTransaction }) => signedTransaction
      );
      for (const { signedTransaction } of signedTransactions) {
        await callbacks?.pre?.(signedTransaction);
      }
      const signatures = await transactionForwarder.forwardInParallel(transactionsToForward);
      for (const [index, signedTransaction] of signedTransactions.entries()) {
        const { mint } = signedTransaction;
        const signature = signatures[index];
        if (signature !== void 0) {
          converted.set(mint, signature);
          await callbacks?.post?.(signedTransaction.signedTransaction, signature);
        }
      }
      return { converted, skipped };
    } catch (error) {
      if (error instanceof ConversionError) {
        throw error;
      }
      if (error instanceof TransactionSigningError) {
        throw new ConversionError("transaction-sign", error.message, error);
      }
      if (error instanceof TransactionError) {
        throw new ConversionError("transaction-send", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new ConversionError("account-fetch", error.message, error);
      }
      throw new ConversionError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  }, "convertToSharedEncryption");
}
__name(getNetworkEncryptionToSharedEncryptionConverterFunction, "getNetworkEncryptionToSharedEncryptionConverterFunction");
var TOKEN_ACCOUNT_STATUS_BITS2 = {
  IS_INITIALISED: 0,
  IS_SHARED_MODE: 3,
  IS_ARCIUM_BALANCE_INITIALISED: 4
};
function isBitSet2(value, bitPosition) {
  return (BigInt(value) & 1n << BigInt(bitPosition)) !== 0n;
}
__name(isBitSet2, "isBitSet");
var DEFAULT_OPTIONAL_DATA_BYTES7 = new Uint8Array(32);
function getMintEncryptionKeyRotatorFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = deps?.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  const mintX25519KeypairDeriver = deps?.keys?.mintX25519KeypairDeriver ?? getMintX25519KeypairDeriver({ client });
  return /* @__PURE__ */ __name(async function rotateMintX25519EncryptionKey(mint, optionalData, callbacks, microLamportsPerAcu, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const userAddress = signer.address;
    const keypairResult = await mintX25519KeypairDeriver(mint);
    const x25519ProvingSigner = await createKeyPairSignerFromPrivateKeyBytes(
      keypairResult.ed25519Keypair.seed
    );
    const tokenAccountPda = await findEncryptedTokenAccountPda(
      userAddress,
      mint,
      client.networkConfig.programId
    );
    const accountMap = await accountInfoProvider([client.networkConfig.mxeAccountAddress, tokenAccountPda], { commitment: accountInfoCommitment });
    const clusterOffset = extractClusterOffsetFromMxeAccount(
      accountMap.get(client.networkConfig.mxeAccountAddress)
    );
    const maybeAccount = accountMap.get(tokenAccountPda);
    if (maybeAccount?.exists !== true) {
      throw new Error(
        `Token account for mint ${mint} does not exist. Cannot rotate X25519 key on a non-existent account.`
      );
    }
    const decodedAccount = decodeEncryptedTokenAccount(
      maybeAccount
    );
    const statusBits = decodedAccount.data.statusBits.first;
    const isInitialised = isBitSet2(statusBits, TOKEN_ACCOUNT_STATUS_BITS2.IS_INITIALISED);
    const isSharedMode = isBitSet2(statusBits, TOKEN_ACCOUNT_STATUS_BITS2.IS_SHARED_MODE);
    const isArciumBalanceInitialised = isBitSet2(
      statusBits,
      TOKEN_ACCOUNT_STATUS_BITS2.IS_ARCIUM_BALANCE_INITIALISED
    );
    if (!isInitialised) {
      throw new Error(
        `Token account for mint ${mint} is not initialized. Cannot rotate X25519 key on an uninitialized account.`
      );
    }
    if (!isSharedMode) {
      throw new Error(
        `Token account for mint ${mint} is in MXE mode, not shared mode. X25519 key rotation requires the account to be in shared mode.`
      );
    }
    if (!isArciumBalanceInitialised) {
      throw new Error(
        `Token account for mint ${mint} has no initialized Arcium balance. Cannot rotate X25519 key on an account without initialized balance.`
      );
    }
    const computationOffset = generateRandomU64();
    const mpcCallbackDataOffset = generateRandomU128();
    const arciumAccounts = await findArciumInfrastructurePdas(
      client.networkConfig.programId,
      client.networkConfig.arciumProgramAddress,
      clusterOffset,
      BigInt(computationOffset),
      "reencrypt_shared_balance_v11"
    );
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getReencryptSharedBalanceV11InstructionAsync({
      // Signers (noop for instruction building — wallet signs later)
      userAddress: noopSigner,
      feePayer: noopSigner,
      // Arcium accounts
      mxeAccount: arciumAccounts.mxeAccount,
      mempoolAccount: arciumAccounts.mempoolAccount,
      executingPool: arciumAccounts.executingPoolAccount,
      computationAccount: arciumAccounts.computationAccount,
      compDefAccount: arciumAccounts.compDefAccount,
      clusterAccount: arciumAccounts.clusterAccount,
      // Token mint
      mint,
      // X25519 proving signer (KeyPairSigner — auto-signed by partiallySign)
      x25519ProvingSigner,
      // Instruction data
      computationOffset: {
        first: computationOffset
      },
      mpcCallbackDataOffset: {
        first: mpcCallbackDataOffset
      },
      priorityFees: { first: microLamportsPerAcu ?? 0n },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES7 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
    const baseTransactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([computeBudgetInstruction, instruction], m)
    );
    const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, "reencrypt_shared_balance_v11");
    const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
      baseTransactionMessage,
      buildAltAddressesRecord(altEntry)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "rotateMintX25519EncryptionKey");
}
__name(getMintEncryptionKeyRotatorFunction, "getMintEncryptionKeyRotatorFunction");
function extractSignatures(result) {
  const signatures = [result.signature];
  if ("callbackResult" in result && result.callbackResult.status === "finalized" && result.callbackResult.callbackSignature !== void 0) {
    signatures.push(result.callbackResult.callbackSignature);
  }
  return signatures;
}
__name(extractSignatures, "extractSignatures");
function buildTransactionMessage(signer, latestBlockhash, instructions, altEntry) {
  const baseTransactionMessage = pipe(
    createTransactionMessage({ version: 0 }),
    (m) => setTransactionMessageFeePayer(signer.address, m),
    (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
    (m) => appendTransactionMessageInstructions([...instructions], m)
  );
  if (altEntry === void 0) {
    return baseTransactionMessage;
  }
  return compressTransactionMessageUsingAddressLookupTables(
    baseTransactionMessage,
    buildAltAddressesRecord(altEntry)
  );
}
__name(buildTransactionMessage, "buildTransactionMessage");
async function signTransactionMessage(signer, transactionMessage) {
  const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
  return await signer.signTransaction(partiallySignedTransaction);
}
__name(signTransactionMessage, "signTransactionMessage");
async function forwardAndMonitor(config, signedTransaction, callbacks, awaitCallback) {
  const { transactionForwarder, computationMonitor } = config;
  const txSignature = getSignatureFromTransaction(
    signedTransaction
  );
  let prepared;
  if (awaitCallback !== void 0) {
    if (computationMonitor === void 0) {
      throw new Error(
        "awaitCallback was provided but no computationMonitor is configured on the client. Pass a computationMonitor when creating the Umbra client, or omit awaitCallback to use fire-and-forget mode."
      );
    }
    const baseMonitorOptions = {
      maxSlotWindow: awaitCallback.maxSlotWindow,
      safetyTimeoutMs: awaitCallback.safetyTimeoutMs,
      onProgress: awaitCallback.onProgress,
      commitment: awaitCallback.computationMonitorCommitment ?? "confirmed",
      retrieveCallbackSignature: awaitCallback.retrieveCallbackSignature ?? false
    };
    prepared = await computationMonitor.prepareMonitor(
      awaitCallback.computationAccountAddress,
      baseMonitorOptions
    );
  }
  try {
    if (callbacks?.pre !== void 0) {
      await callbacks.pre(signedTransaction);
    }
    await transactionForwarder.forwardSequentially([signedTransaction]);
    if (callbacks?.post !== void 0) {
      await callbacks.post(signedTransaction, txSignature);
    }
    if (prepared === void 0 || awaitCallback === void 0) {
      return { signature: txSignature, signedTransaction };
    }
    const callbackResult = await prepared.awaitComputation();
    let rentClaimSignature;
    let rentClaimError;
    if (awaitCallback.reclaimComputationRent !== false) {
      if (awaitCallback.computationOffset === void 0 || awaitCallback.clusterOffset === void 0) {
        rentClaimError = "Rent reclaim skipped: computationOffset or clusterOffset was not provided by the service";
      } else {
        try {
          const noopSigner = createNoopSigner(config.signer.address);
          const rentClaimIx = getClaimComputationRentInstruction({
            signer: noopSigner,
            comp: awaitCallback.computationAccountAddress,
            compOffset: awaitCallback.computationOffset,
            clusterOffset: awaitCallback.clusterOffset
          });
          const rentClaimBlockhash = await config.getLatestBlockhash({
            commitment: awaitCallback.rentReclaimBlockhashCommitment ?? "confirmed"
          });
          const rentClaimTxMsg = buildTransactionMessage(config.signer, rentClaimBlockhash, [
            rentClaimIx
          ]);
          const rentClaimTx = await signTransactionMessage(config.signer, rentClaimTxMsg);
          rentClaimSignature = await config.transactionForwarder.fireAndForget(rentClaimTx);
        } catch (error) {
          rentClaimError = error instanceof Error ? error.message : String(error);
        }
      }
    }
    return {
      signature: txSignature,
      signedTransaction,
      callbackResult,
      rentClaimSignature,
      rentClaimError
    };
  } finally {
    prepared?.cleanup();
  }
}
__name(forwardAndMonitor, "forwardAndMonitor");
async function executeComputationTransaction(config, options, awaitCallback) {
  const { signer, getLatestBlockhash } = config;
  const { instruction, preInstructions, altEntry, callbacks, blockhashCommitment } = options;
  const latestBlockhash = await getLatestBlockhash({
    commitment: blockhashCommitment ?? "confirmed"
  });
  const allInstructions = preInstructions === void 0 ? [instruction] : [...preInstructions, instruction];
  const transactionMessage = buildTransactionMessage(
    signer,
    latestBlockhash,
    allInstructions,
    altEntry
  );
  const signedTransaction = await signTransactionMessage(signer, transactionMessage);
  return forwardAndMonitor(config, signedTransaction, callbacks, awaitCallback);
}
__name(executeComputationTransaction, "executeComputationTransaction");
var USER_STATUS_BIT_X25519_REGISTERED = 4;
var TOKEN_STATUS_BIT_SHARED_MODE = 3;
var TOKEN_STATUS_BIT_BALANCE_INITIALISED = 4;
function isBitSet3(statusBits, bit) {
  return (statusBits & 1n << BigInt(bit)) !== 0n;
}
__name(isBitSet3, "isBitSet");
var DepositInstructionVariant = {
  NewMxeBalance: "NewMxeBalance",
  ExistingMxeBalance: "ExistingMxeBalance",
  NewSharedBalance: "NewSharedBalance",
  ExistingSharedBalance: "ExistingSharedBalance"
};
function getPublicBalanceToEncryptedBalanceDirectDepositorFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = deps?.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  const getEpochInfo = deps?.rpc?.epochInfo ?? client.epochInfoProvider;
  const factoryMicroLamportsPerAcu = deps?.arcium?.microLamportsPerAcu ?? 0n;
  const factoryAwaitComputationFinalization = deps?.arcium?.awaitComputationFinalization;
  const executorConfig = {
    signer,
    getLatestBlockhash,
    transactionForwarder,
    computationMonitor: client.computationMonitor
  };
  async function executeTransaction(instruction, altEntry, preInstructions, awaitCallback) {
    try {
      const result = await executeComputationTransaction(
        executorConfig,
        {
          instruction,
          preInstructions: [
            createSetComputeUnitLimitInstruction(12e5),
            ...preInstructions ?? []
          ],
          altEntry
        },
        awaitCallback
      );
      if ("callbackResult" in result) {
        return {
          queueSignature: result.signature,
          callbackStatus: result.callbackResult.status,
          callbackSignature: "callbackSignature" in result.callbackResult ? result.callbackResult.callbackSignature : void 0,
          callbackElapsedMs: result.callbackResult.elapsedMs,
          rentClaimSignature: result.rentClaimSignature,
          rentClaimError: result.rentClaimError
        };
      }
      return { queueSignature: result.signature };
    } catch (error) {
      if (error instanceof EncryptedDepositError) {
        throw error;
      }
      throw new EncryptedDepositError(
        "transaction-send",
        `Transaction execution failed: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
  }
  __name(executeTransaction, "executeTransaction");
  return /* @__PURE__ */ __name(async function depositIntoEncryptedBalance(destinationAddress, mint, transferAmount, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const epochInfoCommitment = options?.epochInfoCommitment ?? "confirmed";
    const microLamportsPerAcu = factoryMicroLamportsPerAcu;
    const optionalData = options?.optionalData ?? new Uint8Array(32);
    try {
      if (destinationAddress.trim().length === 0) {
        throw new EncryptedDepositError(
          "validation",
          "Invalid destinationAddress: must be a non-empty string"
        );
      }
      if (mint.trim().length === 0) {
        throw new EncryptedDepositError("validation", "Invalid mint: must be a non-empty string");
      }
      assertU64(transferAmount);
      if (transferAmount <= 0n) {
        throw new EncryptedDepositError(
          "validation",
          "Invalid transferAmount: must be greater than zero"
        );
      }
      assertOptionalData32(optionalData, "optionalData");
    } catch (error) {
      if (error instanceof EncryptedDepositError) {
        throw error;
      }
      throw new EncryptedDepositError(
        "validation",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
    let depositAmount;
    let mintOwner;
    try {
      const mintAccountMap = await accountInfoProvider([mint], { commitment: accountInfoCommitment });
      const mintAccount = mintAccountMap.get(mint);
      if (mintAccount?.exists !== true) {
        throw new EncryptedDepositError("mint-fetch", `Mint account not found: ${mint}`);
      }
      mintOwner = mintAccount.programAddress;
      if (mintOwner === TOKEN_2022_PROGRAM_ID) {
        const mintData = new Uint8Array(mintAccount.data);
        const feeConfig = extractTransferFeeConfig(mintData);
        if (feeConfig === null) {
          depositAmount = transferAmount;
        } else {
          const epochInfo = await getEpochInfo({ commitment: epochInfoCommitment });
          const transferFee = calculateTransferFee(feeConfig, epochInfo.epoch, transferAmount);
          const calculatedDepositAmount = transferAmount - transferFee;
          if (calculatedDepositAmount <= 0n) {
            throw new EncryptedDepositError(
              "fee-calculation",
              `Transfer fee (${String(transferFee)}) exceeds or equals transfer amount (${String(transferAmount)}). Deposit amount must be greater than zero.`
            );
          }
          assertU64(calculatedDepositAmount);
          depositAmount = calculatedDepositAmount;
        }
      } else if (mintOwner === SPL_TOKEN_PROGRAM_ID) {
        depositAmount = transferAmount;
      } else {
        throw new EncryptedDepositError(
          "mint-fetch",
          `Unknown token program: ${mintOwner}. Expected SPL Token (${SPL_TOKEN_PROGRAM_ID}) or Token-2022 (${TOKEN_2022_PROGRAM_ID})`
        );
      }
    } catch (error) {
      if (error instanceof EncryptedDepositError) {
        throw error;
      }
      throw new EncryptedDepositError(
        "mint-fetch",
        `Failed to fetch mint account: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    let encryptedTokenAccountPda;
    let encryptedUserAccountPda;
    try {
      [encryptedTokenAccountPda, encryptedUserAccountPda] = await Promise.all([
        findEncryptedTokenAccountPda(destinationAddress, mint, client.networkConfig.programId),
        findEncryptedUserAccountPda(destinationAddress, client.networkConfig.programId)
      ]);
    } catch (error) {
      throw new EncryptedDepositError(
        "pda-derivation",
        `Failed to derive PDA addresses: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    let maybeUserAccount;
    let maybeTokenAccount;
    let clusterOffset;
    try {
      const accountMap = await accountInfoProvider([
        client.networkConfig.mxeAccountAddress,
        encryptedUserAccountPda,
        encryptedTokenAccountPda
      ], { commitment: accountInfoCommitment });
      const encodedUserAccount = accountMap.get(encryptedUserAccountPda);
      const encodedTokenAccount = accountMap.get(encryptedTokenAccountPda);
      if (encodedUserAccount === void 0) {
        throw new EncryptedDepositError(
          "account-fetch",
          `Failed to fetch account info for user PDA: ${encryptedUserAccountPda}`
        );
      }
      if (encodedTokenAccount === void 0) {
        throw new EncryptedDepositError(
          "account-fetch",
          `Failed to fetch account info for token PDA: ${encryptedTokenAccountPda}`
        );
      }
      maybeUserAccount = decodeEncryptedUserAccount(encodedUserAccount);
      maybeTokenAccount = decodeEncryptedTokenAccount(encodedTokenAccount);
      clusterOffset = extractClusterOffsetFromMxeAccount(
        accountMap.get(client.networkConfig.mxeAccountAddress)
      );
    } catch (error) {
      if (error instanceof EncryptedDepositError) {
        throw error;
      }
      throw new EncryptedDepositError(
        "account-fetch",
        `Failed to fetch accounts: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    let instructionVariant;
    if (maybeUserAccount.exists) {
      const userStatusBits = maybeUserAccount.data.statusBits.first;
      const hasX25519Key = isBitSet3(userStatusBits, USER_STATUS_BIT_X25519_REGISTERED);
      const tokenExists = maybeTokenAccount.exists;
      let isSharedMode = false;
      let isBalanceInitialised = false;
      if (tokenExists) {
        const tokenStatusBits = maybeTokenAccount.data.statusBits.first;
        isSharedMode = isBitSet3(tokenStatusBits, TOKEN_STATUS_BIT_SHARED_MODE);
        isBalanceInitialised = isBitSet3(tokenStatusBits, TOKEN_STATUS_BIT_BALANCE_INITIALISED);
      }
      if (tokenExists && isBalanceInitialised) {
        const queueCount = maybeTokenAccount.data.queuedComputationCount.first;
        if (queueCount >= 4) {
          const oldestSlot = maybeTokenAccount.data.oldestPendingSlot.first;
          const epochInfo = await getEpochInfo({ commitment: epochInfoCommitment });
          const currentSlot = epochInfo.absoluteSlot;
          if (currentSlot - oldestSlot >= 180n) {
            const resetCompDefName = isSharedMode ? "reset_shared_eta_queue_v9" : "reset_mxe_eta_queue_v9";
            const resetComputationOffset = generateRandomU64();
            const resetMpcCallbackDataOffset = generateRandomU128();
            const resetArciumAccounts = await findArciumInfrastructurePdas(
              client.networkConfig.programId,
              client.networkConfig.arciumProgramAddress,
              clusterOffset,
              BigInt(resetComputationOffset),
              resetCompDefName
            );
            const resetAltEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, resetCompDefName);
            const resetBaseParams = {
              feePayer: createNoopSigner(signer.address),
              mxeAccount: resetArciumAccounts.mxeAccount,
              mempoolAccount: resetArciumAccounts.mempoolAccount,
              executingPool: resetArciumAccounts.executingPoolAccount,
              computationAccount: resetArciumAccounts.computationAccount,
              compDefAccount: resetArciumAccounts.compDefAccount,
              clusterAccount: resetArciumAccounts.clusterAccount,
              userAddress: destinationAddress,
              userTokenAccount: encryptedTokenAccountPda,
              mint,
              computationOffset: { first: resetComputationOffset },
              mpcCallbackDataOffset: { first: resetMpcCallbackDataOffset },
              priorityFees: { first: 0n },
              optionalData: { first: new Uint8Array(32) }
            };
            const resetInstruction = isSharedMode ? await getResetSharedEncryptedTokenAccountQueueV11InstructionAsync(resetBaseParams, {
              programAddress: client.networkConfig.programId
            }) : await getResetNetworkEncryptedTokenAccountQueueV11InstructionAsync(resetBaseParams, {
              programAddress: client.networkConfig.programId
            });
            await executeTransaction(resetInstruction, resetAltEntry, void 0, {
              retrieveCallbackSignature: true,
              computationAccountAddress: resetArciumAccounts.computationAccount,
              computationOffset: BigInt(resetComputationOffset),
              clusterOffset
            });
          }
        }
      }
      if (hasX25519Key) {
        if (tokenExists && isBalanceInitialised && isSharedMode) {
          instructionVariant = DepositInstructionVariant.ExistingSharedBalance;
        } else if (tokenExists && isBalanceInitialised && !isSharedMode) {
          instructionVariant = DepositInstructionVariant.ExistingMxeBalance;
        } else {
          instructionVariant = DepositInstructionVariant.NewSharedBalance;
        }
      } else {
        if (tokenExists && isBalanceInitialised && isSharedMode) {
          throw new EncryptedDepositError(
            "validation",
            "Invalid state: Token account is in Shared mode but user has no X25519 key registered"
          );
        } else if (tokenExists && isBalanceInitialised && !isSharedMode) {
          instructionVariant = DepositInstructionVariant.ExistingMxeBalance;
        } else {
          instructionVariant = DepositInstructionVariant.NewMxeBalance;
        }
      }
    } else {
      instructionVariant = DepositInstructionVariant.NewMxeBalance;
    }
    const computationOffset = generateRandomU64();
    const compDefInstructionName = {
      [DepositInstructionVariant.NewMxeBalance]: "deposit_from_public_balance_into_new_network_balance_v11",
      [DepositInstructionVariant.ExistingMxeBalance]: "deposit_from_public_balance_into_existing_network_balance_v11",
      [DepositInstructionVariant.NewSharedBalance]: "deposit_from_public_balance_into_new_shared_balance_v11",
      [DepositInstructionVariant.ExistingSharedBalance]: "deposit_from_public_balance_into_existing_shared_balance_v11"
    }[instructionVariant];
    const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, compDefInstructionName);
    const arciumAccounts = await findArciumInfrastructurePdas(
      client.networkConfig.programId,
      client.networkConfig.arciumProgramAddress,
      clusterOffset,
      BigInt(computationOffset),
      compDefInstructionName
    );
    const mpcCallbackDataOffset = generateRandomU128();
    const protocolFeeProvider = getHardcodedDepositProtocolFeeProvider();
    const protocolFeeConfig = await protocolFeeProvider(depositAmount);
    const noopSigner = createNoopSigner(signer.address);
    const commonParams = {
      depositorAddress: noopSigner,
      feePayer: noopSigner,
      receiverAddress: destinationAddress,
      mint,
      tokenProgram: mintOwner,
      // Arcium accounts
      mxeAccount: arciumAccounts.mxeAccount,
      mempoolAccount: arciumAccounts.mempoolAccount,
      executingPool: arciumAccounts.executingPoolAccount,
      computationAccount: arciumAccounts.computationAccount,
      compDefAccount: arciumAccounts.compDefAccount,
      clusterAccount: arciumAccounts.clusterAccount,
      // Instruction data
      computationOffset: { first: computationOffset },
      transferAmount: { first: transferAmount },
      depositAmount: { first: depositAmount },
      feeVaultOffset: { first: 0n },
      mpcCallbackDataOffset: { first: mpcCallbackDataOffset },
      optionalData: { first: optionalData },
      // Fee slab data and merkle proof
      ...feeSlabResultToInstructionFields(protocolFeeConfig)
    };
    let instruction;
    try {
      switch (instructionVariant) {
        case DepositInstructionVariant.NewMxeBalance: {
          instruction = await getDepositFromPublicBalanceIntoNewNetworkBalanceV11InstructionAsync(
            {
              ...commonParams,
              receiverTokenAccount: encryptedTokenAccountPda,
              receiverUserAccount: encryptedUserAccountPda,
              randomGenerationSeed: {
                first: crypto.getRandomValues(new Uint8Array(32))
              },
              priorityFees: { first: microLamportsPerAcu }
            },
            { programAddress: client.networkConfig.programId }
          );
          break;
        }
        case DepositInstructionVariant.ExistingMxeBalance: {
          instruction = await getDepositFromPublicBalanceIntoExistingNetworkBalanceV11InstructionAsync(
            {
              ...commonParams,
              receiverTokenAccount: encryptedTokenAccountPda,
              priorityFees: { first: microLamportsPerAcu }
            },
            { programAddress: client.networkConfig.programId }
          );
          break;
        }
        case DepositInstructionVariant.NewSharedBalance: {
          instruction = await getDepositFromPublicBalanceIntoNewSharedBalanceV11InstructionAsync(
            {
              ...commonParams,
              receiverTokenAccount: encryptedTokenAccountPda,
              receiverUserAccount: encryptedUserAccountPda,
              priorityFees: { first: microLamportsPerAcu }
            },
            { programAddress: client.networkConfig.programId }
          );
          break;
        }
        case DepositInstructionVariant.ExistingSharedBalance: {
          instruction = await getDepositFromPublicBalanceIntoExistingSharedBalanceV11InstructionAsync(
            {
              ...commonParams,
              receiverTokenAccount: encryptedTokenAccountPda,
              priorityFees: { first: microLamportsPerAcu }
            },
            { programAddress: client.networkConfig.programId }
          );
          break;
        }
        default: {
          throw new EncryptedDepositError(
            "instruction-build",
            `Unknown instruction variant: ${String(instructionVariant)}`
          );
        }
      }
    } catch (error) {
      if (error instanceof EncryptedDepositError) {
        throw error;
      }
      throw new EncryptedDepositError(
        "instruction-build",
        `Failed to build instruction: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    const resolvedAwaitConfig = factoryAwaitComputationFinalization === false ? false : factoryAwaitComputationFinalization ?? {};
    const callbackOptions = resolvedAwaitConfig === false ? void 0 : {
      ...resolvedAwaitConfig,
      retrieveCallbackSignature: true,
      computationAccountAddress: arciumAccounts.computationAccount,
      computationOffset: BigInt(computationOffset),
      clusterOffset
    };
    return executeTransaction(
      instruction,
      altEntry,
      void 0,
      callbackOptions
    );
  }, "depositIntoEncryptedBalance");
}
__name(getPublicBalanceToEncryptedBalanceDirectDepositorFunction, "getPublicBalanceToEncryptedBalanceDirectDepositorFunction");
var AES_DOMAIN_SEPARATOR_EPHEMERAL2 = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateSelfClaimableUtxoFromEncryptedBalance")
).slice(0, 12);
var DOMAIN_MODIFIED_GEN_INDEX = "EncryptedBalanceToSelfClaimableUtxoCreatorFunction / modifiedGenerationIndex";
function toBn254FieldElement(value) {
  assertBn254FieldElement(value);
  return value;
}
__name(toBn254FieldElement, "toBn254FieldElement");
function toU256(value) {
  assertU256(value);
  return value;
}
__name(toU256, "toU256");
function toCurve25519FieldElement(value) {
  assertCurve25519FieldElement(value);
  return value;
}
__name(toCurve25519FieldElement, "toCurve25519FieldElement");
function limbToBn254(value) {
  assertBase85Limb(value);
  assertBn254FieldElement(value);
  return value;
}
__name(limbToBn254, "limbToBn254");
function isSignedTransaction(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const tx = value;
  return "messageBytes" in tx && "signatures" in tx && tx.signatures instanceof Object && Object.keys(tx.signatures).length > 0;
}
__name(isSignedTransaction, "isSignedTransaction");
function getEncryptedBalanceToSelfClaimableUtxoCreatorFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps.rpc?.transactionForwarder ?? client.transactionForwarder;
  const executorConfig = {
    signer: client.signer,
    getLatestBlockhash,
    transactionForwarder,
    computationMonitor: client.computationMonitor
  };
  const factoryMicroLamportsPerAcu = deps.arcium?.microLamportsPerAcu ?? 0n;
  const factoryAwaitComputationFinalization = deps.arcium?.awaitComputationFinalization;
  const factoryCloseProofAccountCallbacks = deps.hooks?.closeProofAccount;
  const factoryCreateProofAccountCallbacks = deps.hooks?.createProofAccount;
  const factoryCreateUtxoCallbacks = deps.hooks?.createUtxo;
  const masterViewingKeyGenerator = deps.keys?.masterViewingKeyGenerator ?? getMasterViewingKeyDeriver({ client });
  const masterViewingKeyBlindingFactorGenerator = deps.keys?.masterViewingKeyBlindingFactorGenerator ?? getMasterViewingKeyBlindingFactorDeriver({ client });
  const poseidonPrivateKeyGenerator = deps.keys?.poseidonPrivateKeyGenerator ?? getPoseidonPrivateKeyDeriver({ client });
  const poseidonBlindingFactorGenerator = deps.keys?.poseidonBlindingFactorGenerator ?? getPoseidonBlindingFactorDeriver({ client });
  const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const secondViewingKeyGenerator = deps.keys?.secondViewingKeyGenerator ?? getSecondViewingKeyDeriver({ client });
  const ephemeralUtxoMvkGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyDeriver ?? getEphemeralUtxoMasterViewingKeyDeriver({ client });
  const ephemeralUtxoMvkBlindingFactorGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyBlindingFactorDeriver ?? getEphemeralUtxoMasterViewingKeyBlindingFactorDeriver({
    client
  });
  const ephemeralUtxoPoseidonKeyGenerator = deps.keys?.ephemeralUtxoPoseidonKeyGenerator ?? getEphemeralUtxoPoseidonPrivateKeyDeriver({ client });
  const ephemeralUtxoPoseidonKeyBlindingFactorGenerator = deps.keys?.ephemeralUtxoPoseidonKeyBlindingFactorGenerator ?? getEphemeralUtxoPoseidonPrivateKeyBlindingFactorDeriver({
    client
  });
  const poseidonKeystreamBlindingFactorGenerator = deps.keys?.poseidonKeystreamBlindingFactorGenerator ?? getPoseidonKeystreamBlindingFactorDeriver({ client });
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGenerator = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const h2Generator = deps.crypto?.h2Generator ?? getUtxoCommitmentHashGenerator();
  const keystreamCommitmentGenerator = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const userCommitmentGenerator = getUserCommitmentGeneratorFunction();
  const rescueEncryptionCommitmentGenerator = getRescueCommitmentGenerator();
  const poseidonAggregator = getPoseidonAggregator();
  const fiatShamirChallengeGenerator = getFiatShamirChallengeGeneratorFunction();
  const challengePowersFunction = getChallengePowersFunction();
  const polynomialEvaluator = getPolynomialEvaluatorFunction();
  const aesEncryptor = deps.crypto?.aesEncryptor ?? getAesEncryptor();
  const generateRandomU256Function = deps.random?.generateRandomU256 ?? generateRandomU256;
  const generateComputationOffsetFunction = deps.random?.generateComputationOffset ?? generateComputationOffset;
  return /* @__PURE__ */ __name(async function createSelfClaimableUtxo(args2, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    try {
      const { amount, destinationAddress, mint } = args2;
      const generationIndex = options?.generationIndex;
      const microLamportsPerAcu = factoryMicroLamportsPerAcu;
      assertU64(amount);
      if (generationIndex !== void 0) {
        assertU256(generationIndex);
      }
      const effectiveGenerationIndex = generationIndex ?? generateRandomU256Function();
      const mxeAccountMap = await accountInfoProvider([client.networkConfig.mxeAccountAddress], { commitment: accountInfoCommitment });
      const clusterOffset = extractClusterOffsetFromMxeAccount(
        mxeAccountMap.get(client.networkConfig.mxeAccountAddress)
      );
      const masterSeed = await client.masterSeed.getMasterSeed();
      if (!(masterSeed instanceof Uint8Array) || masterSeed.length === 0) {
        throw new CryptographyError("Failed to retrieve master seed from client");
      }
      const generationIndexBytes = new TextEncoder().encode(effectiveGenerationIndex.toString());
      const modifiedGenerationIndex = kmac256(
        new TextEncoder().encode(DOMAIN_MODIFIED_GEN_INDEX),
        masterSeed,
        { dkLen: 16, personalization: generationIndexBytes }
      );
      const expandedModifiedGenerationIndex = expandModifiedGenerationIndex(modifiedGenerationIndex);
      let expandedModifiedGenerationIndexU256 = 0n;
      for (let index = 0; index < 32; index++) {
        expandedModifiedGenerationIndexU256 |= BigInt(expandedModifiedGenerationIndex[index]) << BigInt(index * 8);
      }
      const proofAccountOffsetBytes = deriveProofAccountOffsetFromModifiedGenerationIndex(modifiedGenerationIndex);
      let proofAccountOffset = 0n;
      for (let index = 0; index < 16; index++) {
        proofAccountOffset |= BigInt(proofAccountOffsetBytes[index]) << BigInt(index * 8);
      }
      const computationOffset = generateComputationOffsetFunction();
      const protocolFeeProvider = getHardcodedCreateUtxoProtocolFeeProvider();
      const protocolFeeConfig = await protocolFeeProvider(amount);
      const baseFeesInSpl = protocolFeeConfig.slab.baseFee;
      const commissionFeeInBps = protocolFeeConfig.slab.bps;
      const amountLowerBound = protocolFeeConfig.slab.amountLowerBound;
      const amountUpperBound = protocolFeeConfig.slab.amountUpperBound;
      const amountAfterBaseFeeForBps = amount - baseFeesInSpl;
      const protocolFeeBpsQuotient = amountAfterBaseFeeForBps * commissionFeeInBps / 16384n;
      const protocolFeeBpsRemainder = amountAfterBaseFeeForBps * commissionFeeInBps % 16384n;
      const protocolFees = baseFeesInSpl + protocolFeeBpsQuotient;
      const amountAfterFees = amount - protocolFees;
      let currentTimestamp;
      let timestampComponents;
      if (deps.random?.getUtcNow === void 0) {
        const now = /* @__PURE__ */ new Date();
        currentTimestamp = Math.floor(now.getTime() / 1e3);
        timestampComponents = extractUtcComponents(now);
      } else {
        timestampComponents = deps.random.getUtcNow();
        const { year: year2, month: month2, day: day2, hour: hour2, minute: minute2, second: second2 } = timestampComponents;
        const dateFromComponents = new Date(
          Date.UTC(
            Number(year2),
            Number(month2) - 1,
            Number(day2),
            Number(hour2),
            Number(minute2),
            Number(second2)
          )
        );
        currentTimestamp = Math.floor(dateFromComponents.getTime() / 1e3);
      }
      const { year, month, day, hour, minute, second } = timestampComponents;
      const transactionViewingKey = await secondViewingKeyGenerator(
        mint,
        year,
        month,
        day,
        hour,
        minute,
        second
      );
      assertBn254FieldElement(transactionViewingKey);
      const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
      const senderMasterViewingKey = await masterViewingKeyGenerator();
      assertBn254FieldElement(senderMasterViewingKey);
      const senderMvkBlindingFactor = await masterViewingKeyBlindingFactorGenerator();
      assertBn254FieldElement(senderMvkBlindingFactor);
      const senderPrivateKey = await poseidonPrivateKeyGenerator();
      assertBn254FieldElement(senderPrivateKey);
      const senderPrivateKeyBlindingFactor = await poseidonBlindingFactorGenerator();
      assertBn254FieldElement(senderPrivateKeyBlindingFactor);
      const senderUserCommitment = await userCommitmentGenerator(
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        senderPrivateKey,
        senderPrivateKeyBlindingFactor
      );
      assertBn254FieldElement(senderUserCommitment);
      const ephemeralOffset = expandedModifiedGenerationIndexU256;
      const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(randomSecret);
      const nullifier = deriveNullifierFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(nullifier);
      const ephemeralMasterViewingKey = await ephemeralUtxoMvkGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralMasterViewingKey);
      const ephemeralMasterViewingKeyBlindingFactor = await ephemeralUtxoMvkBlindingFactorGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralMasterViewingKeyBlindingFactor);
      const ephemeralPoseidonPrivateKey = await ephemeralUtxoPoseidonKeyGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralPoseidonPrivateKey);
      const ephemeralPoseidonPrivateKeyBlindingFactor = await ephemeralUtxoPoseidonKeyBlindingFactorGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralPoseidonPrivateKeyBlindingFactor);
      const ephemeralUserCommitment = await userCommitmentGenerator(
        ephemeralMasterViewingKey,
        ephemeralMasterViewingKeyBlindingFactor,
        ephemeralPoseidonPrivateKey,
        ephemeralPoseidonPrivateKeyBlindingFactor
      );
      assertBn254FieldElement(ephemeralUserCommitment);
      const h2Hash = await h2Generator.generateH2({
        amount: toBn254FieldElement(amountAfterFees),
        nullifier,
        userCommitment: ephemeralUserCommitment,
        finalDestinationAddressLow: toBn254FieldElement(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement(destinationAddressHigh),
        h2BlindingFactor: randomSecret
      });
      assertBn254FieldElement(h2Hash);
      assertPoseidonPlaintext(destinationAddressLow);
      assertPoseidonPlaintext(destinationAddressHigh);
      assertPoseidonPlaintext(amount);
      const poseidonKey = BigInt(transactionViewingKey);
      assertPoseidonKey(poseidonKey);
      const pcCiphertexts = await poseidonEncryptor(
        [
          destinationAddressLow,
          destinationAddressHigh,
          amount
        ],
        poseidonKey
      );
      if (pcCiphertexts.length !== 3) {
        throw new CryptographyError(
          `Expected 3 ciphertexts but got ${String(pcCiphertexts.length)}`
        );
      }
      const pcEncryptedDestinationAddressLow = pcCiphertexts[0];
      const pcEncryptedDestinationAddressHigh = pcCiphertexts[1];
      const pcEncryptedAmount = pcCiphertexts[2];
      const pcKeystreams = await poseidonKeystreamGenerator(
        [0n, 1n, 2n],
        poseidonKey
      );
      const pcKeyForDestinationAddressLow = pcKeystreams.get(0n);
      if (pcKeyForDestinationAddressLow === void 0) {
        throw new CryptographyError("Missing keystream for counter 0");
      }
      const pcKeyForDestinationAddressHigh = pcKeystreams.get(1n);
      if (pcKeyForDestinationAddressHigh === void 0) {
        throw new CryptographyError("Missing keystream for counter 1");
      }
      const pcKeyForAmount = pcKeystreams.get(2n);
      if (pcKeyForAmount === void 0) {
        throw new CryptographyError("Missing keystream for counter 2");
      }
      const firstKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressLow,
        0n
      );
      const secondKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressHigh,
        1n
      );
      const thirdKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForAmount,
        2n
      );
      const firstPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressLow,
        firstKeystreamBlindingFactor
      );
      const secondPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressHigh,
        secondKeystreamBlindingFactor
      );
      const thirdPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForAmount,
        thirdKeystreamBlindingFactor
      );
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      assertX25519PrivateKey(clientX25519PrivateKey);
      const clientX25519PublicKey = keypairResult.x25519Keypair.publicKey;
      const rcEncryptor = getRescueEncryptorFromPrivateKey(clientX25519PrivateKey, {
        umbraX25519PublicKey: client.networkConfig.mxePubkey
      });
      const randomFactorForPolynomialCommitment = deriveRandomFactorForPolynomialCommitmentFromModifiedGenerationIndex(
        modifiedGenerationIndex
      );
      assertU256(randomFactorForPolynomialCommitment);
      const { low: randomFactorLow, high: randomFactorHigh } = splitU256IntoTwoU128s(
        randomFactorForPolynomialCommitment
      );
      const rcPlaintextAmount = BigInt(amount);
      const rcPlaintextProtocolFees = BigInt(protocolFees);
      const rcPlaintextRandomFactorLow = BigInt(randomFactorLow);
      const rcPlaintextRandomFactorHigh = BigInt(randomFactorHigh);
      assertRcPlaintext(rcPlaintextAmount);
      assertRcPlaintext(rcPlaintextProtocolFees);
      assertRcPlaintext(rcPlaintextRandomFactorLow);
      assertRcPlaintext(rcPlaintextRandomFactorHigh);
      const rcCiphertextOutputs = await rcEncryptor([
        rcPlaintextAmount,
        rcPlaintextProtocolFees,
        rcPlaintextRandomFactorLow,
        rcPlaintextRandomFactorHigh
      ]);
      assertRcEncryptionNonce(rcCiphertextOutputs.nonce);
      if (rcCiphertextOutputs.ciphertexts.length !== 4) {
        throw new CryptographyError(
          `Expected 4 ciphertexts but got ${String(rcCiphertextOutputs.ciphertexts.length)}`
        );
      }
      const rcEncryptionNonce = rcCiphertextOutputs.nonce;
      const rcEncryptedAmount = rcCiphertextOutputs.ciphertexts[0];
      const rcEncryptedProtocolFees = rcCiphertextOutputs.ciphertexts[1];
      const rcEncryptedRandomFactorLow = rcCiphertextOutputs.ciphertexts[2];
      const rcEncryptedRandomFactorHigh = rcCiphertextOutputs.ciphertexts[3];
      assertRcCiphertext(rcEncryptedAmount);
      assertRcCiphertext(rcEncryptedProtocolFees);
      assertRcCiphertext(rcEncryptedRandomFactorLow);
      assertRcCiphertext(rcEncryptedRandomFactorHigh);
      const rcKeyGenerator = getRescueKeyGeneratorFromPrivateKey(clientX25519PrivateKey, {
        umbraX25519PublicKey: client.networkConfig.mxePubkey
      });
      const rcEncryptionKeys = await rcKeyGenerator(
        [0n, 1n, 2n, 3n],
        rcEncryptionNonce
      );
      const rcEncryptionKeyForAmount = rcEncryptionKeys.get(0n);
      if (rcEncryptionKeyForAmount === void 0) {
        throw new CryptographyError("Missing key for counter 0");
      }
      const rcEncryptionKeyForProtocolFees = rcEncryptionKeys.get(1n);
      if (rcEncryptionKeyForProtocolFees === void 0) {
        throw new CryptographyError("Missing key for counter 1");
      }
      const rcEncryptionKeyForRandomFactorLow = rcEncryptionKeys.get(2n);
      if (rcEncryptionKeyForRandomFactorLow === void 0) {
        throw new CryptographyError("Missing key for counter 2");
      }
      const rcEncryptionKeyForRandomFactorHigh = rcEncryptionKeys.get(3n);
      if (rcEncryptionKeyForRandomFactorHigh === void 0) {
        throw new CryptographyError("Missing key for counter 3");
      }
      assertRcKey(rcEncryptionKeyForAmount);
      assertRcKey(rcEncryptionKeyForProtocolFees);
      assertRcKey(rcEncryptionKeyForRandomFactorLow);
      assertRcKey(rcEncryptionKeyForRandomFactorHigh);
      const amountAsLimbs = convertU256ToBase85Limbs(toU256(amount));
      const protocolFeesAsLimbs = convertU256ToBase85Limbs(toU256(protocolFees));
      const randomFactorLowAsLimbs = convertU256ToBase85Limbs(toU256(randomFactorLow));
      const randomFactorHighAsLimbs = convertU256ToBase85Limbs(toU256(randomFactorHigh));
      const rcEncryptedAmountAsLimbs = convertU256ToBase85Limbs(toU256(rcEncryptedAmount));
      const rcEncryptedProtocolFeesAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptedProtocolFees)
      );
      const rcEncryptedRandomFactorLowAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptedRandomFactorLow)
      );
      const rcEncryptedRandomFactorHighAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptedRandomFactorHigh)
      );
      const rcEncryptionKeyForAmountAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptionKeyForAmount)
      );
      const rcEncryptionKeyForProtocolFeesAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptionKeyForProtocolFees)
      );
      const rcEncryptionKeyForRandomFactorLowAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptionKeyForRandomFactorLow)
      );
      const rcEncryptionKeyForRandomFactorHighAsLimbs = convertU256ToBase85Limbs(
        toU256(rcEncryptionKeyForRandomFactorHigh)
      );
      const rescueEncryptionCommitmentBlindingFactor = deriveRescueEncryptionCommitmentBlindingFactorFromModifiedGenerationIndex(
        modifiedGenerationIndex
      );
      assertBn254FieldElement(rescueEncryptionCommitmentBlindingFactor);
      const senderX25519PublicKey = clientX25519PublicKey;
      const { low: senderX25519PublicKeyLow, high: senderX25519PublicKeyHigh } = splitX25519PublicKeyIntoTwoU128s(senderX25519PublicKey);
      const rcEncryptionCommitment = await rescueEncryptionCommitmentGenerator(
        [
          rcEncryptedAmount,
          rcEncryptedProtocolFees,
          rcEncryptedRandomFactorLow,
          rcEncryptedRandomFactorHigh
        ],
        [
          toCurve25519FieldElement(amount),
          toCurve25519FieldElement(protocolFees),
          toCurve25519FieldElement(randomFactorLow),
          toCurve25519FieldElement(randomFactorHigh)
        ],
        [
          toCurve25519FieldElement(rcEncryptionKeyForAmount),
          toCurve25519FieldElement(rcEncryptionKeyForProtocolFees),
          toCurve25519FieldElement(rcEncryptionKeyForRandomFactorLow),
          toCurve25519FieldElement(rcEncryptionKeyForRandomFactorHigh)
        ],
        rescueEncryptionCommitmentBlindingFactor,
        rcEncryptionNonce,
        senderX25519PublicKeyLow,
        senderX25519PublicKeyHigh
      );
      assertBn254FieldElement(rcEncryptionCommitment);
      assertU128(rcEncryptionNonce);
      const fiatShamirInputBytes = new Uint8Array([
        // X25519 public key (32 bytes)
        ...senderX25519PublicKey,
        // rescue_encryption_nonce (U128 LE - 16 bytes)
        ...encodeU128ToU128LeBytes(rcEncryptionNonce),
        // rescue_encrypted_total_amount (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU256(rcEncryptedAmount)),
        // rescue_encrypted_protocol_fees (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU256(rcEncryptedProtocolFees)),
        // rescue_encrypted_random_factor_low (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU256(rcEncryptedRandomFactorLow)),
        // rescue_encrypted_random_factor_high (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU256(rcEncryptedRandomFactorHigh)),
        // rescue_encryption_fiat_shamir_commitment (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU256(BigInt(rcEncryptionCommitment)))
      ]);
      const fiatShamirChallenge = fiatShamirChallengeGenerator(fiatShamirInputBytes);
      const fiatShamirChallengeLimbs = convertU256ToBase85Limbs(toU256(fiatShamirChallenge));
      const challengePowers = challengePowersFunction(fiatShamirChallenge, 2);
      const fiatShamirChallengeSquared = challengePowers[2];
      const fiatShamirChallengeSquaredLimbs = convertU256ToBase85Limbs(
        toU256(fiatShamirChallengeSquared)
      );
      const rescueEncryptionValidationPolynomial = polynomialEvaluator(
        [
          toCurve25519FieldElement(amount),
          toCurve25519FieldElement(protocolFees),
          randomFactorForPolynomialCommitment
        ],
        fiatShamirChallenge
      );
      const rescueEncryptionValidationPolynomialAsLimbs = convertU256ToBase85Limbs(
        toU256(rescueEncryptionValidationPolynomial)
      );
      const { low: mintAddressLow, high: mintAddressHigh } = splitAddressToLowHigh(mint);
      const aggregatedHashInputs = [
        // [0-1] Mint address (Address → U128 low/high → Bn254FieldElement)
        toBn254FieldElement(mintAddressLow),
        toBn254FieldElement(mintAddressHigh),
        // [2-7] Timestamp components (bigint → Bn254FieldElement)
        toBn254FieldElement(BigInt(year)),
        toBn254FieldElement(BigInt(month)),
        toBn254FieldElement(BigInt(day)),
        toBn254FieldElement(BigInt(hour)),
        toBn254FieldElement(BigInt(minute)),
        toBn254FieldElement(BigInt(second)),
        // [8] H2 hash
        h2Hash,
        // [9] Sender user commitment
        senderUserCommitment,
        // [10-12] Linker encryptions (PcCiphertext → Bn254FieldElement)
        toBn254FieldElement(pcEncryptedDestinationAddressLow),
        toBn254FieldElement(pcEncryptedDestinationAddressHigh),
        toBn254FieldElement(pcEncryptedAmount),
        // [13-15] Keystream commitments
        firstPcKeystreamCommitment,
        secondPcKeystreamCommitment,
        thirdPcKeystreamCommitment,
        // [16-17] Arcis public key (user's X25519 public key split)
        toBn254FieldElement(senderX25519PublicKeyLow),
        toBn254FieldElement(senderX25519PublicKeyHigh),
        // [18] Nonce
        toBn254FieldElement(rcEncryptionNonce),
        // [19-21] Rescue encryptions - amount (ciphertext limbs)
        limbToBn254(rcEncryptedAmountAsLimbs.low),
        limbToBn254(rcEncryptedAmountAsLimbs.middle),
        limbToBn254(rcEncryptedAmountAsLimbs.high),
        // [22-24] Rescue encryptions - protocol fees (ciphertext limbs)
        limbToBn254(rcEncryptedProtocolFeesAsLimbs.low),
        limbToBn254(rcEncryptedProtocolFeesAsLimbs.middle),
        limbToBn254(rcEncryptedProtocolFeesAsLimbs.high),
        // [25-27] Rescue encryptions - random factor low
        limbToBn254(rcEncryptedRandomFactorLowAsLimbs.low),
        limbToBn254(rcEncryptedRandomFactorLowAsLimbs.middle),
        limbToBn254(rcEncryptedRandomFactorLowAsLimbs.high),
        // [28-30] Rescue encryptions - random factor high
        limbToBn254(rcEncryptedRandomFactorHighAsLimbs.low),
        limbToBn254(rcEncryptedRandomFactorHighAsLimbs.middle),
        limbToBn254(rcEncryptedRandomFactorHighAsLimbs.high),
        // [31] Rescue encryption commitment
        rcEncryptionCommitment,
        // [32-34] Challenge limbs
        limbToBn254(fiatShamirChallengeLimbs.low),
        limbToBn254(fiatShamirChallengeLimbs.middle),
        limbToBn254(fiatShamirChallengeLimbs.high),
        // [35-37] Challenge squared limbs
        limbToBn254(fiatShamirChallengeSquaredLimbs.low),
        limbToBn254(fiatShamirChallengeSquaredLimbs.middle),
        limbToBn254(fiatShamirChallengeSquaredLimbs.high),
        // [38-40] Polynomial evaluation result limbs
        limbToBn254(rescueEncryptionValidationPolynomialAsLimbs.low),
        limbToBn254(rescueEncryptionValidationPolynomialAsLimbs.middle),
        limbToBn254(rescueEncryptionValidationPolynomialAsLimbs.high),
        // [41] Protocol fees root
        toBn254FieldElement(protocolFeeConfig.merkleRoot)
      ];
      const aggregatedPublicInputHash = await poseidonAggregator(aggregatedHashInputs);
      const inverseForSenderMvkBlindingFactor = computeBn254ModularInverse(senderMvkBlindingFactor);
      const inverseForSenderPrivateKeyBlindingFactor = computeBn254ModularInverse(
        senderPrivateKeyBlindingFactor
      );
      const inverseForSenderPrivateKey = computeBn254ModularInverse(senderPrivateKey);
      const inverseForSenderMasterViewingKey = computeBn254ModularInverse(senderMasterViewingKey);
      const inverseForAmount = computeBn254ModularInverse(toBn254FieldElement(amount));
      const protocolFeeDivisorInverse = computeBn254ModularInverse(toBn254FieldElement(16384n));
      const inverseForTotalAmountLimbs = computeBn254LimbwiseSumInverse([
        amountAsLimbs.low,
        amountAsLimbs.middle,
        amountAsLimbs.high
      ]);
      const inverseForKeysForTotalAmountLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForAmountAsLimbs.low,
        rcEncryptionKeyForAmountAsLimbs.middle,
        rcEncryptionKeyForAmountAsLimbs.high
      ]);
      const inverseForKeysForProtocolFeeLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForProtocolFeesAsLimbs.low,
        rcEncryptionKeyForProtocolFeesAsLimbs.middle,
        rcEncryptionKeyForProtocolFeesAsLimbs.high
      ]);
      const inverseForKeysForRandomFactorForPolynomialEvaluationLowLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForRandomFactorLowAsLimbs.low,
        rcEncryptionKeyForRandomFactorLowAsLimbs.middle,
        rcEncryptionKeyForRandomFactorLowAsLimbs.high
      ]);
      const inverseForKeysForRandomFactorForPolynomialEvaluationHighLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForRandomFactorHighAsLimbs.low,
        rcEncryptionKeyForRandomFactorHighAsLimbs.middle,
        rcEncryptionKeyForRandomFactorHighAsLimbs.high
      ]);
      const inverseForNullifier = computeBn254ModularInverse(nullifier);
      const inverseForH2BlindingFactor = computeBn254ModularInverse(randomSecret);
      const inverseForFirstKeystreamBlindingFactor = computeBn254ModularInverse(
        firstKeystreamBlindingFactor
      );
      const inverseForSecondKeystreamBlindingFactor = computeBn254ModularInverse(
        secondKeystreamBlindingFactor
      );
      const inverseForThirdKeystreamBlindingFactor = computeBn254ModularInverse(
        thirdKeystreamBlindingFactor
      );
      const inverseForRandomFactorForPolynomialEvaluationLowLimbs = computeBn254LimbwiseSumInverse([
        randomFactorLowAsLimbs.low,
        randomFactorLowAsLimbs.middle,
        randomFactorLowAsLimbs.high
      ]);
      const inverseForRandomFactorForPolynomialEvaluationHighLimbs = computeBn254LimbwiseSumInverse(
        [randomFactorHighAsLimbs.low, randomFactorHighAsLimbs.middle, randomFactorHighAsLimbs.high]
      );
      const inverseForChallengeLimbs = computeBn254LimbwiseSumInverse([
        fiatShamirChallengeLimbs.low,
        fiatShamirChallengeLimbs.middle,
        fiatShamirChallengeLimbs.high
      ]);
      const inverseForChallengeSquaredLimbs = computeBn254LimbwiseSumInverse([
        fiatShamirChallengeSquaredLimbs.low,
        fiatShamirChallengeSquaredLimbs.middle,
        fiatShamirChallengeSquaredLimbs.high
      ]);
      const zkCircuitInputs = {
        // ================================================================
        // Sender Commitment (8 signals)
        // ================================================================
        senderPrivateKey,
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        inverseForSenderMvkBlindingFactor,
        senderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKey,
        inverseForSenderMasterViewingKey,
        // ================================================================
        // Amount and Protocol Fee (22 signals)
        // ================================================================
        amount: toBn254FieldElement(amount),
        inverseForAmount,
        protocolFeeBaseFee: toBn254FieldElement(baseFeesInSpl),
        protocolFeeLowerBound: toBn254FieldElement(amountLowerBound),
        protocolFeeUpperBound: toBn254FieldElement(amountUpperBound),
        protocolFeeBps: toBn254FieldElement(commissionFeeInBps),
        protocolFeeBpsQuotient: toBn254FieldElement(protocolFeeBpsQuotient),
        protocolFeeBpsRemainder: toBn254FieldElement(protocolFeeBpsRemainder),
        protocolFeeDivisorInverse,
        protocolFeesRoot: toBn254FieldElement(protocolFeeConfig.merkleRoot),
        protocolFeesMerklePath: protocolFeeConfig.merklePath.map((h) => toBn254FieldElement(h)),
        protocolFeesPathIndices: leafIndexToPathIndices(protocolFeeConfig.leafIndex).map((idx) => toBn254FieldElement(idx)),
        totalAmountLimbs: [amountAsLimbs.low, amountAsLimbs.middle, amountAsLimbs.high],
        inverseForTotalAmountLimbs,
        protocolFeeLimbs: [
          protocolFeesAsLimbs.low,
          protocolFeesAsLimbs.middle,
          protocolFeesAsLimbs.high
        ],
        keysForTotalAmountLimbs: [
          rcEncryptionKeyForAmountAsLimbs.low,
          rcEncryptionKeyForAmountAsLimbs.middle,
          rcEncryptionKeyForAmountAsLimbs.high
        ],
        inverseForKeysForTotalAmountLimbs,
        keysForProtocolFeeLimbs: [
          rcEncryptionKeyForProtocolFeesAsLimbs.low,
          rcEncryptionKeyForProtocolFeesAsLimbs.middle,
          rcEncryptionKeyForProtocolFeesAsLimbs.high
        ],
        inverseForKeysForProtocolFeeLimbs,
        keysForRandomFactorForPolynomialEvaluationLowLimbs: [
          rcEncryptionKeyForRandomFactorLowAsLimbs.low,
          rcEncryptionKeyForRandomFactorLowAsLimbs.middle,
          rcEncryptionKeyForRandomFactorLowAsLimbs.high
        ],
        inverseForKeysForRandomFactorForPolynomialEvaluationLowLimbs,
        keysForRandomFactorForPolynomialEvaluationHighLimbs: [
          rcEncryptionKeyForRandomFactorHighAsLimbs.low,
          rcEncryptionKeyForRandomFactorHighAsLimbs.middle,
          rcEncryptionKeyForRandomFactorHighAsLimbs.high
        ],
        inverseForKeysForRandomFactorForPolynomialEvaluationHighLimbs,
        // ================================================================
        // H2 Hash Generation (6 signals)
        // ================================================================
        nullifier,
        inverseForNullifier,
        unlockingUserCommitment: ephemeralUserCommitment,
        h2BlindingFactor: randomSecret,
        inverseForH2BlindingFactor,
        // ================================================================
        // Timestamp (6 signals)
        // ================================================================
        year: toBn254FieldElement(BigInt(year)),
        month: toBn254FieldElement(BigInt(month)),
        day: toBn254FieldElement(BigInt(day)),
        hour: toBn254FieldElement(BigInt(hour)),
        minute: toBn254FieldElement(BigInt(minute)),
        second: toBn254FieldElement(BigInt(second)),
        // ================================================================
        // Mint Address (2 signals)
        // ================================================================
        mintAddressLow: toBn254FieldElement(mintAddressLow),
        mintAddressHigh: toBn254FieldElement(mintAddressHigh),
        // ================================================================
        // Encryption & Keystream (8 signals)
        // ================================================================
        finalDestinationAddressLow: toBn254FieldElement(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement(destinationAddressHigh),
        keystreamBlindingFactor: [
          firstKeystreamBlindingFactor,
          secondKeystreamBlindingFactor,
          thirdKeystreamBlindingFactor
        ],
        inverseForKeystreamBlindingFactor: [
          inverseForFirstKeystreamBlindingFactor,
          inverseForSecondKeystreamBlindingFactor,
          inverseForThirdKeystreamBlindingFactor
        ],
        // ================================================================
        // Polynomial Evaluation (10 signals)
        // ================================================================
        randomFactorForPolynomialEvaluationLowLimbs: [
          randomFactorLowAsLimbs.low,
          randomFactorLowAsLimbs.middle,
          randomFactorLowAsLimbs.high
        ],
        inverseForRandomFactorForPolynomialEvaluationLowLimbs,
        randomFactorForPolynomialEvaluationHighLimbs: [
          randomFactorHighAsLimbs.low,
          randomFactorHighAsLimbs.middle,
          randomFactorHighAsLimbs.high
        ],
        inverseForRandomFactorForPolynomialEvaluationHighLimbs,
        challengeLimbs: [
          fiatShamirChallengeLimbs.low,
          fiatShamirChallengeLimbs.middle,
          fiatShamirChallengeLimbs.high
        ],
        inverseForChallengeLimbs,
        challengeSquaredLimbs: [
          fiatShamirChallengeSquaredLimbs.low,
          fiatShamirChallengeSquaredLimbs.middle,
          fiatShamirChallengeSquaredLimbs.high
        ],
        inverseForChallengeSquaredLimbs,
        // ================================================================
        // Rescue Encryption (4 signals)
        // ================================================================
        nonce: toBn254FieldElement(rcEncryptionNonce),
        arcisPublicKeyLow: toBn254FieldElement(senderX25519PublicKeyLow),
        arcisPublicKeyHigh: toBn254FieldElement(senderX25519PublicKeyHigh),
        blindingFactorForRescueCommitment: rescueEncryptionCommitmentBlindingFactor,
        // ================================================================
        // Public Input (1 signal)
        // ================================================================
        publicAggregatedHash: aggregatedPublicInputHash
      };
      const { proofA, proofB, proofC } = await deps.zkProver.prove(zkCircuitInputs);
      assertGroth16ProofA(proofA);
      assertGroth16ProofB(proofB);
      assertGroth16ProofC(proofC);
      const stealthPoolIndex = 0n;
      const stealthPoolAddress = await findStealthPoolPda(
        stealthPoolIndex,
        client.networkConfig.programId
      );
      const arciumAccounts = await findArciumInfrastructurePdas(
        client.networkConfig.programId,
        client.networkConfig.arciumProgramAddress,
        clusterOffset,
        BigInt(computationOffset),
        "deposit_into_stealth_pool_from_shared_balance_v11"
      );
      const aesSharedSecret = x25519.getSharedSecret(clientX25519PrivateKey, senderX25519PublicKey);
      const aesEncryptionKeyBytes = keccak_256(aesSharedSecret).slice(0, 32);
      assertAesKey(aesEncryptionKeyBytes);
      const destinationAddressBytes = new Uint8Array(
        getAddressEncoder().encode(destinationAddress)
      );
      const aesPlaintextData = new Uint8Array(68);
      aesPlaintextData.set(encodeU64ToU64LeBytes(amountAfterFees), 0);
      aesPlaintextData.set(destinationAddressBytes, 8);
      aesPlaintextData.set(modifiedGenerationIndex, 40);
      aesPlaintextData.set(AES_DOMAIN_SEPARATOR_EPHEMERAL2, 56);
      assertAesPlaintext(aesPlaintextData);
      const aesEncryptedUtxoData = await aesEncryptor(aesEncryptionKeyBytes, aesPlaintextData);
      const optionalDataBytes = options?.optionalData ?? new Uint8Array(32);
      const h2HashBytes = encodeU256ToU256LeBytes(toU256(h2Hash));
      const rcEncryptionCommitmentBytes = encodeU256ToU256LeBytes(toU256(rcEncryptionCommitment));
      const polynomialEvaluationBytes = encodeU256ToU256LeBytes(
        toU256(rescueEncryptionValidationPolynomial)
      );
      const linkerEncryption0Bytes = encodeU256ToU256LeBytes(
        toU256(pcEncryptedDestinationAddressLow)
      );
      const linkerEncryption1Bytes = encodeU256ToU256LeBytes(
        toU256(pcEncryptedDestinationAddressHigh)
      );
      const linkerEncryption2Bytes = encodeU256ToU256LeBytes(toU256(pcEncryptedAmount));
      const keystreamCommitment0Bytes = encodeU256ToU256LeBytes(toU256(firstPcKeystreamCommitment));
      const keystreamCommitment1Bytes = encodeU256ToU256LeBytes(
        toU256(secondPcKeystreamCommitment)
      );
      const keystreamCommitment2Bytes = encodeU256ToU256LeBytes(toU256(thirdPcKeystreamCommitment));
      const utxoAltEntry = lookupAltEntry(
        client.networkConfig.addressLookupTables,
        clusterOffset,
        "deposit_into_stealth_pool_from_shared_balance_v11"
      );
      async function executeTransaction(instruction, instructionName, prefixInstructions, useAlt, callbacks, awaitCallbackOptions) {
        let latestBlockhashData;
        try {
          latestBlockhashData = await getLatestBlockhash();
        } catch (error) {
          throw new RpcError(
            `Failed to fetch latest blockhash: ${error instanceof Error ? error.message : "Unknown error"}`,
            { cause: error }
          );
        }
        const altEntry = useAlt === true ? utxoAltEntry : void 0;
        const allInstructions = prefixInstructions === void 0 ? [instruction] : [...prefixInstructions, instruction];
        const transactionMessage = buildTransactionMessage(
          client.signer,
          latestBlockhashData,
          allInstructions,
          altEntry
        );
        const transaction = compileTransaction(transactionMessage);
        const signedTransaction = await client.signer.signTransaction(transaction);
        if (!isSignedTransaction(signedTransaction)) {
          throw new TransactionError(`${instructionName}: Transaction was not properly signed`);
        }
        const result2 = await forwardAndMonitor(
          executorConfig,
          signedTransaction,
          callbacks,
          awaitCallbackOptions
        );
        const callbackResult = "callbackResult" in result2 ? result2.callbackResult : void 0;
        const callbackSignature = callbackResult !== void 0 && "callbackSignature" in callbackResult ? callbackResult.callbackSignature : void 0;
        const rentClaimSignature = "rentClaimSignature" in result2 ? result2.rentClaimSignature : void 0;
        const rentClaimError = "rentClaimError" in result2 ? result2.rentClaimError : void 0;
        return {
          signedTransaction: result2.signedTransaction,
          signature: result2.signature,
          callbackStatus: callbackResult?.status,
          callbackSignature,
          callbackElapsedMs: callbackResult?.elapsedMs,
          rentClaimSignature,
          rentClaimError
        };
      }
      __name(executeTransaction, "executeTransaction");
      async function checkAndCloseExistingProofAccount(offset) {
        const proofAccountPda = await findUtxoInputBufferPda(
          client.signer.address,
          offset,
          client.networkConfig.programId
        );
        let accountExists = false;
        try {
          const accountMap = await accountInfoProvider([proofAccountPda], { commitment: accountInfoCommitment });
          const accountInfo = accountMap.get(proofAccountPda);
          accountExists = accountInfo?.exists === true;
        } catch {
          return null;
        }
        if (accountExists) {
          const closeInstruction = await getCloseStealthPoolDepositInputBufferInstructionAsync(
            {
              depositor: createNoopSigner(client.signer.address),
              offset: {
                first: offset
              }
            },
            { programAddress: client.networkConfig.programId }
          );
          return executeTransaction(closeInstruction, "CloseConfidentialUtxoProofAccount");
        }
        return null;
      }
      __name(checkAndCloseExistingProofAccount, "checkAndCloseExistingProofAccount");
      assertU128(proofAccountOffset);
      const closeResult = await checkAndCloseExistingProofAccount(proofAccountOffset);
      if (closeResult !== null) {
        if (factoryCloseProofAccountCallbacks?.pre !== void 0) {
          await factoryCloseProofAccountCallbacks.pre(closeResult.signedTransaction);
        }
        if (factoryCloseProofAccountCallbacks?.post !== void 0) {
          await factoryCloseProofAccountCallbacks.post(
            closeResult.signedTransaction,
            closeResult.signature
          );
        }
      }
      assertU256(rcEncryptedAmount);
      assertU256(rcEncryptedProtocolFees);
      assertU256(rcEncryptedRandomFactorLow);
      assertU256(rcEncryptedRandomFactorHigh);
      const proofAccountInstruction = await getCreateStealthPoolDepositInputBufferInstructionAsync(
        {
          depositor: createNoopSigner(client.signer.address),
          feePayer: createNoopSigner(client.signer.address),
          offset: { first: proofAccountOffset },
          rescueEncryptionPublicKey: {
            first: clientX25519PublicKey
          },
          rescueEncryptionNonce: {
            first: rcEncryptionNonce
          },
          rescueEncryptedTotalAmount: {
            first: encodeU256ToU256LeBytes(rcEncryptedAmount)
          },
          rescueEncryptedProtocolFees: {
            first: encodeU256ToU256LeBytes(rcEncryptedProtocolFees)
          },
          rescueEncryptedRandomFactorLow: {
            first: encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow)
          },
          rescueEncryptedRandomFactorHigh: {
            first: encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh)
          },
          encryptionValidationPolynomial: {
            first: polynomialEvaluationBytes
          },
          rescueEncryptionFiatShamirCommitment: {
            first: rcEncryptionCommitmentBytes
          },
          insertionH2Commitment: {
            first: h2HashBytes
          },
          insertionTimestamp: {
            first: BigInt(currentTimestamp)
          },
          linkerEncryption0: {
            first: linkerEncryption0Bytes
          },
          linkerEncryption1: {
            first: linkerEncryption1Bytes
          },
          linkerEncryption2: {
            first: linkerEncryption2Bytes
          },
          keystreamCommitment0: {
            first: keystreamCommitment0Bytes
          },
          keystreamCommitment1: {
            first: keystreamCommitment1Bytes
          },
          keystreamCommitment2: {
            first: keystreamCommitment2Bytes
          },
          groth16ProofA: { first: proofA },
          groth16ProofB: { first: proofB },
          groth16ProofC: { first: proofC },
          aesEncryptedData: {
            first: aesEncryptedUtxoData
          },
          optionalData: {
            first: optionalDataBytes
          }
        },
        { programAddress: client.networkConfig.programId }
      );
      const proofAccountResult = await executeTransaction(
        proofAccountInstruction,
        "CreateConfidentialUtxoProofAccount",
        void 0,
        void 0,
        factoryCreateProofAccountCallbacks
      );
      const utxoInstruction = await getDepositIntoStealthPoolFromSharedBalanceV11InstructionAsync(
        {
          depositor: createNoopSigner(client.signer.address),
          feePayer: createNoopSigner(client.signer.address),
          mxeAccount: arciumAccounts.mxeAccount,
          mempoolAccount: arciumAccounts.mempoolAccount,
          executingPool: arciumAccounts.executingPoolAccount,
          computationAccount: arciumAccounts.computationAccount,
          compDefAccount: arciumAccounts.compDefAccount,
          clusterAccount: arciumAccounts.clusterAccount,
          stealthPool: stealthPoolAddress,
          mint,
          computationOffset: {
            first: computationOffset
          },
          mpcCallbackDataOffset: {
            first: generateRandomU128()
          },
          feeVaultOffset: {
            first: 0n
          },
          stealthPoolDepositInputBufferOffset: {
            first: proofAccountOffset
          },
          priorityFees: { first: microLamportsPerAcu }
        },
        { programAddress: client.networkConfig.programId }
      );
      const computeBudgetInstruction = createSetComputeUnitLimitInstruction(6e5);
      const resolvedAwaitConfig = factoryAwaitComputationFinalization === false ? false : factoryAwaitComputationFinalization ?? {};
      const utxoCallbackOptions = resolvedAwaitConfig === false ? void 0 : {
        ...resolvedAwaitConfig,
        retrieveCallbackSignature: true,
        computationAccountAddress: arciumAccounts.computationAccount,
        computationOffset: BigInt(computationOffset),
        clusterOffset
      };
      const utxoResult = await executeTransaction(
        utxoInstruction,
        "DepositIntoStealthPoolFromSharedBalanceV11",
        [computeBudgetInstruction],
        true,
        factoryCreateUtxoCallbacks,
        utxoCallbackOptions
      );
      void ephemeralMasterViewingKey;
      void ephemeralMasterViewingKeyBlindingFactor;
      void ephemeralPoseidonPrivateKey;
      void ephemeralPoseidonPrivateKeyBlindingFactor;
      const result = {
        closeProofAccountSignature: closeResult?.signature,
        createProofAccountSignature: proofAccountResult.signature,
        queueSignature: utxoResult.signature,
        callbackStatus: utxoResult.callbackStatus,
        callbackSignature: utxoResult.callbackSignature,
        callbackElapsedMs: utxoResult.callbackElapsedMs,
        rentClaimSignature: utxoResult.rentClaimSignature,
        rentClaimError: utxoResult.rentClaimError
      };
      return result;
    } catch (error) {
      if (error instanceof CreateUtxoError) {
        throw error;
      }
      if (error instanceof TransactionSigningError) {
        throw new CreateUtxoError("transaction-sign", error.message, error);
      }
      if (error instanceof CryptographyError) {
        throw new CreateUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof TransactionError) {
        throw new CreateUtxoError("transaction-send", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new CreateUtxoError("account-fetch", error.message, error);
      }
      throw new CreateUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  }, "createSelfClaimableUtxo");
}
__name(getEncryptedBalanceToSelfClaimableUtxoCreatorFunction, "getEncryptedBalanceToSelfClaimableUtxoCreatorFunction");
var AES_DOMAIN_SEPARATOR_RECEIVER2 = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateReceiverClaimableUtxoFromEncryptedBalance")
).slice(0, 12);
var DOMAIN_MODIFIED_GEN_INDEX2 = "CreateEncryptedBalanceToReceiverClaimableUtxoCreatorFunction / modifiedGenerationIndex";
function toBn254FieldElement2(value) {
  assertBn254FieldElement(value);
  return value;
}
__name(toBn254FieldElement2, "toBn254FieldElement");
function toU2562(value) {
  assertU256(value);
  return value;
}
__name(toU2562, "toU256");
function toCurve25519FieldElement2(value) {
  assertCurve25519FieldElement(value);
  return value;
}
__name(toCurve25519FieldElement2, "toCurve25519FieldElement");
function limbToBn2542(value) {
  assertBase85Limb(value);
  assertBn254FieldElement(value);
  return value;
}
__name(limbToBn2542, "limbToBn254");
function isSignedTransaction2(value) {
  if (typeof value !== "object" || value === null) {
    return false;
  }
  const tx = value;
  return "messageBytes" in tx && "signatures" in tx && tx.signatures instanceof Object && Object.keys(tx.signatures).length > 0;
}
__name(isSignedTransaction2, "isSignedTransaction");
function bytesToBigIntLE(bytes) {
  let value = 0n;
  for (const [index, byte] of bytes.entries()) {
    value |= BigInt(byte) << BigInt(index * 8);
  }
  return value;
}
__name(bytesToBigIntLE, "bytesToBigIntLE");
function getEncryptedBalanceToReceiverClaimableUtxoCreatorFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps.rpc?.transactionForwarder ?? client.transactionForwarder;
  const executorConfig = {
    signer: client.signer,
    getLatestBlockhash,
    transactionForwarder,
    computationMonitor: client.computationMonitor
  };
  const factoryMicroLamportsPerAcu = deps.arcium?.microLamportsPerAcu ?? 0n;
  const factoryAwaitComputationFinalization = deps.arcium?.awaitComputationFinalization;
  const factoryCloseProofAccountCallbacks = deps.hooks?.closeProofAccount;
  const factoryCreateProofAccountCallbacks = deps.hooks?.createProofAccount;
  const factoryCreateUtxoCallbacks = deps.hooks?.createUtxo;
  const masterViewingKeyGenerator = deps.keys?.masterViewingKeyGenerator ?? getMasterViewingKeyDeriver({ client });
  const masterViewingKeyBlindingFactorGenerator = deps.keys?.masterViewingKeyBlindingFactorGenerator ?? getMasterViewingKeyBlindingFactorDeriver({ client });
  const poseidonPrivateKeyGenerator = deps.keys?.poseidonPrivateKeyGenerator ?? getPoseidonPrivateKeyDeriver({ client });
  const poseidonBlindingFactorGenerator = deps.keys?.poseidonBlindingFactorGenerator ?? getPoseidonBlindingFactorDeriver({ client });
  const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const secondViewingKeyGenerator = deps.keys?.secondViewingKeyGenerator ?? getSecondViewingKeyDeriver({ client });
  const poseidonKeystreamBlindingFactorGenerator = deps.keys?.poseidonKeystreamBlindingFactorGenerator ?? getPoseidonKeystreamBlindingFactorDeriver({ client });
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGenerator = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const h2Generator = deps.crypto?.h2Generator ?? getUtxoCommitmentHashGenerator();
  const keystreamCommitmentGenerator = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const userCommitmentGenerator = getUserCommitmentGeneratorFunction();
  const rescueEncryptionCommitmentGenerator = getRescueCommitmentGenerator();
  const poseidonAggregator = getPoseidonAggregator();
  const fiatShamirChallengeGenerator = getFiatShamirChallengeGeneratorFunction();
  const challengePowersFunction = getChallengePowersFunction();
  const polynomialEvaluator = getPolynomialEvaluatorFunction();
  const aesEncryptor = deps.crypto?.aesEncryptor ?? getAesEncryptor();
  const generateRandomU256Function = deps.random?.generateRandomU256 ?? generateRandomU256;
  const generateComputationOffsetFunction = deps.random?.generateComputationOffset ?? generateComputationOffset;
  return /* @__PURE__ */ __name(async function createReceiverClaimableUtxo(args2, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    try {
      const { amount, destinationAddress, mint } = args2;
      const generationIndex = options?.generationIndex;
      const microLamportsPerAcu = factoryMicroLamportsPerAcu;
      assertU64(amount);
      if (generationIndex !== void 0) {
        assertU256(generationIndex);
      }
      const effectiveGenerationIndex = generationIndex ?? generateRandomU256Function();
      const masterSeed = await client.masterSeed.getMasterSeed();
      if (!(masterSeed instanceof Uint8Array) || masterSeed.length === 0) {
        throw new CryptographyError("Failed to retrieve master seed from client");
      }
      const generationIndexBytes = new TextEncoder().encode(effectiveGenerationIndex.toString());
      const modifiedGenerationIndex = kmac256(
        new TextEncoder().encode(DOMAIN_MODIFIED_GEN_INDEX2),
        masterSeed,
        { dkLen: 16, personalization: generationIndexBytes }
      );
      const expandedModifiedGenerationIndex = expandModifiedGenerationIndex(modifiedGenerationIndex);
      let _expandedModifiedGenerationIndexU256 = 0n;
      for (let index = 0; index < 32; index++) {
        _expandedModifiedGenerationIndexU256 |= BigInt(expandedModifiedGenerationIndex[index]) << BigInt(index * 8);
      }
      const proofAccountOffsetBytes = deriveProofAccountOffsetFromModifiedGenerationIndex(modifiedGenerationIndex);
      let proofAccountOffset = 0n;
      for (let index = 0; index < 16; index++) {
        proofAccountOffset |= BigInt(proofAccountOffsetBytes[index]) << BigInt(index * 8);
      }
      const receiverUserAccountPda = await findEncryptedUserAccountPda(
        destinationAddress,
        client.networkConfig.programId
      );
      const receiverAccountMap = await accountInfoProvider([
        receiverUserAccountPda,
        client.networkConfig.mxeAccountAddress
      ], { commitment: accountInfoCommitment });
      const receiverAccountInfo = receiverAccountMap.get(receiverUserAccountPda);
      if (receiverAccountInfo?.exists !== true) {
        throw new CryptographyError(`Receiver is not registered: ${destinationAddress}`);
      }
      const receiverAccount = decodeEncryptedUserAccount(receiverAccountInfo);
      const receiverAccountData = receiverAccount.data;
      const clusterOffset = extractClusterOffsetFromMxeAccount(
        receiverAccountMap.get(client.networkConfig.mxeAccountAddress)
      );
      const receiverX25519PublicKeyBytes = receiverAccountData.x25519PublicKeyForTokenEncryption?.first;
      if (receiverX25519PublicKeyBytes === void 0) {
        throw new CryptographyError("Receiver does not have X25519 public key registered");
      }
      const receiverX25519PublicKey = new Uint8Array(receiverX25519PublicKeyBytes);
      assertX25519PublicKey(receiverX25519PublicKey);
      const receiverUserCommitmentBytes = receiverAccountData.userCommitment?.first;
      if (receiverUserCommitmentBytes === void 0) {
        throw new CryptographyError("Receiver does not have user commitment registered");
      }
      const receiverUserCommitment = bytesToBigIntLE(new Uint8Array(receiverUserCommitmentBytes));
      assertBn254FieldElement(receiverUserCommitment);
      const computationOffset = generateComputationOffsetFunction();
      const protocolFeeProvider = getHardcodedCreateUtxoProtocolFeeProvider();
      const protocolFeeConfig = await protocolFeeProvider(amount);
      const baseFeesInSpl = protocolFeeConfig.slab.baseFee;
      const commissionFeeInBps = protocolFeeConfig.slab.bps;
      const amountLowerBound = protocolFeeConfig.slab.amountLowerBound;
      const amountUpperBound = protocolFeeConfig.slab.amountUpperBound;
      const amountAfterBaseFeeForBps = amount - baseFeesInSpl;
      const protocolFeeBpsQuotient = amountAfterBaseFeeForBps * commissionFeeInBps / 16384n;
      const protocolFeeBpsRemainder = amountAfterBaseFeeForBps * commissionFeeInBps % 16384n;
      const protocolFees = baseFeesInSpl + protocolFeeBpsQuotient;
      const amountAfterFees = amount - protocolFees;
      let currentTimestamp;
      let timestampComponents;
      if (deps.random?.getUtcNow === void 0) {
        const now = /* @__PURE__ */ new Date();
        currentTimestamp = Math.floor(now.getTime() / 1e3);
        timestampComponents = extractUtcComponents(now);
      } else {
        timestampComponents = deps.random.getUtcNow();
        const { year: year2, month: month2, day: day2, hour: hour2, minute: minute2, second: second2 } = timestampComponents;
        const dateFromComponents = new Date(
          Date.UTC(
            Number(year2),
            Number(month2) - 1,
            Number(day2),
            Number(hour2),
            Number(minute2),
            Number(second2)
          )
        );
        currentTimestamp = Math.floor(dateFromComponents.getTime() / 1e3);
      }
      const { year, month, day, hour, minute, second } = timestampComponents;
      const transactionViewingKey = await secondViewingKeyGenerator(
        mint,
        year,
        month,
        day,
        hour,
        minute,
        second
      );
      assertBn254FieldElement(transactionViewingKey);
      const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
      const senderMasterViewingKey = await masterViewingKeyGenerator();
      assertBn254FieldElement(senderMasterViewingKey);
      const senderMvkBlindingFactor = await masterViewingKeyBlindingFactorGenerator();
      assertBn254FieldElement(senderMvkBlindingFactor);
      const senderPrivateKey = await poseidonPrivateKeyGenerator();
      assertBn254FieldElement(senderPrivateKey);
      const senderPrivateKeyBlindingFactor = await poseidonBlindingFactorGenerator();
      assertBn254FieldElement(senderPrivateKeyBlindingFactor);
      const senderUserCommitment = await userCommitmentGenerator(
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        senderPrivateKey,
        senderPrivateKeyBlindingFactor
      );
      assertBn254FieldElement(senderUserCommitment);
      const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(randomSecret);
      const nullifier = deriveNullifierFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(nullifier);
      const unlockingUserCommitment = receiverUserCommitment;
      const h2Hash = await h2Generator.generateH2({
        amount: toBn254FieldElement2(amountAfterFees),
        nullifier,
        userCommitment: unlockingUserCommitment,
        finalDestinationAddressLow: toBn254FieldElement2(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement2(destinationAddressHigh),
        h2BlindingFactor: randomSecret
      });
      assertBn254FieldElement(h2Hash);
      assertPoseidonPlaintext(destinationAddressLow);
      assertPoseidonPlaintext(destinationAddressHigh);
      assertPoseidonPlaintext(amount);
      const poseidonKey = BigInt(transactionViewingKey);
      assertPoseidonKey(poseidonKey);
      const pcCiphertexts = await poseidonEncryptor(
        [
          destinationAddressLow,
          destinationAddressHigh,
          amount
        ],
        poseidonKey
      );
      if (pcCiphertexts.length !== 3) {
        throw new CryptographyError(
          `Expected 3 ciphertexts but got ${String(pcCiphertexts.length)}`
        );
      }
      const pcEncryptedDestinationAddressLow = pcCiphertexts[0];
      const pcEncryptedDestinationAddressHigh = pcCiphertexts[1];
      const pcEncryptedAmount = pcCiphertexts[2];
      const pcKeystreams = await poseidonKeystreamGenerator(
        [0n, 1n, 2n],
        poseidonKey
      );
      const pcKeyForDestinationAddressLow = pcKeystreams.get(0n);
      if (pcKeyForDestinationAddressLow === void 0) {
        throw new CryptographyError("Missing keystream for counter 0");
      }
      const pcKeyForDestinationAddressHigh = pcKeystreams.get(1n);
      if (pcKeyForDestinationAddressHigh === void 0) {
        throw new CryptographyError("Missing keystream for counter 1");
      }
      const pcKeyForAmount = pcKeystreams.get(2n);
      if (pcKeyForAmount === void 0) {
        throw new CryptographyError("Missing keystream for counter 2");
      }
      const firstKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressLow,
        0n
      );
      const secondKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressHigh,
        1n
      );
      const thirdKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForAmount,
        2n
      );
      const firstPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressLow,
        firstKeystreamBlindingFactor
      );
      const secondPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressHigh,
        secondKeystreamBlindingFactor
      );
      const thirdPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForAmount,
        thirdKeystreamBlindingFactor
      );
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      assertX25519PrivateKey(clientX25519PrivateKey);
      const clientX25519PublicKey = keypairResult.x25519Keypair.publicKey;
      const rcEncryptor = getRescueEncryptorFromPrivateKey(clientX25519PrivateKey, {
        umbraX25519PublicKey: client.networkConfig.mxePubkey
      });
      const randomFactorForPolynomialCommitment = deriveRandomFactorForPolynomialCommitmentFromModifiedGenerationIndex(
        modifiedGenerationIndex
      );
      assertU256(randomFactorForPolynomialCommitment);
      const { low: randomFactorLow, high: randomFactorHigh } = splitU256IntoTwoU128s(
        randomFactorForPolynomialCommitment
      );
      const rcPlaintextAmount = BigInt(amount);
      const rcPlaintextProtocolFees = BigInt(protocolFees);
      const rcPlaintextRandomFactorLow = BigInt(randomFactorLow);
      const rcPlaintextRandomFactorHigh = BigInt(randomFactorHigh);
      assertRcPlaintext(rcPlaintextAmount);
      assertRcPlaintext(rcPlaintextProtocolFees);
      assertRcPlaintext(rcPlaintextRandomFactorLow);
      assertRcPlaintext(rcPlaintextRandomFactorHigh);
      const rcCiphertextOutputs = await rcEncryptor([
        rcPlaintextAmount,
        rcPlaintextProtocolFees,
        rcPlaintextRandomFactorLow,
        rcPlaintextRandomFactorHigh
      ]);
      assertRcEncryptionNonce(rcCiphertextOutputs.nonce);
      if (rcCiphertextOutputs.ciphertexts.length !== 4) {
        throw new CryptographyError(
          `Expected 4 ciphertexts but got ${String(rcCiphertextOutputs.ciphertexts.length)}`
        );
      }
      const rcEncryptionNonce = rcCiphertextOutputs.nonce;
      const rcEncryptedAmount = rcCiphertextOutputs.ciphertexts[0];
      const rcEncryptedProtocolFees = rcCiphertextOutputs.ciphertexts[1];
      const rcEncryptedRandomFactorLow = rcCiphertextOutputs.ciphertexts[2];
      const rcEncryptedRandomFactorHigh = rcCiphertextOutputs.ciphertexts[3];
      assertRcCiphertext(rcEncryptedAmount);
      assertRcCiphertext(rcEncryptedProtocolFees);
      assertRcCiphertext(rcEncryptedRandomFactorLow);
      assertRcCiphertext(rcEncryptedRandomFactorHigh);
      const rcKeyGenerator = getRescueKeyGeneratorFromPrivateKey(clientX25519PrivateKey, {
        umbraX25519PublicKey: client.networkConfig.mxePubkey
      });
      const rcEncryptionKeys = await rcKeyGenerator(
        [0n, 1n, 2n, 3n],
        rcEncryptionNonce
      );
      const rcEncryptionKeyForAmount = rcEncryptionKeys.get(0n);
      if (rcEncryptionKeyForAmount === void 0) {
        throw new CryptographyError("Missing key for counter 0");
      }
      const rcEncryptionKeyForProtocolFees = rcEncryptionKeys.get(1n);
      if (rcEncryptionKeyForProtocolFees === void 0) {
        throw new CryptographyError("Missing key for counter 1");
      }
      const rcEncryptionKeyForRandomFactorLow = rcEncryptionKeys.get(2n);
      if (rcEncryptionKeyForRandomFactorLow === void 0) {
        throw new CryptographyError("Missing key for counter 2");
      }
      const rcEncryptionKeyForRandomFactorHigh = rcEncryptionKeys.get(3n);
      if (rcEncryptionKeyForRandomFactorHigh === void 0) {
        throw new CryptographyError("Missing key for counter 3");
      }
      assertRcKey(rcEncryptionKeyForAmount);
      assertRcKey(rcEncryptionKeyForProtocolFees);
      assertRcKey(rcEncryptionKeyForRandomFactorLow);
      assertRcKey(rcEncryptionKeyForRandomFactorHigh);
      const amountAsLimbs = convertU256ToBase85Limbs(toU2562(amount));
      const protocolFeesAsLimbs = convertU256ToBase85Limbs(toU2562(protocolFees));
      const randomFactorLowAsLimbs = convertU256ToBase85Limbs(toU2562(randomFactorLow));
      const randomFactorHighAsLimbs = convertU256ToBase85Limbs(toU2562(randomFactorHigh));
      const rcEncryptedAmountAsLimbs = convertU256ToBase85Limbs(toU2562(rcEncryptedAmount));
      const rcEncryptedProtocolFeesAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptedProtocolFees)
      );
      const rcEncryptedRandomFactorLowAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptedRandomFactorLow)
      );
      const rcEncryptedRandomFactorHighAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptedRandomFactorHigh)
      );
      const rcEncryptionKeyForAmountAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptionKeyForAmount)
      );
      const rcEncryptionKeyForProtocolFeesAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptionKeyForProtocolFees)
      );
      const rcEncryptionKeyForRandomFactorLowAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptionKeyForRandomFactorLow)
      );
      const rcEncryptionKeyForRandomFactorHighAsLimbs = convertU256ToBase85Limbs(
        toU2562(rcEncryptionKeyForRandomFactorHigh)
      );
      const rescueEncryptionCommitmentBlindingFactor = deriveRescueEncryptionCommitmentBlindingFactorFromModifiedGenerationIndex(
        modifiedGenerationIndex
      );
      assertBn254FieldElement(rescueEncryptionCommitmentBlindingFactor);
      const senderX25519PublicKey = clientX25519PublicKey;
      const { low: senderX25519PublicKeyLow, high: senderX25519PublicKeyHigh } = splitX25519PublicKeyIntoTwoU128s(senderX25519PublicKey);
      const rcEncryptionCommitment = await rescueEncryptionCommitmentGenerator(
        [
          rcEncryptedAmount,
          rcEncryptedProtocolFees,
          rcEncryptedRandomFactorLow,
          rcEncryptedRandomFactorHigh
        ],
        [
          toCurve25519FieldElement2(amount),
          toCurve25519FieldElement2(protocolFees),
          toCurve25519FieldElement2(randomFactorLow),
          toCurve25519FieldElement2(randomFactorHigh)
        ],
        [
          toCurve25519FieldElement2(rcEncryptionKeyForAmount),
          toCurve25519FieldElement2(rcEncryptionKeyForProtocolFees),
          toCurve25519FieldElement2(rcEncryptionKeyForRandomFactorLow),
          toCurve25519FieldElement2(rcEncryptionKeyForRandomFactorHigh)
        ],
        rescueEncryptionCommitmentBlindingFactor,
        rcEncryptionNonce,
        senderX25519PublicKeyLow,
        senderX25519PublicKeyHigh
      );
      assertBn254FieldElement(rcEncryptionCommitment);
      assertU128(rcEncryptionNonce);
      const fiatShamirInputBytes = new Uint8Array([
        // X25519 public key (32 bytes)
        ...senderX25519PublicKey,
        // rescue_encryption_nonce (U128 LE - 16 bytes)
        ...encodeU128ToU128LeBytes(rcEncryptionNonce),
        // rescue_encrypted_total_amount (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU2562(rcEncryptedAmount)),
        // rescue_encrypted_protocol_fees (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU2562(rcEncryptedProtocolFees)),
        // rescue_encrypted_random_factor_low (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU2562(rcEncryptedRandomFactorLow)),
        // rescue_encrypted_random_factor_high (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU2562(rcEncryptedRandomFactorHigh)),
        // rescue_encryption_fiat_shamir_commitment (32 bytes LE)
        ...encodeU256ToU256LeBytes(toU2562(BigInt(rcEncryptionCommitment)))
      ]);
      const fiatShamirChallenge = fiatShamirChallengeGenerator(fiatShamirInputBytes);
      const fiatShamirChallengeLimbs = convertU256ToBase85Limbs(toU2562(fiatShamirChallenge));
      const challengePowers = challengePowersFunction(fiatShamirChallenge, 2);
      const fiatShamirChallengeSquared = challengePowers[2];
      const fiatShamirChallengeSquaredLimbs = convertU256ToBase85Limbs(
        toU2562(fiatShamirChallengeSquared)
      );
      const rescueEncryptionValidationPolynomial = polynomialEvaluator(
        [
          toCurve25519FieldElement2(amount),
          toCurve25519FieldElement2(protocolFees),
          randomFactorForPolynomialCommitment
        ],
        fiatShamirChallenge
      );
      const rescueEncryptionValidationPolynomialAsLimbs = convertU256ToBase85Limbs(
        toU2562(rescueEncryptionValidationPolynomial)
      );
      const { low: mintAddressLow, high: mintAddressHigh } = splitAddressToLowHigh(mint);
      const aggregatedHashInputs = [
        // [0-1] Mint address (Address → U128 low/high → Bn254FieldElement)
        toBn254FieldElement2(mintAddressLow),
        toBn254FieldElement2(mintAddressHigh),
        // [2-7] Timestamp components (bigint → Bn254FieldElement)
        toBn254FieldElement2(BigInt(year)),
        toBn254FieldElement2(BigInt(month)),
        toBn254FieldElement2(BigInt(day)),
        toBn254FieldElement2(BigInt(hour)),
        toBn254FieldElement2(BigInt(minute)),
        toBn254FieldElement2(BigInt(second)),
        // [8] H2 hash
        h2Hash,
        // [9] Sender user commitment
        senderUserCommitment,
        // [10-12] Linker encryptions (PcCiphertext → Bn254FieldElement)
        toBn254FieldElement2(pcEncryptedDestinationAddressLow),
        toBn254FieldElement2(pcEncryptedDestinationAddressHigh),
        toBn254FieldElement2(pcEncryptedAmount),
        // [13-15] Keystream commitments
        firstPcKeystreamCommitment,
        secondPcKeystreamCommitment,
        thirdPcKeystreamCommitment,
        // [16-17] Arcis public key (user's X25519 public key split)
        toBn254FieldElement2(senderX25519PublicKeyLow),
        toBn254FieldElement2(senderX25519PublicKeyHigh),
        // [18] Nonce
        toBn254FieldElement2(rcEncryptionNonce),
        // [19-21] Rescue encryptions - amount (ciphertext limbs)
        limbToBn2542(rcEncryptedAmountAsLimbs.low),
        limbToBn2542(rcEncryptedAmountAsLimbs.middle),
        limbToBn2542(rcEncryptedAmountAsLimbs.high),
        // [22-24] Rescue encryptions - protocol fees (ciphertext limbs)
        limbToBn2542(rcEncryptedProtocolFeesAsLimbs.low),
        limbToBn2542(rcEncryptedProtocolFeesAsLimbs.middle),
        limbToBn2542(rcEncryptedProtocolFeesAsLimbs.high),
        // [25-27] Rescue encryptions - random factor low
        limbToBn2542(rcEncryptedRandomFactorLowAsLimbs.low),
        limbToBn2542(rcEncryptedRandomFactorLowAsLimbs.middle),
        limbToBn2542(rcEncryptedRandomFactorLowAsLimbs.high),
        // [28-30] Rescue encryptions - random factor high
        limbToBn2542(rcEncryptedRandomFactorHighAsLimbs.low),
        limbToBn2542(rcEncryptedRandomFactorHighAsLimbs.middle),
        limbToBn2542(rcEncryptedRandomFactorHighAsLimbs.high),
        // [31] Rescue encryption commitment
        rcEncryptionCommitment,
        // [32-34] Challenge limbs
        limbToBn2542(fiatShamirChallengeLimbs.low),
        limbToBn2542(fiatShamirChallengeLimbs.middle),
        limbToBn2542(fiatShamirChallengeLimbs.high),
        // [35-37] Challenge squared limbs
        limbToBn2542(fiatShamirChallengeSquaredLimbs.low),
        limbToBn2542(fiatShamirChallengeSquaredLimbs.middle),
        limbToBn2542(fiatShamirChallengeSquaredLimbs.high),
        // [38-40] Polynomial evaluation result limbs
        limbToBn2542(rescueEncryptionValidationPolynomialAsLimbs.low),
        limbToBn2542(rescueEncryptionValidationPolynomialAsLimbs.middle),
        limbToBn2542(rescueEncryptionValidationPolynomialAsLimbs.high),
        // [41] Protocol fees root
        toBn254FieldElement2(protocolFeeConfig.merkleRoot)
      ];
      const aggregatedPublicInputHash = await poseidonAggregator(aggregatedHashInputs);
      const inverseForSenderMvkBlindingFactor = computeBn254ModularInverse(senderMvkBlindingFactor);
      const inverseForSenderPrivateKeyBlindingFactor = computeBn254ModularInverse(
        senderPrivateKeyBlindingFactor
      );
      const inverseForSenderPrivateKey = computeBn254ModularInverse(senderPrivateKey);
      const inverseForSenderMasterViewingKey = computeBn254ModularInverse(senderMasterViewingKey);
      const inverseForAmount = computeBn254ModularInverse(toBn254FieldElement2(amount));
      const protocolFeeDivisorInverse = computeBn254ModularInverse(toBn254FieldElement2(16384n));
      const inverseForTotalAmountLimbs = computeBn254LimbwiseSumInverse([
        amountAsLimbs.low,
        amountAsLimbs.middle,
        amountAsLimbs.high
      ]);
      const inverseForKeysForTotalAmountLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForAmountAsLimbs.low,
        rcEncryptionKeyForAmountAsLimbs.middle,
        rcEncryptionKeyForAmountAsLimbs.high
      ]);
      const inverseForKeysForProtocolFeeLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForProtocolFeesAsLimbs.low,
        rcEncryptionKeyForProtocolFeesAsLimbs.middle,
        rcEncryptionKeyForProtocolFeesAsLimbs.high
      ]);
      const inverseForKeysForRandomFactorForPolynomialEvaluationLowLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForRandomFactorLowAsLimbs.low,
        rcEncryptionKeyForRandomFactorLowAsLimbs.middle,
        rcEncryptionKeyForRandomFactorLowAsLimbs.high
      ]);
      const inverseForKeysForRandomFactorForPolynomialEvaluationHighLimbs = computeBn254LimbwiseSumInverse([
        rcEncryptionKeyForRandomFactorHighAsLimbs.low,
        rcEncryptionKeyForRandomFactorHighAsLimbs.middle,
        rcEncryptionKeyForRandomFactorHighAsLimbs.high
      ]);
      const inverseForNullifier = computeBn254ModularInverse(nullifier);
      const inverseForH2BlindingFactor = computeBn254ModularInverse(randomSecret);
      const inverseForFirstKeystreamBlindingFactor = computeBn254ModularInverse(
        firstKeystreamBlindingFactor
      );
      const inverseForSecondKeystreamBlindingFactor = computeBn254ModularInverse(
        secondKeystreamBlindingFactor
      );
      const inverseForThirdKeystreamBlindingFactor = computeBn254ModularInverse(
        thirdKeystreamBlindingFactor
      );
      const inverseForRandomFactorForPolynomialEvaluationLowLimbs = computeBn254LimbwiseSumInverse([
        randomFactorLowAsLimbs.low,
        randomFactorLowAsLimbs.middle,
        randomFactorLowAsLimbs.high
      ]);
      const inverseForRandomFactorForPolynomialEvaluationHighLimbs = computeBn254LimbwiseSumInverse(
        [randomFactorHighAsLimbs.low, randomFactorHighAsLimbs.middle, randomFactorHighAsLimbs.high]
      );
      const inverseForChallengeLimbs = computeBn254LimbwiseSumInverse([
        fiatShamirChallengeLimbs.low,
        fiatShamirChallengeLimbs.middle,
        fiatShamirChallengeLimbs.high
      ]);
      const inverseForChallengeSquaredLimbs = computeBn254LimbwiseSumInverse([
        fiatShamirChallengeSquaredLimbs.low,
        fiatShamirChallengeSquaredLimbs.middle,
        fiatShamirChallengeSquaredLimbs.high
      ]);
      const zkCircuitInputs = {
        // ================================================================
        // Sender Commitment (8 signals)
        // ================================================================
        senderPrivateKey,
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        inverseForSenderMvkBlindingFactor,
        senderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKey,
        inverseForSenderMasterViewingKey,
        // ================================================================
        // Amount and Protocol Fee (22 signals)
        // ================================================================
        amount: toBn254FieldElement2(amount),
        inverseForAmount,
        protocolFeeBaseFee: toBn254FieldElement2(baseFeesInSpl),
        protocolFeeLowerBound: toBn254FieldElement2(amountLowerBound),
        protocolFeeUpperBound: toBn254FieldElement2(amountUpperBound),
        protocolFeeBps: toBn254FieldElement2(commissionFeeInBps),
        protocolFeeBpsQuotient: toBn254FieldElement2(protocolFeeBpsQuotient),
        protocolFeeBpsRemainder: toBn254FieldElement2(protocolFeeBpsRemainder),
        protocolFeeDivisorInverse,
        protocolFeesRoot: toBn254FieldElement2(protocolFeeConfig.merkleRoot),
        protocolFeesMerklePath: protocolFeeConfig.merklePath.map((h) => toBn254FieldElement2(h)),
        protocolFeesPathIndices: leafIndexToPathIndices(protocolFeeConfig.leafIndex).map((idx) => toBn254FieldElement2(idx)),
        totalAmountLimbs: [amountAsLimbs.low, amountAsLimbs.middle, amountAsLimbs.high],
        inverseForTotalAmountLimbs,
        protocolFeeLimbs: [
          protocolFeesAsLimbs.low,
          protocolFeesAsLimbs.middle,
          protocolFeesAsLimbs.high
        ],
        keysForTotalAmountLimbs: [
          rcEncryptionKeyForAmountAsLimbs.low,
          rcEncryptionKeyForAmountAsLimbs.middle,
          rcEncryptionKeyForAmountAsLimbs.high
        ],
        inverseForKeysForTotalAmountLimbs,
        keysForProtocolFeeLimbs: [
          rcEncryptionKeyForProtocolFeesAsLimbs.low,
          rcEncryptionKeyForProtocolFeesAsLimbs.middle,
          rcEncryptionKeyForProtocolFeesAsLimbs.high
        ],
        inverseForKeysForProtocolFeeLimbs,
        keysForRandomFactorForPolynomialEvaluationLowLimbs: [
          rcEncryptionKeyForRandomFactorLowAsLimbs.low,
          rcEncryptionKeyForRandomFactorLowAsLimbs.middle,
          rcEncryptionKeyForRandomFactorLowAsLimbs.high
        ],
        inverseForKeysForRandomFactorForPolynomialEvaluationLowLimbs,
        keysForRandomFactorForPolynomialEvaluationHighLimbs: [
          rcEncryptionKeyForRandomFactorHighAsLimbs.low,
          rcEncryptionKeyForRandomFactorHighAsLimbs.middle,
          rcEncryptionKeyForRandomFactorHighAsLimbs.high
        ],
        inverseForKeysForRandomFactorForPolynomialEvaluationHighLimbs,
        // ================================================================
        // H2 Hash Generation (6 signals)
        // ================================================================
        nullifier,
        inverseForNullifier,
        unlockingUserCommitment,
        h2BlindingFactor: randomSecret,
        inverseForH2BlindingFactor,
        // ================================================================
        // Timestamp (6 signals)
        // ================================================================
        year: toBn254FieldElement2(BigInt(year)),
        month: toBn254FieldElement2(BigInt(month)),
        day: toBn254FieldElement2(BigInt(day)),
        hour: toBn254FieldElement2(BigInt(hour)),
        minute: toBn254FieldElement2(BigInt(minute)),
        second: toBn254FieldElement2(BigInt(second)),
        // ================================================================
        // Mint Address (2 signals)
        // ================================================================
        mintAddressLow: toBn254FieldElement2(mintAddressLow),
        mintAddressHigh: toBn254FieldElement2(mintAddressHigh),
        // ================================================================
        // Encryption & Keystream (8 signals)
        // ================================================================
        finalDestinationAddressLow: toBn254FieldElement2(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement2(destinationAddressHigh),
        keystreamBlindingFactor: [
          firstKeystreamBlindingFactor,
          secondKeystreamBlindingFactor,
          thirdKeystreamBlindingFactor
        ],
        inverseForKeystreamBlindingFactor: [
          inverseForFirstKeystreamBlindingFactor,
          inverseForSecondKeystreamBlindingFactor,
          inverseForThirdKeystreamBlindingFactor
        ],
        // ================================================================
        // Polynomial Evaluation (10 signals)
        // ================================================================
        randomFactorForPolynomialEvaluationLowLimbs: [
          randomFactorLowAsLimbs.low,
          randomFactorLowAsLimbs.middle,
          randomFactorLowAsLimbs.high
        ],
        inverseForRandomFactorForPolynomialEvaluationLowLimbs,
        randomFactorForPolynomialEvaluationHighLimbs: [
          randomFactorHighAsLimbs.low,
          randomFactorHighAsLimbs.middle,
          randomFactorHighAsLimbs.high
        ],
        inverseForRandomFactorForPolynomialEvaluationHighLimbs,
        challengeLimbs: [
          fiatShamirChallengeLimbs.low,
          fiatShamirChallengeLimbs.middle,
          fiatShamirChallengeLimbs.high
        ],
        inverseForChallengeLimbs,
        challengeSquaredLimbs: [
          fiatShamirChallengeSquaredLimbs.low,
          fiatShamirChallengeSquaredLimbs.middle,
          fiatShamirChallengeSquaredLimbs.high
        ],
        inverseForChallengeSquaredLimbs,
        // ================================================================
        // Rescue Encryption (4 signals)
        // ================================================================
        nonce: toBn254FieldElement2(rcEncryptionNonce),
        arcisPublicKeyLow: toBn254FieldElement2(senderX25519PublicKeyLow),
        arcisPublicKeyHigh: toBn254FieldElement2(senderX25519PublicKeyHigh),
        blindingFactorForRescueCommitment: rescueEncryptionCommitmentBlindingFactor,
        // ================================================================
        // Public Input (1 signal)
        // ================================================================
        publicAggregatedHash: aggregatedPublicInputHash
      };
      const { proofA, proofB, proofC } = await deps.zkProver.prove(zkCircuitInputs);
      assertGroth16ProofA(proofA);
      assertGroth16ProofB(proofB);
      assertGroth16ProofC(proofC);
      const stealthPoolIndex = 0n;
      const stealthPoolAddress = await findStealthPoolPda(
        stealthPoolIndex,
        client.networkConfig.programId
      );
      const arciumAccounts = await findArciumInfrastructurePdas(
        client.networkConfig.programId,
        client.networkConfig.arciumProgramAddress,
        clusterOffset,
        BigInt(computationOffset),
        "deposit_into_stealth_pool_from_shared_balance_v11"
      );
      const sharedSecret = x25519.getSharedSecret(clientX25519PrivateKey, receiverX25519PublicKey);
      const aesEncryptionKeyBytes = keccak_256(sharedSecret).slice(0, 32);
      assertAesKey(aesEncryptionKeyBytes);
      const destinationAddressBytes = new Uint8Array(
        getAddressEncoder().encode(destinationAddress)
      );
      const aesPlaintextData = new Uint8Array(68);
      aesPlaintextData.set(encodeU64ToU64LeBytes(amountAfterFees), 0);
      aesPlaintextData.set(destinationAddressBytes, 8);
      aesPlaintextData.set(modifiedGenerationIndex, 40);
      aesPlaintextData.set(AES_DOMAIN_SEPARATOR_RECEIVER2, 56);
      assertAesPlaintext(aesPlaintextData);
      const aesEncryptedUtxoData = await aesEncryptor(aesEncryptionKeyBytes, aesPlaintextData);
      const optionalDataBytes = options?.optionalData ?? new Uint8Array(32);
      const h2HashBytes = encodeU256ToU256LeBytes(toU2562(h2Hash));
      const rcEncryptionCommitmentBytes = encodeU256ToU256LeBytes(toU2562(rcEncryptionCommitment));
      const polynomialEvaluationBytes = encodeU256ToU256LeBytes(
        toU2562(rescueEncryptionValidationPolynomial)
      );
      const linkerEncryption0Bytes = encodeU256ToU256LeBytes(
        toU2562(pcEncryptedDestinationAddressLow)
      );
      const linkerEncryption1Bytes = encodeU256ToU256LeBytes(
        toU2562(pcEncryptedDestinationAddressHigh)
      );
      const linkerEncryption2Bytes = encodeU256ToU256LeBytes(toU2562(pcEncryptedAmount));
      const keystreamCommitment0Bytes = encodeU256ToU256LeBytes(toU2562(firstPcKeystreamCommitment));
      const keystreamCommitment1Bytes = encodeU256ToU256LeBytes(
        toU2562(secondPcKeystreamCommitment)
      );
      const keystreamCommitment2Bytes = encodeU256ToU256LeBytes(toU2562(thirdPcKeystreamCommitment));
      const utxoAltEntry = lookupAltEntry(
        client.networkConfig.addressLookupTables,
        clusterOffset,
        "deposit_into_stealth_pool_from_shared_balance_v11"
      );
      async function executeTransaction(instruction, instructionName, prefixInstructions, useAlt, callbacks, awaitCallbackOptions) {
        let latestBlockhashData;
        try {
          latestBlockhashData = await getLatestBlockhash();
        } catch (error) {
          throw new RpcError(
            `Failed to fetch latest blockhash: ${error instanceof Error ? error.message : "Unknown error"}`,
            { cause: error }
          );
        }
        const altEntry = useAlt === true ? utxoAltEntry : void 0;
        const allInstructions = prefixInstructions === void 0 ? [instruction] : [...prefixInstructions, instruction];
        const transactionMessage = buildTransactionMessage(
          client.signer,
          latestBlockhashData,
          allInstructions,
          altEntry
        );
        const transaction = compileTransaction(transactionMessage);
        const signedTransaction = await client.signer.signTransaction(transaction);
        if (!isSignedTransaction2(signedTransaction)) {
          throw new TransactionError(`${instructionName}: Transaction was not properly signed`);
        }
        const result2 = await forwardAndMonitor(
          executorConfig,
          signedTransaction,
          callbacks,
          awaitCallbackOptions
        );
        const callbackResult = "callbackResult" in result2 ? result2.callbackResult : void 0;
        const callbackSignature = callbackResult !== void 0 && "callbackSignature" in callbackResult ? callbackResult.callbackSignature : void 0;
        const rentClaimSignature = "rentClaimSignature" in result2 ? result2.rentClaimSignature : void 0;
        const rentClaimError = "rentClaimError" in result2 ? result2.rentClaimError : void 0;
        return {
          signedTransaction: result2.signedTransaction,
          signature: result2.signature,
          callbackStatus: callbackResult?.status,
          callbackSignature,
          callbackElapsedMs: callbackResult?.elapsedMs,
          rentClaimSignature,
          rentClaimError
        };
      }
      __name(executeTransaction, "executeTransaction");
      async function checkAndCloseExistingProofAccount(offset) {
        const proofAccountPda = await findUtxoInputBufferPda(
          client.signer.address,
          offset,
          client.networkConfig.programId
        );
        let accountExists = false;
        try {
          const accountMap = await accountInfoProvider([proofAccountPda], { commitment: accountInfoCommitment });
          const accountInfo = accountMap.get(proofAccountPda);
          accountExists = accountInfo?.exists === true;
        } catch {
          return null;
        }
        if (accountExists) {
          const closeInstruction = await getCloseStealthPoolDepositInputBufferInstructionAsync(
            {
              depositor: createNoopSigner(client.signer.address),
              offset: {
                first: offset
              }
            },
            { programAddress: client.networkConfig.programId }
          );
          return executeTransaction(closeInstruction, "CloseConfidentialUtxoProofAccount");
        }
        return null;
      }
      __name(checkAndCloseExistingProofAccount, "checkAndCloseExistingProofAccount");
      assertU128(proofAccountOffset);
      const closeResult = await checkAndCloseExistingProofAccount(proofAccountOffset);
      if (closeResult !== null) {
        if (factoryCloseProofAccountCallbacks?.pre !== void 0) {
          await factoryCloseProofAccountCallbacks.pre(closeResult.signedTransaction);
        }
        if (factoryCloseProofAccountCallbacks?.post !== void 0) {
          await factoryCloseProofAccountCallbacks.post(
            closeResult.signedTransaction,
            closeResult.signature
          );
        }
      }
      assertU256(rcEncryptedAmount);
      assertU256(rcEncryptedProtocolFees);
      assertU256(rcEncryptedRandomFactorLow);
      assertU256(rcEncryptedRandomFactorHigh);
      const proofAccountInstruction = await getCreateStealthPoolDepositInputBufferInstructionAsync(
        {
          depositor: createNoopSigner(client.signer.address),
          feePayer: createNoopSigner(client.signer.address),
          offset: { first: proofAccountOffset },
          rescueEncryptionPublicKey: {
            first: clientX25519PublicKey
          },
          rescueEncryptionNonce: {
            first: rcEncryptionNonce
          },
          rescueEncryptedTotalAmount: {
            first: encodeU256ToU256LeBytes(rcEncryptedAmount)
          },
          rescueEncryptedProtocolFees: {
            first: encodeU256ToU256LeBytes(rcEncryptedProtocolFees)
          },
          rescueEncryptedRandomFactorLow: {
            first: encodeU256ToU256LeBytes(rcEncryptedRandomFactorLow)
          },
          rescueEncryptedRandomFactorHigh: {
            first: encodeU256ToU256LeBytes(rcEncryptedRandomFactorHigh)
          },
          encryptionValidationPolynomial: {
            first: polynomialEvaluationBytes
          },
          rescueEncryptionFiatShamirCommitment: {
            first: rcEncryptionCommitmentBytes
          },
          insertionH2Commitment: {
            first: h2HashBytes
          },
          insertionTimestamp: {
            first: BigInt(currentTimestamp)
          },
          linkerEncryption0: {
            first: linkerEncryption0Bytes
          },
          linkerEncryption1: {
            first: linkerEncryption1Bytes
          },
          linkerEncryption2: {
            first: linkerEncryption2Bytes
          },
          keystreamCommitment0: {
            first: keystreamCommitment0Bytes
          },
          keystreamCommitment1: {
            first: keystreamCommitment1Bytes
          },
          keystreamCommitment2: {
            first: keystreamCommitment2Bytes
          },
          groth16ProofA: { first: proofA },
          groth16ProofB: { first: proofB },
          groth16ProofC: { first: proofC },
          aesEncryptedData: {
            first: aesEncryptedUtxoData
          },
          optionalData: {
            first: optionalDataBytes
          }
        },
        { programAddress: client.networkConfig.programId }
      );
      const proofAccountResult = await executeTransaction(
        proofAccountInstruction,
        "CreateConfidentialUtxoProofAccount",
        void 0,
        void 0,
        factoryCreateProofAccountCallbacks
      );
      const utxoInstruction = await getDepositIntoStealthPoolFromSharedBalanceV11InstructionAsync(
        {
          depositor: createNoopSigner(client.signer.address),
          feePayer: createNoopSigner(client.signer.address),
          mxeAccount: arciumAccounts.mxeAccount,
          mempoolAccount: arciumAccounts.mempoolAccount,
          executingPool: arciumAccounts.executingPoolAccount,
          computationAccount: arciumAccounts.computationAccount,
          compDefAccount: arciumAccounts.compDefAccount,
          clusterAccount: arciumAccounts.clusterAccount,
          stealthPool: stealthPoolAddress,
          mint,
          computationOffset: {
            first: computationOffset
          },
          mpcCallbackDataOffset: {
            first: generateRandomU128()
          },
          feeVaultOffset: {
            first: 0n
          },
          stealthPoolDepositInputBufferOffset: {
            first: proofAccountOffset
          },
          priorityFees: { first: microLamportsPerAcu }
        },
        { programAddress: client.networkConfig.programId }
      );
      const computeBudgetInstruction = createSetComputeUnitLimitInstruction(6e5);
      const resolvedAwaitConfig = factoryAwaitComputationFinalization === false ? false : factoryAwaitComputationFinalization ?? {};
      const utxoCallbackOptions = resolvedAwaitConfig === false ? void 0 : {
        ...resolvedAwaitConfig,
        retrieveCallbackSignature: true,
        computationAccountAddress: arciumAccounts.computationAccount,
        computationOffset: BigInt(computationOffset),
        clusterOffset
      };
      const utxoResult = await executeTransaction(
        utxoInstruction,
        "DepositIntoStealthPoolFromSharedBalanceV11",
        [computeBudgetInstruction],
        true,
        factoryCreateUtxoCallbacks,
        utxoCallbackOptions
      );
      const result = {
        closeProofAccountSignature: closeResult?.signature,
        createProofAccountSignature: proofAccountResult.signature,
        queueSignature: utxoResult.signature,
        callbackStatus: utxoResult.callbackStatus,
        callbackSignature: utxoResult.callbackSignature,
        callbackElapsedMs: utxoResult.callbackElapsedMs,
        rentClaimSignature: utxoResult.rentClaimSignature,
        rentClaimError: utxoResult.rentClaimError
      };
      return result;
    } catch (error) {
      if (error instanceof CreateUtxoError) {
        throw error;
      }
      if (error instanceof TransactionSigningError) {
        throw new CreateUtxoError("transaction-sign", error.message, error);
      }
      if (error instanceof CryptographyError) {
        throw new CreateUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof TransactionError) {
        throw new CreateUtxoError("transaction-send", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new CreateUtxoError("account-fetch", error.message, error);
      }
      throw new CreateUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  }, "createReceiverClaimableUtxo");
}
__name(getEncryptedBalanceToReceiverClaimableUtxoCreatorFunction, "getEncryptedBalanceToReceiverClaimableUtxoCreatorFunction");
var AES_DOMAIN_SEPARATOR = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateSelfClaimableUtxoFromPublicBalance")
).slice(0, 12);
var DOMAIN_MODIFIED_GEN_INDEX3 = "PublicBalanceToSelfClaimableUtxoCreatorFunction / modifiedGenerationIndex";
function toBn254FieldElement3(value) {
  assertBn254FieldElement(value);
  return value;
}
__name(toBn254FieldElement3, "toBn254FieldElement");
function toU2563(value) {
  assertU256(value);
  return value;
}
__name(toU2563, "toU256");
function isSignedTransaction3(value) {
  return typeof value === "object" && value !== null && "messageBytes" in value && "signatures" in value && typeof value.signatures === "object" && value.signatures !== null && Object.keys(value.signatures).length > 0;
}
__name(isSignedTransaction3, "isSignedTransaction");
function getPublicBalanceToSelfClaimableUtxoCreatorFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps.rpc?.transactionForwarder ?? client.transactionForwarder;
  const factoryCreateUtxoCallbacks = deps.hooks?.createUtxo;
  const getEpochInfo = deps.rpc?.epochInfo ?? client.epochInfoProvider;
  const masterViewingKeyGenerator = deps.keys?.masterViewingKeyGenerator ?? getMasterViewingKeyDeriver({ client });
  const masterViewingKeyBlindingFactorGenerator = deps.keys?.masterViewingKeyBlindingFactorGenerator ?? getMasterViewingKeyBlindingFactorDeriver({ client });
  const poseidonPrivateKeyGenerator = deps.keys?.poseidonPrivateKeyGenerator ?? getPoseidonPrivateKeyDeriver({ client });
  const poseidonBlindingFactorGenerator = deps.keys?.poseidonBlindingFactorGenerator ?? getPoseidonBlindingFactorDeriver({ client });
  const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const secondViewingKeyGenerator = deps.keys?.secondViewingKeyGenerator ?? getSecondViewingKeyDeriver({ client });
  const ephemeralUtxoMvkGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyDeriver ?? getEphemeralUtxoMasterViewingKeyDeriver({ client });
  const ephemeralUtxoMvkBlindingFactorGenerator = deps.keys?.ephemeralUtxoMasterViewingKeyBlindingFactorDeriver ?? getEphemeralUtxoMasterViewingKeyBlindingFactorDeriver({ client });
  const ephemeralUtxoPoseidonKeyGenerator = deps.keys?.ephemeralUtxoPoseidonKeyGenerator ?? getEphemeralUtxoPoseidonPrivateKeyDeriver({ client });
  const ephemeralUtxoPoseidonKeyBlindingFactorGenerator = deps.keys?.ephemeralUtxoPoseidonKeyBlindingFactorGenerator ?? getEphemeralUtxoPoseidonPrivateKeyBlindingFactorDeriver({ client });
  const poseidonKeystreamBlindingFactorGenerator = deps.keys?.poseidonKeystreamBlindingFactorGenerator ?? getPoseidonKeystreamBlindingFactorDeriver({ client });
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGenerator = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const h2Generator = deps.crypto?.h2Generator ?? getUtxoCommitmentHashGenerator();
  const keystreamCommitmentGenerator = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const userCommitmentGenerator = getUserCommitmentGeneratorFunction();
  const poseidonAggregator = getPoseidonAggregator();
  const aesEncryptor = deps.crypto?.aesEncryptor ?? getAesEncryptor();
  const generateRandomU256Function = deps.random?.generateRandomU256 ?? generateRandomU256;
  return /* @__PURE__ */ __name(async function createSelfClaimableUtxoFromPublicBalance(args2, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const epochInfoCommitment = options?.epochInfoCommitment ?? "confirmed";
    try {
      const { amount, destinationAddress, mint } = args2;
      const generationIndex = options?.generationIndex;
      assertU64(amount);
      if (generationIndex !== void 0) {
        assertU256(generationIndex);
      }
      const effectiveGenerationIndex = generationIndex ?? generateRandomU256Function();
      const masterSeed = await client.masterSeed.getMasterSeed();
      if (!(masterSeed instanceof Uint8Array) || masterSeed.length === 0) {
        throw new CryptographyError("Failed to retrieve master seed from client");
      }
      const generationIndexBytes = new TextEncoder().encode(effectiveGenerationIndex.toString());
      const modifiedGenerationIndex = kmac256(
        new TextEncoder().encode(DOMAIN_MODIFIED_GEN_INDEX3),
        masterSeed,
        { dkLen: 16, personalization: generationIndexBytes }
      );
      const proofAccountOffsetBytes = deriveProofAccountOffsetFromModifiedGenerationIndex(modifiedGenerationIndex);
      let proofAccountOffset = 0n;
      for (let index = 0; index < 16; index++) {
        proofAccountOffset |= BigInt(proofAccountOffsetBytes[index]) << BigInt(index * 8);
      }
      const expandedModifiedGenerationIndex = expandModifiedGenerationIndex(modifiedGenerationIndex);
      let expandedModifiedGenerationIndexU256 = 0n;
      for (let index = 0; index < 32; index++) {
        expandedModifiedGenerationIndexU256 |= BigInt(expandedModifiedGenerationIndex[index]) << BigInt(index * 8);
      }
      let actualReceived;
      let mintTokenProgram;
      const mintAccountMap = await accountInfoProvider([
        mint,
        client.networkConfig.mxeAccountAddress
      ], { commitment: accountInfoCommitment });
      const mintAccount = mintAccountMap.get(mint);
      if (mintAccount?.exists !== true) {
        throw new TransactionError(`Mint account not found: ${mint}`);
      }
      const clusterOffset = extractClusterOffsetFromMxeAccount(
        mintAccountMap.get(client.networkConfig.mxeAccountAddress)
      );
      const mintOwner = mintAccount.programAddress;
      if (mintOwner === TOKEN_2022_PROGRAM_ID) {
        const mintData = new Uint8Array(mintAccount.data);
        const feeConfig = extractTransferFeeConfig(mintData);
        if (feeConfig === null) {
          actualReceived = amount;
        } else {
          const epochInfo = await getEpochInfo({ commitment: epochInfoCommitment });
          const transferFee = calculateTransferFee(feeConfig, epochInfo.epoch, amount);
          actualReceived = amount - transferFee;
          if (actualReceived <= 0n) {
            throw new TransactionError(
              `Transfer fee (${String(transferFee)}) exceeds or equals transfer amount (${String(amount)}).`
            );
          }
        }
        mintTokenProgram = TOKEN_2022_PROGRAM_ID;
      } else if (mintOwner === SPL_TOKEN_PROGRAM_ID) {
        actualReceived = amount;
        mintTokenProgram = SPL_TOKEN_PROGRAM_ID;
      } else {
        throw new TransactionError(
          `Unknown token program: ${mintOwner}. Expected SPL Token or Token-2022.`
        );
      }
      const protocolFeeProvider = getHardcodedCreateUtxoProtocolFeeProvider();
      const protocolFeeConfig = await protocolFeeProvider(actualReceived);
      const baseFeesInSpl = protocolFeeConfig.slab.baseFee;
      const commissionFeeInBps = protocolFeeConfig.slab.bps;
      const amountAfterBaseFeeForBps = actualReceived - baseFeesInSpl;
      const commissionFee = amountAfterBaseFeeForBps * commissionFeeInBps / 16384n;
      const totalProtocolFees = baseFeesInSpl + commissionFee;
      const netDepositAmount = actualReceived - totalProtocolFees;
      if (netDepositAmount <= 0n) {
        throw new TransactionError(
          `Net deposit amount is zero or negative after fees. Transfer amount: ${String(amount)}, actual received: ${String(actualReceived)}, protocol fees: ${String(totalProtocolFees)}.`
        );
      }
      assertU64(netDepositAmount);
      let currentTimestamp;
      let timestampComponents;
      if (deps.random?.getUtcNow === void 0) {
        const now = /* @__PURE__ */ new Date();
        currentTimestamp = Math.floor(now.getTime() / 1e3);
        timestampComponents = extractUtcComponents(now);
      } else {
        timestampComponents = deps.random.getUtcNow();
        const { year: year2, month: month2, day: day2, hour: hour2, minute: minute2, second: second2 } = timestampComponents;
        const dateFromComponents = new Date(
          Date.UTC(
            Number(year2),
            Number(month2) - 1,
            Number(day2),
            Number(hour2),
            Number(minute2),
            Number(second2)
          )
        );
        currentTimestamp = Math.floor(dateFromComponents.getTime() / 1e3);
      }
      const { year, month, day, hour, minute, second } = timestampComponents;
      const transactionViewingKey = await secondViewingKeyGenerator(
        mint,
        year,
        month,
        day,
        hour,
        minute,
        second
      );
      assertBn254FieldElement(transactionViewingKey);
      const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
      const senderMasterViewingKey = await masterViewingKeyGenerator();
      assertBn254FieldElement(senderMasterViewingKey);
      const senderMvkBlindingFactor = await masterViewingKeyBlindingFactorGenerator();
      assertBn254FieldElement(senderMvkBlindingFactor);
      const senderPrivateKey = await poseidonPrivateKeyGenerator();
      assertBn254FieldElement(senderPrivateKey);
      const senderPrivateKeyBlindingFactor = await poseidonBlindingFactorGenerator();
      assertBn254FieldElement(senderPrivateKeyBlindingFactor);
      const senderUserCommitment = await userCommitmentGenerator(
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        senderPrivateKey,
        senderPrivateKeyBlindingFactor
      );
      assertBn254FieldElement(senderUserCommitment);
      const ephemeralOffset = expandedModifiedGenerationIndexU256;
      const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(randomSecret);
      const nullifier = deriveNullifierFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(nullifier);
      const ephemeralMasterViewingKey = await ephemeralUtxoMvkGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralMasterViewingKey);
      const ephemeralMasterViewingKeyBlindingFactor = await ephemeralUtxoMvkBlindingFactorGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralMasterViewingKeyBlindingFactor);
      const ephemeralPoseidonPrivateKey = await ephemeralUtxoPoseidonKeyGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralPoseidonPrivateKey);
      const ephemeralPoseidonPrivateKeyBlindingFactor = await ephemeralUtxoPoseidonKeyBlindingFactorGenerator(ephemeralOffset);
      assertBn254FieldElement(ephemeralPoseidonPrivateKeyBlindingFactor);
      const ephemeralUserCommitment = await userCommitmentGenerator(
        ephemeralMasterViewingKey,
        ephemeralMasterViewingKeyBlindingFactor,
        ephemeralPoseidonPrivateKey,
        ephemeralPoseidonPrivateKeyBlindingFactor
      );
      assertBn254FieldElement(ephemeralUserCommitment);
      const h2Hash = await h2Generator.generateH2({
        amount: toBn254FieldElement3(netDepositAmount),
        nullifier,
        userCommitment: ephemeralUserCommitment,
        finalDestinationAddressLow: toBn254FieldElement3(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement3(destinationAddressHigh),
        h2BlindingFactor: randomSecret
      });
      assertBn254FieldElement(h2Hash);
      assertPoseidonPlaintext(destinationAddressLow);
      assertPoseidonPlaintext(destinationAddressHigh);
      const poseidonKey = BigInt(transactionViewingKey);
      assertPoseidonKey(poseidonKey);
      const pcCiphertexts = await poseidonEncryptor(
        [destinationAddressLow, destinationAddressHigh],
        poseidonKey
      );
      if (pcCiphertexts.length !== 2) {
        throw new CryptographyError(
          `Expected 2 ciphertexts but got ${String(pcCiphertexts.length)}`
        );
      }
      const pcEncryptedDestinationAddressLow = pcCiphertexts[0];
      const pcEncryptedDestinationAddressHigh = pcCiphertexts[1];
      const pcKeystreams = await poseidonKeystreamGenerator(
        [0n, 1n],
        poseidonKey
      );
      const pcKeyForDestinationAddressLow = pcKeystreams.get(0n);
      if (pcKeyForDestinationAddressLow === void 0) {
        throw new CryptographyError("Missing keystream for counter 0");
      }
      const pcKeyForDestinationAddressHigh = pcKeystreams.get(1n);
      if (pcKeyForDestinationAddressHigh === void 0) {
        throw new CryptographyError("Missing keystream for counter 1");
      }
      const firstKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressLow,
        0n
      );
      const secondKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressHigh,
        1n
      );
      const firstPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressLow,
        firstKeystreamBlindingFactor
      );
      const secondPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressHigh,
        secondKeystreamBlindingFactor
      );
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      assertX25519PrivateKey(clientX25519PrivateKey);
      const clientX25519PublicKey = keypairResult.x25519Keypair.publicKey;
      const aesSharedSecret = x25519.getSharedSecret(clientX25519PrivateKey, clientX25519PublicKey);
      const aesEncryptionKeyBytes = keccak_256(aesSharedSecret).slice(0, 32);
      assertAesKey(aesEncryptionKeyBytes);
      const destinationAddressBytes = new Uint8Array(
        getAddressEncoder().encode(destinationAddress)
      );
      const aesPlaintextData = new Uint8Array(68);
      aesPlaintextData.set(encodeU64ToU64LeBytes(netDepositAmount), 0);
      aesPlaintextData.set(destinationAddressBytes, 8);
      aesPlaintextData.set(modifiedGenerationIndex, 40);
      aesPlaintextData.set(AES_DOMAIN_SEPARATOR, 56);
      assertAesPlaintext(aesPlaintextData);
      const aesEncryptedUtxoData = await aesEncryptor(aesEncryptionKeyBytes, aesPlaintextData);
      const optionalDataBytes = options?.optionalData ?? new Uint8Array(32);
      const { low: mintAddressLow, high: mintAddressHigh } = splitAddressToLowHigh(mint);
      const aggregatedHashInputs = [
        // [0-1] Mint address
        toBn254FieldElement3(mintAddressLow),
        toBn254FieldElement3(mintAddressHigh),
        // [2] Amount (net deposit amount, matching on-chain)
        toBn254FieldElement3(netDepositAmount),
        // [3-8] Timestamp components
        toBn254FieldElement3(BigInt(year)),
        toBn254FieldElement3(BigInt(month)),
        toBn254FieldElement3(BigInt(day)),
        toBn254FieldElement3(BigInt(hour)),
        toBn254FieldElement3(BigInt(minute)),
        toBn254FieldElement3(BigInt(second)),
        // [9] H2 hash
        h2Hash,
        // [10] Sender user commitment
        senderUserCommitment,
        // [11-12] Linker encryptions (encrypted destination address)
        toBn254FieldElement3(pcEncryptedDestinationAddressLow),
        toBn254FieldElement3(pcEncryptedDestinationAddressHigh),
        // [13-14] Keystream commitments
        firstPcKeystreamCommitment,
        secondPcKeystreamCommitment,
        // [15] Protocol fees root
        toBn254FieldElement3(protocolFeeConfig.merkleRoot)
      ];
      const aggregatedPublicInputHash = await poseidonAggregator(aggregatedHashInputs);
      const inverseForSenderMvkBlindingFactor = computeBn254ModularInverse(senderMvkBlindingFactor);
      const inverseForSenderPrivateKeyBlindingFactor = computeBn254ModularInverse(
        senderPrivateKeyBlindingFactor
      );
      const inverseForSenderPrivateKey = computeBn254ModularInverse(senderPrivateKey);
      const inverseForSenderMasterViewingKey = computeBn254ModularInverse(senderMasterViewingKey);
      const inverseForAmount = computeBn254ModularInverse(toBn254FieldElement3(netDepositAmount));
      const inverseForNullifier = computeBn254ModularInverse(nullifier);
      const inverseForH2BlindingFactor = computeBn254ModularInverse(randomSecret);
      const inverseForFirstKeystreamBlindingFactor = computeBn254ModularInverse(
        firstKeystreamBlindingFactor
      );
      const inverseForSecondKeystreamBlindingFactor = computeBn254ModularInverse(
        secondKeystreamBlindingFactor
      );
      const zkCircuitInputs = {
        // Sender Commitment (8 signals)
        senderPrivateKey,
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        inverseForSenderMvkBlindingFactor,
        senderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKey,
        inverseForSenderMasterViewingKey,
        // Amount (2 signals)
        amount: toBn254FieldElement3(netDepositAmount),
        inverseForAmount,
        // H2 Hash (6 signals)
        nullifier,
        inverseForNullifier,
        unlockingUserCommitment: ephemeralUserCommitment,
        h2BlindingFactor: randomSecret,
        inverseForH2BlindingFactor,
        // Timestamp (6 signals)
        year: toBn254FieldElement3(BigInt(year)),
        month: toBn254FieldElement3(BigInt(month)),
        day: toBn254FieldElement3(BigInt(day)),
        hour: toBn254FieldElement3(BigInt(hour)),
        minute: toBn254FieldElement3(BigInt(minute)),
        second: toBn254FieldElement3(BigInt(second)),
        // Mint Address (2 signals)
        mintAddressLow: toBn254FieldElement3(mintAddressLow),
        mintAddressHigh: toBn254FieldElement3(mintAddressHigh),
        // Encryption & Keystream (6 signals)
        finalDestinationAddressLow: toBn254FieldElement3(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement3(destinationAddressHigh),
        keystreamBlindingFactor: [firstKeystreamBlindingFactor, secondKeystreamBlindingFactor],
        inverseForKeystreamBlindingFactor: [
          inverseForFirstKeystreamBlindingFactor,
          inverseForSecondKeystreamBlindingFactor
        ],
        // Protocol Fees (1 signal)
        protocolFeesRoot: toBn254FieldElement3(protocolFeeConfig.merkleRoot),
        // Public Input (1 signal)
        publicAggregatedHash: aggregatedPublicInputHash
      };
      const { proofA, proofB, proofC } = await deps.zkProver.prove(zkCircuitInputs);
      assertGroth16ProofA(proofA);
      assertGroth16ProofB(proofB);
      assertGroth16ProofC(proofC);
      const stealthPoolIndex = 0n;
      const stealthPoolAddress = await findStealthPoolPda(
        stealthPoolIndex,
        client.networkConfig.programId
      );
      const h2HashBytes = encodeU256ToU256LeBytes(toU2563(h2Hash));
      const linkerEncryption0Bytes = encodeU256ToU256LeBytes(
        toU2563(pcEncryptedDestinationAddressLow)
      );
      const linkerEncryption1Bytes = encodeU256ToU256LeBytes(
        toU2563(pcEncryptedDestinationAddressHigh)
      );
      const keystreamCommitment0Bytes = encodeU256ToU256LeBytes(toU2563(firstPcKeystreamCommitment));
      const keystreamCommitment1Bytes = encodeU256ToU256LeBytes(
        toU2563(secondPcKeystreamCommitment)
      );
      async function buildAndSendTransaction(instructions, instructionName, useAlt) {
        let latestBlockhashData;
        try {
          latestBlockhashData = await getLatestBlockhash();
        } catch (error) {
          throw new RpcError(
            `Failed to fetch latest blockhash: ${error instanceof Error ? error.message : "Unknown error"}`,
            { cause: error }
          );
        }
        const altEntry = useAlt === true ? lookupAltEntry(
          client.networkConfig.addressLookupTables,
          clusterOffset,
          "create_deposit_into_mixer_tree_from_public_balance"
        ) : void 0;
        const baseTransactionMessage = pipe(
          createTransactionMessage({ version: 0 }),
          (m) => setTransactionMessageFeePayer(client.signer.address, m),
          (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhashData, m),
          (m) => appendTransactionMessageInstructions(instructions, m)
        );
        const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
          baseTransactionMessage,
          buildAltAddressesRecord(altEntry)
        );
        const transaction = compileTransaction(transactionMessage);
        const signed = await client.signer.signTransaction(transaction);
        if (!isSignedTransaction3(signed)) {
          throw new TransactionError(
            `${instructionName}: Transaction was not properly signed`
          );
        }
        const signature = getSignatureFromTransaction(signed);
        try {
          await transactionForwarder.forwardSequentially([signed]);
        } catch (error) {
          throw new TransactionError(
            `${instructionName}: Failed to send transaction: ${error instanceof Error ? error.message : "Unknown error"}`,
            { cause: error }
          );
        }
        return { signedTransaction: signed, signature };
      }
      __name(buildAndSendTransaction, "buildAndSendTransaction");
      assertU128(proofAccountOffset);
      let closeProofAccountSignature;
      const proofAccountPda = await findPublicUtxoInputBufferPda(
        client.signer.address,
        proofAccountOffset,
        client.networkConfig.programId
      );
      let proofAccountExists = false;
      try {
        const accountMap = await accountInfoProvider([proofAccountPda], { commitment: accountInfoCommitment });
        const accountInfo = accountMap.get(proofAccountPda);
        proofAccountExists = accountInfo?.exists === true;
      } catch {
      }
      if (proofAccountExists) {
        const closeInstruction = await getClosePublicStealthPoolDepositInputBufferInstructionAsync(
          {
            depositor: createNoopSigner(client.signer.address),
            offset: { first: proofAccountOffset }
          },
          { programAddress: client.networkConfig.programId }
        );
        const closeResult = await buildAndSendTransaction(
          [closeInstruction],
          "ClosePublicUtxoProofAccount"
        );
        closeProofAccountSignature = closeResult.signature;
      }
      const proofAccountInstruction = await getCreatePublicStealthPoolDepositInputBufferInstructionAsync(
        {
          depositor: createNoopSigner(client.signer.address),
          feePayer: createNoopSigner(client.signer.address),
          offset: { first: proofAccountOffset },
          insertionH2Commitment: { first: h2HashBytes },
          insertionTimestamp: { first: BigInt(currentTimestamp) },
          linkerEncryption0: { first: linkerEncryption0Bytes },
          linkerEncryption1: { first: linkerEncryption1Bytes },
          keystreamCommitment0: { first: keystreamCommitment0Bytes },
          keystreamCommitment1: { first: keystreamCommitment1Bytes },
          groth16ProofA: { first: proofA },
          groth16ProofB: { first: proofB },
          groth16ProofC: { first: proofC },
          aesEncryptedData: { first: aesEncryptedUtxoData },
          optionalData: { first: optionalDataBytes }
        },
        { programAddress: client.networkConfig.programId }
      );
      const proofAccountResult = await buildAndSendTransaction(
        [proofAccountInstruction],
        "CreatePublicUtxoProofAccount"
      );
      const depositInstruction = await getDepositIntoStealthPoolFromPublicBalanceInstructionAsync(
        {
          feePayer: createNoopSigner(client.signer.address),
          depositor: createNoopSigner(client.signer.address),
          stealthPool: stealthPoolAddress,
          mint,
          tokenProgram: mintTokenProgram,
          feeVaultOffset: { first: 0n },
          publicStealthPoolDepositInputBufferOffset: { first: proofAccountOffset },
          ...feeSlabResultToInstructionFields(protocolFeeConfig),
          transferAmount: { first: amount }
        },
        { programAddress: client.networkConfig.programId }
      );
      const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
      if (factoryCreateUtxoCallbacks?.pre !== void 0) {
        await factoryCreateUtxoCallbacks.pre(proofAccountResult.signedTransaction);
      }
      const depositResult = await buildAndSendTransaction(
        [computeBudgetInstruction, depositInstruction],
        "CreateDepositIntoMixerTreeFromPublicBalance",
        true
      );
      if (factoryCreateUtxoCallbacks?.post !== void 0) {
        await factoryCreateUtxoCallbacks.post(
          depositResult.signedTransaction,
          depositResult.signature
        );
      }
      void ephemeralMasterViewingKey;
      void ephemeralMasterViewingKeyBlindingFactor;
      void ephemeralPoseidonPrivateKey;
      void ephemeralPoseidonPrivateKeyBlindingFactor;
      return {
        closeProofAccountSignature,
        createProofAccountSignature: proofAccountResult.signature,
        createUtxoSignature: depositResult.signature
      };
    } catch (error) {
      if (error instanceof CreateUtxoError) {
        throw error;
      }
      if (error instanceof TransactionSigningError) {
        throw new CreateUtxoError("transaction-sign", error.message, error);
      }
      if (error instanceof CryptographyError) {
        throw new CreateUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof TransactionError) {
        throw new CreateUtxoError("transaction-send", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new CreateUtxoError("account-fetch", error.message, error);
      }
      throw new CreateUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  }, "createSelfClaimableUtxoFromPublicBalance");
}
__name(getPublicBalanceToSelfClaimableUtxoCreatorFunction, "getPublicBalanceToSelfClaimableUtxoCreatorFunction");
var AES_DOMAIN_SEPARATOR2 = keccak_256(
  new TextEncoder().encode("UmbraPrivacy / CreateReceiverClaimableUtxoFromPublicBalance")
).slice(0, 12);
var DOMAIN_MODIFIED_GEN_INDEX4 = "PublicBalanceToReceiverClaimableUtxoCreatorFunction / modifiedGenerationIndex";
function toBn254FieldElement4(value) {
  assertBn254FieldElement(value);
  return value;
}
__name(toBn254FieldElement4, "toBn254FieldElement");
function toU2564(value) {
  assertU256(value);
  return value;
}
__name(toU2564, "toU256");
function isSignedTransaction4(value) {
  return typeof value === "object" && value !== null && "messageBytes" in value && "signatures" in value && typeof value.signatures === "object" && value.signatures !== null && Object.keys(value.signatures).length > 0;
}
__name(isSignedTransaction4, "isSignedTransaction");
function getPublicBalanceToReceiverClaimableUtxoCreatorFunction(args, deps) {
  const { client } = args;
  const accountInfoProvider = deps.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps.rpc?.transactionForwarder ?? client.transactionForwarder;
  const factoryCreateUtxoCallbacks = deps.hooks?.createUtxo;
  const getEpochInfo = deps.rpc?.epochInfo ?? client.epochInfoProvider;
  const masterViewingKeyGenerator = deps.keys?.masterViewingKeyGenerator ?? getMasterViewingKeyDeriver({ client });
  const masterViewingKeyBlindingFactorGenerator = deps.keys?.masterViewingKeyBlindingFactorGenerator ?? getMasterViewingKeyBlindingFactorDeriver({ client });
  const poseidonPrivateKeyGenerator = deps.keys?.poseidonPrivateKeyGenerator ?? getPoseidonPrivateKeyDeriver({ client });
  const poseidonBlindingFactorGenerator = deps.keys?.poseidonBlindingFactorGenerator ?? getPoseidonBlindingFactorDeriver({ client });
  const mvkX25519KeypairDeriver = deps.keys?.mvkX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const secondViewingKeyGenerator = deps.keys?.secondViewingKeyGenerator ?? getSecondViewingKeyDeriver({ client });
  const poseidonKeystreamBlindingFactorGenerator = deps.keys?.poseidonKeystreamBlindingFactorGenerator ?? getPoseidonKeystreamBlindingFactorDeriver({ client });
  const poseidonEncryptor = deps.crypto?.poseidonEncryptor ?? getPoseidonEncryptor();
  const poseidonKeystreamGenerator = deps.crypto?.poseidonKeystreamGenerator ?? getPoseidonKeystreamGenerator();
  const h2Generator = deps.crypto?.h2Generator ?? getUtxoCommitmentHashGenerator();
  const keystreamCommitmentGenerator = deps.crypto?.keystreamCommitmentGenerator ?? getKeystreamCommitmentGenerator();
  const userCommitmentGenerator = getUserCommitmentGeneratorFunction();
  const poseidonAggregator = getPoseidonAggregator();
  const aesEncryptor = deps.crypto?.aesEncryptor ?? getAesEncryptor();
  const generateRandomU256Function = deps.random?.generateRandomU256 ?? generateRandomU256;
  return /* @__PURE__ */ __name(async function createReceiverClaimableUtxoFromPublicBalance(args2, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const epochInfoCommitment = options?.epochInfoCommitment ?? "confirmed";
    try {
      const { amount, destinationAddress, mint } = args2;
      const generationIndex = options?.generationIndex;
      assertU64(amount);
      if (generationIndex !== void 0) {
        assertU256(generationIndex);
      }
      const effectiveGenerationIndex = generationIndex ?? generateRandomU256Function();
      const masterSeed = await client.masterSeed.getMasterSeed();
      if (!(masterSeed instanceof Uint8Array) || masterSeed.length === 0) {
        throw new CryptographyError("Failed to retrieve master seed from client");
      }
      const generationIndexBytes = new TextEncoder().encode(effectiveGenerationIndex.toString());
      const modifiedGenerationIndex = kmac256(
        new TextEncoder().encode(DOMAIN_MODIFIED_GEN_INDEX4),
        masterSeed,
        { dkLen: 16, personalization: generationIndexBytes }
      );
      const proofAccountOffsetBytes = deriveProofAccountOffsetFromModifiedGenerationIndex(modifiedGenerationIndex);
      let proofAccountOffset = 0n;
      for (let index = 0; index < 16; index++) {
        proofAccountOffset |= BigInt(proofAccountOffsetBytes[index]) << BigInt(index * 8);
      }
      const receiverUserAccountPda = await findEncryptedUserAccountPda(
        destinationAddress,
        client.networkConfig.programId
      );
      const receiverAccountMap = await accountInfoProvider([
        receiverUserAccountPda,
        client.networkConfig.mxeAccountAddress
      ], { commitment: accountInfoCommitment });
      const receiverAccountInfo = receiverAccountMap.get(receiverUserAccountPda);
      if (receiverAccountInfo?.exists !== true) {
        throw new CryptographyError(`Receiver is not registered: ${destinationAddress}`);
      }
      const receiverAccount = decodeEncryptedUserAccount(receiverAccountInfo);
      const receiverAccountData = receiverAccount.data;
      const clusterOffset = extractClusterOffsetFromMxeAccount(
        receiverAccountMap.get(client.networkConfig.mxeAccountAddress)
      );
      const receiverX25519PublicKeyBytes = receiverAccountData.x25519PublicKeyForTokenEncryption?.first;
      if (receiverX25519PublicKeyBytes === void 0) {
        throw new CryptographyError("Receiver does not have X25519 public key registered");
      }
      const receiverX25519PublicKey = new Uint8Array(receiverX25519PublicKeyBytes);
      assertX25519PublicKey(receiverX25519PublicKey);
      const receiverUserCommitmentBytes = receiverAccountData.userCommitment?.first;
      if (receiverUserCommitmentBytes === void 0) {
        throw new CryptographyError("Receiver does not have user commitment registered");
      }
      const receiverUserCommitmentLeBytes = new Uint8Array(receiverUserCommitmentBytes);
      assertU256LeBytes(receiverUserCommitmentLeBytes);
      const receiverUserCommitment = decodeU256LeBytesToU256(receiverUserCommitmentLeBytes);
      assertBn254FieldElement(receiverUserCommitment);
      let actualReceived;
      let mintTokenProgram;
      const mintAccountMap = await accountInfoProvider([mint], { commitment: accountInfoCommitment });
      const mintAccount = mintAccountMap.get(mint);
      if (mintAccount?.exists !== true) {
        throw new TransactionError(`Mint account not found: ${mint}`);
      }
      const mintOwner = mintAccount.programAddress;
      if (mintOwner === TOKEN_2022_PROGRAM_ID) {
        const mintData = new Uint8Array(mintAccount.data);
        const feeConfig = extractTransferFeeConfig(mintData);
        if (feeConfig === null) {
          actualReceived = amount;
        } else {
          const epochInfo = await getEpochInfo({ commitment: epochInfoCommitment });
          const transferFee = calculateTransferFee(feeConfig, epochInfo.epoch, amount);
          actualReceived = amount - transferFee;
          if (actualReceived <= 0n) {
            throw new TransactionError(
              `Transfer fee (${String(transferFee)}) exceeds or equals transfer amount (${String(amount)}).`
            );
          }
        }
        mintTokenProgram = TOKEN_2022_PROGRAM_ID;
      } else if (mintOwner === SPL_TOKEN_PROGRAM_ID) {
        actualReceived = amount;
        mintTokenProgram = SPL_TOKEN_PROGRAM_ID;
      } else {
        throw new TransactionError(
          `Unknown token program: ${mintOwner}. Expected SPL Token or Token-2022.`
        );
      }
      const protocolFeeProvider = getHardcodedCreateUtxoProtocolFeeProvider();
      const protocolFeeConfig = await protocolFeeProvider(actualReceived);
      const baseFeesInSpl = protocolFeeConfig.slab.baseFee;
      const commissionFeeInBps = protocolFeeConfig.slab.bps;
      const amountAfterBaseFeeForBps = actualReceived - baseFeesInSpl;
      const commissionFee = amountAfterBaseFeeForBps * commissionFeeInBps / 16384n;
      const totalProtocolFees = baseFeesInSpl + commissionFee;
      const netDepositAmount = actualReceived - totalProtocolFees;
      if (netDepositAmount <= 0n) {
        throw new TransactionError(
          `Net deposit amount is zero or negative after fees. Transfer amount: ${String(amount)}, actual received: ${String(actualReceived)}, protocol fees: ${String(totalProtocolFees)}.`
        );
      }
      assertU64(netDepositAmount);
      let currentTimestamp;
      let timestampComponents;
      if (deps.random?.getUtcNow === void 0) {
        const now = /* @__PURE__ */ new Date();
        currentTimestamp = Math.floor(now.getTime() / 1e3);
        timestampComponents = extractUtcComponents(now);
      } else {
        timestampComponents = deps.random.getUtcNow();
        const { year: year2, month: month2, day: day2, hour: hour2, minute: minute2, second: second2 } = timestampComponents;
        const dateFromComponents = new Date(
          Date.UTC(
            Number(year2),
            Number(month2) - 1,
            Number(day2),
            Number(hour2),
            Number(minute2),
            Number(second2)
          )
        );
        currentTimestamp = Math.floor(dateFromComponents.getTime() / 1e3);
      }
      const { year, month, day, hour, minute, second } = timestampComponents;
      const transactionViewingKey = await secondViewingKeyGenerator(
        mint,
        year,
        month,
        day,
        hour,
        minute,
        second
      );
      assertBn254FieldElement(transactionViewingKey);
      const { low: destinationAddressLow, high: destinationAddressHigh } = splitAddressToLowHigh(destinationAddress);
      const senderMasterViewingKey = await masterViewingKeyGenerator();
      assertBn254FieldElement(senderMasterViewingKey);
      const senderMvkBlindingFactor = await masterViewingKeyBlindingFactorGenerator();
      assertBn254FieldElement(senderMvkBlindingFactor);
      const senderPrivateKey = await poseidonPrivateKeyGenerator();
      assertBn254FieldElement(senderPrivateKey);
      const senderPrivateKeyBlindingFactor = await poseidonBlindingFactorGenerator();
      assertBn254FieldElement(senderPrivateKeyBlindingFactor);
      const senderUserCommitment = await userCommitmentGenerator(
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        senderPrivateKey,
        senderPrivateKeyBlindingFactor
      );
      assertBn254FieldElement(senderUserCommitment);
      const randomSecret = deriveRandomSecretFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(randomSecret);
      const nullifier = deriveNullifierFromModifiedGenerationIndex(modifiedGenerationIndex);
      assertBn254FieldElement(nullifier);
      const h2Hash = await h2Generator.generateH2({
        amount: toBn254FieldElement4(netDepositAmount),
        nullifier,
        userCommitment: receiverUserCommitment,
        finalDestinationAddressLow: toBn254FieldElement4(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement4(destinationAddressHigh),
        h2BlindingFactor: randomSecret
      });
      assertBn254FieldElement(h2Hash);
      assertPoseidonPlaintext(destinationAddressLow);
      assertPoseidonPlaintext(destinationAddressHigh);
      const poseidonKey = BigInt(transactionViewingKey);
      assertPoseidonKey(poseidonKey);
      const pcCiphertexts = await poseidonEncryptor(
        [destinationAddressLow, destinationAddressHigh],
        poseidonKey
      );
      if (pcCiphertexts.length !== 2) {
        throw new CryptographyError(
          `Expected 2 ciphertexts but got ${String(pcCiphertexts.length)}`
        );
      }
      const pcEncryptedDestinationAddressLow = pcCiphertexts[0];
      const pcEncryptedDestinationAddressHigh = pcCiphertexts[1];
      const pcKeystreams = await poseidonKeystreamGenerator(
        [0n, 1n],
        poseidonKey
      );
      const pcKeyForDestinationAddressLow = pcKeystreams.get(0n);
      if (pcKeyForDestinationAddressLow === void 0) {
        throw new CryptographyError("Missing keystream for counter 0");
      }
      const pcKeyForDestinationAddressHigh = pcKeystreams.get(1n);
      if (pcKeyForDestinationAddressHigh === void 0) {
        throw new CryptographyError("Missing keystream for counter 1");
      }
      const firstKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressLow,
        0n
      );
      const secondKeystreamBlindingFactor = await poseidonKeystreamBlindingFactorGenerator(
        pcKeyForDestinationAddressHigh,
        1n
      );
      const firstPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressLow,
        firstKeystreamBlindingFactor
      );
      const secondPcKeystreamCommitment = await keystreamCommitmentGenerator(
        pcKeyForDestinationAddressHigh,
        secondKeystreamBlindingFactor
      );
      const keypairResult = await mvkX25519KeypairDeriver();
      const clientX25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      assertX25519PrivateKey(clientX25519PrivateKey);
      const aesSharedSecret = x25519.getSharedSecret(
        clientX25519PrivateKey,
        receiverX25519PublicKey
      );
      const aesEncryptionKeyBytes = keccak_256(aesSharedSecret).slice(0, 32);
      assertAesKey(aesEncryptionKeyBytes);
      const destinationAddressBytes = new Uint8Array(
        getAddressEncoder().encode(destinationAddress)
      );
      const aesPlaintextData = new Uint8Array(68);
      aesPlaintextData.set(encodeU64ToU64LeBytes(netDepositAmount), 0);
      aesPlaintextData.set(destinationAddressBytes, 8);
      aesPlaintextData.set(modifiedGenerationIndex, 40);
      aesPlaintextData.set(AES_DOMAIN_SEPARATOR2, 56);
      assertAesPlaintext(aesPlaintextData);
      const aesEncryptedUtxoData = await aesEncryptor(aesEncryptionKeyBytes, aesPlaintextData);
      const optionalDataBytes = options?.optionalData ?? new Uint8Array(32);
      const { low: mintAddressLow, high: mintAddressHigh } = splitAddressToLowHigh(mint);
      const aggregatedHashInputs = [
        // [0-1] Mint address
        toBn254FieldElement4(mintAddressLow),
        toBn254FieldElement4(mintAddressHigh),
        // [2] Amount (net deposit amount, matching on-chain)
        toBn254FieldElement4(netDepositAmount),
        // [3-8] Timestamp components
        toBn254FieldElement4(BigInt(year)),
        toBn254FieldElement4(BigInt(month)),
        toBn254FieldElement4(BigInt(day)),
        toBn254FieldElement4(BigInt(hour)),
        toBn254FieldElement4(BigInt(minute)),
        toBn254FieldElement4(BigInt(second)),
        // [9] H2 hash
        h2Hash,
        // [10] Sender user commitment
        senderUserCommitment,
        // [11-12] Linker encryptions (encrypted destination address)
        toBn254FieldElement4(pcEncryptedDestinationAddressLow),
        toBn254FieldElement4(pcEncryptedDestinationAddressHigh),
        // [13-14] Keystream commitments
        firstPcKeystreamCommitment,
        secondPcKeystreamCommitment,
        // [15] Protocol fees root
        toBn254FieldElement4(protocolFeeConfig.merkleRoot)
      ];
      const aggregatedPublicInputHash = await poseidonAggregator(aggregatedHashInputs);
      const inverseForSenderMvkBlindingFactor = computeBn254ModularInverse(senderMvkBlindingFactor);
      const inverseForSenderPrivateKeyBlindingFactor = computeBn254ModularInverse(
        senderPrivateKeyBlindingFactor
      );
      const inverseForSenderPrivateKey = computeBn254ModularInverse(senderPrivateKey);
      const inverseForSenderMasterViewingKey = computeBn254ModularInverse(senderMasterViewingKey);
      const inverseForAmount = computeBn254ModularInverse(toBn254FieldElement4(netDepositAmount));
      const inverseForNullifier = computeBn254ModularInverse(nullifier);
      const inverseForH2BlindingFactor = computeBn254ModularInverse(randomSecret);
      const inverseForFirstKeystreamBlindingFactor = computeBn254ModularInverse(
        firstKeystreamBlindingFactor
      );
      const inverseForSecondKeystreamBlindingFactor = computeBn254ModularInverse(
        secondKeystreamBlindingFactor
      );
      const zkCircuitInputs = {
        // Sender Commitment (8 signals)
        senderPrivateKey,
        senderMasterViewingKey,
        senderMvkBlindingFactor,
        inverseForSenderMvkBlindingFactor,
        senderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKeyBlindingFactor,
        inverseForSenderPrivateKey,
        inverseForSenderMasterViewingKey,
        // Amount (2 signals)
        amount: toBn254FieldElement4(netDepositAmount),
        inverseForAmount,
        // H2 Hash (6 signals)
        nullifier,
        inverseForNullifier,
        unlockingUserCommitment: receiverUserCommitment,
        h2BlindingFactor: randomSecret,
        inverseForH2BlindingFactor,
        // Timestamp (6 signals)
        year: toBn254FieldElement4(BigInt(year)),
        month: toBn254FieldElement4(BigInt(month)),
        day: toBn254FieldElement4(BigInt(day)),
        hour: toBn254FieldElement4(BigInt(hour)),
        minute: toBn254FieldElement4(BigInt(minute)),
        second: toBn254FieldElement4(BigInt(second)),
        // Mint Address (2 signals)
        mintAddressLow: toBn254FieldElement4(mintAddressLow),
        mintAddressHigh: toBn254FieldElement4(mintAddressHigh),
        // Encryption & Keystream (6 signals)
        finalDestinationAddressLow: toBn254FieldElement4(destinationAddressLow),
        finalDestinationAddressHigh: toBn254FieldElement4(destinationAddressHigh),
        keystreamBlindingFactor: [firstKeystreamBlindingFactor, secondKeystreamBlindingFactor],
        inverseForKeystreamBlindingFactor: [
          inverseForFirstKeystreamBlindingFactor,
          inverseForSecondKeystreamBlindingFactor
        ],
        // Protocol Fees (1 signal)
        protocolFeesRoot: toBn254FieldElement4(protocolFeeConfig.merkleRoot),
        // Public Input (1 signal)
        publicAggregatedHash: aggregatedPublicInputHash
      };
      const { proofA, proofB, proofC } = await deps.zkProver.prove(zkCircuitInputs);
      assertGroth16ProofA(proofA);
      assertGroth16ProofB(proofB);
      assertGroth16ProofC(proofC);
      const stealthPoolIndex = 0n;
      const stealthPoolAddress = await findStealthPoolPda(
        stealthPoolIndex,
        client.networkConfig.programId
      );
      const h2HashBytes = encodeU256ToU256LeBytes(toU2564(h2Hash));
      const linkerEncryption0Bytes = encodeU256ToU256LeBytes(
        toU2564(pcEncryptedDestinationAddressLow)
      );
      const linkerEncryption1Bytes = encodeU256ToU256LeBytes(
        toU2564(pcEncryptedDestinationAddressHigh)
      );
      const keystreamCommitment0Bytes = encodeU256ToU256LeBytes(toU2564(firstPcKeystreamCommitment));
      const keystreamCommitment1Bytes = encodeU256ToU256LeBytes(
        toU2564(secondPcKeystreamCommitment)
      );
      async function buildAndSendTransaction(instructions, instructionName, useAlt) {
        let latestBlockhashData;
        try {
          latestBlockhashData = await getLatestBlockhash();
        } catch (error) {
          throw new RpcError(
            `Failed to fetch latest blockhash: ${error instanceof Error ? error.message : "Unknown error"}`,
            { cause: error }
          );
        }
        const altEntry = useAlt === true ? lookupAltEntry(
          client.networkConfig.addressLookupTables,
          clusterOffset,
          "create_deposit_into_mixer_tree_from_public_balance"
        ) : void 0;
        const baseTransactionMessage = pipe(
          createTransactionMessage({ version: 0 }),
          (m) => setTransactionMessageFeePayer(client.signer.address, m),
          (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhashData, m),
          (m) => appendTransactionMessageInstructions(instructions, m)
        );
        const transactionMessage = altEntry === void 0 ? baseTransactionMessage : compressTransactionMessageUsingAddressLookupTables(
          baseTransactionMessage,
          buildAltAddressesRecord(altEntry)
        );
        const transaction = compileTransaction(transactionMessage);
        const signed = await client.signer.signTransaction(transaction);
        if (!isSignedTransaction4(signed)) {
          throw new TransactionError(
            `${instructionName}: Transaction was not properly signed`
          );
        }
        const signature = getSignatureFromTransaction(signed);
        try {
          await transactionForwarder.forwardSequentially([signed]);
        } catch (error) {
          throw new TransactionError(
            `${instructionName}: Failed to send transaction: ${error instanceof Error ? error.message : "Unknown error"}`,
            { cause: error }
          );
        }
        return { signedTransaction: signed, signature };
      }
      __name(buildAndSendTransaction, "buildAndSendTransaction");
      assertU128(proofAccountOffset);
      let closeProofAccountSignature;
      const proofAccountPda = await findPublicUtxoInputBufferPda(
        client.signer.address,
        proofAccountOffset,
        client.networkConfig.programId
      );
      let proofAccountExists = false;
      try {
        const accountMap = await accountInfoProvider([proofAccountPda], { commitment: accountInfoCommitment });
        const accountInfo = accountMap.get(proofAccountPda);
        proofAccountExists = accountInfo?.exists === true;
      } catch {
      }
      if (proofAccountExists) {
        const closeInstruction = await getClosePublicStealthPoolDepositInputBufferInstructionAsync(
          {
            depositor: createNoopSigner(client.signer.address),
            offset: { first: proofAccountOffset }
          },
          { programAddress: client.networkConfig.programId }
        );
        const closeResult = await buildAndSendTransaction(
          [closeInstruction],
          "ClosePublicUtxoProofAccount"
        );
        closeProofAccountSignature = closeResult.signature;
      }
      const proofAccountInstruction = await getCreatePublicStealthPoolDepositInputBufferInstructionAsync(
        {
          depositor: createNoopSigner(client.signer.address),
          feePayer: createNoopSigner(client.signer.address),
          offset: { first: proofAccountOffset },
          insertionH2Commitment: { first: h2HashBytes },
          insertionTimestamp: { first: BigInt(currentTimestamp) },
          linkerEncryption0: { first: linkerEncryption0Bytes },
          linkerEncryption1: { first: linkerEncryption1Bytes },
          keystreamCommitment0: { first: keystreamCommitment0Bytes },
          keystreamCommitment1: { first: keystreamCommitment1Bytes },
          groth16ProofA: { first: proofA },
          groth16ProofB: { first: proofB },
          groth16ProofC: { first: proofC },
          aesEncryptedData: { first: aesEncryptedUtxoData },
          optionalData: { first: optionalDataBytes }
        },
        { programAddress: client.networkConfig.programId }
      );
      const proofAccountResult = await buildAndSendTransaction(
        [proofAccountInstruction],
        "CreatePublicUtxoProofAccount"
      );
      const depositInstruction = await getDepositIntoStealthPoolFromPublicBalanceInstructionAsync(
        {
          feePayer: createNoopSigner(client.signer.address),
          depositor: createNoopSigner(client.signer.address),
          stealthPool: stealthPoolAddress,
          mint,
          tokenProgram: mintTokenProgram,
          feeVaultOffset: { first: 0n },
          publicStealthPoolDepositInputBufferOffset: { first: proofAccountOffset },
          ...feeSlabResultToInstructionFields(protocolFeeConfig),
          transferAmount: { first: amount }
        },
        { programAddress: client.networkConfig.programId }
      );
      const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
      if (factoryCreateUtxoCallbacks?.pre !== void 0) {
        await factoryCreateUtxoCallbacks.pre(proofAccountResult.signedTransaction);
      }
      const depositResult = await buildAndSendTransaction(
        [computeBudgetInstruction, depositInstruction],
        "CreateDepositIntoMixerTreeFromPublicBalance",
        true
      );
      if (factoryCreateUtxoCallbacks?.post !== void 0) {
        await factoryCreateUtxoCallbacks.post(
          depositResult.signedTransaction,
          depositResult.signature
        );
      }
      return {
        closeProofAccountSignature,
        createProofAccountSignature: proofAccountResult.signature,
        createUtxoSignature: depositResult.signature
      };
    } catch (error) {
      if (error instanceof CreateUtxoError) {
        throw error;
      }
      if (error instanceof TransactionSigningError) {
        throw new CreateUtxoError("transaction-sign", error.message, error);
      }
      if (error instanceof CryptographyError) {
        throw new CreateUtxoError("zk-proof-generation", error.message, error);
      }
      if (error instanceof TransactionError) {
        throw new CreateUtxoError("transaction-send", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new CreateUtxoError("account-fetch", error.message, error);
      }
      throw new CreateUtxoError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  }, "createReceiverClaimableUtxoFromPublicBalance");
}
__name(getPublicBalanceToReceiverClaimableUtxoCreatorFunction, "getPublicBalanceToReceiverClaimableUtxoCreatorFunction");
function bytesToBigIntLe(bytes) {
  const array = bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes);
  let result = 0n;
  const length = array.length;
  for (let index = length - 1; index >= 0; index--) {
    const byte = getByteAt(array, index);
    result = result << 8n | BigInt(byte);
  }
  return result;
}
__name(bytesToBigIntLe, "bytesToBigIntLe");
function extractNonce(account) {
  return account.nonce.first;
}
__name(extractNonce, "extractNonce");
function extractBalanceCiphertext(account) {
  const balanceBytes = account.balance.first;
  return bytesToBigIntLe(balanceBytes);
}
__name(extractBalanceCiphertext, "extractBalanceCiphertext");
var TOKEN_ACCOUNT_STATUS_BITS3 = {
  IS_SHARED_MODE: 3,
  IS_ARCIUM_BALANCE_INITIALISED: 4
};
function isBitSet4(value, bitPosition) {
  return (BigInt(value) & 1n << BigInt(bitPosition)) !== 0n;
}
__name(isBitSet4, "isBitSet");
function getEncryptedBalanceQuerierFunction(args, deps) {
  const { client } = args;
  const userAddress = client.signer.address;
  const accountInfoProvider = deps?.accountInfoProvider ?? client.accountInfoProvider;
  const tokenX25519KeypairDeriver = getUserAccountX25519KeypairDeriver({
    client
  });
  let cachedDecryptor = null;
  async function getDecryptor() {
    if (deps?.rcDecryptor !== void 0) {
      return deps.rcDecryptor;
    }
    if (cachedDecryptor === null) {
      let keypairResult;
      try {
        keypairResult = await tokenX25519KeypairDeriver();
      } catch (error) {
        throw new QueryError(
          "key-derivation",
          `Failed to derive X25519 keypair: ${error instanceof Error ? error.message : String(error)}`,
          error instanceof Error ? error : void 0
        );
      }
      const x25519PrivateKey = keypairResult.x25519Keypair.privateKey;
      cachedDecryptor = getRescueDecryptorFromPrivateKey(x25519PrivateKey, {
        umbraX25519PublicKey: client.networkConfig.mxePubkey
      });
    }
    return cachedDecryptor;
  }
  __name(getDecryptor, "getDecryptor");
  return /* @__PURE__ */ __name(async function queryEncryptedBalance(mints, options) {
    options?.accountInfoCommitment ?? "confirmed";
    const results = /* @__PURE__ */ new Map();
    if (mints.length === 0) {
      return results;
    }
    let pdaDerivations;
    try {
      pdaDerivations = await Promise.all(
        mints.map(async (mint) => ({
          mint,
          pda: await findEncryptedTokenAccountPda(
            userAddress,
            mint,
            client.networkConfig.programId
          )
        }))
      );
    } catch (error) {
      throw new QueryError(
        "pda-derivation",
        `Failed to derive token account PDAs: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    const pdaToMint = /* @__PURE__ */ new Map();
    const pdas = [];
    for (const { mint, pda } of pdaDerivations) {
      pdaToMint.set(pda, mint);
      pdas.push(pda);
    }
    let accountMap;
    try {
      accountMap = await accountInfoProvider(pdas);
    } catch (error) {
      throw new QueryError(
        "account-fetch",
        `Failed to fetch token account data: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    for (const { mint, pda } of pdaDerivations) {
      const maybeAccount = accountMap.get(pda);
      if (maybeAccount === void 0 || !maybeAccount.exists || maybeAccount.data === void 0) {
        results.set(mint, { state: "non_existent" });
        continue;
      }
      let decodedAccount;
      try {
        decodedAccount = decodeEncryptedTokenAccount(
          maybeAccount
        );
      } catch (error) {
        throw new QueryError(
          "account-decode",
          `Failed to decode token account data for mint ${mint}: ${error instanceof Error ? error.message : String(error)}`,
          error instanceof Error ? error : void 0
        );
      }
      const statusBits = decodedAccount.data.statusBits.first;
      if (!isBitSet4(statusBits, TOKEN_ACCOUNT_STATUS_BITS3.IS_ARCIUM_BALANCE_INITIALISED)) {
        results.set(mint, { state: "uninitialized" });
        continue;
      }
      if (!isBitSet4(statusBits, TOKEN_ACCOUNT_STATUS_BITS3.IS_SHARED_MODE)) {
        results.set(mint, { state: "mxe" });
        continue;
      }
      const rcDecryptor = await getDecryptor();
      const nonce = extractNonce(decodedAccount.data);
      const balanceCiphertext = extractBalanceCiphertext(decodedAccount.data);
      let plaintexts;
      try {
        plaintexts = await rcDecryptor([balanceCiphertext], nonce);
      } catch (error) {
        throw new QueryError(
          "decryption",
          `Failed to decrypt balance for mint ${mint}: ${error instanceof Error ? error.message : String(error)}`,
          error instanceof Error ? error : void 0
        );
      }
      const decryptedBalance = plaintexts[0];
      if (decryptedBalance === void 0) {
        results.set(mint, { state: "non_existent" });
        continue;
      }
      const balance = BigInt(decryptedBalance);
      results.set(mint, {
        state: "shared",
        balance
      });
    }
    return results;
  }, "queryEncryptedBalance");
}
__name(getEncryptedBalanceQuerierFunction, "getEncryptedBalanceQuerierFunction");
function parseStatusBits(statusBits) {
  return {
    isInitialised: (statusBits & 1n << 0n) !== 0n,
    isActiveForAnonymousUsage: (statusBits & 1n << 1n) !== 0n,
    isUserCommitmentRegistered: (statusBits & 1n << 2n) !== 0n,
    isUserAccountX25519KeyRegistered: (statusBits & 1n << 4n) !== 0n
  };
}
__name(parseStatusBits, "parseStatusBits");
function bytesToBigIntLe2(bytes) {
  const array = bytes instanceof Uint8Array ? bytes : new Uint8Array(bytes);
  let result = 0n;
  const length = array.length;
  for (let index = length - 1; index >= 0; index--) {
    const byte = getByteAt(array, index);
    result = result << 8n | BigInt(byte);
  }
  return result;
}
__name(bytesToBigIntLe2, "bytesToBigIntLe");
function convertToSdkTypes(codamaAccount) {
  const statusBitsValue = codamaAccount.statusBits.first;
  const statusFlags = parseStatusBits(statusBitsValue);
  const userCommitmentBigInt = bytesToBigIntLe2(codamaAccount.userCommitment.first);
  return {
    versionByte: BigInt(codamaAccount.versionByte.first),
    canonicalBump: BigInt(codamaAccount.canonicalBump.first),
    ...statusFlags,
    x25519PublicKey: new Uint8Array(codamaAccount.x25519PublicKeyForMasterViewingKeyEncryption.first),
    userCommitment: userCommitmentBigInt,
    generationIndex: codamaAccount.generationIndex.first,
    randomGenerationSeed: new Uint8Array(codamaAccount.randomGenerationSeed.first)
  };
}
__name(convertToSdkTypes, "convertToSdkTypes");
function getUserAccountQuerierFunction(args, deps) {
  const { client } = args;
  const umbraProgram = client.networkConfig.programId;
  const accountInfoProvider = deps?.accountInfoProvider ?? client.accountInfoProvider;
  return /* @__PURE__ */ __name(async function queryUserAccount(userAddress, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    let userAccountPda;
    try {
      userAccountPda = await findEncryptedUserAccountPda(userAddress, umbraProgram);
    } catch (error) {
      throw new QueryError(
        "pda-derivation",
        `Failed to derive user account PDA: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    let accountMap;
    try {
      accountMap = await accountInfoProvider([userAccountPda], { commitment: accountInfoCommitment });
    } catch (error) {
      throw new QueryError(
        "account-fetch",
        `Failed to fetch user account data: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    const maybeAccount = accountMap.get(userAccountPda);
    if (maybeAccount === void 0 || !maybeAccount.exists || maybeAccount.data === void 0) {
      return { state: "non_existent" };
    }
    let sdkAccount;
    try {
      const decodedAccount = decodeEncryptedUserAccount(
        maybeAccount
      );
      sdkAccount = convertToSdkTypes(decodedAccount.data);
    } catch (error) {
      throw new QueryError(
        "account-decode",
        `Failed to decode user account data: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    return { state: "exists", data: sdkAccount };
  }, "queryUserAccount");
}
__name(getUserAccountQuerierFunction, "getUserAccountQuerierFunction");
var DEFAULT_OPTIONAL_DATA_BYTES8 = new Uint8Array(32);
function getStagedSolRecovererFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function claimStagedSolFromPool(mint, amount, destination, optionalData, callbacks) {
    const userAddress = signer.address;
    if (amount === 0n) {
      throw new TransactionError("amount must be greater than zero");
    }
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getClaimStagedSolFromPoolInstructionAsync({
      feePayer: noopSigner,
      user: noopSigner,
      mint,
      destination,
      amount: { first: amount },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES8 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const transactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "claimStagedSolFromPool");
}
__name(getStagedSolRecovererFunction, "getStagedSolRecovererFunction");
var DEFAULT_OPTIONAL_DATA_BYTES9 = new Uint8Array(32);
function getStagedSplRecovererFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function claimStagedSplFromPool(mint, amount, destinationAta, optionalData, callbacks) {
    const userAddress = signer.address;
    if (amount === 0n) {
      throw new TransactionError("amount must be greater than zero");
    }
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getClaimStagedSplFromPoolInstructionAsync({
      feePayer: noopSigner,
      user: noopSigner,
      mint,
      destinationAta,
      amount: { first: amount },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES9 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const transactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "claimStagedSplFromPool");
}
__name(getStagedSplRecovererFunction, "getStagedSplRecovererFunction");
var STATUS_BIT_USER_COMMITMENT_REGISTERED = 1n << 2n;
var STATUS_BIT_USER_ACCOUNT_X25519_PUBKEY_REGISTERED = 1n << 4n;
function getUserRegistrationFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const accountInfoProvider = deps?.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  const userAccountX25519KeypairDeriver = deps?.keys?.userAccountX25519KeypairDeriver ?? getUserAccountX25519KeypairDeriver({ client });
  const masterViewingKeyEncryptingX25519KeypairDeriver = deps?.keys?.masterViewingKeyEncryptingX25519KeypairDeriver ?? getMasterViewingKeyX25519KeypairDeriver({ client });
  const masterViewingKeyGenerator = deps?.keys?.masterViewingKeyGenerator ?? getMasterViewingKeyDeriver({ client });
  const masterViewingKeyBlindingFactorGenerator = deps?.keys?.masterViewingKeyBlindingFactorGenerator ?? getMasterViewingKeyBlindingFactorDeriver({ client });
  const poseidonPrivateKeyGenerator = deps?.keys?.poseidonPrivateKeyGenerator ?? getPoseidonPrivateKeyDeriver({ client });
  const poseidonBlindingFactorGenerator = deps?.keys?.poseidonBlindingFactorGenerator ?? getPoseidonBlindingFactorDeriver({ client });
  const rescueCommitmentBlindingFactorGenerator = deps?.crypto?.rescueCommitmentBlindingFactorGenerator ?? getRescueCommitmentBlindingFactorDeriver({
    client
  });
  const randomFactorGenerator = deps?.crypto?.randomFactorGenerator ?? getPolynomialCommitmentFactorDeriver({ client });
  const getRcKeyGenerator = deps?.crypto?.getRcKeyGenerator ?? ((pk) => getRescueKeyGeneratorFromPrivateKey(pk, {
    umbraX25519PublicKey: client.networkConfig.mxePubkey
  }));
  const getRcEncryptor = deps?.crypto?.getRcEncryptor ?? ((pk) => getRescueEncryptorWithNonceFromPrivateKey(pk, {
    umbraX25519PublicKey: client.networkConfig.mxePubkey
  }));
  const rescueCommitmentGenerator = deps?.crypto?.rescueCommitmentGenerator ?? getRescueCommitmentGenerator();
  const userCommitmentGenerator = deps?.crypto?.userCommitmentGenerator ?? getUserCommitmentGeneratorFunction();
  const fiatShamirChallengeGenerator = deps?.crypto?.fiatShamirChallengeGenerator ?? getFiatShamirChallengeGeneratorFunction();
  const challengePowersFunction = deps?.crypto?.challengePowersFunction ?? getChallengePowersFunction();
  const polynomialEvaluator = deps?.crypto?.polynomialEvaluator ?? getPolynomialEvaluatorFunction();
  const poseidonAggregator = deps?.crypto?.poseidonAggregator ?? getPoseidonAggregator();
  const bn254ModuleInverter = deps?.crypto?.bn254ModInverter ?? getBn254ModularInverter();
  const computeLimbwiseSumInverse = deps?.crypto?.computeLimbwiseSumInverse ?? computeBn254LimbwiseSumInverse$1;
  const zkProver = deps?.zkProver;
  const executorConfig = {
    signer,
    getLatestBlockhash,
    transactionForwarder,
    computationMonitor: client.computationMonitor
  };
  async function executeTransactionWithCallbacks(instruction, callbacks, prefixInstructions, altEntry, awaitCallback) {
    const result = await executeComputationTransaction(
      executorConfig,
      {
        instruction,
        preInstructions: prefixInstructions,
        altEntry,
        callbacks
      },
      awaitCallback
    );
    return result.signature;
  }
  __name(executeTransactionWithCallbacks, "executeTransactionWithCallbacks");
  return async (options) => {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    try {
      const {
        confidential = true,
        anonymous = true,
        callbacks,
        optionalData,
        microLamportsPerAcu: userMicroLamportsPerAcu
      } = options ?? {};
      const microLamportsPerAcu = userMicroLamportsPerAcu ?? 0n;
      const defaultOptionalData = new Uint8Array(32);
      if (anonymous && zkProver === void 0) {
        throw new Error(
          "ZK prover is required for anonymous mode registration. Provide zkProver in the deps argument."
        );
      }
      const userAccountPda = await findEncryptedUserAccountPda(
        signer.address,
        client.networkConfig.programId
      );
      const accountMap = await accountInfoProvider([userAccountPda, client.networkConfig.mxeAccountAddress], { commitment: accountInfoCommitment });
      const maybeAccount = accountMap.get(userAccountPda);
      const clusterOffset = extractClusterOffsetFromMxeAccount(
        accountMap.get(client.networkConfig.mxeAccountAddress)
      );
      if (maybeAccount === void 0) {
        throw new Error(`Failed to fetch account info for PDA: ${userAccountPda}`);
      }
      let accountExists = maybeAccount.exists;
      let statusBits = 0n;
      let onChainX25519TokenPublicKey;
      let onChainX25519MvkPublicKey;
      let onChainUserCommitment;
      if (accountExists) {
        const existingAccount = maybeAccount;
        const isOwnedByUmbra = existingAccount.programAddress === client.networkConfig.programId;
        const hasCorrectSize = existingAccount.data.length >= getEncryptedUserAccountSize();
        const hasCorrectDiscriminator = hasCorrectSize && existingAccount.data.slice(0, 8).every((byte, index) => byte === ENCRYPTED_USER_ACCOUNT_DISCRIMINATOR[index]);
        if (isOwnedByUmbra && hasCorrectSize && hasCorrectDiscriminator) {
          const decodedAccount = decodeEncryptedUserAccount(maybeAccount);
          if (decodedAccount.exists) {
            statusBits = decodedAccount.data.statusBits.first;
            onChainX25519TokenPublicKey = new Uint8Array(
              decodedAccount.data.x25519PublicKeyForTokenEncryption.first
            );
            onChainX25519MvkPublicKey = new Uint8Array(
              decodedAccount.data.x25519PublicKeyForMasterViewingKeyEncryption.first
            );
            const ucBytes = new Uint8Array(decodedAccount.data.userCommitment.first);
            let ucBigInt = 0n;
            for (let index = ucBytes.length - 1; index >= 0; index--) {
              ucBigInt = ucBigInt << 8n | BigInt(ucBytes[index] ?? 0);
            }
            onChainUserCommitment = ucBigInt;
          }
        } else {
          accountExists = false;
        }
      }
      const isX25519Registered = accountExists && (statusBits & STATUS_BIT_USER_ACCOUNT_X25519_PUBKEY_REGISTERED) !== 0n;
      const isUserCommitmentRegistered = accountExists && (statusBits & STATUS_BIT_USER_COMMITMENT_REGISTERED) !== 0n;
      const signatures = [];
      if (accountExists) {
      } else {
        const randomSeed = crypto.getRandomValues(new Uint8Array(32));
        const noopSigner = createNoopSigner(signer.address);
        const createAccountInstruction = await getInitialiseEncryptedUserAccountInstructionAsync(
          {
            userAddress: signer.address,
            signer: noopSigner,
            feePayer: noopSigner,
            randomGenerationSeed: {
              first: randomSeed
            },
            optionalData: {
              first: optionalData?.accountInitialisation ?? defaultOptionalData
            }
          },
          {
            programAddress: client.networkConfig.programId
          }
        );
        const signature = await executeTransactionWithCallbacks(
          createAccountInstruction,
          callbacks?.userAccountInitialisation
        );
        signatures.push(signature);
      }
      if (confidential) {
        const keypairResult = await userAccountX25519KeypairDeriver();
        const localX25519PublicKey = keypairResult.x25519Keypair.publicKey;
        const tokenPubkey = onChainX25519TokenPublicKey;
        const x25519KeyMatches = isX25519Registered && localX25519PublicKey.length === tokenPubkey?.length && localX25519PublicKey.every((byte, index) => byte === tokenPubkey[index]);
        if (x25519KeyMatches) {
        } else {
          const userNoopSigner = createNoopSigner(signer.address);
          const registerX25519Instruction = await getRegisterTokenPublicKeyInstructionAsync(
            {
              feePayer: userNoopSigner,
              user: userNoopSigner,
              x25519ProvingSigner: await createKeyPairSignerFromPrivateKeyBytes(
                keypairResult.ed25519Keypair.seed
              ),
              optionalData: {
                first: optionalData?.registerX25519PublicKey ?? defaultOptionalData
              }
            },
            {
              programAddress: client.networkConfig.programId
            }
          );
          const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
          const signature = await executeTransactionWithCallbacks(
            registerX25519Instruction,
            callbacks?.registerX25519PublicKey,
            [computeBudgetInstruction]
          );
          signatures.push(signature);
        }
      }
      if (anonymous) {
        const keypairResult = await masterViewingKeyEncryptingX25519KeypairDeriver();
        const x25519PrivateKey = keypairResult.x25519Keypair.privateKey;
        const x25519PublicKey = keypairResult.x25519Keypair.publicKey;
        const masterViewingKey = await masterViewingKeyGenerator();
        const poseidonPrivateKey = await poseidonPrivateKeyGenerator();
        const masterViewingKeyBlindingFactor = await masterViewingKeyBlindingFactorGenerator();
        const poseidonPrivateKeyBlindingFactor = await poseidonBlindingFactorGenerator();
        const mvkPubkey = onChainX25519MvkPublicKey;
        const mvkKeyMatches = isUserCommitmentRegistered && x25519PublicKey.length === mvkPubkey?.length && x25519PublicKey.every((byte, index) => byte === mvkPubkey[index]);
        let shouldSkipAnonymous = false;
        if (mvkKeyMatches) {
          const localUserCommitment = await userCommitmentGenerator(
            masterViewingKey,
            masterViewingKeyBlindingFactor,
            poseidonPrivateKey,
            poseidonPrivateKeyBlindingFactor
          );
          shouldSkipAnonymous = localUserCommitment === onChainUserCommitment;
        }
        if (shouldSkipAnonymous) {
        } else {
          const rescueEncryptionCommitmentBlindingFactor = await rescueCommitmentBlindingFactorGenerator(0n);
          const randomFactorForPolynomialCommitment = await randomFactorGenerator(0n);
          const mvkParts = splitU256IntoTwoU128s(createU256(masterViewingKey, "masterViewingKey"));
          const masterViewingKeyLow = mvkParts.low;
          const masterViewingKeyHigh = mvkParts.high;
          const x25519PubKeyParts = splitX25519PublicKeyIntoTwoU128s(x25519PublicKey);
          const arcisPublicKeyLow = x25519PubKeyParts.low;
          const arcisPublicKeyHigh = x25519PubKeyParts.high;
          const randomFactorParts = splitU256IntoTwoU128s(
            createU256(randomFactorForPolynomialCommitment, "randomFactorForPolynomialCommitment")
          );
          const randomFactorLow = randomFactorParts.low;
          const randomFactorHigh = randomFactorParts.high;
          const masterViewingKeyLowLimbs = convertU256ToBase85Limbs(
            createU256(masterViewingKeyLow, "masterViewingKeyLow")
          );
          const masterViewingKeyHighLimbs = convertU256ToBase85Limbs(
            createU256(masterViewingKeyHigh, "masterViewingKeyHigh")
          );
          const randomFactorLowLimbs = convertU256ToBase85Limbs(
            createU256(randomFactorLow, "randomFactorLow")
          );
          const randomFactorHighLimbs = convertU256ToBase85Limbs(
            createU256(randomFactorHigh, "randomFactorHigh")
          );
          const rescueEncryptionNonce = generateRandomU128();
          const rcKeyGenerator = getRcKeyGenerator(x25519PrivateKey);
          const rescueKeys = await rcKeyGenerator(
            [0n, 1n, 2n, 3n],
            rescueEncryptionNonce
          );
          const keyForMvkLow = rescueKeys.get(0n);
          if (keyForMvkLow === void 0) {
            throw new Error("Missing rescue key for counter 0");
          }
          const keyForMvkHigh = rescueKeys.get(1n);
          if (keyForMvkHigh === void 0) {
            throw new Error("Missing rescue key for counter 1");
          }
          const keyForRfLow = rescueKeys.get(2n);
          if (keyForRfLow === void 0) {
            throw new Error("Missing rescue key for counter 2");
          }
          const keyForRfHigh = rescueKeys.get(3n);
          if (keyForRfHigh === void 0) {
            throw new Error("Missing rescue key for counter 3");
          }
          const keyForMvkLowLimbs = convertU256ToBase85Limbs(
            createU256(keyForMvkLow, "keyForMvkLow")
          );
          const keyForMvkHighLimbs = convertU256ToBase85Limbs(
            createU256(keyForMvkHigh, "keyForMvkHigh")
          );
          const keyForRfLowLimbs = convertU256ToBase85Limbs(createU256(keyForRfLow, "keyForRfLow"));
          const keyForRfHighLimbs = convertU256ToBase85Limbs(
            createU256(keyForRfHigh, "keyForRfHigh")
          );
          const rcEncryptor = getRcEncryptor(x25519PrivateKey);
          const ciphertexts = await rcEncryptor(
            [
              createRcPlaintext(masterViewingKeyLow, "masterViewingKeyLow"),
              createRcPlaintext(masterViewingKeyHigh, "masterViewingKeyHigh"),
              createRcPlaintext(randomFactorLow, "randomFactorLow"),
              createRcPlaintext(randomFactorHigh, "randomFactorHigh")
            ],
            rescueEncryptionNonce
          );
          const encryptedMvkLow = ciphertexts[0];
          if (encryptedMvkLow === void 0) {
            throw new Error("Missing ciphertext at index 0");
          }
          const encryptedMvkHigh = ciphertexts[1];
          if (encryptedMvkHigh === void 0) {
            throw new Error("Missing ciphertext at index 1");
          }
          const encryptedRfLow = ciphertexts[2];
          if (encryptedRfLow === void 0) {
            throw new Error("Missing ciphertext at index 2");
          }
          const encryptedRfHigh = ciphertexts[3];
          if (encryptedRfHigh === void 0) {
            throw new Error("Missing ciphertext at index 3");
          }
          const encryptedMvkLowLimbs = convertU256ToBase85Limbs(
            createU256(encryptedMvkLow, "encryptedMvkLow")
          );
          const encryptedMvkHighLimbs = convertU256ToBase85Limbs(
            createU256(encryptedMvkHigh, "encryptedMvkHigh")
          );
          const encryptedRfLowLimbs = convertU256ToBase85Limbs(
            createU256(encryptedRfLow, "encryptedRfLow")
          );
          const encryptedRfHighLimbs = convertU256ToBase85Limbs(
            createU256(encryptedRfHigh, "encryptedRfHigh")
          );
          const rescueEncryptionCommitment = await rescueCommitmentGenerator(
            [encryptedMvkLow, encryptedMvkHigh, encryptedRfLow, encryptedRfHigh],
            [
              createCurve25519FieldElement(masterViewingKeyLow, "masterViewingKeyLow"),
              createCurve25519FieldElement(masterViewingKeyHigh, "masterViewingKeyHigh"),
              createCurve25519FieldElement(randomFactorLow, "randomFactorLow"),
              createCurve25519FieldElement(randomFactorHigh, "randomFactorHigh")
            ],
            [
              createCurve25519FieldElement(keyForMvkLow, "keyForMvkLow"),
              createCurve25519FieldElement(keyForMvkHigh, "keyForMvkHigh"),
              createCurve25519FieldElement(keyForRfLow, "keyForRfLow"),
              createCurve25519FieldElement(keyForRfHigh, "keyForRfHigh")
            ],
            rescueEncryptionCommitmentBlindingFactor,
            rescueEncryptionNonce,
            arcisPublicKeyLow,
            arcisPublicKeyHigh
          );
          const userCommitment = await userCommitmentGenerator(
            masterViewingKey,
            masterViewingKeyBlindingFactor,
            poseidonPrivateKey,
            poseidonPrivateKeyBlindingFactor
          );
          const fiatShamirTranscript = new Uint8Array(240);
          let offset = 0;
          fiatShamirTranscript.set(x25519PublicKey, offset);
          offset += 32;
          const nonceBytes = new Uint8Array(16);
          let nonceValue = BigInt(rescueEncryptionNonce);
          for (let index = 0; index < 16; index++) {
            nonceBytes[index] = Number(nonceValue & 0xffn);
            nonceValue >>= 8n;
          }
          fiatShamirTranscript.set(nonceBytes, offset);
          offset += 16;
          const ciphertextValues = [
            encryptedMvkLow,
            encryptedMvkHigh,
            encryptedRfLow,
            encryptedRfHigh
          ];
          for (const ct of ciphertextValues) {
            const ctBytes = encodeU256ToU256LeBytes(createU256(ct, "ciphertext"));
            fiatShamirTranscript.set(ctBytes, offset);
            offset += 32;
          }
          const commitmentBytes = encodeU256ToU256LeBytes(
            createU256(rescueEncryptionCommitment, "rescueEncryptionCommitment")
          );
          fiatShamirTranscript.set(commitmentBytes, offset);
          offset += 32;
          const userCommitmentBytes = encodeU256ToU256LeBytes(
            createU256(userCommitment, "userCommitment")
          );
          fiatShamirTranscript.set(userCommitmentBytes, offset);
          const fiatShamirChallenge = fiatShamirChallengeGenerator(fiatShamirTranscript);
          const challengePowers = challengePowersFunction(fiatShamirChallenge, 2);
          const challengeSquared = challengePowers[2];
          if (challengeSquared === void 0) {
            throw new Error("Missing challenge power at index 2");
          }
          const challengeLimbs = convertU256ToBase85Limbs(
            createU256(fiatShamirChallenge, "fiatShamirChallenge")
          );
          const challengeSquaredLimbs = convertU256ToBase85Limbs(
            createU256(challengeSquared, "challengeSquared")
          );
          const polynomialResult = polynomialEvaluator(
            [
              createCurve25519FieldElement(masterViewingKeyLow, "masterViewingKeyLow"),
              createCurve25519FieldElement(masterViewingKeyHigh, "masterViewingKeyHigh"),
              randomFactorForPolynomialCommitment
            ],
            fiatShamirChallenge
          );
          const polynomialResultLimbs = convertU256ToBase85Limbs(
            createU256(polynomialResult, "polynomialResult")
          );
          const aggregatorInputs = [
            // 1. userCommitment
            userCommitment,
            // 2-3. arcisPublicKey low/high
            createBn254FieldElement(arcisPublicKeyLow, "arcisPublicKeyLow"),
            createBn254FieldElement(arcisPublicKeyHigh, "arcisPublicKeyHigh"),
            // 4. nonce
            createBn254FieldElement(rescueEncryptionNonce, "rescueEncryptionNonce"),
            // 5-7. encrypted MVK low limbs
            createBn254FieldElement(encryptedMvkLowLimbs.low, "encryptedMvkLowLimbs.low"),
            createBn254FieldElement(encryptedMvkLowLimbs.middle, "encryptedMvkLowLimbs.middle"),
            createBn254FieldElement(encryptedMvkLowLimbs.high, "encryptedMvkLowLimbs.high"),
            // 8-10. encrypted MVK high limbs
            createBn254FieldElement(encryptedMvkHighLimbs.low, "encryptedMvkHighLimbs.low"),
            createBn254FieldElement(encryptedMvkHighLimbs.middle, "encryptedMvkHighLimbs.middle"),
            createBn254FieldElement(encryptedMvkHighLimbs.high, "encryptedMvkHighLimbs.high"),
            // 11-13. encrypted RF low limbs
            createBn254FieldElement(encryptedRfLowLimbs.low, "encryptedRfLowLimbs.low"),
            createBn254FieldElement(encryptedRfLowLimbs.middle, "encryptedRfLowLimbs.middle"),
            createBn254FieldElement(encryptedRfLowLimbs.high, "encryptedRfLowLimbs.high"),
            // 14-16. encrypted RF high limbs
            createBn254FieldElement(encryptedRfHighLimbs.low, "encryptedRfHighLimbs.low"),
            createBn254FieldElement(encryptedRfHighLimbs.middle, "encryptedRfHighLimbs.middle"),
            createBn254FieldElement(encryptedRfHighLimbs.high, "encryptedRfHighLimbs.high"),
            // 17. rescueEncryptionCommitment
            rescueEncryptionCommitment,
            // 18-20. challenge limbs
            createBn254FieldElement(challengeLimbs.low, "challengeLimbs.low"),
            createBn254FieldElement(challengeLimbs.middle, "challengeLimbs.middle"),
            createBn254FieldElement(challengeLimbs.high, "challengeLimbs.high"),
            // 21-23. challenge squared limbs
            createBn254FieldElement(challengeSquaredLimbs.low, "challengeSquaredLimbs.low"),
            createBn254FieldElement(challengeSquaredLimbs.middle, "challengeSquaredLimbs.middle"),
            createBn254FieldElement(challengeSquaredLimbs.high, "challengeSquaredLimbs.high"),
            // 24-26. polynomial evaluation result limbs
            createBn254FieldElement(polynomialResultLimbs.low, "polynomialResultLimbs.low"),
            createBn254FieldElement(polynomialResultLimbs.middle, "polynomialResultLimbs.middle"),
            createBn254FieldElement(polynomialResultLimbs.high, "polynomialResultLimbs.high")
          ];
          const aggregatedPublicInput = await poseidonAggregator(aggregatorInputs);
          const inverseForPrivateKey = createBn254FieldElement(
            bn254ModuleInverter(poseidonPrivateKey),
            "inverseForPrivateKey"
          );
          const inverseForMasterViewingKeyNative = createBn254FieldElement(
            bn254ModuleInverter(masterViewingKey),
            "inverseForMasterViewingKeyNative"
          );
          const inverseForBlindingFactorRescueCommitment = createBn254FieldElement(
            bn254ModuleInverter(rescueEncryptionCommitmentBlindingFactor),
            "inverseForBlindingFactorRescueCommitment"
          );
          const inverseForMasterViewingKeyBlindingFactor = createBn254FieldElement(
            bn254ModuleInverter(masterViewingKeyBlindingFactor),
            "inverseForMasterViewingKeyBlindingFactor"
          );
          const inverseForPrivateKeyBlindingFactor = createBn254FieldElement(
            bn254ModuleInverter(poseidonPrivateKeyBlindingFactor),
            "inverseForPrivateKeyBlindingFactor"
          );
          const inverseForNonce = createBn254FieldElement(
            bn254ModuleInverter(BigInt(rescueEncryptionNonce)),
            "inverseForNonce"
          );
          const inverseForMasterViewingKeyLow = computeLimbwiseSumInverse(masterViewingKeyLowLimbs);
          const inverseForMasterViewingKeyHigh = computeLimbwiseSumInverse(masterViewingKeyHighLimbs);
          const inverseForRandomFactorLowLimbs = computeLimbwiseSumInverse(randomFactorLowLimbs);
          const inverseForRandomFactorHighLimbs = computeLimbwiseSumInverse(randomFactorHighLimbs);
          const inverseForKeysMasterViewingKeyLow = computeLimbwiseSumInverse(keyForMvkLowLimbs);
          const inverseForKeysMasterViewingKeyHigh = computeLimbwiseSumInverse(keyForMvkHighLimbs);
          const inverseForKeysRandomFactorLowLimbs = computeLimbwiseSumInverse(keyForRfLowLimbs);
          const inverseForKeysRandomFactorHighLimbs = computeLimbwiseSumInverse(keyForRfHighLimbs);
          const inverseForChallengeLimbs = computeLimbwiseSumInverse(challengeLimbs);
          const inverseForChallengeSquaredLimbs = computeLimbwiseSumInverse(challengeSquaredLimbs);
          const toZkLimbTuple = /* @__PURE__ */ __name((limbs) => [
            limbs.low,
            limbs.middle,
            limbs.high
          ], "toZkLimbTuple");
          const zkCircuitInputs = {
            // Native BN254 Elements (10 signals)
            privateKey: poseidonPrivateKey,
            masterViewingKey,
            masterViewingKeyLow: createBn254FieldElement(
              masterViewingKeyLow,
              "zkCircuitInputs.masterViewingKeyLow"
            ),
            masterViewingKeyHigh: createBn254FieldElement(
              masterViewingKeyHigh,
              "zkCircuitInputs.masterViewingKeyHigh"
            ),
            blindingFactorForRescueCommitment: rescueEncryptionCommitmentBlindingFactor,
            masterViewingKeyBlindingFactor,
            privateKeyBlindingFactor: poseidonPrivateKeyBlindingFactor,
            arcisPublicKeyLow: createBn254FieldElement(
              arcisPublicKeyLow,
              "zkCircuitInputs.arcisPublicKeyLow"
            ),
            arcisPublicKeyHigh: createBn254FieldElement(
              arcisPublicKeyHigh,
              "zkCircuitInputs.arcisPublicKeyHigh"
            ),
            nonce: createBn254FieldElement(rescueEncryptionNonce, "zkCircuitInputs.nonce"),
            // Limbwise Elements (10 arrays × 3 limbs = 30 signals)
            masterViewingKeyLowLimbs: toZkLimbTuple(masterViewingKeyLowLimbs),
            masterViewingKeyHighLimbs: toZkLimbTuple(masterViewingKeyHighLimbs),
            randomFactorForPolynomialEvaluationLowLimbs: toZkLimbTuple(randomFactorLowLimbs),
            randomFactorForPolynomialEvaluationHighLimbs: toZkLimbTuple(randomFactorHighLimbs),
            keysForMasterViewingKeyLowLimbs: toZkLimbTuple(keyForMvkLowLimbs),
            keysForMasterViewingKeyHighLimbs: toZkLimbTuple(keyForMvkHighLimbs),
            keysForRandomFactorLowLimbs: toZkLimbTuple(keyForRfLowLimbs),
            keysForRandomFactorHighLimbs: toZkLimbTuple(keyForRfHighLimbs),
            challengeLimbs: toZkLimbTuple(challengeLimbs),
            challengeSquaredLimbs: toZkLimbTuple(challengeSquaredLimbs),
            // Inverse Inputs (16 signals)
            inverseForPrivateKey,
            inverseForMasterViewingKeyNative,
            inverseForMasterViewingKeyLow,
            inverseForMasterViewingKeyHigh,
            inverseForBlindingFactorRescueCommitment,
            inverseForMasterViewingKeyBlindingFactor,
            inverseForPrivateKeyBlindingFactor,
            inverseForRandomFactorLowLimbs,
            inverseForRandomFactorHighLimbs,
            inverseForKeysMasterViewingKeyLow,
            inverseForKeysMasterViewingKeyHigh,
            inverseForKeysRandomFactorLowLimbs,
            inverseForKeysRandomFactorHighLimbs,
            inverseForChallengeLimbs,
            inverseForChallengeSquaredLimbs,
            inverseForNonce,
            // Public Input (1 signal)
            publicAggregatedHash: aggregatedPublicInput
          };
          if (zkProver === void 0) {
            throw new Error("zkProver is required for anonymous registration");
          }
          const zkProof = await zkProver.prove(zkCircuitInputs);
          const computationOffset = generateRandomU64();
          const arciumAccounts = await findArciumInfrastructurePdas(
            client.networkConfig.programId,
            client.networkConfig.arciumProgramAddress,
            clusterOffset,
            BigInt(computationOffset),
            "register_user_for_anonymous_usage_v11"
          );
          const userNoopSigner = createNoopSigner(signer.address);
          const registerAnonymousInstruction = await getRegisterUserForAnonymousUsageV11InstructionAsync(
            {
              user: userNoopSigner,
              feePayer: userNoopSigner,
              mxeAccount: arciumAccounts.mxeAccount,
              mempoolAccount: arciumAccounts.mempoolAccount,
              executingPool: arciumAccounts.executingPoolAccount,
              computationAccount: arciumAccounts.computationAccount,
              compDefAccount: arciumAccounts.compDefAccount,
              clusterAccount: arciumAccounts.clusterAccount,
              computationOffset: {
                first: computationOffset
              },
              rescueEncryptionNonce: {
                first: rescueEncryptionNonce
              },
              rescueEncryptedMasterViewingKeyLow: {
                first: encodeU256ToU256LeBytes(createU256(encryptedMvkLow, "encryptedMvkLow"))
              },
              rescueEncryptedMasterViewingKeyHigh: {
                first: encodeU256ToU256LeBytes(createU256(encryptedMvkHigh, "encryptedMvkHigh"))
              },
              rescueEncryptedRandomFactorForPolynomialLow: {
                first: encodeU256ToU256LeBytes(createU256(encryptedRfLow, "encryptedRfLow"))
              },
              rescueEncryptedRandomFactorForPolynomialHigh: {
                first: encodeU256ToU256LeBytes(createU256(encryptedRfHigh, "encryptedRfHigh"))
              },
              rescueEncryptionCommitment: {
                first: encodeU256ToU256LeBytes(
                  createU256(rescueEncryptionCommitment, "rescueEncryptionCommitment")
                )
              },
              rescueEncryptionPolynomialValidator: {
                first: encodeU256ToU256LeBytes(createU256(polynomialResult, "polynomialResult"))
              },
              userCommitment: {
                first: encodeU256ToU256LeBytes(createU256(userCommitment, "userCommitment"))
              },
              groth16ProofA: {
                first: zkProof.proofA
              },
              groth16ProofB: {
                first: zkProof.proofB
              },
              groth16ProofC: {
                first: zkProof.proofC
              },
              x25519ProvingSigner: await createKeyPairSignerFromPrivateKeyBytes(
                keypairResult.ed25519Keypair.seed
              ),
              priorityFees: { first: microLamportsPerAcu },
              optionalData: {
                first: optionalData?.registerUserForAnonymousUsage ?? defaultOptionalData
              }
            },
            {
              programAddress: client.networkConfig.programId
            }
          );
          const computeBudgetInstruction = createSetComputeUnitLimitInstruction(12e5);
          const registrationAltEntry = lookupAltEntry(client.networkConfig.addressLookupTables, clusterOffset, "register_user_for_anonymous_usage_v11");
          const signature = await executeTransactionWithCallbacks(
            registerAnonymousInstruction,
            callbacks?.registerUserForAnonymousUsage,
            [computeBudgetInstruction],
            registrationAltEntry,
            {
              retrieveCallbackSignature: true,
              computationAccountAddress: arciumAccounts.computationAccount,
              computationOffset: BigInt(computationOffset),
              clusterOffset
            }
          );
          signatures.push(signature);
        }
      }
      return signatures;
    } catch (error) {
      if (error instanceof RegistrationError) {
        throw error;
      }
      if (error instanceof MasterSeedSigningRejectedError) {
        throw new RegistrationError("master-seed-derivation", error.message, error);
      }
      if (error instanceof TransactionSigningError) {
        throw new RegistrationError("transaction-sign", error.message, error);
      }
      if (error instanceof CryptographyError) {
        throw new RegistrationError("zk-proof-generation", error.message, error);
      }
      if (error instanceof TransactionError) {
        throw new RegistrationError("transaction-send", error.message, error);
      }
      if (error instanceof RpcError) {
        throw new RegistrationError("account-fetch", error.message, error);
      }
      throw new RegistrationError(
        "transaction-send",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
  };
}
__name(getUserRegistrationFunction, "getUserRegistrationFunction");

// src/umbra/services/user/rotate-mvk-x25519-key.ts
function getMasterViewingKeyRotatorFunction(register) {
  return () => register({ confidential: false, anonymous: true });
}
__name(getMasterViewingKeyRotatorFunction, "getMasterViewingKeyRotatorFunction");

// src/umbra/services/user/rotate-user-account-x25519-key.ts
function getUserEncryptionKeyRotatorFunction(register) {
  return () => register({ confidential: true, anonymous: false });
}
__name(getUserEncryptionKeyRotatorFunction, "getUserEncryptionKeyRotatorFunction");
var DEFAULT_OPTIONAL_DATA_BYTES10 = new Uint8Array(32);
function getUserEntropySeedRotatorFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function updateRandomGenerationSeed(newSeed, optionalData, callbacks) {
    const userAddress = signer.address;
    if (newSeed.length !== 32) {
      throw new TransactionError("newSeed must be exactly 32 bytes");
    }
    if (newSeed.every((b) => b === 0)) {
      throw new TransactionError("newSeed must not be all zeros");
    }
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getUpdateRandomGenerationSeedInstructionAsync({
      feePayer: noopSigner,
      user: noopSigner,
      newSeed: { first: newSeed },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES10 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const transactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "updateRandomGenerationSeed");
}
__name(getUserEntropySeedRotatorFunction, "getUserEntropySeedRotatorFunction");
var DEFAULT_OPTIONAL_DATA_BYTES11 = new Uint8Array(32);
function getTokenEntropySeedRotatorFunction(args, deps) {
  const { client } = args;
  const { signer } = client;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  return /* @__PURE__ */ __name(async function updateTokenAccountRandomGenerationSeed(mint, newSeed, optionalData, callbacks) {
    const userAddress = signer.address;
    if (newSeed.length !== 32) {
      throw new TransactionError("newSeed must be exactly 32 bytes");
    }
    if (newSeed.every((b) => b === 0)) {
      throw new TransactionError("newSeed must not be all zeros");
    }
    const noopSigner = createNoopSigner(userAddress);
    const instruction = await getUpdateTokenAccountRandomGenerationSeedInstructionAsync({
      feePayer: noopSigner,
      user: noopSigner,
      mint,
      newSeed: { first: newSeed },
      optionalData: { first: optionalData ?? DEFAULT_OPTIONAL_DATA_BYTES11 }
    }, { programAddress: client.networkConfig.programId });
    const latestBlockhash = await getLatestBlockhash();
    const transactionMessage = pipe(
      createTransactionMessage({ version: 0 }),
      (m) => setTransactionMessageFeePayer(userAddress, m),
      (m) => setTransactionMessageLifetimeUsingBlockhash(latestBlockhash, m),
      (m) => appendTransactionMessageInstructions([instruction], m)
    );
    const partiallySignedTransaction = await partiallySignTransactionMessageWithSigners(transactionMessage);
    const signedTransaction = await signer.signTransaction(
      partiallySignedTransaction
    );
    await callbacks?.pre?.(signedTransaction);
    const signatures = await transactionForwarder.forwardInParallel([signedTransaction]);
    const signature = signatures[0];
    await callbacks?.post?.(signedTransaction, signature);
    return signature;
  }, "updateTokenAccountRandomGenerationSeed");
}
__name(getTokenEntropySeedRotatorFunction, "getTokenEntropySeedRotatorFunction");
function assertValidClient(client) {
  if (client === void 0 || client === null) {
    throw new EncryptedWithdrawalError("initialization", "client is required");
  }
}
__name(assertValidClient, "assertValidClient");
function assertValidSigner(signer) {
  if (signer === void 0 || signer === null) {
    throw new EncryptedWithdrawalError("initialization", "client.signer is required");
  }
  if (typeof signer.address !== "string" || signer.address.trim().length === 0) {
    throw new EncryptedWithdrawalError(
      "initialization",
      "client.signer.address must be a non-empty string"
    );
  }
  if (typeof signer.signTransaction !== "function") {
    throw new EncryptedWithdrawalError(
      "initialization",
      "client.signer.signTransaction must be a function"
    );
  }
}
__name(assertValidSigner, "assertValidSigner");
function assertValidBlockhashProvider(provider) {
  if (typeof provider !== "function") {
    throw new EncryptedWithdrawalError("initialization", "getLatestBlockhash must be a function");
  }
}
__name(assertValidBlockhashProvider, "assertValidBlockhashProvider");
function assertValidTransactionForwarder(forwarder) {
  if (forwarder === void 0 || forwarder === null) {
    throw new EncryptedWithdrawalError("initialization", "transactionForwarder is required");
  }
  if (typeof forwarder.forwardSequentially !== "function") {
    throw new EncryptedWithdrawalError(
      "initialization",
      "transactionForwarder.forwardSequentially must be a function"
    );
  }
}
__name(assertValidTransactionForwarder, "assertValidTransactionForwarder");
function assertValidAddress(address, name) {
  if (typeof address !== "string" || address.trim().length === 0) {
    throw new EncryptedWithdrawalError("validation", `Invalid ${name}: must be a non-empty string`);
  }
}
__name(assertValidAddress, "assertValidAddress");
function assertValidArciumAccounts(accounts) {
  const requiredAccounts = [
    "mxeAccount",
    "mempoolAccount",
    "executingPoolAccount",
    "computationAccount",
    "compDefAccount",
    "clusterAccount"
  ];
  for (const accountName of requiredAccounts) {
    const account = accounts[accountName];
    if (typeof account !== "string" || account.trim().length === 0) {
      throw new EncryptedWithdrawalError(
        "pda-derivation",
        `Failed to derive ${accountName}: account address is invalid`
      );
    }
  }
}
__name(assertValidArciumAccounts, "assertValidArciumAccounts");
function getEncryptedBalanceToPublicBalanceDirectWithdrawerFunction(args, deps) {
  const { client } = args;
  assertValidClient(client);
  const { signer } = client;
  assertValidSigner(signer);
  const accountInfoProvider = deps?.rpc?.accountInfoProvider ?? client.accountInfoProvider;
  const getLatestBlockhash = deps?.rpc?.blockhashProvider ?? client.blockhashProvider;
  const transactionForwarder = deps?.rpc?.transactionForwarder ?? client.transactionForwarder;
  assertValidBlockhashProvider(getLatestBlockhash);
  assertValidTransactionForwarder(transactionForwarder);
  const factoryMicroLamportsPerAcu = deps?.arcium?.microLamportsPerAcu ?? 0n;
  const factoryAwaitComputationFinalization = deps?.arcium?.awaitComputationFinalization;
  const executorConfig = {
    signer,
    getLatestBlockhash,
    transactionForwarder,
    computationMonitor: client.computationMonitor
  };
  async function executeTransaction(instruction, fetchedClusterOffset, awaitCallback) {
    const altEntry = lookupAltEntry(client.networkConfig.addressLookupTables, fetchedClusterOffset, "withdraw_from_shared_balance_into_public_balance_v11");
    try {
      const result = await executeComputationTransaction(
        executorConfig,
        {
          instruction,
          preInstructions: [createSetComputeUnitLimitInstruction(4e5)],
          altEntry
        },
        awaitCallback
      );
      if ("callbackResult" in result) {
        return {
          queueSignature: result.signature,
          callbackStatus: result.callbackResult.status,
          callbackSignature: "callbackSignature" in result.callbackResult ? result.callbackResult.callbackSignature : void 0,
          callbackElapsedMs: result.callbackResult.elapsedMs,
          rentClaimSignature: result.rentClaimSignature,
          rentClaimError: result.rentClaimError
        };
      }
      return { queueSignature: result.signature };
    } catch (error) {
      if (error instanceof EncryptedWithdrawalError) {
        throw error;
      }
      throw new EncryptedWithdrawalError(
        "transaction-send",
        `Transaction execution failed: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
  }
  __name(executeTransaction, "executeTransaction");
  return /* @__PURE__ */ __name(async function withdrawIntoPublicBalance(destinationAddress, mint, withdrawalAmount, options) {
    const accountInfoCommitment = options?.accountInfoCommitment ?? "confirmed";
    const microLamportsPerAcu = factoryMicroLamportsPerAcu;
    const optionalData = options?.optionalData ?? new Uint8Array(32);
    try {
      assertValidAddress(destinationAddress, "destinationAddress");
      assertValidAddress(mint, "mint");
      assertU64(withdrawalAmount);
      if (withdrawalAmount <= 0n) {
        throw new EncryptedWithdrawalError(
          "validation",
          "Invalid withdrawalAmount: must be greater than zero"
        );
      }
      assertOptionalData32(optionalData, "optionalData");
    } catch (error) {
      if (error instanceof EncryptedWithdrawalError) {
        throw error;
      }
      throw new EncryptedWithdrawalError(
        "validation",
        error instanceof Error ? error.message : String(error),
        error instanceof Error ? error : void 0
      );
    }
    let mintTokenProgram;
    let clusterOffset;
    try {
      const mintAccountMap = await accountInfoProvider([mint, client.networkConfig.mxeAccountAddress], { commitment: accountInfoCommitment });
      const mintAccount = mintAccountMap.get(mint);
      if (mintAccount?.exists !== true) {
        throw new EncryptedWithdrawalError("mint-fetch", `Mint account not found: ${mint}`);
      }
      const mintOwner = mintAccount.programAddress;
      if (mintOwner !== SPL_TOKEN_PROGRAM_ID && mintOwner !== TOKEN_2022_PROGRAM_ID) {
        throw new EncryptedWithdrawalError(
          "mint-fetch",
          `Unknown token program: ${mintOwner}. Expected SPL Token (${SPL_TOKEN_PROGRAM_ID}) or Token-2022 (${TOKEN_2022_PROGRAM_ID})`
        );
      }
      mintTokenProgram = mintOwner;
      clusterOffset = extractClusterOffsetFromMxeAccount(
        mintAccountMap.get(client.networkConfig.mxeAccountAddress)
      );
    } catch (error) {
      if (error instanceof EncryptedWithdrawalError) {
        throw error;
      }
      throw new EncryptedWithdrawalError(
        "mint-fetch",
        `Failed to fetch mint account: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    const computationOffset = generateRandomU64();
    let arciumAccounts;
    try {
      arciumAccounts = await findArciumInfrastructurePdas(
        client.networkConfig.programId,
        client.networkConfig.arciumProgramAddress,
        clusterOffset,
        BigInt(computationOffset),
        "withdraw_from_shared_balance_into_public_balance_v11"
      );
    } catch (error) {
      throw new EncryptedWithdrawalError(
        "pda-derivation",
        `Failed to get Arcium account addresses: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    assertValidArciumAccounts(arciumAccounts);
    const mpcCallbackDataOffset = generateRandomU128();
    let instruction;
    const protocolFeeProvider = getHardcodedWithdrawalProtocolFeeProvider();
    const protocolFeeConfig = await protocolFeeProvider(withdrawalAmount);
    try {
      const noopSigner = createNoopSigner(signer.address);
      instruction = await getWithdrawFromSharedBalanceIntoPublicBalanceV11InstructionAsync({
        // Signers
        userAddress: noopSigner,
        feePayer: noopSigner,
        // Arcium accounts
        mxeAccount: arciumAccounts.mxeAccount,
        mempoolAccount: arciumAccounts.mempoolAccount,
        executingPool: arciumAccounts.executingPoolAccount,
        computationAccount: arciumAccounts.computationAccount,
        compDefAccount: arciumAccounts.compDefAccount,
        clusterAccount: arciumAccounts.clusterAccount,
        // Token mint and program
        mint,
        tokenProgram: mintTokenProgram,
        // Instruction data
        computationOffset: {
          first: computationOffset
        },
        feeVaultOffset: {
          first: 0n
        },
        mpcCallbackDataOffset: {
          first: mpcCallbackDataOffset
        },
        withdrawalAmount: {
          first: withdrawalAmount
        },
        priorityFees: { first: microLamportsPerAcu },
        optionalData: {
          first: optionalData
        },
        // Fee slab data and merkle proof
        ...feeSlabResultToInstructionFields(protocolFeeConfig)
      }, { programAddress: client.networkConfig.programId });
    } catch (error) {
      if (error instanceof EncryptedWithdrawalError) {
        throw error;
      }
      throw new EncryptedWithdrawalError(
        "instruction-build",
        `Failed to build withdrawal instruction: ${error instanceof Error ? error.message : String(error)}`,
        error instanceof Error ? error : void 0
      );
    }
    if (instruction === void 0 || instruction === null) {
      throw new EncryptedWithdrawalError(
        "instruction-build",
        "getWithdrawFromSharedBalanceIntoPublicBalanceV11InstructionAsync returned null or undefined"
      );
    }
    const resolvedAwaitConfig = factoryAwaitComputationFinalization === false ? false : factoryAwaitComputationFinalization ?? {};
    const callbackOptions = resolvedAwaitConfig === false ? void 0 : {
      ...resolvedAwaitConfig,
      retrieveCallbackSignature: true,
      computationAccountAddress: arciumAccounts.computationAccount,
      computationOffset: BigInt(computationOffset),
      clusterOffset
    };
    return executeTransaction(instruction, clusterOffset, callbackOptions);
  }, "withdrawIntoPublicBalance");
}
__name(getEncryptedBalanceToPublicBalanceDirectWithdrawerFunction, "getEncryptedBalanceToPublicBalanceDirectWithdrawerFunction");

export { BPS_DIVISOR, DEFAULT_MAX_SLOT_WINDOW, DEFAULT_POLLING_INTERVAL_MS, DEFAULT_SAFETY_TIMEOUT_MS, DEFAULT_SIGNATURE_RETRIEVAL_LIMIT, FEE_OFFSETS, UMBRA_MESSAGE_TO_SIGN, buildTransactionMessage, calculateFee, enrichWithMerkleProof, executeComputationTransaction, extractMerklePath, extractSignatures, feeSlabResultToInstructionFields, forwardAndMonitor, getClaimableUtxoScannerFunction, getComplianceGrantIssuerFunction, getComplianceGrantRevokerFunction, getDefaultKeyLoader, getDefaultKeyStorer, getDefaultMasterSeedGenerator, getDefaultMasterSeedStorage, getEncryptedBalanceQuerierFunction, getEncryptedBalanceToPublicBalanceDirectWithdrawerFunction, getEncryptedBalanceToReceiverClaimableUtxoCreatorFunction, getEncryptedBalanceToSelfClaimableUtxoCreatorFunction, getHardcodedClaimUtxoProtocolFeeProvider, getHardcodedClaimUtxoRelayerFeeProvider, getHardcodedCreateUtxoProtocolFeeProvider, getHardcodedDepositProtocolFeeProvider, getHardcodedWithdrawalProtocolFeeProvider, getMasterViewingKeyRotatorFunction, getMintEncryptionKeyRotatorFunction, getNetworkCiphertextReencryptorForNetworkGrantFunction, getNetworkComplianceGrantQuerierFunction, getNetworkEncryptionToSharedEncryptionConverterFunction, getPollingComputationMonitor, getPublicBalanceToEncryptedBalanceDirectDepositorFunction, getPublicBalanceToReceiverClaimableUtxoCreatorFunction, getPublicBalanceToSelfClaimableUtxoCreatorFunction, getReceiverClaimableUtxoToEncryptedBalanceClaimerFunction, getSelfClaimableUtxoToEncryptedBalanceClaimerFunction, getSelfClaimableUtxoToPublicBalanceClaimerFunction, getSharedCiphertextReencryptorForNetworkGrantFunction, getSharedCiphertextReencryptorForUserGrantFunction, getSharedComplianceGrantQuerierFunction, getStagedSolRecovererFunction, getStagedSplRecovererFunction, getTokenEntropySeedRotatorFunction, getUmbraClient, getUserAccountQuerierFunction, getUserComplianceGrantQuerierFunction, getUserEncryptionKeyRotatorFunction, getUserEntropySeedRotatorFunction, getUserRegistrationFunction, getWebsocketComputationMonitor, leafIndexToPathIndices, protocolFeeSlabResultToInstructionFields, relayerFeeSlabResultToInstructionFields, signTransactionMessage };
//# sourceMappingURL=index.js.map
//# sourceMappingURL=index.js.map