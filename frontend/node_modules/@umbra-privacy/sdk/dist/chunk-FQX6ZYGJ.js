import { assertU8LeBytes, assertU8BeBytes, assertU16LeBytes, U16_BYTE_LENGTH, assertU16BeBytes, assertU32LeBytes, U32_BYTE_LENGTH, assertU32BeBytes, assertU64LeBytes, U64_BYTE_LENGTH, assertU64BeBytes, assertU128LeBytes, U128_BYTE_LENGTH, assertU128BeBytes, assertU256LeBytes, U256_BYTE_LENGTH, assertU256BeBytes, assertU512LeBytes, U512_BYTE_LENGTH, assertU512BeBytes, assertU1024LeBytes, U1024_BYTE_LENGTH, assertU1024BeBytes, assertU8, assertU16, assertU32, assertU64, assertU128, assertU256, assertU512, assertU1024, U8_BYTE_LENGTH } from './chunk-CFTW5WNG.js';
import { __name } from './chunk-7QVYU63E.js';

// src/common/converters/byte-encoding.ts
function convertU8LeBytesToU8BeBytes(value) {
  assertU8LeBytes(value);
  const result = new Uint8Array(value);
  assertU8BeBytes(result);
  return result;
}
__name(convertU8LeBytesToU8BeBytes, "convertU8LeBytesToU8BeBytes");
function convertU8BeBytesToU8LeBytes(value) {
  assertU8BeBytes(value);
  const result = new Uint8Array(value);
  assertU8LeBytes(result);
  return result;
}
__name(convertU8BeBytesToU8LeBytes, "convertU8BeBytesToU8LeBytes");
function convertU16LeBytesToU16BeBytes(value) {
  assertU16LeBytes(value);
  const result = new Uint8Array(U16_BYTE_LENGTH);
  for (let index = 0; index < U16_BYTE_LENGTH; index++) {
    result[index] = value[U16_BYTE_LENGTH - 1 - index];
  }
  assertU16BeBytes(result);
  return result;
}
__name(convertU16LeBytesToU16BeBytes, "convertU16LeBytesToU16BeBytes");
function convertU16BeBytesToU16LeBytes(value) {
  assertU16BeBytes(value);
  const result = new Uint8Array(U16_BYTE_LENGTH);
  for (let index = 0; index < U16_BYTE_LENGTH; index++) {
    result[index] = value[U16_BYTE_LENGTH - 1 - index];
  }
  assertU16LeBytes(result);
  return result;
}
__name(convertU16BeBytesToU16LeBytes, "convertU16BeBytesToU16LeBytes");
function convertU32LeBytesToU32BeBytes(value) {
  assertU32LeBytes(value);
  const result = new Uint8Array(U32_BYTE_LENGTH);
  for (let index = 0; index < U32_BYTE_LENGTH; index++) {
    result[index] = value[U32_BYTE_LENGTH - 1 - index];
  }
  assertU32BeBytes(result);
  return result;
}
__name(convertU32LeBytesToU32BeBytes, "convertU32LeBytesToU32BeBytes");
function convertU32BeBytesToU32LeBytes(value) {
  assertU32BeBytes(value);
  const result = new Uint8Array(U32_BYTE_LENGTH);
  for (let index = 0; index < U32_BYTE_LENGTH; index++) {
    result[index] = value[U32_BYTE_LENGTH - 1 - index];
  }
  assertU32LeBytes(result);
  return result;
}
__name(convertU32BeBytesToU32LeBytes, "convertU32BeBytesToU32LeBytes");
function convertU64LeBytesToU64BeBytes(value) {
  assertU64LeBytes(value);
  const result = new Uint8Array(U64_BYTE_LENGTH);
  for (let index = 0; index < U64_BYTE_LENGTH; index++) {
    result[index] = value[U64_BYTE_LENGTH - 1 - index];
  }
  assertU64BeBytes(result);
  return result;
}
__name(convertU64LeBytesToU64BeBytes, "convertU64LeBytesToU64BeBytes");
function convertU64BeBytesToU64LeBytes(value) {
  assertU64BeBytes(value);
  const result = new Uint8Array(U64_BYTE_LENGTH);
  for (let index = 0; index < U64_BYTE_LENGTH; index++) {
    result[index] = value[U64_BYTE_LENGTH - 1 - index];
  }
  assertU64LeBytes(result);
  return result;
}
__name(convertU64BeBytesToU64LeBytes, "convertU64BeBytesToU64LeBytes");
function convertU128LeBytesToU128BeBytes(value) {
  assertU128LeBytes(value);
  const result = new Uint8Array(U128_BYTE_LENGTH);
  for (let index = 0; index < U128_BYTE_LENGTH; index++) {
    result[index] = value[U128_BYTE_LENGTH - 1 - index];
  }
  assertU128BeBytes(result);
  return result;
}
__name(convertU128LeBytesToU128BeBytes, "convertU128LeBytesToU128BeBytes");
function convertU128BeBytesToU128LeBytes(value) {
  assertU128BeBytes(value);
  const result = new Uint8Array(U128_BYTE_LENGTH);
  for (let index = 0; index < U128_BYTE_LENGTH; index++) {
    result[index] = value[U128_BYTE_LENGTH - 1 - index];
  }
  assertU128LeBytes(result);
  return result;
}
__name(convertU128BeBytesToU128LeBytes, "convertU128BeBytesToU128LeBytes");
function convertU256LeBytesToU256BeBytes(value) {
  assertU256LeBytes(value);
  const result = new Uint8Array(U256_BYTE_LENGTH);
  for (let index = 0; index < U256_BYTE_LENGTH; index++) {
    result[index] = value[U256_BYTE_LENGTH - 1 - index];
  }
  assertU256BeBytes(result);
  return result;
}
__name(convertU256LeBytesToU256BeBytes, "convertU256LeBytesToU256BeBytes");
function convertU256BeBytesToU256LeBytes(value) {
  assertU256BeBytes(value);
  const result = new Uint8Array(U256_BYTE_LENGTH);
  for (let index = 0; index < U256_BYTE_LENGTH; index++) {
    result[index] = value[U256_BYTE_LENGTH - 1 - index];
  }
  assertU256LeBytes(result);
  return result;
}
__name(convertU256BeBytesToU256LeBytes, "convertU256BeBytesToU256LeBytes");
function convertU512LeBytesToU512BeBytes(value) {
  assertU512LeBytes(value);
  const result = new Uint8Array(U512_BYTE_LENGTH);
  for (let index = 0; index < U512_BYTE_LENGTH; index++) {
    result[index] = value[U512_BYTE_LENGTH - 1 - index];
  }
  assertU512BeBytes(result);
  return result;
}
__name(convertU512LeBytesToU512BeBytes, "convertU512LeBytesToU512BeBytes");
function convertU512BeBytesToU512LeBytes(value) {
  assertU512BeBytes(value);
  const result = new Uint8Array(U512_BYTE_LENGTH);
  for (let index = 0; index < U512_BYTE_LENGTH; index++) {
    result[index] = value[U512_BYTE_LENGTH - 1 - index];
  }
  assertU512LeBytes(result);
  return result;
}
__name(convertU512BeBytesToU512LeBytes, "convertU512BeBytesToU512LeBytes");
function convertU1024LeBytesToU1024BeBytes(value) {
  assertU1024LeBytes(value);
  const result = new Uint8Array(U1024_BYTE_LENGTH);
  for (let index = 0; index < U1024_BYTE_LENGTH; index++) {
    result[index] = value[U1024_BYTE_LENGTH - 1 - index];
  }
  assertU1024BeBytes(result);
  return result;
}
__name(convertU1024LeBytesToU1024BeBytes, "convertU1024LeBytesToU1024BeBytes");
function convertU1024BeBytesToU1024LeBytes(value) {
  assertU1024BeBytes(value);
  const result = new Uint8Array(U1024_BYTE_LENGTH);
  for (let index = 0; index < U1024_BYTE_LENGTH; index++) {
    result[index] = value[U1024_BYTE_LENGTH - 1 - index];
  }
  assertU1024LeBytes(result);
  return result;
}
__name(convertU1024BeBytesToU1024LeBytes, "convertU1024BeBytesToU1024LeBytes");
function encodeU8ToU8LeBytes(value) {
  assertU8(value);
  const result = new Uint8Array(U8_BYTE_LENGTH);
  result[0] = Number(value);
  assertU8LeBytes(result);
  return result;
}
__name(encodeU8ToU8LeBytes, "encodeU8ToU8LeBytes");
function encodeU8ToU8BeBytes(value) {
  assertU8(value);
  const result = new Uint8Array(U8_BYTE_LENGTH);
  result[0] = Number(value);
  assertU8BeBytes(result);
  return result;
}
__name(encodeU8ToU8BeBytes, "encodeU8ToU8BeBytes");
function encodeU16ToU16LeBytes(value) {
  assertU16(value);
  const result = new Uint8Array(U16_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U16_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU16LeBytes(result);
  return result;
}
__name(encodeU16ToU16LeBytes, "encodeU16ToU16LeBytes");
function encodeU16ToU16BeBytes(value) {
  assertU16(value);
  const result = new Uint8Array(U16_BYTE_LENGTH);
  let temporary = value;
  for (let index = U16_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU16BeBytes(result);
  return result;
}
__name(encodeU16ToU16BeBytes, "encodeU16ToU16BeBytes");
function encodeU32ToU32LeBytes(value) {
  assertU32(value);
  const result = new Uint8Array(U32_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U32_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU32LeBytes(result);
  return result;
}
__name(encodeU32ToU32LeBytes, "encodeU32ToU32LeBytes");
function encodeU32ToU32BeBytes(value) {
  assertU32(value);
  const result = new Uint8Array(U32_BYTE_LENGTH);
  let temporary = value;
  for (let index = U32_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU32BeBytes(result);
  return result;
}
__name(encodeU32ToU32BeBytes, "encodeU32ToU32BeBytes");
function encodeU64ToU64LeBytes(value) {
  assertU64(value);
  const result = new Uint8Array(U64_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U64_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU64LeBytes(result);
  return result;
}
__name(encodeU64ToU64LeBytes, "encodeU64ToU64LeBytes");
function encodeU64ToU64BeBytes(value) {
  assertU64(value);
  const result = new Uint8Array(U64_BYTE_LENGTH);
  let temporary = value;
  for (let index = U64_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU64BeBytes(result);
  return result;
}
__name(encodeU64ToU64BeBytes, "encodeU64ToU64BeBytes");
function encodeU128ToU128LeBytes(value) {
  assertU128(value);
  const result = new Uint8Array(U128_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U128_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU128LeBytes(result);
  return result;
}
__name(encodeU128ToU128LeBytes, "encodeU128ToU128LeBytes");
function encodeU128ToU128BeBytes(value) {
  assertU128(value);
  const result = new Uint8Array(U128_BYTE_LENGTH);
  let temporary = value;
  for (let index = U128_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU128BeBytes(result);
  return result;
}
__name(encodeU128ToU128BeBytes, "encodeU128ToU128BeBytes");
function encodeU256ToU256LeBytes(value) {
  assertU256(value);
  const result = new Uint8Array(U256_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U256_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU256LeBytes(result);
  return result;
}
__name(encodeU256ToU256LeBytes, "encodeU256ToU256LeBytes");
function encodeU256ToU256BeBytes(value) {
  assertU256(value);
  const result = new Uint8Array(U256_BYTE_LENGTH);
  let temporary = value;
  for (let index = U256_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU256BeBytes(result);
  return result;
}
__name(encodeU256ToU256BeBytes, "encodeU256ToU256BeBytes");
function encodeU512ToU512LeBytes(value) {
  assertU512(value);
  const result = new Uint8Array(U512_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U512_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU512LeBytes(result);
  return result;
}
__name(encodeU512ToU512LeBytes, "encodeU512ToU512LeBytes");
function encodeU512ToU512BeBytes(value) {
  assertU512(value);
  const result = new Uint8Array(U512_BYTE_LENGTH);
  let temporary = value;
  for (let index = U512_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU512BeBytes(result);
  return result;
}
__name(encodeU512ToU512BeBytes, "encodeU512ToU512BeBytes");
function encodeU1024ToU1024LeBytes(value) {
  assertU1024(value);
  const result = new Uint8Array(U1024_BYTE_LENGTH);
  let temporary = value;
  for (let index = 0; index < U1024_BYTE_LENGTH; index++) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU1024LeBytes(result);
  return result;
}
__name(encodeU1024ToU1024LeBytes, "encodeU1024ToU1024LeBytes");
function encodeU1024ToU1024BeBytes(value) {
  assertU1024(value);
  const result = new Uint8Array(U1024_BYTE_LENGTH);
  let temporary = value;
  for (let index = U1024_BYTE_LENGTH - 1; index >= 0; index--) {
    result[index] = Number(temporary & 0xffn);
    temporary >>= 8n;
  }
  assertU1024BeBytes(result);
  return result;
}
__name(encodeU1024ToU1024BeBytes, "encodeU1024ToU1024BeBytes");
function decodeU8LeBytesToU8(value) {
  assertU8LeBytes(value);
  const result = BigInt(value[0]);
  assertU8(result);
  return result;
}
__name(decodeU8LeBytesToU8, "decodeU8LeBytesToU8");
function decodeU8BeBytesToU8(value) {
  assertU8BeBytes(value);
  const result = BigInt(value[0]);
  assertU8(result);
  return result;
}
__name(decodeU8BeBytesToU8, "decodeU8BeBytesToU8");
function decodeU16LeBytesToU16(value) {
  assertU16LeBytes(value);
  let result = 0n;
  for (let index = U16_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU16(result);
  return result;
}
__name(decodeU16LeBytesToU16, "decodeU16LeBytesToU16");
function decodeU16BeBytesToU16(value) {
  assertU16BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U16_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU16(result);
  return result;
}
__name(decodeU16BeBytesToU16, "decodeU16BeBytesToU16");
function decodeU32LeBytesToU32(value) {
  assertU32LeBytes(value);
  let result = 0n;
  for (let index = U32_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU32(result);
  return result;
}
__name(decodeU32LeBytesToU32, "decodeU32LeBytesToU32");
function decodeU32BeBytesToU32(value) {
  assertU32BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U32_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU32(result);
  return result;
}
__name(decodeU32BeBytesToU32, "decodeU32BeBytesToU32");
function decodeU64LeBytesToU64(value) {
  assertU64LeBytes(value);
  let result = 0n;
  for (let index = U64_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU64(result);
  return result;
}
__name(decodeU64LeBytesToU64, "decodeU64LeBytesToU64");
function decodeU64BeBytesToU64(value) {
  assertU64BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U64_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU64(result);
  return result;
}
__name(decodeU64BeBytesToU64, "decodeU64BeBytesToU64");
function decodeU128LeBytesToU128(value) {
  assertU128LeBytes(value);
  let result = 0n;
  for (let index = U128_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU128(result);
  return result;
}
__name(decodeU128LeBytesToU128, "decodeU128LeBytesToU128");
function decodeU128BeBytesToU128(value) {
  assertU128BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U128_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU128(result);
  return result;
}
__name(decodeU128BeBytesToU128, "decodeU128BeBytesToU128");
function decodeU256LeBytesToU256(value) {
  assertU256LeBytes(value);
  let result = 0n;
  for (let index = U256_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU256(result);
  return result;
}
__name(decodeU256LeBytesToU256, "decodeU256LeBytesToU256");
function decodeU256BeBytesToU256(value) {
  assertU256BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U256_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU256(result);
  return result;
}
__name(decodeU256BeBytesToU256, "decodeU256BeBytesToU256");
function decodeU512LeBytesToU512(value) {
  assertU512LeBytes(value);
  let result = 0n;
  for (let index = U512_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU512(result);
  return result;
}
__name(decodeU512LeBytesToU512, "decodeU512LeBytesToU512");
function decodeU512BeBytesToU512(value) {
  assertU512BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U512_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU512(result);
  return result;
}
__name(decodeU512BeBytesToU512, "decodeU512BeBytesToU512");
function decodeU1024LeBytesToU1024(value) {
  assertU1024LeBytes(value);
  let result = 0n;
  for (let index = U1024_BYTE_LENGTH - 1; index >= 0; index--) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU1024(result);
  return result;
}
__name(decodeU1024LeBytesToU1024, "decodeU1024LeBytesToU1024");
function decodeU1024BeBytesToU1024(value) {
  assertU1024BeBytes(value);
  let result = 0n;
  for (let index = 0; index < U1024_BYTE_LENGTH; index++) {
    result = result << 8n | BigInt(value[index]);
  }
  assertU1024(result);
  return result;
}
__name(decodeU1024BeBytesToU1024, "decodeU1024BeBytesToU1024");

export { convertU1024BeBytesToU1024LeBytes, convertU1024LeBytesToU1024BeBytes, convertU128BeBytesToU128LeBytes, convertU128LeBytesToU128BeBytes, convertU16BeBytesToU16LeBytes, convertU16LeBytesToU16BeBytes, convertU256BeBytesToU256LeBytes, convertU256LeBytesToU256BeBytes, convertU32BeBytesToU32LeBytes, convertU32LeBytesToU32BeBytes, convertU512BeBytesToU512LeBytes, convertU512LeBytesToU512BeBytes, convertU64BeBytesToU64LeBytes, convertU64LeBytesToU64BeBytes, convertU8BeBytesToU8LeBytes, convertU8LeBytesToU8BeBytes, decodeU1024BeBytesToU1024, decodeU1024LeBytesToU1024, decodeU128BeBytesToU128, decodeU128LeBytesToU128, decodeU16BeBytesToU16, decodeU16LeBytesToU16, decodeU256BeBytesToU256, decodeU256LeBytesToU256, decodeU32BeBytesToU32, decodeU32LeBytesToU32, decodeU512BeBytesToU512, decodeU512LeBytesToU512, decodeU64BeBytesToU64, decodeU64LeBytesToU64, decodeU8BeBytesToU8, decodeU8LeBytesToU8, encodeU1024ToU1024BeBytes, encodeU1024ToU1024LeBytes, encodeU128ToU128BeBytes, encodeU128ToU128LeBytes, encodeU16ToU16BeBytes, encodeU16ToU16LeBytes, encodeU256ToU256BeBytes, encodeU256ToU256LeBytes, encodeU32ToU32BeBytes, encodeU32ToU32LeBytes, encodeU512ToU512BeBytes, encodeU512ToU512LeBytes, encodeU64ToU64BeBytes, encodeU64ToU64LeBytes, encodeU8ToU8BeBytes, encodeU8ToU8LeBytes };
//# sourceMappingURL=chunk-FQX6ZYGJ.js.map
//# sourceMappingURL=chunk-FQX6ZYGJ.js.map